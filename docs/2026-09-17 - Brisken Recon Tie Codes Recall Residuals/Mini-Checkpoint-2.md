# Mini-Checkpoint: Brisken Recon Tie Codes Recall Residuals

**Date:** 2026-09-17
**Status:** Four audit defects and three residuals shipped, merged on green CI and live on Fly v177; the five queued SPA prompts are still unpublished
**Type:** mini

---

## Summary
Five parallel builder subagents took the unclaimed tail of the voids-audit queue: items 103, 115, 130, the 111 box trace and three residuals. Four shipped (PRs #1056, #1057, #1060, #1061), the fifth found no defect. The months list and the month page now report the same counts, a wire transfer no longer suggests a private card, a correction is recalled on the next arrival, and every refusal carries a code for the Portuguese screen.

## What Was Done
- **#111 box trace (PR #1051, docs only).** July reads 19 inherited rows against a box of 14, not the predicted 12: item 105 restored the AWS and Tricarico invoices at 17:00 UTC, after the prediction read the month (31 + 2 = 33, minus 19 = 14) and before #1045 merged. August matched the prediction exactly. Not a defect, nothing fixed.
- **#103 (PR #1056, item 72).** A pass-1 tie now holds its receipts against the greedy pass, with round B's "spoken for" carried into tie detection (narrowed to a clean EXACT elsewhere, so it only ever dissolves a tie). One derivation behind `n_matched` / `n_review` / `n_unmatched_tx` / `n_refunds` on the months list, the stored summary, the re-match event and its reply. Attribution before and after: July, August and the six bundles byte-identical, 0 of 79 live and 0 of 218 bundle rows moved, scorer 76.0, guard 4/4.
- **Residuals (PR #1057, item 144).** The grid's Books-as line runs the export's chart gate; eight write routes answer with the same dispatch `GET /api/runs/{id}` serves; a bank-transfer tender and a settled-outside receipt no longer suggest a private card.
- **#115 (PR #1060).** Memory is consulted for a receipt with line items, a rule reaches a row with no company, and sign-off teaches confirmed pairs. With today's memory nothing changes live (every rule is Zoho-seeded and unvalidated); after a simulated July sign-off it teaches 11 categories, 4 aliases and 1 FX rate, and August's next arrivals move 10 of 42 lines, none against a reviewer edit.
- **#130 (PR #1061).** 285 refusal sites, 155 codes, `{error, code, ...values}`, advisories carrying their numbers, nine source-scan tests that fail when a new refusal lands uncoded. `docs/lovable-error-codes-prompt.md` written with EN and PT-BR for every key.
- **Live checks after the deploys.** July's list row and its page both read 31 / 7 / 73 / 1 (was 8 / 72 on the list); `n_suggested_private` 8 to 7 with Tricarico's row `suggested_private: false`; an unauthenticated call answers `{"error": "authentication required", "code": "unauthenticated"}`. Two cold drives (EN and PT), zero non-GET requests besides login.

## What Did NOT Work (and why)
- **The five queued SPA prompts could not be verified.** The published bundle is unchanged (44 chunks, 1,103 KB): `n_confirm_matched`, `__private_card__`, `sum.dl.pdf`, `row.status.posted.tipFile` and `settled_charge` are all absent, so nothing was pasted and published yet. No dialog keys means no safe click, so the checking lists stay unrun.
- **The item-133 tie test had to change with item 103.** `test_the_rule_never_breaks_a_tie_a_person_should_settle` pinned the raw outcome where a charge outside the tie still took a tied receipt. CI went red on it. The page never rendered that pairing (`held_by` gave the receipt to the pick), which is the split item 103 closes, so the test now asserts the held outcome and names the cost: the other charge waits for the pick and the next re-match.
- **Six refusal sites and one normalizer raise landed uncoded while #130 was building.** Siblings merged item 107 and the charge-category routes mid-flight; CI builds the merge with main, so the scan caught them there rather than locally. Coded in three rounds.
- **The MCP browser tools could not do the consumer drive.** Logging in needs the operator code typed into a tool call, which would print it. The drives ran through the loop's own headless Playwright script instead, which the deploy-consumer hook does not recognize.

## Current Status
Fly v177 carries items 103, 115, 130 and the residuals. Backlog Shipped rows 75, 76, 77 and 81 (main's numbering moved four times during the session; two rows numbered 75 exist on main from a sibling merge). Unclaimed queue items left: #133 rule (b), which session 418ca3f9 was building in `agentic-ops1-item-133`. Brisken platform line: unknown plan, no ops data; comms-log 9 days stale.

## Next Steps
1. Verify the five pasted prompts once the owner publishes: bundle field names first, then the checking lists as a cold drive in EN and PT.
2. Paste `lovable-error-codes-prompt.md` (item 130) after this deploy, then the other Not-applied rows.
3. Owner ruling for #115: may a Zoho-seeded rule nobody validated beat a line read? Today it does not.
4. Owner ruling for the residuals: a bank-paid company invoice still sits in the company-or-person box, and both sanctioned exits are wrong for it.

## Files to Read First
- `workspace/clients/brisken/status/p1-improvement-backlog.md` (items 103, 111, 115, 130, 144 and Shipped rows 75-81)
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PROMPT-STATUS.md`

---

## Continuation Prompt

````markdown
/comd_resume brisken

Continue the Brisken expense-recon (p1) defect loop. Read first: `docs/2026-09-17 - Brisken Recon Tie Codes Recall Residuals/Mini-Checkpoint-2.md`, then in `workspace/clients/brisken/status/p1-improvement-backlog.md` items 103, 115, 130, 133 and 144 (with every "Reviewer corrections" paragraph) and the Shipped table rows 75-81, then `workspace/clients/brisken/automations/expense-reconciliation/docs/PROMPT-STATUS.md`, and the memories `project_brisken_expense_recon_usability_loop`, `project_brisken_expense_recon_voids_audit`, `project_brisken_recon_matching_program`, `feedback_recon_no_live_writes_criss_acts`, `project_brisken_retainer_600_licence`, `feedback_continuation_prompt_carries_loop`, `feedback_end_iterations_with_remaining_items`.

## Where it stands

- Shipped 2026-09-17 late (merged on green CI, deployed, live-checked and cold-driven): **#103 + item 72** (PR #1056) a pass-1 tie holds its receipts against the greedy pass, round B's "spoken for" reaches tie detection narrowed to a clean EXACT elsewhere, and one effective derivation sits behind `n_matched` / `n_review` / `n_unmatched_tx` / `n_refunds` on the months list, the stored summary, the re-match event and its reply; **residuals = item 144** (PR #1057) the grid's Books-as runs the export's chart gate, eight write routes answer with the same dispatch `GET /api/runs/{id}` serves, and a bank-transfer tender or a settled-outside receipt no longer suggests a private card; **#115** (PR #1060) memory is consulted for a receipt with line items, a rule reaches a row with no company, and sign-off teaches confirmed pairs; **#130** (PR #1061) 285 refusal sites carry a stable `code` beside the unchanged English, advisories carry their values, nine source-scan tests fail when a new refusal lands uncoded. **#111 box trace** (PR #1051, docs only): 14 vs the predicted 12 is item 105's restore of the AWS and Tricarico invoices at 17:00 UTC, not a defect. Fly **v177** carries all of it.
- Live after the deploys: July's months-list row and its page both read 31 matched / 7 review / 73 unmatched / 1 credit (the list read 8 / 72 before); `n_suggested_private` 8 -> 7 with `0017` Tricarico `suggested_private: false`; an unauthenticated call answers `{"error": "authentication required", "code": "unauthenticated"}`. Two cold drives (EN month page, PT expenses page), zero non-GET requests besides login.
- **#133 rule (b)** was being built by sibling session 418ca3f9 in worktree `agentic-ops1-item-133` (branch `client/brisken/p1-item-133-merchant-precedence`). Check whether it merged before touching the matcher. Its api-contract section was already on main at 19:00 UTC.
- Trap worth keeping: the item-133 test `test_the_rule_never_breaks_a_tie_a_person_should_settle` now asserts item 103's held outcome. The page never rendered the old raw pairing (`held_by`), and the cost is named in item 103's entry: while a pick is open the other charge waits, and the freed receipt reaches it at the next re-match, because a decision does not re-match a month.
- Owner ruling: covered-class defects only until the EUR 600 licence is signed; new function is quoted separately.
- Waiting on the owner or Criss, not on you: publish the five queued prompts (`lovable-bulk-confirm-dialog`, `lovable-entity-from-charge`, `lovable-private-card-in-picker`, `lovable-one-download-each`, `lovable-booked-hint-names-workbook`) plus `lovable-error-codes-prompt.md` (item 130, backend now live), `lovable-invoice-read-as-statement`, `lovable-untrusted-flag`, `lovable-duplicates-side-by-side`; quotes for #98, #104, #107, #109; operations #119-#128; cost-center data #118; Dirk's statement for card 9693 (#108); #129's UI prompt (nobody has written it); Criss restoring nothing further (#105's two invoices are restored and visible).

## Queue, in order

0. **Verify the pasted prompts.** Bundle check first, no login needed: `python <scratch>/bundle.py "<comma-separated keys>"` fetches every chunk of `https://expenses.brisken.com` and greps it (44 chunks / 1,103 KB on 2026-09-17, every queued key ABSENT). Only when a prompt's decisive keys are present, run its "Checking it landed" list as a cold drive in EN and PT with no non-GET request besides login. **Safety:** never click "Confirm all matched", "Reject N shown", a card-picker option or "Paid with a private card" unless the bundle carries that prompt's dialog keys (`sum.confirmMatched.dialog`, `wb.bulkReject.dialog`, `__private_card__`); without the dialog the click writes to Criss's month. Cancel or Escape in every dialog. Move each verified row in PROMPT-STATUS to Applied, one docs PR for all of them.
1. **Two owner rulings, both from this session's work.** (a) Item 115: may a Zoho-seeded rule nobody validated beat a disagreeing line read? Today it does not; all 103 live rules are seeded, count 1, validated 0, and the seeds file `slack`, `supabase`, `perplexity ai`, `hugging face` under Marketing & Advertising while Criss's own edits put Perplexity and Pressmaster under Software & Subscriptions. (b) Item 144: July's `0017` Tricarico invoice (BRL 27,203.34, wire transfer, settled outside) still sits in `needs_entity` + `needs_person` + the company-or-person box, and both sanctioned exits (pick a company card, confirm private) are wrong for a bank-paid company invoice; person resolution is card-only by the item-40 ruling.
2. **#133 rule (b)** if the sibling did not land it: demote an exact-amount pair with a disagreeing merchant to review (0.55) when a rival pair for the same receipt has a matching merchant (vendor >= 0.5 and +0.25). No pair on the eight datasets moves today.
3. **Item 115's residual:** a re-match never re-categorizes stored rows, so a recalled correction reaches a month only at its next arrival. If the owner wants July's 78 REGISTRY->LEARNED lines to move, that is a re-categorize path nobody has built.
4. **Ledger hygiene:** main carries two Shipped rows numbered 75 (a sibling merge). Renumber in the next docs PR that touches the table.

Before each item: `gh pr list -R 011matthias/agentic-ops1.01 --state open`, `git worktree list`, and a grep of recently modified transcripts in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/*.jsonl` for the item number (skip a claimed item), and read `GET /feedback.jsonl` for new notes (70 lines on 2026-09-17 evening, last is #69, all answered but #61, which a sibling holds; a new note outranks the queue).

## How to work

- Parallel builder subagents worked well again (five in one session): one git worktree off origin/main per item (`client/brisken/p1-item-<N>-<slug>`), a `general-purpose` Agent per worktree in the background with a self-contained brief (hard rules below, measurement, docs, commit on the branch, do NOT push), `uv run tools/bg_watch.py watch` per builder. You review the diff of the load-bearing functions, merge origin/main, push, PR, wait for CI by head SHA, merge on green, deploy, verify. Send a builder back with SendMessage when its result trades one wrong outcome for another.
- **Merge races are the tax.** Siblings merged eight times during this session; every PR needed main merged two or three times, and CI builds the MERGE, so a scan-style test can go red on code that is green locally. Keep a resolver script in the scratchpad: for the backlog Shipped table keep origin/main's rows first and renumber yours above main's highest (match rows by content, never by index); for api-contract and PROMPT-STATUS keep main's section first and append yours.
- Never `git stash`. Stage explicit paths. The primary clone is behind origin/main: use `( cd X && ... )` or `git -C`.
- No writes to Criss's live months; reads only. Never probe publish live. `GET /runs/{id}/expense-report.pdf` WRITES render outcomes: never fetch it live (render locally from a read-only DB copy; `reconciliation-report.pdf` is read-only). Never print or save the operator code or a token; tell every subagent the same.
- Live reads: `python <scratch>/api.py GET /api/path "<python expr on d>"` (bearer from `POST /api/login` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env`, held in memory only). `MSYS_NO_PATHCONV=1` for every `/api/...` arg in Git Bash, or the path is rewritten and the call returns nothing. Windows Python cannot open `/c/...` paths: pass `C:\...` forms.
- Every backend fix: a route-level test through the caller, then `uv run tools/regress_check.py` with single-line literals (sources are CRLF; "no pytest summary line" means reproduce by hand with a cp backup and cp restore). Python heredocs with triple quotes are blocked: write scripts with the Write tool. Do not import a pytest fixture from another test module (CI ruff F811). Full module suite in the FOREGROUND before the PR: `uv run --extra dev --extra web pytest -q -p no:cacheprovider` (no `-n`; about 5 min). Deploy with `( cd <module> && flyctl deploy . -a brisken-expense-recon --remote-only )` from a clean detached origin/main worktree (pre-authorized).
- After a deploy: a live API probe of the changed field, then a cold consumer drive with `<scratch>/drive.py ROUTE "needle,needle" [en|pt]` (headless Playwright `channel="chrome"`: goto, set localStorage `brisken.lang`, fill the first input with the code, Enter, goto the route, read `body` inner text, record non-GET requests, which must be none besides login). The month page shows one section at a time: switch with a JS click on the summary pill button and show decided rows with a JS click on `button[role=switch]`; a Playwright `locator.click` on the pill does not switch it, and a `?view=` param is stripped on load. The Playwright MCP tools cannot do this drive: the login gate needs the operator code typed into a tool call, which would print it. The deploy-consumer hook does not recognize the script, so say plainly what the drive covered rather than the bare word "verified".
- In the same PR, update the backlog item's heading/status and its Shipped row, plus `status/p1-expense-reconciliation.md`. SPA work goes out as a Lovable prompt in the reply inside a FOUR-backtick fence ("Do NOT add Supabase or any database", EN + PT-BR strings, a checking list), saved as `docs/lovable-<slug>-prompt.md` with a Not-applied PROMPT-STATUS row.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while this session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `--root <that worktree>` BEFORE the `finalize` subcommand (it is a global flag); write the checkpoint prose after `finalize` or rename your file to the number it prints; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````
