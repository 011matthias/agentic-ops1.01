We are continuing the One Assessment treasury-assessment tool (workspace/clients/Jochen Projekt/automations/treasury-assessment/, a FastAPI inline-HTML app + a Python pipeline package, deployed to Fly as one-assessment-demo, live at one-assessment-demo.fly.dev). The whole Jochen Projekt tree is gitignored (ripgrep needs --no-ignore; nothing to commit or PR). The only path to production is `flyctl deploy --ha=false` from site-host/, and every deploy waits for my explicit "deploy". render.py changes reach the demo only via re-render + showcase republish (a separate volume write, also gated). Tests: `uv run --directory <treasury-assessment> --extra dev pytest -q` (currently 101/101).

Read first: docs/2026-07-16 - One Assessment Document Channel + Gate + Manual-LLM/Checkpoint.md, DESIGN.md §8a + §15 (build-state table), PIPELINE-NOTES.md §Q/§R/§S, FEEDBACK-REGISTER.md.

Where we are. Jochen requires THREE data sources to flow into the assessment: (1) Fragebogen, (2) uploaded documents, (3) workshop. ALL THREE now flow into scoring, each with its own separate anti-fabrication haystack and honest origin label ("aus Ihrem Fragebogen" / "aus den Unterlagen" / "aus dem Workshop"). Last session shipped three thrusts (all operator-side, no deploy):
- **Document channel (source 2, oa-evidence/v1):** `cli extract --inbox inbox/<id>` runs typed extractors (xlsx/csv/docx) over the mirrored uploads → an immutable manifest.json of verbatim anchored quotes + an operator curation.md + 00-facts.md. The operator routes each quote to a Bereich (the machine never auto-routes — that would fabricate area assignments). stage1_document reconciles + re-verifies sha256 + caps per area; stage2 tests a third `hay_doc` PER discrete quote (kills cross-file splice); source_origin="dokument" renders as Unterlagen sheets. Big tables (>200 rows) → schema-summary only.
- **Metrics release gate:** `cli verify` asserts the deterministic MockLLMClient CITTI metrics vs the committed data/expected-metrics.json baseline; hard-asserts cells_without_source==0; exits non-zero with a field diff on drift (DoD proven). `--update` regenerates after a legit golden change; `--real` = true LLM with tolerance floors. The green-on-clean test makes it CI-enforced.
- **Offline / manual-LLM testing mode (my directive):** for testing, run scoring through THIS environment (Claude), not gpt-4o. `assess --export-prompts <dir>` writes the exact per-area prompts + PROMPTS.md (no API call, STOPS); I produce the JSON; save it as responses.json; `assess --responses <dir>` (llm.FileLLMClient) scores + renders. The anti-fab guard still runs on my output (a fabricated quote is forced to n/a). Transport swap only; gpt-4o / --mock / server / reassess unchanged.

The current TEST loop (my preferred flow): `inbox --pull <id>` → (`extract` + edit curation.md, if documents) → `assess --export-prompts <dir>` → paste PROMPTS.md into the chat, you return the scores → save as responses.json → `assess --responses <dir>` → (`publish` when ready).

Your task: drive the tool down its roadmap. For anything that changes pipeline logic, scoring, or a client-facing surface, go into plan mode first, design it, wait for my go, then build + verify (pytest + `cli verify` + a mock CLI E2E + a NextDecade byte-regression where render changes) and stop at the deploy gate.

Recommended next thrust: **mined Reifegrad anchors** (thrust A, the #1 accuracy/trust lever; DoD: projection rg_exact rises above the 38% baseline). RG calibration is the number-one weakness Jochen named ("den Reifegrad hat er nicht gut übernommen"). Mine per-family few-shot exemplars (As-Is → golden RG) from the goldens into score_area, stamp anchor_set_version, and it is now SAFELY gated by `cli verify` (a regressing anchor set reddens the gate). CITTI is the only complete input→golden pair. This is the natural follow-on now that all three sources flow and the release gate backs scoring changes.

The rest of the map (pick any; I may reprioritize):
- **Fold-back learning loop:** append-only corrections store (JSONL keyed by funktionsgruppe/funktion/field, before/after + evidence quote) → versioned anchors-vN.json consumed by llm.py; the actual "the tool learns which questions it needs" half. Pairs with mined anchors.
- **Operator review triage queue:** review.json + accept/correct panel over REVIEW/low-confidence cells; the missing operator surface and the on-ramp to fold-back.
- **Editable As-Is workbench** (Dirk's open item 3, DESIGN §PLANNED): reopen input post-submit, edit answers/docs, add ad-hoc topics outside the TCF spine. Biggest open feature.
- **First real document-bearing submission** through the full loop, to shake out the extract/curation ergonomics (only mock-tested so far).
- **Client-facing depth:** client verification playback (play the As-Is/workshop memo back to confirm), gap-to-action, Full tier + binding estimate, PDF one-pager.

Owner decisions still open (no code): question-bank curation (Quick-Satz), reifegrad_pct band mapping (25/50/75/100) + the "100" level, benefit voice, product ownership, Nagarro as first use case.

House rules: no invasive live action without explicit per-action yes; anti-fabrication is structural (own field + own haystack + source_origin per source, never merge, never claim a source a run lacks — NextDecade stays byte-identical when a source is absent); no client data into git; dates passed in are never invented; zero em-dashes client-facing; every new capability gets a DoD in DESIGN §15 before build; run `cli verify` before shipping any scoring change (and `cli verify --update` only after a legitimate golden/contract change).

Start by confirming the next thrust (or telling me to pick), then plan it.
