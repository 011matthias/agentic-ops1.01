# Mini-Checkpoint: Brisken Recon Memory M1 Merchant Category Default

**Date:** 2026-09-18
**Status:** M1 shipped and live (PR #1094, merge d1c8e733, Fly v184); M2-M4 handed to the next MEMORY session; the registry write for the three vendors declined by the owner ("not yet, do not re-ask")
**Type:** mini

---

## Summary
The MEMORY session of the 2026-09-18 owner note built M1: a merchant with a registry default reads that category on every receipt and every receiptless charge, and the (company, vendor) memory rule decides the posting account, which is Dirk's description of how the books work. Shipped as backlog item 149 with a grouped `by_vendor[]` memory view; the three merchant entries themselves stay unwritten by the owner's decision.

## What Was Done
- `categorize_receipts_with_registry`: the registry default stamps every receipt of its merchant before any line read; `_registry_account` takes the account from the rule under the receipt's company, else a no-company rule, else the vendor's rules when they agree; a Zoho-seeded rule under another category contributes nothing until validated; the line's `provenance` names the rule. Item 115's `registry_backed` plumbing and `_company_recall` removed.
- `categorize_charges(registry=...)`, wired in `rematch_month`, so a bank description naming a registry merchant takes its default with the company's account (live July had LOVABLE as Meals & Entertainment four times).
- `GET /api/memory`: `by_vendor[]` (one entry per vendor, one line per company, the registry merchant and the category its receipts read on the vendor line) and `categories[].seeded`; pinned in `test_view_contract.py` (`MEMORY_CONTRACT`).
- Tests: `tests/test_registry_category_by_company_m1.py` (6, route-level); the three 2026-08-07 precedence tests rewritten; three item-115 tests updated and the glance re-pinned on a Memory-page rule. Suite 2511 -> 2519 passed / 2 skipped. Four wires proven red under `regress_check`.
- Docs: api-contract section "Merchant-to-category is the default; the exceptions vary by company, on the account"; `docs/lovable-memory-by-company-prompt.md` (Not-applied row in PROMPT-STATUS); backlog item 149; status row; memory `project_brisken_expense_recon_merchant_registry` reshaped.
- Deployed d1c8e733 as Fly v184 from a detached worktree; live `GET /api/memory` carries `by_vendor` (97 vendors) and `seeded` on all 103 rows; cold scripted Playwright drive of `/memory`: 105 rows, "103 total facts", no fallback string, zero non-GET requests.
- M2 measured for the next session: July-September, 60 merchants, 34 single-card, 5 multi-card (all three target vendors among them), 21 never carded.

## What Did NOT Work (and why)
- **`page.fill` + Enter on the SPA's `input#code` login gate:** the token never appeared (timeout 60 s, `localStorage` empty, the form still on screen). The React input needs real key events: `page.click` then `page.keyboard.type`, then the "Log in" button through JS `click()`. The deploy-consumer gate printed "[CONSUMER DRIVEN] ... closed" on the failed attempt, so its advisory is not evidence the drive asserted anything.
- **Keeping item 115's "learned category over the registry" tests as they were:** three of them pinned the row source as `learned` after a sign-off, and the sign-off's registry upsert now makes the corrected merchant read its default from the registry (`registry`, no glance). That is the directive, not a defect; the glance survives only on a merchant with no default, and is now pinned that way.
- **`git rebase origin/main` before the first commit:** refused ("You have unstaged changes"); commit first, then rebase, then push.

## Current Status
M1 live on Fly v184 (brisken-expense-recon; platform: unknown plan per `pre`). Registry still 28 merchants: the owner declined the write of OpenAI / Anthropic / Lovable at the end of this session ("not yet, and dont re ask, i will bring this up again when the time is right"), so the three vendors keep reading the model (receipts) and the seeded `anthropic` rules (charges) until he raises it. Dirk's 3x3 account table (2 of 9 cells known from the seed) asked once in the closing reply. Lovable prompt for the Memory page not pasted. Backlog item 149 heading still reads "pending PR" and has no Shipped-table row.

## Next Steps
1. Next MEMORY session: M2 (registry `card_key`, learned at publish when one card, `card_source: "merchant"`), M3 (two-year Zoho Books seed, both halves owner-gated), M4 (`profile` on the registry entry + item 118), per the continuation prompt below; first update item 149's heading to PR #1094 / Fly v184 and append Shipped row 90.
2. Owner: paste `docs/lovable-memory-by-company-prompt.md`; raise the registry write when the time is right (never re-asked by the agent); Dirk's 3x3 account table.
3. `p2-product-decks.md` (57d) and `p2-targeting.md` (58d) are stale per `project_status.py`; not this session's workstreams, left for their owners.

## Files to Read First
- `workspace/clients/brisken/automations/expense-reconciliation/docs/api-contract.md` (last section, "Merchant-to-category is the default")
- `workspace/clients/brisken/automations/expense-reconciliation/src/expense_recon/categorize.py` (`categorize_receipts_with_registry`, `_registry_account`)
- `workspace/clients/brisken/status/p1-improvement-backlog.md` (item 149)
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`

---

## Continuation prompt (for the next MEMORY session; the same text was handed in the closing reply)

````markdown
/comd_resume brisken

Owner directive 2026-09-18: the items in this prompt have priority and are to be built now; the covered-vs-quote licence question does not gate them. You are the MEMORY session (second iteration). Two sibling sessions (TRACEABILITY and MATCHING) run at the same time on the same repo and the same Fly app: follow the parallel-safety rules in "How to work" exactly.

Read first: `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`, then in that module `docs/api-contract.md` section "Merchant-to-category is the default; the exceptions vary by company, on the account (note item M1)" (the last section of the file), `src/expense_recon/categorize.py` (`categorize_receipts_with_registry` + `_registry_account`), `src/expense_recon/merchant_registry.py` (docstring + `normalize_merchants_setting`), `src/expense_recon/learning/{capture,consult}.py`, `src/expense_recon/web/service.py` `resolve_batch_row_cards` (the item-87 card chain, around line 5991) and `registry_upserts_from_expense_run` (around line 4619), backlog items 47, 87, 118, 149 in `workspace/clients/brisken/status/p1-improvement-backlog.md`, and the memories `project_brisken_expense_recon_merchant_registry`, `project_brisken_expense_recon_usability_loop`, `project_brisken_zoho_books`, `reference_brisken_zoho_expense_orgs`, `feedback_recon_no_live_writes_criss_acts`, `feedback_no_invasive_action_without_ask`, `feedback_continuation_prompt_carries_loop`.

## Where it stands

**Shipped this session (2026-09-18): M1 as backlog item 149, PR #1094, merge `d1c8e733`, Fly v184.** Suite 2511 -> 2519 passed / 2 skipped. What is live:

- The registry default is the CATEGORY on every receipt of its merchant, itemized or not, before any line read (`categorize_receipts_with_registry`; the 2026-08-07 "learned outranks registry" order is retired for the category, kept for the account). The (company, vendor) rule decides the ACCOUNT (`_registry_account`: own company, else no-company rule, else the vendor's rules when they agree; a Zoho-seeded rule under another category contributes nothing until validated). The line's `provenance` names the rule.
- Receiptless charges consult the registry on every re-match (`rematch_month` -> `categorize_charges(registry=...)`).
- `GET /api/memory` carries `by_vendor[]` (one entry per vendor, one line per company, the registry merchant and the category its receipts read on the vendor line) and `categories[].seeded`. SPA half `docs/lovable-memory-by-company-prompt.md`, NOT pasted (PROMPT-STATUS Not-applied row).
- Live verification: authenticated `GET /api/memory` on v184 carries `by_vendor` (97 vendors grouped) and `seeded` on all 103 rows; the live `anthropic` group reads Cloud Services (COGS - DEV Infrastructure, seeded) + Corporate Services (Other Infra and IT Costs, NOT seeded: `manual-set`, so it is person-taught and its account applies the moment the registry entry exists), `merchant: ""` because the registry entry is not written yet. Cold scripted Playwright drive of `/memory` (headless Chrome, no session, operator code typed from the .env): 105 table rows, "103 total facts", the anthropic rows with their accounts, no fallback string, zero non-GET requests. The SPA has no `by_vendor` renderer yet, so the drive proves no regression, not the new render. Backlog item 149 still reads "(SHIPPED 2026-09-18, pending PR)" and has no Shipped-table row: update the heading to PR #1094 / Fly v184 and append Shipped row 90 in the M2 PR.

**Waits on the owner (from M1):**
- (d) The live registry write for OpenAI / Anthropic / Lovable. The exact read-modify-write is `scratchpad`-only (`registry_write.py` in the 2026-09-18 memory session's scratchpad; rewrite it: 28 live entries carried verbatim + `Anthropic` {aliases `Anthropic, PBC`, `Anthropic, PBC @anthropic`, `Anthropic, PBC (@anthropic)`}, `Lovable` {aliases `Lovable Labs`, `Lovable Labs Incorporated`, `Lovable Labs Incorporated (@lovable)`}, `OpenAI` {aliases `OpenAI`}, each category "Software & Subscriptions", `zoho_account` null, no `multi_category`; PUT `/api/settings {"merchants": <31>}`, then GET and diff). The owner was asked once, at the end of the M1 session, with the diff shown (28 carried verbatim, 3 added, 31 after) and answered: "not yet, and dont re ask, i will bring this up again when the time is right." DO NOT re-ask. When he raises it, rebuild the script, dry-run, show the diff, PUT on his yes, GET and verify. Until the entries exist, the three vendors keep reading the model (receipts) and the seeded `anthropic` rules (charges), exactly as before M1.
- Dirk's 3x3 account table (OpenAI / Anthropic / Lovable x Cloud Services / Corporate Services / Consulting). Two cells known from the seed: `anthropic` Cloud Services -> "COGS - DEV Infrastructure (SAP Apps & others)", Corporate Services -> "Other Infra and IT Costs for Cloud Business". When the answers arrive: `PUT /api/memory/categories` per cell (`legal_entity_id`, `vendor` = the vendor_norm the receipts key on: `anthropic pbc`, `anthropic pbc anthropic`, `openai`, `lovable labs incorporated`, `lovable labs`, plus the bank descriptions `anthropic`, `openai`, `lovable` for the charges), category "Software & Subscriptions", then `POST /api/memory/categories/validate` so they apply to itemized receipts. Each write is a production mutation: one `AskUserQuestion` for the batch with the rows shown.
- Lovable paste of `docs/lovable-memory-by-company-prompt.md`.

## Queue, in order (note items M2-M4; M1 is done)

Append each to the backlog as its own item when you first push, next free number at that moment (150 was free at the M1 push), "(note item M2)" etc. kept in the heading.

1. **M2 Card-specific, exclusive vendor spend.** `card_key` is already learnable per merchant as a field correction (`learning/capture.py` `_LEARNABLE_FIELDS`, `consult.py` `_CORRECTABLE_FIELDS`), consulted in the item-87 chain in `service.resolve_batch_row_cards` (pick `override` > printed method or hint `hint` > printed number > confirmed private > `settled_charge` (item 111) > remembered `learned`; the remembered card applies only when the printed method carries no card number). Build: a registry field `card_key` on the merchant entry (parallel, absent until set; `normalize_merchants_setting` keeps it only when set, like `cost_center`), editable in Settings; learned at publish in `commit_to_memory`'s expense branch beside `registry_upserts_from_expense_run` (recommended shape: a machine-maintained `cards_seen` list on the entry, accumulated from the month's resolved cards per registry merchant; `card_key` written by the learner only when `cards_seen` has exactly one card and no `card_key` is set, marked `card_key_learned: true`; never when two cards were seen; a learned key is dropped when a second card appears, an editor-set key is never touched); shown on the row as `card_source: "merchant"`, added to the chain AFTER `learned` and under the same guard (no printed number, not private), and pinned in `tests/test_view_contract.py` `test_card_source_...` (the allowed set `{hint, override, learned, settled_charge, none}` gains `merchant`) and in api-contract's `card_source` table. Wire the display in the Expenses view caller of `resolve_batch_row_cards` (service.py ~6948) and the report caller (~7992); the matcher's bake (~12401) is MATCHING's, leave it and say so in the PR. Measured on July-September (this session, by display vendor, cards from each row's resolved card): 60 merchants, 34 single-card, 5 multi-card, 21 never carded. Multi-card: `anthropic, pbc` {3645: 2, card-2838: 7, card-1176: 1, card-9693: 1}; `anthropic, pbc (@anthropic)` {3645: 3, card-2838: 2}; `anthropic, pbc @anthropic` {card-1176: 1, card-9693: 4}; `lovable labs incorporated` {card-2838: 3, 3645: 4, 3876: 2}; `openai` {card-9693: 5, 3645: 1}. Single-card examples: `github, inc.` card-2838 x2, `obsidian` 3645 x2, `zoho corporation` card-2838 x2, `martino supermercado` 3876 x6, `afi technologies inc` card-9693 x2, `eleven labs inc. @elevenlabs` card-1176 x2, `sendgrid` card-9693, `posto santos` 3876 x2. Put the full list in the PR body (re-measure with `GET /api/expense-batches/{id}` `expenses[].card`, `card_source` on the three months). Whole-map-replace warning in the Lovable prompt: the Settings editor must carry `card_key`, `card_key_learned` and `cards_seen` on every save.
2. **M3 Seed memory from two years of Zoho Books.** The 2026-08-06 seed importer was deleted with item 23 (`learning_cli.py` docstring says so); `.scratch/zoho_books_probe.py` in the main checkout is a read-only org/surface probe with the token exchange you can copy (loads `ZOHO_BOOKS_REFRESH_TOKEN`, `ZOHO_CLIENT_ID`, `ZOHO_CLIENT_SECRET`, `ZOHO_DC` from `workspace/clients/brisken/context/.env`; API `https://www.zohoapis.{DC}/books/v3`, `organization_id` per call). Scopes granted: `ZohoBooks.expenses.READ`, `ZohoBooks.bills.READ`, `ZohoBooks.accountants.READ`. Orgs: Cloud Services 697686691, Consulting 808232536, Corporate Services 822741658, GmbH 696750461 (the other four in `project_brisken_zoho_books`). Zoho Expense was never set up in 6 of 8 orgs: read Books expenses and bills, not expense reports. Pull 24 months, one read, into a scratch JSON; derive (i) merchant registry candidates (canonical name, aliases by the item-117 vocabulary, majority category, `multi_category` where the history genuinely splits within one company), (ii) (company, vendor) -> account rules with `validated` = "zoho-seed-2026-09" (write them through `LearningStore.record_merchant_category` with `source_run` "zoho-seed-2026-09:<org_id>" so `seeded` stays true; validation is a separate owner decision) and a count, (iii) a per-merchant card observation for M2. Apply nothing live before an owner yes through `AskUserQuestion`: one question for the read (it touches Zoho once, against the 08-22 directive's spirit), one for the write (registry + memory, with counts and a sample of 20 rows shown). Store the derived JSON in the module's gitignored context (`workspace/clients/brisken/context/expense-reconciliation/`), never in the repo.
3. **M4 A profile of unstructured knowledge beside the registry.** Add `profile` (free text, parallel, absent until set, capped like `receipt_portal`) to each registry entry in `normalize_merchants_setting`: what the business buys from this merchant, on which card and for which company, anything Criss or Dirk would tell a new bookkeeper. The categorizer reads it as context for that merchant's receipts (prompt-level, in `_classify_lines_via_llm` / `_classify_vendor_via_llm` via the LLM client's classify calls; marked as untrusted data per `rule_untrusted_inbound`, nonce-fenced like `expense_recon/untrusted.py` does: it informs the category, never instructs), the Memory page shows it (`by_vendor[].profile`), the Settings editor edits it (whole-map replace: the Lovable prompt must carry the field on every save or it is erased), and the learning path may append observations to it only as clearly marked machine lines (`[tool 2026-09-18] ...`). Then read item 118 (a cost-center pick teaches nothing) and close it if the same registry-upsert path (`registry_upserts_from_expense_run`, which already carries the whole entry per item 116) makes it a one-line addition (fold a row's cost-center pick into the merchant entry's `cost_center` at sign-off with the same conflict-skip rule categories use); otherwise leave it open and say so.

When the queue is empty, follow SESSION LOOP step 9.

## How to work

- Your worktree, cut fresh: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 fetch origin` then `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add -b client/brisken/p1-memory-<slug> C:\Users\neuma_p1qrsic\Repo\agentic-ops1-memory origin/main` (remove the previous session's `agentic-ops1-memory` worktree first if it is still listed: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree remove C:\Users\neuma_p1qrsic\Repo\agentic-ops1-memory`). One branch per queue item (`p1-memory-m2`, ...), reusing the `agentic-ops1-memory` directory after each merge. Never edit, commit or stash in the main checkout; never `git stash` anywhere. A hook refuses `cd X && ...`; use `git -C`, `uv run --directory`, absolute paths.
- Siblings (TRACEABILITY in `agentic-ops1-trace`, MATCHING in `agentic-ops1-match`) edit `web/service.py`, `web/app.py`, `tests/test_view_contract.py`, `docs/api-contract.md`, `docs/PROMPT-STATUS.md`, the backlog and the status file all day. Append at the END of the relevant block; never renumber, re-sort or reflow. Before the first push `git rebase origin/main` is fine (commit first: a dirty tree refuses the rebase); after any push only `git merge origin/main`, never rebase, never force-push. Re-run the module suite after every merge from main. On a conflict in a shared file keep both sides. Before claiming a backlog number or a Shipped row number, merge main and take the next free one. `learning/` and `merchant_registry.py` are yours alone; `matching/` is MATCHING's, `intake_mail.py` is TRACEABILITY's: do not edit them, add a local helper instead.
- Live reads only. API `https://brisken-expense-recon.fly.dev` (also `api.expenses.brisken.com`): `POST /api/login {"code": ...}` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env` (never print it). Months: July `50622baec444`, August `074a7b8905d7`, September `51a22ad72864`; list `GET /api/expense-batches` (key `batch_id`). Never fetch `GET /runs/{id}/expense-report.pdf` live. No writes to Criss's months; the live writes in this queue (registry, memory seed, the 3x3 rules) and the Zoho read go through `AskUserQuestion` as decisions with a recommendation and the diff shown, never "say the word". Windows Python cannot open `/c/...` paths: pass `C:\...`.
- Build: route-level tests through the FastAPI app, at least one through the CALLER the fix changed; one `uv run tools/regress_check.py --test "<cmd>" --cwd <module dir> --file <src> --replace "<wired>" --with "<disabled>"` per fix, watched going red (on a doubtful "RED (no pytest summary line)" mutate by hand with a backup; restore with `cp`, never `git checkout --`). Suite: `uv run --directory workspace/clients/brisken/automations/expense-reconciliation --extra web --extra dev pytest -q` (about 7 minutes, 2,519 tests at the M1 merge; run it in the background with `tools/bg_watch.py` registered and a Monitor on the summary line). Ruff: `uv run --no-project --with ruff ruff check src tests` from the module dir. Heredocs with triple quotes are blocked: write scripts with the Write tool. Document every new field in `docs/api-contract.md` and pin every new list field in `tests/test_view_contract.py` in the same PR. The accuracy gate (`categorization_gate.py`, `tools/recon-accuracy-guard.py`) must stay green: a precedence change that moves a labelled category is a regression, not a feature.
- Ship: commit with explicit pathspecs, push, `gh pr create --repo 011matthias/agentic-ops1.01` (suite count before and after plus the regress line in the body), wait for CI (`gh pr checks <n> --json name,bucket` in a background loop; `expense-recon-tests.yml` runs the module suite), `gh pr merge --squash --delete-branch` on green. Backlog move to Shipped and the status file in the same PR.
- Deploy (pre-authorized, one Fly app shared with the siblings): after the merge, probe the live API for YOUR field first; a sibling's deploy may already carry your commit (`flyctl releases -a brisken-expense-recon`). If absent and no release is in progress: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add --detach C:\Users\neuma_p1qrsic\Repo\agentic-ops1-deploy-memory origin/main`, confirm your merge is in `git log -1` there, `flyctl deploy . -a brisken-expense-recon --remote-only` from the module directory, remove that worktree. Then a live API probe and a cold browser read-back.
- Cold browser drives: headless Playwright with `channel="chrome"` (`uv run --no-project --with playwright python <script>`; the sign-in field is `input#code`, wait for localStorage `erc-token`, dismiss "Leave feedback anywhere" with JS `click()`, record every non-GET request and assert only the login), or `agent-browser --session recon-memory` (never the default session). The deploy-consumer gate recognizes Playwright MCP and agent-browser only; a scripted drive must be named as such in the closing text.
- SPA work (M2's `card_key` in Settings, M4's `profile` editor and display): write `docs/lovable-<slug>-prompt.md` (background, exact JSON shapes, render rule, EN + PT-BR strings, the whole-map-replace warning, do-not-change list, checks), add a Not-applied row to `docs/PROMPT-STATUS.md`, and hand the whole prompt in the reply inside a FOUR-backtick fence under the label **"Paste into Lovable:"**. Label every fence: "Paste into Lovable:" or "Continuation prompt for Claude Code:".
- Checkpoint branches: `docs/checkpoint-memory-<n>` in `C:\Users\neuma_p1qrsic\Repo\agentic-ops1-ckpt-memory`, cut off origin/main at checkpoint time; run `uv run tools/repair-session-log.py docs/sessions/<date>.md --check` before pushing (three sessions write the same log today). Remove only the worktrees and branches YOU created; print `git worktree list` and `gh pr list --state open --search memory` at every stop.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while this session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `finalize --root <that worktree>`; write the checkpoint prose after `finalize`; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````
