# Friction Register — Archive

Resolved rows older than the active window, moved out of
`docs/friction-register.md` by `tools/checkpoint_scaffold.py archive-register`.
Same column format. The active register holds the live working set; this file
is the historical record (grep it the same way).

| Date | Client | Type | Description | Resolved? | Fix |
|------|--------|------|-------------|-----------|-----|
| 2026-03-03 | kunde-inc | platform-limitation | n8n Cloud Code node sandbox blocks `this.helpers.httpRequestWithAuthentication()` — must use HTTP Request nodes with predefinedCredentialType instead | Yes | ext-limit |
| 2026-03-04 | kunde-inc | platform-limitation | n8n Cloud Code node sandbox blocks `fetch()` AND `$helpers.httpRequest()` — only HTTP Request nodes work for external API calls | Yes | ext-limit |
| 2026-03-04 | kunde-inc | syntax-bug | Python f-string collapses `{{...}}` to `{...}` — n8n expressions require double braces; use string concatenation to build expression strings | Yes | documented |
| 2026-03-04 | kunde-inc | platform-limitation | n8n Split In Batches typeVersion 1 never fires done output[1]; need typeVersion 3 for the done signal | Yes | ext-limit |
| 2026-03-04 | kunde-inc | platform-gotcha | Google Sheets API 429 when Clear Sheet node receives N items (1 per campaign) instead of 1 — must place Clear before Read Campaigns in flow | Yes | documented |
| 2026-03-06 | meji-media | missed-tool | `/draft` creates messages but doesn't auto-log to comms-log.md — comms log fell 4 days stale across 3 sessions despite messages being drafted via the system. Need `/draft` to auto-append comms-log entries. | Yes | structural |
| 2026-03-09 | meji-media | agent-deferred | Asked user to edit cell K4=TRUE for A2 test setup — before building autonomous cell writer. User explicitly refused further manual edits, leading to creation of `eu2-cell-write.py`. Core gap: Google Sheets read/write MCP. | Yes | structural |
| 2026-03-11 | meji-media | agent-deferred | Asked user to explore phpMyAdmin for table schema when should have attempted autonomous DB connection first. User explicitly flagged: "Why do you need me?" Tried Python connection after being called out — port 3306 was firewalled (legit blocker), but should have tried first before asking. | Yes | structural |
| 2026-03-11 | meji-media | agent-deferred | Wasted ~8 Make.com operations trying to extract MySQL query results by writing to data store via patched investigation scenarios. Make.com execution detail API only returns status, not module output. Should have recognized this limitation earlier and pivoted to website form analysis. | Yes | structural |
| 2026-03-12 | meji-media | missed-tool | Row number estimation failed in Phase 5 interaction test — estimated row 22/23 by counting A1 executions, but no eu2 sheet reader exists to verify actual row numbers. Cell writes went to wrong rows. Root cause: no Google Sheet read capability on eu2 (only eu1 has Sheet Reader fixture). Need to build eu2 sheet reader utility. | Yes | structural |
| 2026-03-12 | meji-media | missed-tool | E2E test plan verified A0→A1→A2→A3 components but never tested the full website→MySQL→A0→A1 path. Assumed website→MySQL link worked without verifying. Investigation revealed A1 is being triggered DIRECTLY by the website webhook (not via A0 at all), meaning the pipeline architecture assumption ("no webhook on form submission") was wrong. This creates a double-send risk: website POSTs to A1 directly AND A0 would also POST from MySQL. Future E2E test plans must include the full trigger-to-outcome path starting from the actual user-facing entry point (website form). | Yes | structural |
| 2026-03-14 | meji-media | missing-verification | No pre-build ops feasibility check — 4 scenarios deployed to 10k ops/month plan without estimating that they'd consume ~201k ops/month. Account got paused. Should have been caught at spec/build time. | Yes | structural |
| 2026-03-14 | system | missing-verification | No onboarding platform feasibility gate — subscription tier, ops limits, and capability blockers were never checked during client setup. No ongoing usage sanity check to compare actual vs plan. | Yes | structural |
| 2026-03-14 | meji-media | missed-verification | Email template plan omitted output verification — user had to point out that rendered output, empty field behavior, and replace chain audit were missing from the plan. ##venue_dates_count## bug would have gone unnoticed without the audit. | Yes | memory |
| 2026-03-14 | platform | agent-deferred | Told user to merge PR manually on GitHub instead of using `gh pr merge` | Yes | memory |
| 2026-03-14 | platform | agent-deferred | Suggested splitting to a separate session for 5 min of infra work (DNS/env vars) — user corrected to stay in same session | Yes | memory |
| 2026-03-15 | platform | agent-deferred | OAuth fix presented as all-manual GCP steps; should have immediately asked for client ID and updated Vercel env directly | Yes | memory |
| 2026-03-15 | system | redundant-output | Checkpoint confirm line duplicated full path as both link text and URL — `[docs/{DATE}...](docs/{DATE}...)`. Link text should be short (e.g. `Checkpoint.md`), path only in URL. User had to flag the disconnect. | Yes | structural |
| 2026-03-15 | platform | delivery-gap | Delivered HTML infographic without baseline QoL features (dark mode, copy buttons, keyboard shortcuts, state persistence). User had to identify missing features. No HTML delivery checklist existed. | Yes | memory |
| 2026-03-15 | meji-media | redundant-escalation | Exceeded 3-iteration limit on Make.com API Gmail scenario execution — tried 6+ approaches (webhook linking, hookId:0, datastore trigger, repeater trigger, bodyType placement) before escalating to "UI step required". Should have escalated after 3 failures. Cost ~45 min. | Yes | structural |
| 2026-03-15 | platform | content-error | Wrote "UnpausAI" (wrong) and "nicholas.neuman@unpauseai.com" (wrong) throughout Phase 4 code, propagated from Phase 1. User had to correct both to "UnpauseAI" and "nicolas.neumann@unpauseai.com". Should verify brand name + contact email from checkpoint at session start. | Yes | structural |
| 2026-03-16 | platform | missed-tool | Used `echo "..." \| vercel env add` which appends trailing newline, corrupting OAuth client ID. Should have used `--value` flag. Shell literacy failure. | Yes | memory |
| 2026-03-16 | platform | agent-deferred | Declared OAuth setup done without testing end-to-end. User discovered the failure. Should have clicked "Continue with Google" or fetched the OAuth URL to verify before moving on. | Yes | memory |
| 2026-03-16 | system | agent-deferred | Assumed user didn't know about parallel terminal workflow and presented it as a "discovery" -- user was already running multiple Claude Code sessions in parallel. Should have asked to see their workflow before assuming. | Yes | memory |
| 2026-03-17 | platform | agent-deferred | Asked user to confirm commit/PR/push after build passed instead of shipping autonomously. Also initially told user to set up Vercel Blob manually instead of doing it via CLI/API. | Yes | memory |
| 2026-03-17 | system | system-dev | Feedback memories not reliably surfaced at session start (4 misses in 4 days). Iteration limit not enforced (6 attempts vs 3 rule). Shell gotchas undocumented. | Yes | structural |
| 2026-03-17 | meji-media | agent-deferred | Asked user to clarify connection swap approach multiple times when the approach (setup scenario like S0) was implied by user's initial request. Over-complicated OAuth discussion instead of just building the automation. | Yes | structural |
| 2026-03-17 | meji-media | agent-deferred | Claimed sending email (enquire@christmasofficeparty.co.uk) was "never confirmed in comms" when it was documented in process-notes.md and 2026-03-05 checkpoint. Failed to search client context files before asking. | Yes | structural |
| 2026-03-19 | meji-media | agent-deferred | Asked user to verify Gmail connection in Make UI instead of checking via MCP tools. Should have checked tools first, stated unavailability, and pivoted. | Yes | memory |
| 2026-03-19 | meji-media | missed-tool | Spent 4+ ToolSearch rounds looking for MCP tools instead of recognizing immediately that the MCP server wasn't connected this session. Wasted time and user patience. | Yes | memory |
| 2026-03-19 | meji-media | agent-deferred | Framed MCP unavailability as a choice for the user ("which route do you want to take?") instead of stating the limitation directly and recommending the fix. | Yes | memory |
| 2026-03-20 | platform | misdiagnosis | Told user autopilot tables were missing when they existed. Error was constraint violation (undefined buildId from empty test payload), not missing tables. Caused two rounds of unnecessary manual testing. | Yes | memory |
| 2026-03-20 | platform | agent-deferred | Previous session deferred Trigger.dev smoke test to user via dashboard link instead of triggering via API. Had API access (deployed successfully). | Yes | memory |
| 2026-03-20 | platform | agent-deferred | Told user to test magic link manually when email verification could be automated via Resend API. Built email-test.py to close gap. | Yes | structural |
| 2026-03-20 | platform | missed-tool | Prod Trigger.dev key already existed in dashboard but wasn't requested. User had to manually provide it. | Yes | memory |
| 2026-03-20 | platform | redundant-escalation | User had to re-test Trigger.dev after wrong diagnosis. Tables existed; error was payload-related. | Yes | memory |
| 2026-03-20 | system | planning-gap | Misread chat naming intent in plan mode — proposed user adopts a convention; user wanted fully automatic renaming. Caught and corrected before implementation. | Yes | structural |
| 2026-03-20 | platform | agent-deferred | Told user to "verify env vars in Vercel" instead of running `vercel env ls` autonomously — gate exists but wasn't applied to deployment config checks | Yes | memory |
| 2026-03-20 | meji-media | missed-context | Comms log entry parsed Zoom scheduling artifacts as a meeting — "The meeting ended (Duration: 15:28)" appeared to confirm a call. All attempts actually failed; no call took place. Lesson: only treat a meeting as confirmed when the user explicitly states the content discussed. Duration alone is not sufficient. | Yes | memory |
| 2026-03-20 | system | skipped-gate | Didn't check permission config before first Edit attempt — user had to flag that permission prompts were friction. B1 should have investigated why edits were being gated. | Yes | structural |
| 2026-03-22 | platform | scope-creep | Asked user to confirm PR merge instead of merging autonomously. Full shipping chain (commit->push->PR->merge) should be one action. User: "where's the disconnect?" | Yes | memory |
| 2026-03-23 | meji-media | verification-theater | Fabricated 6 scoring field names (weight_has_org, weight_message_length, etc.) in lead-scoring.html without querying Pipeline Config data store. User caught it. | Yes | memory (feedback_verify_against_live_data.md) |
| 2026-03-23 | meji-media | verification-theater | Published "< 30s response time" stat on hub page without any source. A0 polls every 30 min; A1 is instant on webhook. The stat was meaningless filler. | Yes | memory |
| 2026-03-23 | meji-media | slow-path | Relative path 404 bug took 3 deploy-test cycles to diagnose. WebFetch returned 200 (cache), user reported 404. Only screenshot of wrong URL (/docs/guide instead of /docs/meji-media/guide) revealed root cause. | Yes | memory (feedback_vercel_static_absolute_paths.md) |
| 2026-05-08 | system | infrastructure-deferred | Hooks (post-write-gate.py) and rules (rule_deliverables.md, rule_behaviors.md) referenced validate-output.py / validate-deliverable.py / lint-comms-draft.py / validate-html.py / rename-chat.py — none existed. Hook silently no-op'd on missing tools. Structural enforcement was hollow. | Yes | structural; built all 5 referenced tools (matching the JSON contract the hook expects) + tools/INDEX.md manifest + friction-watch.py push-based /system-dev trigger |
| 2026-05-08 | system | missed-tool | No tools manifest existed; agent rediscovered tools/ each session. 5+ register entries traced to this gap. | Yes | structural; tools/INDEX.md is the manifest, auto-loadable at session start |
| 2026-05-08 | system | infrastructure-deferred | Self-annealing loop documented in rule_behaviors.md was pull-based (fires only when user types /system-dev or /review). Recurrent friction patterns aged silently — friction-watch surfaced 5 stale items >49 days old on first run. | Yes | structural; friction-watch.py surfaces concentration / memory-sprawl / staleness / recurrence signals from the register, supports --quiet --format json for hook injection |
| 2026-05-08 | system | infrastructure-deferred | Node.js + Vercel CLI not pre-installed despite being load-bearing for two workflows: (a) `mcp-remote` bridge for Make.com MCP server (the original `.mcp.json` config used `npx -y mcp-remote ...` which silently failed because npx wasn't on PATH), and (b) Vercel CLI for any platform observability (deploy logs, env vars, project link). Both came up as blockers in the same session. Also: CLAUDE.md references `tools/vercel-force-deploy.sh` (cited in two rules) but the file doesn't exist in the repo. Pattern: documented infrastructure that was never actually built. | Yes | structural; installed Node.js v24.15.0 LTS via `winget install OpenJS.NodeJS.LTS --silent --scope user` + `npm install -g vercel` + `vercel link --yes --project platform` in platform/. PATH is persistent. Auth `akkton` was already cached. Still TBD: build `tools/vercel-force-deploy.sh` so the CLAUDE.md reference resolves. |
| 2026-05-08 | system | platform-gotcha | PowerShell execution policy (default `Restricted` or `RemoteSigned` for current user) blocks `*.ps1` script wrappers that npm + vercel CLIs ship alongside their `.cmd` versions. Calling `npm --version` from PowerShell errors with "execution of scripts is disabled on this system". Fix: call `npm.cmd` and `vercel.cmd` explicitly (the `.cmd` shims work fine). Bash via Git Bash auto-resolves to `.cmd`. Caught when verifying npm install post-Node.js install. Worth knowing for any future PowerShell tool calls invoking node-based CLIs. | Yes | documented in this register entry; if recurring, save as `feedback_powershell_use_cmd_for_node_clis.md`. |
| 2026-05-11 | system | infrastructure-deferred | Video-script abbreviation rule was added as memory-only (`feedback_video_script_human_language.md`) — relied on agent recall. User flagged it should be "consistently implemented" not just remembered. Per Layer 1 (tool > structural gate > memory), an unenforced style memory drifts. Without a check, future proposals would leak unexplained abbreviations (SKU, CLI, LLM, FAISS, sqlite-vec, JS SDK, vector DB, etc.) into SAY: lines and only catch on manual review. | Yes | structural; added `check_video_script_abbreviations()` to `tools/validate-proposal.py` with a `COMMON_ABBREVIATIONS` exemption set (AI, API, UI, URL, HTML, CSS, JS, JSON, SQL, OS, CSV, HTTP, HTTPS, PDF, CRM, CEO, CTO, B2B, B2C, KPI, ROI, EU, US, UK, USA, EMEA, APAC, TBD, FAQ, etc.) and `EXPLANATION_PATTERNS` (matches `, which`, `, the`, `, a`, `, two`, `(gloss)`, `, essentially`, `, just`, `stands for`, etc.). Scans only SAY: lines; LOOM NOTES section is sliced out before scanning. Flags the first occurrence of any non-exempt abbreviation that isn't followed by a gloss within 120 chars. WARN-severity to avoid retroactively failing legacy proposals; agent action triggered by the WARN per the memory file. |
| 2026-05-19 | platform | infrastructure-deferred | `tools/vercel-force-deploy.sh` is referenced by the ship-gate / Deploy verification gate but had been left uncommitted and broken across 3 sessions. Both live-run bugs (deploy URL parsed via `tail -1` on JSON; `vercel ls` piped `2>/dev/null` discarding the stderr table) found and fixed; script committed + indexed (PR #16). Detected by agent. Regression? Yes (2026-05-15 "built the script but left it uncommitted" did not hold — uncommitted infra was treated as done). | Yes (2026-05-19) | structural (committed + tools/INDEX.md entry; principle: uncommitted infra = not built; a CLI-wrapping tool must be run live before a gate depends on it) |
| 2026-05-19 | system | infrastructure-deferred | ROOT CAUSE of a user-reported "increase in friction + degradation in output quality". The ENTIRE structural enforcement layer (all 9 `.claude/hooks/*`: stop-b1, em-dash-strip, post-write/validate, reference-anchor B4, instantly-invasive B5, post-action ship-gate, gate-skip, input-classifier, auto-approve) was silently DEAD on this machine since 2026-05-18 ~15:41. Cause: hook wiring was committed to TRACKED `settings.json` in `974c6bb` with machine-specific absolute paths (`C:/Users/neuma/Downloads/...` + bare `python`); commit `20eb235` "Sync ... before switching devices" stripped it (49 deletions) back to PreCompact+SessionStart; the working-tree copy ran until settings.json was overwritten at 15:41:22 (last hook fire 15:40:46 — exact correlation), and the gitignored `settings.local.json` on this device never carried a hooks block. Every "structural" fix operationalized as a hook was inert; rules promised backstops that weren't running (matches register #75 em-dash regression + recurring 73/74 closing-offers reaching the user). Self-anneal Layer 2: initial diagnosis ("stop-b1-gate too blunt, add classifier") was verification-theater — read 12 days of historical hook-log without checking CURRENT wiring; corrected by inspecting settings.json/.local.json before coding (B3 attribution: questioned own reading of stale state). Detected: user-reported symptom, agent-diagnosed root cause. Regression of the device-portability class in 2026-05-08 entry #67 (documented-infra-never-built). | Yes (2026-05-19) | structural — (1) re-wired all 9 hooks into gitignored `settings.local.json` (correct home for machine-specific config) using PORTABLE `uv run python .claude/hooks/X.py` relative paths (cwd=project root), so a device sync can never silently kill it again AND it survives the `python`-not-on-PATH issue; (2) rewrote `stop-b1-gate.py` (tracked) from unconditional-block (279 blocks/12d, 100% false-positive, the per-turn tax = the felt friction) to a transcript-reading deferral classifier — blocks only on real closing-offer/ask-permission phrasing, fail-open on any parse error, decisions logged `BLOCK/ALLOW:*` for ongoing false-positive measurement; 10/10 behavioral smoke (clean/genuine-question/code-quoted allow, "want me to"/"should I deploy" block); PostToolUse:Bash hooks confirmed LIVE-firing in the same session. Resolves the 2026-05-08 #65/#68 stop-b1 classifier TBDs and the 73/74 "memory not holding, hook is only backstop" — hook is now precise instead of blunt. |
| 2026-05-19 | system | infrastructure-deferred | RESOLUTION of the large-scale self-anneal continuation (STEP 1 + F2 + F3). STEP 1 (PR #31): `tools/wire-hooks.py` (TRACKED) carries the canonical 9-hook block as source of truth; a machine-agnostic SessionStart hook runs `--ensure` every session on every device, so the gitignored-`settings.local.json` cross-device gap from the 2026-05-19 root cause can no longer leave any device with zero enforcement past one session start. F2 (PR #33): killed 4 enforcement-classifier false-positives (3 observed live this session): pre-publish now recognizes py_compile/`--check`/hook-tests/json.load as validation; publish-verbs inside quoted strings or hook-test payloads no longer count; iteration-3x excludes read-only/idempotent repeats; `stop-b1-gate.strip_code()` blanks double-quoted example spans (resolves the row directly above, the F2-deferred meta-text blind spot). F3 (this PR): `validate-output.py` gains `unsourced-claim` (HIGH) firing via post-write-gate on comms drafts/deliverables when a flat client-facing PROBLEM-claim has no source attribution within ±2 lines; also fixed a pre-existing latent bug where the `<`/`>` symbol branches of two `unverified-claim` stat rules silently never matched (leading-`\b` before a non-word literal), so the 2026-03-23 #15 `< Ns response time` shape they exist to catch was a no-op. Verified behaviorally: STEP 1 5/5, F2 10/10, F3 9/9 (incl. the exact register #7 message text flagged HIGH). Resolves register #7 (verification-theater, memory-failed-twice) and the 2026-03-23 #14/#15 entries with the structural gate the #7 resolution column prescribed. | Yes (2026-05-19) | structural — memory→tool escalation per Layer 1: the claim-verification rule that failed as memory twice is now a write-time HIGH gate on the exact artifact scope (comms/deliverables); rule_behaviors.md B4 updated to cite the now-real backstop; tools/INDEX.md updated. |
| 2026-05-19 | local-web | verification-theater | First ERR_CONNECTION_RESET fix (relative-redirect) declared done on a server-side `curl` 200. `curl` has no cache, so it could never reproduce the user's actual failure: a persistently-cached bad `301` in their browser. User-detected ("it still wont work"). Gate B2 (tested server state, not the client behavior the bug lived in). | Yes (2026-05-19) | structural — skil_web-build §6 hardened (PR #25/#27): emit no 301 at all (serve index directly); "a cached-redirect bug is invisible to curl — reproduce the client path or use a fresh profile, never declare fixed on a server 200 alone." Partial regression of the verify-behavior-not-state principle in a new (client-cache) manifestation. |
| 2026-05-19 | local-web | slow-path | `cd` into `app/src` made relative hook paths (`.claude/hooks/...`) resolve against the subdir, so PreToolUse hooks errored and blocked an Edit; needed a PowerShell `Set-Location` recovery. Agent-detected. Gate none. | Yes (2026-05-19) | documented — never `cd` into subdirs when hooks use repo-relative paths; use `npm --prefix` / `git -C` / absolute paths. Shell cwd persists across Bash calls and is shared with hook resolution. |
| 2026-05-19 | system | agent-deferred | First response on the PDF-protocol rule fix ended "Want me to make that edit?" — a bounded, low-blast-radius autonomous action offered instead of taken. Agent-detected (the `stop-b1-gate.py` Stop hook fired and flagged the deferral pattern); edit was done immediately after. Gate B1. Regression? No — prior agent-deferred fixes were memory/structural for different triggers; the B1 stop-hook (the structural backstop) functioned exactly as designed and self-corrected it same-turn. | Yes (2026-05-19) | structural (held) — no new fix needed; the existing stop-b1-gate classifier caught it. Confirms the 2026-05-19 stop-hook rewrite is doing its job on a real soft closing-offer. |
| 2026-05-19 | platform | infrastructure-deferred | Meji doc-site `/docs/meji-media/build-plan` + `/volume-forecast` returned hard Vercel 404 while same-commit `system-overview` served; root cause = production alias on a 1-day-stale build (Vercel git-integration auto-deploy lagged behind main), fixed by manual `vercel-force-deploy.sh` (new prod platform-3snvisd60, live-verified). Agent-diagnosed (clean B3: full headers, working-sibling compare, own-attributable infra before blaming gate/git). Gate none. Regression? Recurring stale-prod class (deploy-verification rule step 6 exists for it). | Yes (2026-05-19) | structural-candidate — pages restored; underlying lag unresolved. /system-dev: confirm whether main-merge auto-deploy is reliable or the ship-gate must always run vercel-force-deploy.sh after a platform merge. |
| 2026-05-20 | platform | missed-memory-recall | About to present a comp-framing question for p025 that included "anchor higher hourly (€60-65/h, justified on UnpauseAI's USD 95-120/hr agency default)". User pre-empted with "my rate is 36-50$/hr unpause ai does not have an agency hourly rate. just remember that." The "USD 95-120/hr agency default" line in workspace/projects/platform/upwork-agency/profile-copy.md is stale; no rule or memory caught it before I baked it into the option. Detected by user. Gate B4 (writing data values into a deliverable). | Yes (2026-05-20) | memory (user_rates_unpauseai.md, Matthias $36-50/hr USD, no UnpauseAI agency hourly, profile-copy.md $95-120 line is stale). Fragile fix, structural alternative: edit profile-copy.md to remove the stale rate so it can't seed future agents. |
| 2026-05-20 | system | verification-theater | Verified Resend send via `curl` from local, shipped routine sent via Python `urllib` whose default User-Agent is Cloudflare-403-blocked (code 1010) at api.resend.com. Scheduled 06:05 firing executed (last_fired_at confirmed) but no email arrived. User-detected ("it wasn't sent at 6AM"). Gate B2 (verify via the exact mechanism shipped, not a proxy). Fixed by explicit User-Agent header + moving send logic to committed `tools/morning_briefing.py` so the same code path can be locally tested. Regression? Yes (2026-05-18 verification-theater on stale Lighthouse JSON — same class, structural-fix-on-skill did not generalize). | Yes (2026-05-20) | structural (committed script + loud-failure design: agent emails own traceback; principle: unattended remote jobs must be deterministic scripts, not LLM heredoc execution) |
| 2026-05-20 | system | verification-theater | Designed full email-delivery flow around the claude.ai Gmail connector without verifying a `send_message` primitive existed. Connector only exposes drafts/labels/read — no send. Discovered only after user reported "it didn't work". Gate B2 (didn't verify the delivery primitive before building around it). | Yes (2026-05-20) | documented (memory project_morning_briefing.md gotcha #2; principle: enumerate the available tool surface of any connector BEFORE designing the integration) |
| 2026-05-20 | system | verification-theater | Asserted GitHub connection was a hard prerequisite for the remote routine, then over-corrected to "not required because the repo is public" without testing. Platform's source-gate runs the GitHub access check on any declared `git_repository` source EVEN for public repos — second assertion wrong too. User-prompted both corrections. Gate B1 (verify limitations before asserting) + B3 (read full error: 403 github_repo_access_denied response was explicit). | Yes (2026-05-20) | documented (memory gotcha #1; workaround: `sources: []` + self-clone via `git clone` in prompt) |
| 2026-05-20 | system | over-literal | Instructed user to sign up to Resend with `neumath4@icloud.com` to enable icloud delivery without first verifying Resend's free-tier rule (no verified domain → only deliver to account-owner address). User had already signed up with their Gmail; recipient pragmatically switched to gmail. Detected when first `curl` test returned the validation error stating "matneumann07@gmail.com" was the only allowed recipient. Gate B3 (would have caught with one minute of doc-reading before instructing). | Yes (2026-05-20) | documented (memory gotcha #3) |
| 2026-05-20 | local-web | verification-theater | Initial `tools/depth-probe.cjs` used `gl.readPixels` on a WebGL context created with `alpha:false` but no `preserveDrawingBuffer` to assert "canvas drew the photo" — but post-composite reads are undefined per the WebGL spec, so coffee/pronto produced false fails (mean 0, std 0) while pronto's hero visibly rendered the pizza in the screenshot. Agent-detected when the composited screenshot contradicted the readPixels heuristic. Gate B2 (verification method couldn't actually detect the failure mode it was checking). Regression? Yes (verification-theater is a recurring class; 2026-05-19 local-web + 2026-05-18 server-side checks + earlier sessions). | Yes (2026-05-20) | structural — `tools/depth-live.cjs` codifies the working full-page `captureBeyondViewport` pattern; `skil_web-build` 4b documents the transferable lesson ("for WebGL canvases trust composited screenshots; never readPixels a non-preserveDrawingBuffer context"). Bespoke `depth-probe.cjs` deleted. |
| 2026-05-20 | local-web | slow-path | Burned ~6 iterations hardening a bespoke CDP-`clip`-after-`scrollIntoView` screenshot probe (returned blank cream regions because clip-vs-viewport coordinate semantics are ambiguous after scroll) before reverting to the proven full-page `captureBeyondViewport` pattern already in `local-web-shot.cjs` from earlier the same session. Agent-detected after the 3rd blank-screenshot iteration. Gate none. Regression? No (first log of this specific class). | Yes (2026-05-20) | structural — `tools/depth-live.cjs` is the codified working pattern; skill points to it for future 4b deploys; rule: when an established working pattern exists in `tools/`, do not write a parallel bespoke probe. |
| 2026-05-24 | meji-media | over-literal cluster | Four related instances in one session, same transferable principle: (1) speculated beyond Gurmej's stated 3 cities (added "and out from there" + a UK-wide-vs-regional open question); (2) misread Gurmej's "EAs in 3 cities" as a separate cold-list build, claimed scope had doubled to ~12-14 hrs (user: "we are not doing a cold list on christmas"); (3) embedded 4 client-review questions, 3 of which asked about items Gurmej had already defined (user: "no overload in questions, everything that he has already defined we shall do the way he defined"); (4) initial 3-touch cadence draft paraphrased rather than lifting Gurmej's verbatim Instantly phrasing (user: "use the vocabulary already implemented by gurmej on instantly as a language reference"). All user-detected. Gate: none direct (closest is B4 fidelity, which covers data values not directives). Consolidated principle: anchor on the client's actual words; don't paraphrase, speculate, or re-ask what they've defined; lift voice verbatim. | Yes (2026-05-24) | memory (new `feedback_anchor_on_clients_words.md` operationalizes the principle). Fragile fix — consider structural alternative: a comms-draft post-write linter that flags (a) scope-expansion phrases beyond the client's stated terms ("and out from there", "or wider", "UK-wide vs"), and (b) review questions about items the client has already explicitly defined (cross-ref against parsed client directives). |
| 2026-05-25 | brisken | agent-deferred | Ended a turn with "If you want, the natural next move is to drive the three §38 research items..." — a closing-offer deferral on autonomous, bounded work (the §38 research items are read-only WebFetch/WebSearch synthesis I can do). `stop-b1-gate` hook fired; I corrected by dropping the offer and stopping cleanly. Hook caught it before user. Gate B1. Detected by agent (hook). Regression? Recurring — feedback_no_closing_offers.md notes this hook fires "5+ times in a single session" (2026-05-08). Each fire still costs a turn even when no user-visible failure. | Yes (2026-05-25) | memory (existing feedback_no_closing_offers.md). Fragile fix — the hook fix works but doesn't prevent generation; the pattern is now embedded enough in agent behavior to consider a stronger structural prevention (e.g., a draft-time linter on the final paragraph before flush, not just a post-flush hook). |
| 2026-05-25 | meji-media | unsolicited-client-ask | Created `reply-to-gurmej-piece1-sender-mailbox-2026-05-24.md` asking Gurmej to confirm sending from gurmej@mejimedia.com — an internal operational decision he didn't need to weigh in on. User questioned its purpose, file deleted, decision locked internally. Gate B1. Regression? Yes — regression of `feedback_anchor_on_clients_words` principle (captured 2026-05-24). Memory exists but didn't prevent recurrence within 24h. | Yes (2026-05-25, deleted same session) | memory (existing feedback_anchor_on_clients_words). Fragile fix — memory failed within 24h of being saved. Structural alternative: a comms post-write gate flagging client questions where the answer is obvious + Matthias-decidable (would have caught the sender-mailbox question). |
| 2026-05-25 | meji-media | agent-deferred | "Want me to (a) draft a short separate ops note to Gurmej confirming X, (b) start the Piece 2 Apollo filter spec, or (c) hold..." closing-offer pattern. stop-b1-gate hook fired and flagged the deferral; corrected by executing the bounded autonomous actions. Gate B1. Regression? Recurring (closing-offer pattern is a frequent flier per feedback_no_closing_offers; multiple fires in earlier sessions). Hook caught it before user. | Yes (2026-05-25, self-corrected via hook) | structural (held) — stop-b1-gate caught and re-prompted. Each fire still costs a turn even when no user-visible failure. |
