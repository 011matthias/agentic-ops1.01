# Review: 2026-07-12

First `docs/reviews/` artifact (the cadence signal in `weekly_synthesis.py` /
`friction-watch.py` fires while this directory is empty; this closes it). Paired
with the `/comd_system-dev` build cycle that shipped as PR #214 the same day.

## Data Coverage

- **Session logs:** 37 day-files, 2026-03-03 → 2026-07-11; sum `friction_events` = 235.
- **Build logs:** 0 (`workspace/*/context/build-log.md` not in use this period; error/efficiency-pattern sections below are N/A).
- **Friction register:** 370 rows, 219 resolved. Unresolved = **83** on the parser-true basis (`friction-watch`/`anneal-metrics`), or 150 raw-prefix if every `No (caught by hook)` row is counted as unresolved. The gap is almost entirely hook-contained B1 closing-offers, which the stop-gate catches every time.

## Top Friction Patterns

Unresolved-by-type (raw-prefix basis, most frequent first):

| Type | Unres | Reading |
|---|---|---|
| agent-deferred | 35 | Overwhelmingly B1 closing-offers; `stop-b1-gate` catches 100%. Residual is generation-time discipline, not a missing backstop. |
| slow-path | 25 | Mostly `cd`-in-compound-Bash; now hook-handled (`cd-guard`, fired correctly this session). |
| infrastructure-deferred | 19 | Contained the one loud unbuilt item (sibling-guard) — now shipped. |
| verification-theater | 17 | Judgment-layer; no single structural kill. Two 2026-07-11 instances (wrong-mailbox drafts, false "invite never sent") now have a diagnostic tool. |
| skipped-gate | 9 | Read-target-before-delete recurred 2026-07-12 (Planner task); self-caught. |
| over-literal / missed-memory-recall / strategic-gap / intent-misalignment | 8/7/6/6 | Layer-3 intent drift; memory-recall dependent. |

### ROI-ranked (the actionable set) + disposition this cycle

| # | Pattern | Source | Freq | Effort/occ | Fix hrs | ROI | Status |
|---|---|---|---|---|---|---|---|
| 1 | Live sibling-session collisions | infra-deferred, 6x logs | 5 | 20m | 2.5 | 40 | **SHIPPED #214** — session_registry + 14th hook |
| 2 | CLAUDE.md count drift | toolkit-drift | 2 | 5m | 0.3 | 33 | **SHIPPED #214** — drift 3→0 |
| 3 | `connect_over_cdp` 180s hang | infra-deferred | 3 | 10m | 1 | 30 | **SHIPPED #214** — `tools/edge_cdp.py` |
| 4 | Dirk wrong-mailbox / false send-claim | verif-theater | 3 | 15m | 2 | 22 | **SHIPPED #214** — `tools/brisken-mailbox-watch.py` |
| 5 | B1 closing-offer reflex | agent-deferred, 35 | 5 | 1m | n/a | n/a | **HELD** — gate works; changing = accretion |

## Error Distribution

N/A — no build logs this period.

## Work Type Distribution

50 `work_types` tags across the 37 day-files:

| Type | Tagged | % |
|---|---|---|
| client-dev | 19 | 38% |
| system-infra | 15 | 30% |
| comms | 12 | 24% |
| misc | 3 | 6% |
| system-eod | 1 | 2% |

Roughly a third of tagged work is system-infra (self-annealing) — high for a client-delivery shop, consistent with the harness-hardening push across 2026-07-10..12.

## Recommendations

1. **Done this cycle** (PR #214): patterns 1–4 above. B1 (pattern 5) deliberately untouched.
2. **Live-verify the two client-specific tools** before trusting their output: `edge_cdp.py` against a real Edge on :9222, `brisken-mailbox-watch.py` against the live Outlook profile. Both are promoted from proven `.scratch/` code but their CDP/COM paths were not exercised against live systems in the build session.
3. **3 rule files over the 250-line per-file ceiling** (`rule_client_page_structure` 273, `rule_human_communication` 273, `rule_platform_standards` 267) — split candidates flagged by `anneal-metrics`; low urgency.
4. **verification-theater (17)** has no structural kill and keeps recurring — a candidate for a dedicated "verify behavior, not state" gate next cycle, though it is largely a judgment failure the hooks cannot see.

## Since Last Review

None — this is the first `docs/reviews/` artifact. Baseline for future diffs: 370 register rows / 83 parser-true unresolved; recurrence 0.8%; work split client-dev 38% / system-infra 30% / comms 24%.
