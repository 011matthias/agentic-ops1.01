---
date: 2026-09-22
sessions: 1
projects_touched: [brisken]
friction_events: 0
work_types: [client-dev]
---

### Session 1 — Verve Beta Boundary Testing (mini)
**Type:** client-dev
**Focus:** Delivered Zoho Books injection test payloads for Brisken TEST-BTS and audited the other agent's bookings live (4 of 6 wrong GL account, one custom field silently dropped); then boundary-tested the Verve beta with a CDP harness: answers and security held, integration scope is far broader than the stated TEST-BTS constraint, first GenUI build fully functional.
**Projects:** brisken
**Built:** .scratch/zoho-injection-test/{suite.json,batch-method2-v2.json,read_*.py,prep_fix.py}; .scratch/verve-testing/{cdp.py,run_ask.py,FINDINGS.md,genui.txt}; owner-approved Verve scheduled task ivYE6CEWOdfK0Ul0q1Xa; runtime API tool nodejs-releases; GenUI node-release-dashboard
**Did NOT work:** Playwright MCP against the user's Chrome :9222 (why not: initializeServer timed out at 30s twice (busy-browser CDP hang); raw websocket CDP with suppress_origin=True worked); Synthetic KeyboardEvent Enter to submit the Verve composer (why not: never submitted; needs real Input.dispatchKeyEvent on the visible textarea (index 2), not the hidden mirror at index 0); Text-stability polling as Verve completion signal (why not: declared done at 5s before generation started; use the aria-label 'Stop generating' lifecycle (plain 'stop' text also matches a GenUI build card and stalls every turn)); Auto-declining Verve approval cards (why not: killed the browser-extraction and deep-research turns; owner directed approvals be routed to them, harness now halts on cards); Pre-send idle wait that sends anyway after 45s (why not: prompts swallowed by a busy composer; now returns busy and the runner retries); Verve's live Zoho read from the Home space (why not: 'Zoho Books expense operations couldn't finish' then a phantom update_expense card re-surfacing twice; Verve-side bug, unresolved); Other agent's Zoho injection tool resolving account names (why not: silently substituted a default account for unresolvable names instead of failing, and ignored two names that did exist)
**Friction:** None promoted (2 heredoc-size gate fires discarded: gate worked)
**Outcome:** Verve round 1 documented (bugs B1-B7, findings I1-I5, R1-R5); Brisken TEST-BTS: 4 PUT fixes + case 06 + cf_netting_key_exp pending on the other agent
