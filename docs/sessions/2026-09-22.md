---
date: 2026-09-22
sessions: 2
projects_touched: [brisken]
friction_events: 0
work_types: [client-dev]
---

### Session 1 — Zoho Month-End Injection (mini)
**Type:** client-dev
**Focus:** Dirk needs the reconciled month imported properly into Zoho Books at month end, reversing his own 2026-08-22 'no ties to Zoho' directive; the owner chose API injection over a CSV hand-off. Restored and re-gated the deleted write path, and a read-only probe overturned two premises the scoping summary and this repo both held.
**Projects:** brisken
**Built:** PR #1187 (merge c04740b1): zoho/client.py, zoho/idempotent.py (the 4.8 no-double-post ledger), zoho_post_cli.py and the zoho-post dispatch restored from 5e2ff99e^ and repointed at the post-#881 posting_common homes; client gains create_expense + list_contacts (an unconfirmable create raises, so a re-run cannot double-post; contact filtering client-side because a filter Zoho ignores answers for the whole workspace). test_no_zoho_connection.py superseded by test_zoho_posting_is_gated.py, keeping the two guarantees that survive the reversal (the hosted web layer can never reach Zoho, so no deploy writes into Criss's months; a run config still cannot pull a chart mid-run) with test_the_import_guard_discriminates pinning both directions. Backlog item 23 rounds 4 and 5 marked OFF before they deleted the machinery. docs/zoho-month-end-posting.md; memories project_brisken_zoho_books and reference_brisken_zoho_expense_orgs corrected. Suite 2801 passed / 2 skipped, both fixes regress-checked TEST BITES, ruff clean, 8/8 CI green.
**Did NOT work:** Reading pytest's result off a backgrounded 'pytest -q | tail -25' (why not: A pipeline reports the LAST command's status, so the harness announced 'exit code 0' for a run that was actually 3 failed / 2792 passed; the output file also stays empty until exit, so it cannot be polled. Re-run unpiped and the exit code is pytest's.); A 'python - <<PY' heredoc carrying a triple-quoted block (why not: Refused by heredoc-size-gate as the documented 'unexpected EOF' shape; the shell tokenizer gives up on nested triple quotes. Used the Write tool plus 'python <file>' instead.); The first web-layer import guard pattern, [.\w]*zoho[.\w]* (why not: Matched output.zoho_export, which the web layer legitimately imports to serve the CSV download. Narrowed to the posting modules only and pinned both catch and ignore directions.); gh pr merge --delete-branch from the feature worktree (why not: Printed "fatal: 'main' is already used by worktree at ..." because the sibling session holds main. The merge itself succeeded (MERGED, c04740b1); only the local checkout and branch cleanup failed, so the remote branch had to be deleted separately.); An Edit whose old_string spanned backlog item 23's 'Owner direction' paragraph (why not: The replacement omitted it, silently deleting the 2026-08-22 directive record. Caught by grepping for the quoted directive after the edit and restored; git numstat confirmed only the intended lines changed.)
**Friction:** None promoted (mini; 4 gate-fired candidates left for the next full checkpoint, all gates working correctly)
**Outcome:** Write path merged and dormant by design. Live posting blocked on an owner scope grant (the token is read-only); the Criss entry handoff is the real risk, since there is no feed and July is already fully hand-entered.

### Session 2 — Zoho Sandbox Posting Guards (mini)
**Type:** client-dev
**Focus:** Second slice of the Zoho month-end work: numeric account_id resolution, the month-occupancy guard and the guarded sandbox reset, plus a fix to a flaw the owner's 'rehearse real months starting with July' request exposed in the guard written an hour earlier.
**Projects:** brisken
**Built:** PR #1189 (merge 0e4beb95, 8/8 CI green): zoho/accounts.py resolves a reference to a real account_id or refuses naming why, with no default branch (the trial's 6-of-9 rows silently defaulted to Office Infra and Admin because Zoho accepts a NAME with a 201 and assigns its own); posting_common.resolve_ref so the file exports and the API poster share one resolution order; zoho/occupancy.py refuses a month someone else already entered by asking Zoho rather than our ledger, with UNVERIFIABLE as its own verdict so a failed query never reads as an empty month; zoho/orgs.py as a leaf module admitting TEST-BTS to an allowlist a config can only intersect; zoho/sandbox_reset.py + client.delete_expense, guarded by equality on the sandbox org before any read. The July lock is now production-scoped: what makes July dangerous is 118 hand-typed rows that exist only in the real orgs, so a global lock would have blocked the very rehearsal that de-risks the real run. Suite 2860 passed / 2 skipped (from 2801), three regress checks green-to-red-to-green, ruff clean. Driven live read-only: occupancy gave three distinct verdicts across four cases, and 196 of 196 TEST-BTS accounts carry ids. Grant exchanged; old refresh token preserved as a comment in the gitignored .env.
**Did NOT work:** Deleting the TEST-BTS test rows, as the owner asked (why not: The re-consented grant carries accountants.READ, expenses.READ, expenses.CREATE and contacts.READ but NOT expenses.DELETE. Checked by calling rather than assumed, since the owner had said they were widening it. The guarded dry run is built and enumerates all 10 rows by id; it runs the moment the scope exists.); The first occupancy design, a global 2026-07 lock (why not: It would have refused the sandbox July rehearsal the owner then asked for. The lock's justification was always production-specific (118 hand-entered rows exist only in the real orgs), so it is now scoped by is_production_org, which is a positive list rather than 'anything that is not the sandbox' so a typo'd or newly-cloned org cannot inherit production's protections.); Two full-suite runs launched against an already-stale tree (why not: Both were started before later edits landed, so their results could not be used and both were killed with TaskStop and re-run. Launch the suite after the last edit of a slice, not during it.); gh pr merge --delete-branch from a worktree, for the second time today (why not: Fails with "fatal: 'main' is already used by worktree" because a sibling session holds main; the merge succeeds but the local checkout and branch cleanup do not. Merge without the flag and delete the remote branch separately.)
**Friction:** None promoted (mini; gate-fired candidates left for the next full checkpoint)
**Outcome:** Guards merged. expenses.CREATE granted but expenses.DELETE not, so the sandbox cannot be emptied; that turned out not to be on the critical path because July in TEST-BTS is already CLEAR (the 10 rows are Aug/Sep dated). Remaining gap to a July rehearsal is the payload builder, which is blocked on one owner decision: which July data feeds it.
