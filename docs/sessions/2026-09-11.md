---
date: 2026-09-11
sessions: 3
projects_touched: [brisken]
friction_events: 5
work_types: [client-dev]
---

### Session 1 — Cost Centers Step 2, Criss's July Receipts, Item 50 Closed
**Type:** client-dev
**Focus:** Backlog item 47 step 2 of 6: the three carriers of a cost center (card, merchant registry entry, trip) and the resolution of that chain onto every expense row, with the override riding the existing field-override path. Alongside it Criss's mailbox came unblocked and holds the July receipts three sessions were hunting, and item 50 turned out to be half closed already.
**Projects:** brisken
**Built:** cards[].default_cost_center through all five card code points; merchants[].cost_center on the entry and MerchantMatch; a real cost_center column on trips plus its idempotent migration; resolve_batch_row_cost_centers emitting cost_center / cost_center_source / cost_center_source_label / needs_cost_center per row; n_needs_cost_center and cost_center_options; cost_center in EXPENSE_HEADER_FIELDS validated against the registry. 29 tests (test_cost_center_carriers.py, test_cost_center_resolution.py), suite 1535 to 1564, six wiring points regress-checked green-red-green. Re-pinned test_view_contract.py + docs/api-contract.md in the same round. Separately: Criss's mailbox re-probed OK (32606, dirk+matthias controls in the same run), 81 folders swept, 19 July receipt PDFs recovered for 20 of the 97 uncovered charges; Fly memory raised to 1024MB and verified; six stranded commits pushed.
**Friction:** 2 - verification-theater (two self-caught near-misses of the same shape: a contract pin that passed against an empty list so the element kind was never observed, and the empty-registry guard implemented in two places so a mutation of either left the suite green), slow-path (four tool calls to guards firing on shapes the register has carried since August, plus an MSYS path-form slip and a guessed route)
**Gates:** B1:2 B2:8 B3:2 skipped:0
**Autonomy:** 2 human interventions
**Outcome:** client/brisken/p1-cost-centers at 1e9fea1d, pushed, four commits ahead of main, no PR yet. Suite 1564 passed / 2 skipped, tree clean. Steps 3-6 open; the July ingest waits on an explicit per-action go.

### Session 2 — Expense-Recon xlsx Sign Defect and Statement Re-read
**Type:** client-dev
**Focus:** Criss's July/August reconciled 0 because the Excel statement parser kept Chase's printed sign (purchases negative) while the CSV parser had canonicalized it since 3.15. Fixed the parser, mapped Chase's Type column, made an entity-less receipt unscoped in the matcher, added POST .../statements/reread, deployed v114, and re-read both live months: August 0 -> 14 reconciled + 7 review, July 0 -> 24 + 15, SPA-driven. Ten-void list brainstormed for a fresh session (items 1 and 2 first).
**Projects:** brisken
**Built:** PR #805 (statement_xlsx sign canonicalization, inspect type heuristic, matcher empty-entity rule, reread route + service + store.rekey_decisions, fly.toml re-synced to live, test_statement_reread.py, api-contract section, backlog items 55/56 + Shipped row 31); Fly v114; live re-read on 074a7b8905d7 + 50622baec444
**Friction:** 2 — agent-deferred (brainstorm closed with an open offer; stop hook B1 caught it; rephrased as a decision via AskUserQuestion), missed-memory-recall (gh pr create used slug akkton/agentic-ops1 despite reference_repo_tooling_gotchas naming 011matthias/agentic-ops1.01; one retry). Discarded: Playwright MCP tried first (Edge CDP :9222 not running) then agent-browser — the sanctioned drive worked on the fallback. Stale memory corrected: 'no fly.toml in the repo' was false and would have regressed the platform config on a source deploy.
**Gates:** B1:1 B2:4 B3:1 skipped:0
**Autonomy:** 1 human intervention
**Outcome:** Fix live (v114), both months repaired and rendering; void list 1-10 queued for a fresh chat starting with 1+2

### Session 3 — Expense-Recon Void List Round 1
**Type:** client-dev
**Focus:** The ten voids from the morning session became backlog items 57-64; round 1 shipped (PR #811, Fly v115): summary.month_health refuses readiness on a month the matcher could not see and names the broken input (sign / currency / entity / card), and every rematch_month commit leaves an event the dev notifier mails as one line. Items 59 and 56 go to the owner as decisions before code.
**Projects:** brisken
**Built:** web/month_health.py + ready_to_post wiring on both payloads; rematch_log -> /api/operator/state rematches[] -> brisken-recon-notify.py one line per event; test_month_health.py (9), test_rematch_log.py (4), notifier tests (+3), contract pins; three regress proofs RED first; docs/api-contract.md, lovable-month-health-prompt.md, PROMPT-STATUS row; backlog items 57-64 + Shipped row 32; status file paragraph + elements row
**Friction:** 1 - slow-path (cd-guard refused `cd <worktree> && ...` twice, same class as the 2026-09-11 morning row; a POSIX path in a Python string on Windows; ~70 small sequential context reads at resume). Two external limits reported, not friction: github.com refused connections twice; the Lovable custom-domain host refused connections at close so the SPA drive is pending.
**Gates:** B1:0 B2:5 B3:1 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Round 1 live at Fly v115 and API-verified on July and August (both `ok`, 18 and 14 exact pairs); SPA drive pending on the Lovable host; notifier re-match lines start once the main checkout is pulled; rulings for items 59 and 56 open
