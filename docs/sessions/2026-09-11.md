---
date: 2026-09-11
sessions: 1
projects_touched: [brisken]
friction_events: 2
work_types: [client-dev]
---

### Session 1 — Cost Centers Step 2, Criss's July Receipts, Item 50 Closed
**Type:** client-dev
**Focus:** Backlog item 47 step 2 of 6: the three carriers of a cost center (card, merchant registry entry, trip) and the resolution of that chain onto every expense row, with the override riding the existing field-override path. Alongside it Criss's mailbox came unblocked and holds the July receipts three sessions were hunting, and item 50 turned out to be half closed already.
**Projects:** brisken
**Built:** cards[].default_cost_center through all five card code points; merchants[].cost_center on the entry and MerchantMatch; a real cost_center column on trips plus its idempotent migration; resolve_batch_row_cost_centers emitting cost_center / cost_center_source / cost_center_source_label / needs_cost_center per row; n_needs_cost_center and cost_center_options; cost_center in EXPENSE_HEADER_FIELDS validated against the registry. 29 tests (test_cost_center_carriers.py, test_cost_center_resolution.py), suite 1535 to 1564, six wiring points regress-checked green-red-green. Re-pinned test_view_contract.py + docs/api-contract.md in the same round. Separately: Criss's mailbox re-probed OK (32606, dirk+matthias controls in the same run), 81 folders swept, 19 July receipt PDFs recovered for 20 of the 97 uncovered charges; Fly memory raised to 1024MB and verified; six stranded commits pushed.
**Friction:** 2 - verification-theater (two self-caught near-misses of the same shape: a contract pin that passed against an empty list so the element kind was never observed, and the empty-registry guard implemented in two places so a mutation of either left the suite green), slow-path (four tool calls to guards firing on shapes the register has carried since August, plus an MSYS path-form slip and a guessed route)
**Gates:** B1:2 B2:8 B3:2 skipped:0
**Autonomy:** 2 human interventions
**Outcome:** client/brisken/p1-cost-centers at 1e9fea1d, pushed, four commits ahead of main, no PR yet. Suite 1564 passed / 2 skipped, tree clean. Steps 3-6 open; the July ingest waits on an explicit per-action go.
