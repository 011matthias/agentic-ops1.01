---
date: 2026-09-19
sessions: 1
projects_touched: [brisken]
friction_events: 5
work_types: [client-dev]
---

### Session 1 — Brisken Recon Traceability Round Close
**Type:** client-dev
**Focus:** TRACEABILITY session of three parallel sessions on the owner's 2026-09-18 note, closed: a charge carries a record of which statement upload printed it and where (T3), a receipt is named the same way on every surface (T1), and the mail archive was scanned to settle what the intake should and should not be taught to read (T4).
**Projects:** brisken
**Built:** T3 backlog 152 (PR #1107, merge b1fe1d28, Fly v188): snapshot key statement_origins; statement_file / statement_id / source_row / source_page on rows[] and expenses[] plus transaction_id from the effective settlement; Chase PDF parser records the page; statement_id columns on decisions, decision_history and receipt_claims resolved in-store; item 72 marked closed by 103. T1 backlog 153 (PR #1109, merge 16de8a1b, Fly v189): audit of every receipt surface on three live months, set_aside[].document_id beside file. T4 backlog 155 (PR #1111, merge f721e094): 91-eml in-machine archive scan, two characterisation pins, three builds declined with counts, item 155 filed. Mini-checkpoint PR #1113 mid-round.
**Did NOT work:** Scanning the mail archive at /data/mail (why not: the path does not exist, so the scan reported 0 mails with a PDF and a body while every command exited 0; the archive is <data_root>/inbound. Caught only because the 0 contradicted a known count. The rerun prints the .eml count first so the instrument proves itself.); Red-proving set_decision's statement stamp by anchoring on 'self.conn.commit()' + 'return cur.rowcount > 0' (why not: that text ends set_tool_decision, not set_decision, so the mutation left the suite green and read as 'does not bite'. Re-anchoring on set_decision's own tail made it red at 3 tests.); A test asserting the matcher's own verdict carries the statement id (why not: a fresh attach writes zero rows to decisions and the fixture has no decided_by='tool' row at all, so the test asserted a premise the month never reaches. Dropped; it is now backlog queue item 2.); tools/regress_check.py for the wiring proofs (why not: it prints 'RED (no pytest summary line)' whether or not the suite failed, so all twelve proofs were run by hand: cp aside, one-line mutation, targeted tests, cp back, sha256 equal.)
**Friction:** 4 - verification-theater (a mail-archive scan at a non-existent path reported zero findings with exit 0), verification-theater (a red proof anchored in the wrong function read as 'does not bite' and a test was then built on that false premise), slow-path (two sibling ledger collisions: backlog/Shipped numbers taken mid-branch and this session's own status rows relabelled, plus a duplicated session-log number), infrastructure-deferred (tools/regress_check.py prints RED regardless of outcome, hand-rolled around for a second session)
**Gates:** B1:1 B2:4 B3:3 skipped:1
**Autonomy:** 1 human intervention
**Outcome:** Queue closed; everything merged and live where it needed deploying. Two follow-on items open: backlog 155 (operator prose onto provenance, needs a Lovable prompt) and a pin for set_tool_decision's statement_id stamp.
