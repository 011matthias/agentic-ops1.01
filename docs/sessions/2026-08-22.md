---
date: 2026-08-22
sessions: 4
projects_touched: [brisken]
friction_events: 3
work_types: [client-dev]
---

### Session 1 — Brisken Recon SPA Crash Diagnosis (mini)
**Type:** client-dev
**Focus:** Diagnosed two independent breakages of Criss's reviewer SPA ("This page didn't load") from outside the app: a hosting 404 on route-derived chunk names (fixed, live) and a parse_issues object-vs-string contract mismatch that crashes the batch page via React #31 (fix prompt handed, awaiting the owner's Lovable publish). Backend and the created batch were never at fault.
**Projects:** brisken
**Built:** Vite chunkFileNames fix (proved by a local build of the Lovable repo: 46 assets, zero unsafe names; also un-shadowed expenses.index vs expenses.new) + the parse_issues fix prompt (ParseIssue[] typing, field-wise render, string-tolerant). Live asset sweep: 42/42 referenced files now 200.
**Friction:** None
**Outcome:** Backend verified healthy (job 3e13fb3d13f4 done, batch ae61e122a505 intact); chunk-404 fixed and live; parse_issues crash still live until the owner publishes

### Session 2 — Brisken Recon SPA Contract Test + Batch-Lock Threadpool
**Type:** client-dev
**Focus:** The parse_issues SPA crash is cleared (owner published the Lovable fix, DOM-verified against a batch that really carries an issue) and its recurrence-kill shipped as a view-shape contract test. Backlog item 18, the batch-lock event-loop freeze, shipped and deployed in the same session.
**Projects:** brisken
**Built:** PR #571 tests/test_view_contract.py + docs/api-contract.md (pins the element type of every list field on both view payloads over HTTP, with a MUST_COVER non-vacuity guard); PR #572 set-aside/restore + cards endpoints hand their locked span to run_in_threadpool, plus a static AST guard that fails CI on the next async route blocking on _BATCH_ADD_LOCK, deployed Fly v74 and live-verified; PR #573 backlog/runbook/status bookkeeping.
**Friction:** 2 - verification-theater (both new tests passed against the very bug they guarded on first draft; caught in-session by regressing the real code, root cause was fixtures aimed at code behind a 404 validation short-circuit), slow-path (bash heredoc failed on a large Python script and again on a non-ASCII bytes literal; ~2 calls lost before switching to the Write tool). 4 hook candidates all discarded: 2 gate-skip-pre-publish misfires on merges where suite+calibrate+ruff had run, 2 no-auto-commit firings where the gate worked correctly.
**Gates:** B1:3 B2:5 B3:2 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Wave rounds 1-7 of 8 shipped; suite 1221/2 and calibrate green; Fly v74 live with all 6 batches intact. Round 8 (Cards R4) blocked on owner answers to backlog item 10.

### Session 3 — Brisken Recon Count Semantics + Issue Codes
**Type:** client-dev
**Focus:** An operator note on the live app ("it sayz 35 categorized but when you click on open it says only 5 categorized") was a real backend split: n_categorized meant "has a category" on the batch list and "review state is ready" on the batch page. Fixed, shipped, DOM-verified; backlog item 20 (issue codes) shipped behind it.
**Projects:** brisken
**Built:** PR #575 count semantics (service.categorized_counts as the one rule, readiness moved to n_ready, batch_list_summary derives the list screen from the live overlay, api-contract counts table); PR #576 issue codes (upload_issue builds prose+code together, parallel issue_details/upload_issue_details at all three emission sites, pinned in the contract test); PR #577 runbook. Two Lovable prompts (both optional). Suite 1225/2, calibrate green, deployed twice and verified live.
**Friction:** 1 — slow-path (a blind except Exception in the new list-summary derive swallowed a closed-store bug and served stale-but-plausible numbers; caught only because a test asserted the value)
**Gates:** B1:1 B2:6 B3:2 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Both rounds merged and deployed; no unblocked code item left in the backlog — round 8 (Cards R4) waits on owner answers to item 10.

### Session 4 — Brisken Recon Zoho Decoupling + Cards R4 (mini)
**Type:** client-dev
**Focus:** The owner answered the five open questions, unblocking the last wave round and opening a bigger directive: cut every tie to Zoho. Layer 1 of that and the Cards R4 export half shipped and are live.
**Projects:** brisken
**Built:** PR #579 deletes the live Zoho connection (API client, journal poster, zoho-post, the coa_source api pull, seed-zoho: ~1,600 lines + 65 tests) behind a guard test proven red first; PR #580 pins the one-file/entity-column ruling and adds MultiEntityCoaGate, closing a live hole where every entity-less batch exported with no chart validation at all. Suite 1163/2, calibrate green, deployed and verified.
**Outcome:** Both merged and deployed. Item 19 (re-ingest) is the owner-approved build still to start; Zoho layers 2-4 are scoped in backlog item 23, with layer 4 blocked on which accounting system the CSV imports into now.
