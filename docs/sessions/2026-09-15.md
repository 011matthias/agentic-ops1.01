---
date: 2026-09-15
sessions: 19
projects_touched: [brisken, meji-media]
friction_events: 15
work_types: [client-dev]
---

### Session 1 — Expense-Recon Void List Round 3 (mini)
**Type:** client-dev
**Focus:** The owner published the combined Lovable prompt, so rounds 1 and 2 were audited into the published bundle rather than assumed, and item 60 shipped (PR #820, Fly v117): a candidate receipt another charge holds now names its holder, so a dispossessed charge stops reading as "No receipt found". The bundle audit was blind on its first run and was rewritten and promoted to tools/ (PR #821).
**Projects:** brisken
**Built:** rows[].candidates[].held_by + summary.n_charges_receipt_taken, both from the effective verdict; test_receipt_taken.py (3, route-level via POST /manual-match); two regress proofs; api-contract section + lovable-receipt-taken-prompt.md; PROMPT-STATUS moves the month-health and charge-entity prompts to Applied and records three renderers with no live case; tools/lovable-bundle-audit.py with control-field validation and its INDEX row
**Outcome:** Live at v117, browser-driven on August (1094 cells, no fallback strings). Void-list items 57, 58, 59, 56 and 60 shipped; 61-64 remain; the receipt-taken prompt awaits the owner's paste

### Session 2 — Expense-Recon Parallel Round Setup
**Type:** client-dev
**Focus:** Closed the 2026-09-11 void-list brief (items 57, 58, 59, 56, 60 shipped over three rounds, Fly v117, receipt-taken Lovable prompt handed as text), then opened the next ten (61-68 + 16 + 17) as ten simultaneous sessions under one protocol file on main.
**Projects:** brisken
**Built:** PARALLEL-ROUND-PROTOCOL.md (#824, c798b899); backlog items 65-68 carved out of item 49; status row (#825); ten self-contained session prompts with function/line pointers, live-read-first, the one ruling (62), Lovable half or none, and a leave-nothing-behind cleanup
**Friction:** 2 - agent-deferred (round-3 close offered the Lovable prompt instead of handing it; Stop hook B1 blocked, handed next turn), slow-path (cd-guard refusals x2 + one MSYS-mangled git show origin/main:path after four that survived)
**Gates:** B1:3 B2:12 B3:4 skipped:1
**Autonomy:** 1 correction + 3 brief-required rulings
**Outcome:** Setup merged and cleaned up; ten sessions not yet launched; receipt-taken prompt pending the owner's paste

### Session 3 — Expense-Recon Matching Program Prompt
**Type:** client-dev
**Focus:** Wrote the pasteable measure-first program prompt for raising the expense-recon matcher's success on Criss's real months (label July+August live, attribute every unmatched receipt to the gate that lost it, fix in ranked rounds with the pinned six-bundle scorer as floor). Corrected to v2 in the checkpoint after the parallel round shipped items 56/59 (#815, v116) and 60 (#820) underneath it.
**Projects:** brisken
**Built:** Prompt v1 (chat) + v2 (checkpoint Working Notes); no code, no status-file edits (docs branch only, ten p1 siblings on client branches)
**Friction:** None
**Gates:** B1:0 B2:1 B3:0 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Prompt v2 ready to paste once items 61-63 of the parallel round have merged; program not started

### Session 4 — Expense-Recon Item 61 (mini)
**Type:** client-dev
**Focus:** Backlog item 61: a month's candidate pool now reaches the company months either side of it, so a receipt printed on the last day of a month can settle the charge that posts on the 1st of the next statement. The item's named instance did not reproduce; the same mechanism's failure is real one month further on.
**Projects:** brisken
**Built:** service.adjacent_pool_for_month + statement_period_for_month + borrowed_source_view, wired into rematch_month after the trip pool; rows[].candidates[].from_batch and summary.n_adjacent_borrowed; api-contract section; tests/test_adjacent_month_pool.py (6, four route-level); docs/lovable-adjacent-month-prompt.md + PROMPT-STATUS Pending row; backlog Shipped row 36 + p1 status element row. PR #832, merge 7c3f42c6, Fly v118.
**Friction:** None
**Outcome:** Live at v118, n_adjacent_borrowed present and 0 on both months (correct: neighbours lend nothing in period today). SPA renderer pending the owner's paste; the drive asserted no regression on the changed route, not the renderer.

### Session 5 — Brisken P1 Cost Centers Backend Complete
**Type:** client-dev
**Focus:** Steps 3 and 5 of backlog item 47 landed: the month report partitions its listing per cost center, and GET /api/cost-centers/totals rolls spend up across batches. With steps 1 and 2 already on the branch the backend is complete; PR #834 merged on CI green and Fly v120 serves it, proven by an authenticated live probe and a cold SPA drive.
**Projects:** brisken
**Built:** Report partition (generic section caption/label + heading/note in the PDF builder; build_expense_report partitions on the resolved cost center, unassigned last, trip reports unchanged); build_cost_center_totals + the totals route (per centre, per currency, explicit unassigned bucket, n_undated, note verbatim; private rows excluded); row_cost_centers on refresh-master-data; docs/lovable-cost-centers-prompt.md promoted and registered; api-contract re-pinned; 13 new tests (suite 1564 -> 1578 on the branch, 1628 on the merged tree), 8 mutation points proven; merged origin/main (docs-only conflicts), PR #834 merged, v120 deployed and verified by behaviour
**Friction:** 2 - slow-path (POSIX /c/ paths in regress_check --test and python -c, third recurrence; agent-browser networkidle hang), skipped-gate (session header not output at start)
**Gates:** B1:0 B2:14 B3:2 skipped:1
**Autonomy:** 0 human interventions
**Outcome:** Item 47 backend live on v120; registry empty in prod so every field is inert; Lovable half pasteable after its sections 1-2 gate; July ingest, item 50 probe and four stale p2 status files remain open

### Session 6 — Expense-Recon Item 16 (mini)
**Type:** client-dev
**Focus:** Backlog item 16: a rejected match released its receipt and then re-rendered it under the same row as though it were still on offer, with nothing saying it had been turned down. Shipped the missing fact plus its count; the undo the item asked for already existed, so none was added.
**Projects:** brisken
**Built:** rows[].candidates[].rejected (parallel, absent not false, charge-level because apply_decisions / effective_settlements / sync_claim_for_decision all read the status alone and a bulk reject writes chosen_document_id NULL) + summary.n_rejected_pairings (run payload, pairings not rows); api-contract section + counts row; tests/test_rejected_pairings.py (5, route-level); docs/lovable-rejected-pairing-prompt.md + PROMPT-STATUS rows. PR #828, merge 9d6b8849, live from Fly v118. Suite 1550 -> 1555; two regress proofs RED first.
**Friction:** None
**Outcome:** Backend live and probed on both months (n_rejected_pairings 0, all 55 candidates absent the key). Cold browser drive of /runs/074a7b8905d7 shows no regression; the renderer itself is unverifiable until the owner publishes AND a month carries a reject.

### Session 7 — Expense-Recon Item 65 (mini)
**Type:** client-dev
**Focus:** Backlog item 65: report totals formed in Decimal at both PDF total sites, and a row whose amount cannot be read carries a caption plus a counted footer instead of vanishing from the total in silence. Storage-description section 12 row 13 marked remediated.
**Projects:** brisken
**Built:** output/_pdf_common.py money helpers (parse_amount / sum_amounts / format_totals / excluded_note) used by the month report's header total and its per-person section sums; row caption 'amount unreadable, not in total' + footer 'N receipts excluded from the total: expenses ...'; summary.n_amounts_unreadable on the batch payload; docs/lovable-amounts-unreadable-prompt.md (Pending). PR #831, merge ce75fced, Fly v119. Suite 1550 -> 1562 alone, 1573 after merging siblings #828 and #832; three regress proofs RED first (silent drop, float sum, payload count), each biting through the route-level tests.
**Friction:** None
**Outcome:** Live at v119; both months probed (n_amounts_unreadable 0), both reports rebuilt through the deployed route with identical Decimal totals, both batch pages driven cold in agent-browser with zero fallback strings. SPA half pending the owner's paste; it renders nothing at 0, which is both live months.

### Session 8 — Expense-Recon Item 17 (mini)
**Type:** client-dev
**Focus:** Backlog item 17 (workbench filter/sort): the live read found PR #454/#455's Lovable half already published with a control bar the 2026-07-27 note predates, so the round's one scoped backend field was built, proven, then reverted as redundant, and the browser drive found an unreported live defect instead.
**Projects:** brisken
**Built:** PR #836 (merge 648599c2), docs only: api-contract field guide for the filter/sort controls, docs/lovable-workbench-filter-sort-prompt.md (SPA delta), PROMPT-STATUS pending row, backlog item 17 closed as Shipped row 40, status element row. rows[].vendor_sort_key built (suite 1557, regress RED on the caller-level route test) and REVERTED after the live Vendor A-Z proved already correct; suite back to 1550, then 1660 after merging main.
**Friction:** None promoted (no candidates). Worth noting for the round: the PR ran with zero CI checks for ~15 minutes because a CONFLICTING PR has no mergeable ref for GitHub to build, which reads exactly like a queue backlog.
**Outcome:** Merged, no deploy (nothing under src/ changed). Live defect open on the SPA side: 'Amount, high to low' sorts August's 2,484.00 and 1,574.24 to the bottom of the unmatched group because amount is a f"{v:,.2f}" display string. Waits on the owner pasting the delta prompt.

### Session 9 — Expense-Recon Item 64 (mini)
**Type:** client-dev
**Focus:** Backlog item 64: an unrecognised statement Type label keeps the sign the export printed instead of being read as a purchase, and each statements[] entry records the column map and card currency it was read with so the re-read reuses them rather than re-deriving both.
**Projects:** brisken
**Built:** ingest/_common.py DEBIT_TYPE_VALUES + is_known_type + unknown_type_issues; the Type branch in statement_csv.py and statement_xlsx.py; statements[].column_map + card_currency on build_statement_entry with reread_statements reusing them; tests/test_statement_parser_gaps.py; api-contract.md section; docs/lovable-statement-record-prompt.md + PROMPT-STATUS row. PR #830, merge 92552b82, Fly v122.
**Friction:** None
**Outcome:** Live at v122. Both gaps were inert on production (one statement per month, only Sale/Payment/Fee labels), so both fixtures are constructed. Suite 1550 to 1557 on the branch, 1647 after three siblings merged; three regress proofs RED first. Lovable half pending the owner's paste.

### Session 10 — Expense-Recon Item 67 (mini)
**Type:** client-dev
**Focus:** Item 67: one unrenderable receipt used to 500 the whole month's report. The renderability probe now copies pages the way assembly does, every document is guarded on its own, and the blocked file reaches the review screen instead of only a caption inside the PDF.
**Projects:** brisken
**Built:** probe_pdf (copy, not count) + per-file guard in stitch + caption naming file and reason; expenses[].receipt_render and summary.n_receipts_unrenderable, both absent until a report was built; 60-page cap per receipt and a spooled assembly; storage-description 7.3/7.5 corrected. PR #839, merge 0499db9b, Fly v123. Suite 1550 -> 1671; three regressions RED first, a fourth discarded as a semantic no-op.
**Friction:** None
**Outcome:** Live at v123: both months build (200, byte sizes unchanged), 31/31 and 51/51 receipts read ok, 0 unrenderable. SPA renderer pending the owner's paste of docs/lovable-render-failed-prompt.md.

### Session 11 — Expense-Recon Item 63 (mini)
**Type:** client-dev
**Focus:** Backlog item 63: the matcher's 20% probable band is the restaurant-tip allowance, but it ignored the merchant, so any two same-currency charges of similar size paired. The band is now merchant-scoped; exact amounts and every FX path untouched.
**Projects:** brisken
**Built:** PR #829 (merge 1b99914d, Fly v122): `_same_currency_band_allowed` + `amount_probable_min_vendor_score` (0.50, file-tunable, 0.0 disables) in matching/deterministic.py; tests/test_same_currency_vendor_band.py (13 tests, route-level through GET /api/runs/{id}); test_match_score.py fixtures repointed; backlog + p1 status rows.
**Friction:** None promoted (no candidates; the 502 mid-request was a sibling's concurrent deploy, checked against the rematch log before any retry rather than retried blind)
**Outcome:** Live August and July re-matched on the owner's yes: different-merchant same-currency candidates 4->0 and 1->0, true pairs intact, invariant_ok true. SPA drive: ADOBE 16.23 and ANTHROPIC* CLAUDE SUB 104.95 carry no candidates; the wrong pairs render as NEAR MISS with the gap named and Confirm match DISABLED. OPEN: the S1 labelled fixture no longer replays (pre-existing), so the matcher's anti-overfit guard is non-functional.

### Session 12 — Meji Christmas Suppression Build
**Type:** client-dev
**Focus:** Compiled the Christmas stop-list that Gurmej made the precondition of his re-engagement go, and executed it: 25 addresses now on the account-wide block list, up from 1. The 3-touch wave itself does not exist yet.
**Projects:** meji-media
**Built:** Two-tier suppression set from a live pull of all 2,459 lead rows and a read of all 355 inbound messages, plus a booked cross-match against the client's own parties table (35 hits); 24 block-list writes under OWNER GO #1, both batches read-back verified; context/suppression/2026-09-15-stop-list.{md,json}; status record via PR #823
**Friction:** 2 - agent-deferred (B1 closing-offer block, 5th consecutive session), verification-theater (claimed a lead could still receive a follow-up without querying its state; own B5 readiness check disproved it)
**Gates:** B1:1 B2:3 B3:0 skipped:2
**Autonomy:** 3 human interventions
**Outcome:** Tier 1 complete 25/25 and verified; 1,445 mailable; wave not built, OWNER GO #2 outstanding

### Session 13 — Expense-Recon Item 62 (mini)
**Type:** client-dev
**Focus:** Backlog item 62: receipts paid by bank transfer, cash or PayPal get a reviewer disposition that retires them from the reconciliation pool without taking them out of the month. Reading the live months first killed the item's own premise, since the three invoices it names carry an empty payment_mode and the one live 'Pay with a bank transfer' receipt had already settled on a card.
**Projects:** brisken
**Built:** POST/DELETE /api/runs/{id}/receipts/{doc}/settled-outside (snapshot key, batch lock, fresh re-read, view-time application so the undo is immediate); run payload drops the receipt from unmatched_receipts, n_unmatched_rec and month health's pair scan and counts summary.n_settled_outside; grid keeps the row with settled_outside {how, note, at}; unmatched_receipts[].suggested_settled_outside suggests and never applies; month report still prints the row behind a tender caption (owner ruling). n_receipts deliberately unchanged; receipt_match_rate read over the receipts a card could settle. tests/test_settled_outside.py (15 route-level), api-contract section, docs/lovable-settled-outside-prompt.md, PROMPT-STATUS pending row, backlog Shipped 42. PR #843, merge 336611e4, Fly v124.
**Friction:** None
**Outcome:** Backend live on v124 and browser-driven on both changed routes; SPA half pending the owner's paste, so no control renders yet.

### Session 14 — Expense-Recon Item 68 (mini)
**Type:** client-dev
**Focus:** Item 68: the Receipt column now answers "is it in the report" instead of "is there a file", and the reconciliation report is built from the reviewer's live overlay so a deleted expense leaves both documents at once.
**Projects:** brisken
**Built:** expenses[].receipt_in_report + summary.n_receipts_in_report (parallel, absent until known, derived from item 67's receipt_render); build_reconciliation_report takes the expense overlay and hands the live pool to build_view; the month report's listing column reads the pages prepare_evidence admitted. tests/test_receipt_coverage.py (6, route-level) + a view-contract pin. PR #838, Fly v125.
**Friction:** None
**Outcome:** Live at v125; both months read n_receipts_in_report == n_receipts (31 and 51), matching the pre-build page count. Lovable half pending the owner's paste, after item 67's.

### Session 15 — Expense-Recon Item 66 (mini)
**Type:** client-dev
**Focus:** Backlog item 66: the manual per-charge attach and the bulk receipt-folder ingest each rewrote the whole period record from a stale copy with no lock and no re-read, and two writes destroyed stored bytes. Both paths now commit under _BATCH_ADD_LOCK against a fresh re-read, and a superseded file is archived rather than overwritten or unlinked.
**Projects:** brisken
**Built:** service.py: locked fresh-re-read commits on attach_emailed_receipt and ingest_receipts_folder_into_run (rematch_month shape, never-drop-a-charge refusal, mid-flight receipts carried in as unmatched); _store_manual_receipt + _archive_superseded_file + _record_receipt_file (snapshot receipt_files, stored only); replace_intake_files archives before writing. app.py: post_manual_receipt hands the locked span to run_in_threadpool. tests/test_web_unlocked_write_paths.py (7 tests incl. a two-route interleaving with a rendezvous at the pre-write read). electronic-storage-system-description.md section 12 rows 3 and 14 marked remediated.
**Friction:** None promoted. The session's one real obstacle was external: GitHub scheduled no Actions suite for either PR across six pushes, a close-and-reopen, a fresh branch and a fresh PR, while sibling PRs got runs throughout.
**Outcome:** PR #846 merged 75e46ba8 on an owner-authorised override, deployed Fly v126, SPA driven cold on the August workbench (zero fallback strings), deployed attach handler proven non-mutatingly (400 with the code's own message, /healthz 57 ms, payload byte-identical before and after). Suite 1693 passed / 2 skipped on the merged tree; four regress proofs RED first.

### Session 16 — Brisken P1 Failure Probe And Cleanup Round
**Type:** client-dev
**Focus:** The autonomous half of the previous checkpoint's next steps: item 50's client-failure probe built, merged, deployed and driven live; the module's twelve lint findings cleared; two p2 status files corrected where the repo provably contradicted them; the one undated live row diagnosed as a correctly-flagged review exception rather than a defect.
**Projects:** brisken
**Built:** PR #855 the probe (web/machine.py as one home for process identity and age; healthz gains a parallel server block; POST/GET /api/client-errors bounded to 500 rows with a burst cap; process_predates_failure as the decisive field, driven by the browser's own elapsed time rather than by comparing clocks, and null-stays-null for unknown; 12 route-level tests, 6 wiring points regress-checked; lovable-failure-probe-prompt.md written and registered not-applied). PR #856 twelve lint findings cleared, none runtime faults, suite count unchanged at 1700. PR #857 p2-rome and p2-lead-gen-general corrected with an explicit what-was-and-was-not-verified note.
**Friction:** 2 - slow-path (heredoc guard plus an over-long script anchor, both documented classes, ~4 wasted calls), verification-theater (deploy-consumer-gate closed its marker on observing a browser command that had asserted nothing; caught and the drive completed)
**Gates:** B1:0 B2:11 B3:2 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Four PRs merged and the app deployed; item 50 and item 47 both now wait only on their Lovable prompts; p2-targeting and p2-product-decks left deliberately stale

### Session 17 — Brisken P1 Statement And Receipt Defects, CI Ruff Scope, Consumer Gate
**Type:** client-dev
**Focus:** Closed the two live p1 defects. Item 51 (a statement parsing to zero rows reporting success) was worse than recorded: the month graduated to the workbench holding zero charges, so it is refused now rather than advised. Item 52 (Criss, 'Recibo nao estar abrindo') is not a backend defect at all; driving the live SPA refuted the recorded lead and found a dead View receipt button, with a real flag defect underneath it. Then closed the brief's two system items: CI's ruff scope and a consumer gate that closed on a command which observed nothing.
**Projects:** brisken
**Built:** PRs #859 (attach + re-read refuse a zero-charge statement, 7 route-level tests), #860 (service.receipt_image_file, one disk-resolved answer for receipt_image_available on both payloads, 5 tests), #861 (deploy record), #862 (CI ruff over the recon module), #863 (the consumer gate closes on an observation, not a command; 3 tests rewritten, 13 added). Fly v128 live and verified against the deployed API. docs/lovable-view-receipt-prompt.md written and registered as not applied.
**Friction:** 3 — verification-theater (called every View receipt button disabled from the a11y tree before reading b.disabled, self-caught two calls later), scope-creep (extended item 51's guard to the re-read, which the item did not ask for; judged a gap in an existing deny-by-default contract and stated as such), slow-path (used a heredoc with a Python triple-quoted block despite the session brief naming it as a trap; the gate caught it, cost one turn). 31 further candidates were the hook test suites firing their own gates during preflight — discarded.
**Gates:** B1:0 B2:9 B3:4 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Items 51 and 52 closed on the backend and live on Fly v128; item 52's SPA half waits on an owner paste, which is what Criss actually needs. CI lints the recon module. The deploy-consumer gate can no longer be closed by a command that proves nothing.

### Session 18 — Brisken P1 Item 54 Refuted Before Build
**Type:** client-dev
**Focus:** Item 54 (OCR date misreads) checked before any code: both instances were read correctly and the proposed rule flags only pairs the human labels call wrong, so it was closed unbuilt.
**Projects:** brisken
**Built:** PR #866 item 54 closure with measurement + reopen condition; PR #867 PARALLEL-ROUND-PROTOCOL refuted-item branch; usability-loop memory labels-first note
**Friction:** 2 - verification-theater (item 54's 'certain' instance never checked against the printed invoice when written, carried by three checkpoints); slow-path (cd-guard let `cd ~ 2>/dev/null;` through and moved the primary working directory)
**Gates:** B1:0 B2:2 B3:1 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** No code, no deploy (Fly v128). 48-54 band has no autonomous item left; owner pastes and go-decisions pending.

### Session 19 — Brisken P1 Edits Stick, Card List, Scorer Repin, Matching Round A
**Type:** client-dev
**Focus:** The owner reported two defects in plain words (month edits that stay unchanged, a card list that buries the work); both were real, neither was in the backlog, and the first was Criss's own untriaged 2026-09-14 feedback. Item 70 shipped and deployed (Fly v129), item 71's prompt and the owner-ordered scorer re-pin merged, matching round A built and reviewed with its fix pass in flight.
**Projects:** brisken
**Built:** PR #870 item 70 (five edit routes reopened on statement months with a re-match, bake from baseline_receipts, posting_category_proposed on review rows, whole-receipt reclassify that moves the account; suite 1724 -> 1746, nine regress proofs, live-data replay old-vs-new identical); PR #869 item 71 (lovable-card-chips-prompt + PROMPT-STATUS re-audit against the live bundle, receipt viewer browser-driven); PR #871 (rapidfuzz in the pinned scorer + guard, re-pinned under SCORER_LOCK_ALLOW=1, floor reproduced standalone: train 49.8 / holdout 15.7 / all 65.5 / 0 wrong / guard 4/4); matching round A committed at 404c07da (reference-keyed duplicate groups + card inheritance: August wrong 3 -> 0, undetected copies 2 -> 0 on both months, suite 1746 -> 1785) and reviewed by five lenses.
**Friction:** 2 - verification-theater (a build subagent presented the six-bundle tables and the pinned scorer floor as round-A regression evidence, though neither path executes pool-layer code; caught by the review lenses before merge), slow-path (a 69-agent workflow hit the account session limit and killed 52 agents mid-flight, costing the verify+fix phase, which had to be re-launched as a second workflow)
**Gates:** B1:2 B2:6 B3:2 skipped:0
**Autonomy:** 2 human interventions
**Outcome:** Fly v129 serves item 70 and the live pages show it; round A sits unmerged at 404c07da with its fix workflow running, round B specified and queued; two Lovable prompts wait on the owner (month-edits first, card-chips second).
