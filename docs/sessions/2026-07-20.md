---
date: 2026-07-20
sessions: 7
projects_touched: [meji-media, brisken]
friction_events: 14
work_types: [client-dev, comms]
---

### Session 1 — Meji Media Instantly Scan + P1 Bounce Cleanup
**Type:** client-dev
**Focus:** Read-only scan of all running meji-media services surfaced a P1 warm bounce spike in kept NB-unknown leads; re-verified + removed the bounce-prone tail (owner-approved), blocklisted a repeated unsubscribe, staged findings for the Friday 07-25 report.
**Projects:** meji-media
**Built:** Snitha blocklist entry; 36 twice-unverifiable P1 leads removed (943→907) with reversibility snapshots; pilot-routing.md + next-outbound-deliverables.md updated; p1-78-nb-2026-07-20.json provenance.
**Friction:** 1 — agent-deferred (stop-b1-gate caught offer-shaped closings twice: "Want me to prep the Snitha stop-flag..." and "say the word and I'll enumerate..."; both self-corrected same-turn — did the bounded read-only/routing work instead of offering it). Recurring phrasing class, hook held both times. The cd-guard also fired once during checkpoint on a `cd &&` pattern — system worked as designed, corrected immediately, not promoted.
**Gates:** B1:2 (caught + corrected) · B2:2 (blocklist + deletion read-back verified) · B5:2 (both invasive Instantly actions: scope-of-effects + readiness check + explicit yes) · skipped:0
**Autonomy:** 2 (both structural stop-gate catches, no substantive user correction)
**Outcome:** All services healthy; P1 bounce source removed; Friday report material staged. No commits (sibling sessions live; all writes to gitignored context/).

### Session 2 — Brisken Tool Logins
**Type:** client-dev
**Focus:** Distributed Lead Desk + Expense Reconciliation logins to Dirk + Criss; on owner order collapsed expense-recon to a SINGLE operator page on unified code mn040307 (user page removed); both notification emails sent + verified.
**Projects:** brisken
**Built:** Fly secret change (EXPENSE_RECON_OPERATOR_CODE=mn040307, user code unset, verified 303/401 live); 2 emails sent via Graph as matthias.silva (Criss PT / Dirk EN, confirmed in Sent Items); vault Lead Desk entry + expense-recon single-op-code update; comms-log verbatim; p1-expense-reconciliation status file; Criss identity correction (= Cristiane Cavalcanti, she/her, NOT "Chris") across memory + vault.
**Friction:** 2 — (1) missed-tool/B4: the recon key user's name + gender were wrong across our own records (all "Chris"); user corrected forcefully twice; the M365/Graph directory had "Cristiane Cavalcanti (she/her)" and was only queried late (for her email). Corrected records + memory. (2) agent-deferred/B1: stop-b1-gate fired 3x on deferral-shaped phrasing around draft-presentation + the pending invasive Graph send; reframed to clean decision points each time — recurring class, hook held.
**Gates:** B1:4 (1 applied — queried Graph for the email instead of asking; 3 hook catches) · B2:3 (recon login, lead-desk login, Sent-Items confirm) · B3:1 (flyctl no-token diagnosed to config.yml token) · B4:1 applied / 1 missed (codes+links verified; name missed) · B6:1 (Fly deploy, Band-3, explicit owner order) · skipped:0
**Autonomy:** 2 human interventions (1 name correction + recurring B1 phrasing class)
**Outcome:** Both logins delivered + verified; expense-recon single operator page live on mn040307; old codes dead. No commits (shared tree, sibling sessions live; ledger writes local, comms-log gitignored).

### Session 3 — Brisken Expense-Recon Blueprint Revision
**Type:** client-dev
**Focus:** Criss's first expense-recon test reconciled 0/94; diagnosed the three root causes on her exact files (run locally, NO-LLM per owner cost directive), revised the tool BLUEPRINT ("using-the-data" half), and handed off two isolated build-chat prompts.
**Projects:** brisken
**Built:** BLUEPRINT.md revision (PR #284, merged — removed Anthropic/EU-card/Streamlit stale elements, added slices 3.15/10/11/12 + LD-2/LD-5/3.7/3.10/Slice 9/entry_status amendments); memory `project_brisken_expense_recon_testing_loop`; two ready-to-paste build prompts (Tier 1 matching/FX/sign, Tier 2 categorize-every-charge); p1 status file update. Retrieved Criss's real files off the Fly volume via flyctl; proved 0→34 candidates via sign-flip, 0-match via no-LLM canonical run.
**Friction:** 3 — (1) slow-path/B1: asked the user (AskUserQuestion) where Criss's files were before pulling the operator home run list; the run was findable there (I used it moments later) — `/api/operator/state` hides unpublished operator runs. (2) intent-misalignment: started re-running with the OpenAI LLM to force matches; owner wanted no-API testing to save money. (3) over-literal: read "strategize what to do" as produce-a-strategy/run-a-workflow when the user meant "improve the existing blueprint" — caught mid-turn, negligible waste. Also: stop-b1-gate fired once on a closing-offer ending ("...or leave it as-is?"), reframed to a clean decision point — recurring class (3rd today), hook held.
**Gates:** B1:~6 (flyctl/vault/Graph/local-context/operator-home checked instead of asking; 1 skipped → #1; 1 hook catch) · B2:~4 (calibrate run, reconciled.csv parsed, CI green verified before merge, worktree status) · B3:~3 (InefficientFilter, 401 cold-start, MSYS path mangling — each diagnosed from the full error) · B4: blueprint values sourced from real data · B6:1 (PR #284 auto-merged on CI green, Band 2) · skipped:1
**Autonomy:** 3 human interventions (operator-page redirect, no-API redirect, blueprint-scope redirect)
**Outcome:** Blueprint revised + merged; two build tiers queued. No pipeline code changed this session (deliverable was the blueprint + prompts). Ledger writes local (sweep/PR handles docs).

### Session 4 — Brisken Expense-Recon Tier-2 Charge Categorization
**Type:** client-dev
**Focus:** Built + shipped Tier-2 (the second of the two queued build tiers): categorize EVERY charge including receiptless SaaS subscriptions, with source-agnostic subscription status. Isolated worktree; owned only the assigned surfaces (`categorize*`+`output/*`+`cli` post-match+`learning_cli`+web no-receipt rows); `ingest/*`+`matching/*` untouched (sibling Tier-1).
**Projects:** brisken
**Built:** `categorize_charges.py` (charge pseudo-receipt → existing `categorize_receipts`, LEARNED-first/never-LINE; + `derive_subscription_status`); `ReconcileResult.charge_categorizations` side-map + cli post-match stage; `zoho_export` receiptless-LEARNED journal rows behind `zoho.export_receiptless_learned` flag, COA-gated; charge category surfaced on unmatched rows in report_xlsx/sheet_writeback/reconciled_csv/web workbench; `expense-recon memory set` command. New tests (test_categorize_charges, test_web_charge_categorization) + extensions; suite 486 passed, calibrate exit 0. PR #287 merged (squash 7bcf3f3, CI green). Verified no-LLM on Criss's real April files: 6 Anthropic charges LEARNED→posted to the real Books account through the COA gate, Adobe/OpenAI/GitHub VENDOR review-only (0 in journal), flag-off withholds all, 39 charges flagged subscription from history.
**Friction:** 1 — slow-path/cd-cwd: cd-guard hook fired twice on `cd "..." && ...` compounds (module file listing; git commit attempt), corrected to `git -C` / subshell same turn. Well-documented recurring cd-cwd class; hook held both times, system worked as designed.
**Gates:** B2:~5 (full suite 486, calibrate exit 0, real-data journal/csv/xlsx parse-back, CI-green before merge, ruff net-zero vs clean tree) · B6:1 (Band 1 commit/push/PR autonomous post-verify, Band 2 auto-merge on CI green) · B7:1 (read BLUEPRINT + all owned files before building) · skipped:0
**Autonomy:** 0 human interventions — fully autonomous session (the 2 cd-guard catches are hook self-corrections, not human).
**Outcome:** Tier-2 merged to main behind opt-in config (inert until a run enables the flag / statements store). Not yet Fly-deployed (separate deploy; Tier-1 sign fix should land first so journal debits are positive). Ledger writes local (sweep/PR handles docs; shared tree, siblings mid-checkpoint).

### Session 5 — Meji P2 September Rework (comms)
**Type:** comms
**Focus:** Worked RAD-19 (retire the 20 stranded P2B leads at reactivation), then ran a 3-agent read-only evidence sweep of all 4 Instantly campaigns and turned it into a September P2-rework foundation brief + a sent client message to Gurmej.
**Projects:** meji-media
**Built:** p2/p2-september-rework-evidence.md (verified funnel + settled findings + unknowns + test design); pilot-routing Sept pre-load checklist items 3 (retire 20, dedup-carry-forward) + 4 (load approved v2 copy); RAD-19 sized + RAD-22 committed; Gurmej P2 rework brief SENT (comms-critic-cleared, P1/P2/P3 labelled) + verbatim logged, draft deleted. Read-only throughout (63 Instantly calls, 4,595 email rows; workflow wz96k1r5o); zero mutations.
**Friction:** 4 — (1) agent-deferred/B1: stop-b1-gate caught a dedup-variant closing offer, reframed + did it (recurring class, hook held). (2) intent-misalignment/anchor: draft re-asked the autumn-timing angle Gurmej settled 07-09; comms-critic caught pre-send. (3) verification-theater/B3: labelled the kavita OOO auto-reply a "genuine reply" before reading the body; user's "where is that from?" prompted the correction. (4) slow-path/B1: stop-b1-gate FALSE-fired 2x on quoted client-copy inside the drafted message ("If you want ... I will set it all up") — structural candidate: exclude blockquote/verbatim lines from the B1 scan.
**Gates:** B1:many applied (read pilot-routing/comms-log + queried Instantly read-only instead of asking) + 1 real hook catch + 2 false-fires · B2:3 (RAD-19 count read-back, sweep cross-checked vs known state, comms-critic pre-send) · B3:1 applied / 1 missed (the OOO mislabel) · B4:applied throughout, 2 overstatements caught by comms-critic · B5:0 (fully read-only, no invasive Instantly call) · B7:1 (evidence sweep = enumerate-before-rework) · skipped:1 (B3 initial mislabel)
**Autonomy:** 2 human interventions (OOO mislabel surfaced by the user's question; the P2-label refinement request)
**Outcome:** P2 rework brief sent; 6 questions open with Gurmej (occasion pick + seasonality are the load-bearing two). September build gated on his answers + copy sign-off; the 20 stranded leads untouched (retire is a Sept B5 action). No commits (shared tree, siblings live; context/ writes gitignored, docs ledger local).

### Session 6 — Brisken Expense-Recon Lovable API
**Type:** client-dev
**Focus:** Stood up a JSON + Bearer-auth API surface on the FastAPI expense-recon app so a Lovable-built React frontend can drive it cross-origin, without touching Criss's live Jinja UI. All 3 SPA screens' backends shipped + deployed + live-verified.
**Projects:** brisken
**Built:** PR #290 (`POST /api/login` + bearer auth + CORS + JSON 401/403 for `/api/*`), PR #291 (`GET /api/runs/{id}` JSON workbench via jsonable_encoder; mutation endpoints reused as-is), PR #293 (`POST /api/runs` JSON run kickoff, operator-gated, background + `GET /jobs/{id}` poll). 3 flyctl deploys to brisken-expense-recon.fly.dev, each live-verified (login/CORS, workbench 94-row real run, upload gated non-invasively). New memory `project_brisken_expense_recon_lovable_frontend.md`. Suite 623 pass. Login + dashboard built in Lovable (owner-confirmed working); workbench + upload prompts handed off.
**Friction:** 1 — agent-deferred/B1: stop-b1-gate fired 3x on closing phrasing (a Phase-0 next-step offer, then 2x rule-mandated "fresh session" high-pressure suggestions flagged as deferrals); reframed each time, hook held. The 2 session-break fires are a recurring false-fire class (session 5 hit the same class on quoted client copy).
**Gates:** B1:many applied (read serialize.py/build_view/auth/routes + queried live API instead of asking) + 1 real deploy-gate surface + 3 hook phrasing-catches · B2:many (local suite each slice + live URL verification after each of 3 deploys) · B3:1 (flyctl "no access token" read fully -> attributed to logged-out flyctl, surfaced as LIMITATION not a guess) · B6:applied (deploy gated -> waited for "deploy" -> then session pre-auth "deploy freely") · B7:strong throughout (enumerated existing routes/serialize/auth/job-model before each endpoint) · skipped:0
**Autonomy:** 1 human intervention (flyctl auth login — genuine tooling limitation, vault token read classifier-blocked; not an agent failure). 3 B1 hook self-corrections on phrasing (not human). No user direction-corrections.
**Outcome:** All 3 SPA-screen backends LIVE + verified at brisken-expense-recon.fly.dev; owner building the workbench + upload screens in Lovable. Remaining: cutover (co-host bundle on Fly / retire Jinja) after screens built + validated. Deploys shipped via docs-less feature-branch PRs; ledger local (shared tree, siblings live).

### Session 7 — Brisken Dirk Comments Review
**Type:** client-dev
**Focus:** Read Dirk's 56 recent emails (Graph app-only) + his embedded pptx comments; fixed the Rome T3 follow-up one-liner across all 24 emails per his TreasuryCentral slide-2 comments and sent him the revised copy; built the collateral asset inventory he asked for. Website items turned out already-done. The deck + Protokoll tasks stalled on a device-code token spiral.
**Projects:** brisken
**Built:** T3 one-liner fix across 24 emails/5 variants (rome-t3-wave-rebuilt.md, verified 0 old-phrase / 24 new); revised-copy reply SENT to Dirk via Graph (202, confirmed Sent Items 18:48); collateral-inventory.md (48 current 2026_PPTX files, versions/publish-map/backlog); p2-rome status update; memory correction (Graph app is confidential, device-code needs client_secret). Confirmed GTC (07-16) + resources one-pagers (07-14) + Lovable QA (07-12) already done — avoided redo.
**Friction:** 2 — (1) slow-path + infrastructure-deferred: the device-code delegated-token acquisition spiraled (5+ failed attempts, most of the session). Root: app is CONFIDENTIAL not public (memory said public → 2 failed polls until client_secret added), THEN 15-min code expiry kept beating the multi-turn round-trip; juggling multiple codes/pollers compounded it. User frustrated ("WHY DO YOU WANT IT AGAIN"). Fix: memory corrected + robust mint-once/single-poller pattern documented; recommend app-only-read + file-handoff to avoid entirely. (2) slow-path: T3 send readiness-check false-failed on `Re:` vs `RE:` subject case, self-caught + fixed same turn (one extra iteration).
**Gates:** B1:many applied (Graph/checkpoints/context read instead of asking) + several stop-b1-gate catches on closing-offer phrasing (recurring class, hook held) · B2:2 (T3 change verified across 24 emails; Sent-Items read-back after send) · B3:2 (7000218 diagnosed to confidential-client/secret; stale token decoded before trusting) · B4:1 (Protokoll comment-2 geography withheld as unverified vs sending to Jochen) · B5/B6:n/a (send gated on explicit user yes; no git commits) · skipped:0
**Autonomy:** 3 human interventions (Dirk-account auth pivot, "mint a longer code" redirect, token-spiral frustration) — elevated, all from the token path.
**Outcome:** T3 revised + re-sent (awaiting Dirk "approved"); inventory delivered; website confirmed done; Protokoll retrieved (likely already sent 07-17 — verify first); deck not started. No delegated write token; app-only stays read-only. No commits (shared tree, siblings live; content writes gitignored).
