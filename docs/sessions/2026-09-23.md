---
date: 2026-09-23
sessions: 2
projects_touched: [brisken]
friction_events: 0
work_types: [client-dev]
---

### Session 1 — Zoho July Rehearsal Posted (mini)
**Type:** client-dev
**Focus:** Closed Gap 1 of the Zoho month-end injection with a per-org category-to-account map, then posted and readback-verified the 13 USD July purchases in the TEST-BTS sandbox under owner greenlight.
**Projects:** brisken
**Built:** zoho/category_accounts.py (per-org category to GL code, fallback-only, production orgs unmapped) + leaf-account assertion; 15 tests in test_zoho_category_accounts.py; runbook sections for the map and the rehearsal. PRs #1193 (f4f31d32) and #1194 (93ca41c0).
**Did NOT work:** First category-to-account table mapped three categories to PARENT accounts (Professional Fees E600060, Travel Expense E100010, Marketing & Selling Expenses E600010) (why not: Zoho posts only to leaf accounts; parents are roll-ups. ChartOfAccounts.leaf_accounts already encoded the rule and caught all three before anything posted. Now asserted by test_every_shipped_mapping_targets_a_leaf_account, with a companion test proving that assertion can fail.); Mapping the 'Travel & Transport' category to any single leaf account (why not: Its two July rows are gasoline (E100010-01 Automobile Expense) and a vehicle rental (E100010-36 Rental Cars), and Travel Expense has no generic leaf to hold both. Left deliberately unmapped: a default wrong half the time is worse than a refusal that names the row.); Regenerating the payloads for readback verification via plan_expense_post (why not: It refused all 13 with already_in_ledger because the ledger had just recorded them. Correct behaviour, wrong tool: the readback uses build_expense_payload, which does not consult the ledger, and the refusal became a separate duplicate-guard assertion.); Sending vendor attribution as the text field vendor_name (why not: Zoho accepts it, returns 201 and stores nothing: 0 of 13 posted records carry vendor_name or vendor_id, and the audit note does not carry the vendor either. The payload builder's comment claimed it 'keeps the name visible'; measured false on 2026-09-23.); A name-only comparison of July account references against the charts (earlier in the session) (why not: resolve_ref also matches the 'CODE - name' label form by splitting the leading token as a code, so a name-only diff reported a confident miss for references that do resolve. Re-run through the real resolver with a known-present control.)
**Friction:** None promoted (mini mode; 7 gate-fired candidates left for the next full checkpoint)
**Outcome:** 13 of 46 July purchases posted to TEST-BTS and verified field by field, $30,864.42 exact; 32 still refused on foreign currency, 1 on (uncategorized - assign). Gap 2 reachable without re-consent via the sandbox fullaccess credential.

### Session 2 — Recon Learning Loop Save Previews And Undoes (mini)
**Type:** client-dev
**Focus:** The 2026-09-23 feedback wave's notes #76, #78, #80 and #81 are one subject, now grouped in the backlog as 'The learning loop'. Note #81 shipped as item 163: a memory save is computed as a plan before it is applied, so it can be previewed, journalled with every touched row's pre-image, and undone.
**Projects:** brisken
**Built:** Item 163 (PR #1202, merge dedd7260, Fly commit dedd726002e4): learning/commits.py RecordingStore; GET /api/runs/{id}/memory-plan; GET /api/memory/commits; POST /api/memory/commits/{id}/undo; memory_journal table; read_row/restore_row on the learning store; journal_id on both write paths; docs/lovable-memory-journal-prompt.md (unpasted). Backlog items 164/165/166 recorded, not built. Suite 2977 passed / 2 skipped, calibrate exit 0, ruff clean, seven wires proven RED, cold read-only SPA drive PASSED.
**Did NOT work:** Passing a padded, invented commit SHA to the Fly build arg (dedd7260...00000000) (why not: The real merge SHA is dedd726002e4eec37428744aed142571e7731e81; the invented one would have made /healthz report a commit that exists nowhere, defeating the item-120 build stamp. Caught before it landed: the deploy was stopped, the live commit re-read to confirm it was still the previous build, and the deploy redone with the real SHA.); Asserting the month list on the page sign-in lands on (why not: Sign-in lands on the NEW-BATCH screen, so the first cold drive reported 'month list does not render September 2026' for all three months while the app was fine. The months list is its own route, /months. An instrument fault that read exactly like a broken deploy.); Running tools/regress_check.py with an MSYS-style --test path (why not: uv run --directory /c/Users/... makes the BASELINE read RED with 'no pytest summary line' and exits before mutating anything. Windows paths ("C:\Users\...") work.); Returning a new key from commit_month_memory without widening its route (why not: POST /api/runs/{id}/commit-memory returned only ["learned"], so journal_id never reached the caller and seven tests failed on a KeyError while the service layer was already correct.); Reading July and August expenses from GET /api/runs/{id} (why not: It returns ZERO expenses for both (and 74 for September), because the first two are read through GET /api/expense-batches/{id}. The first measurement pass took the runs endpoint alone, reported a third of the estate, and looked like a finding.)
**Friction:** None promoted (mini)
**Outcome:** Live at dedd726002e4 and verified three ways (health stamp, both new routes read on the real September month, cold browser drive). SPA half awaits the owner's paste; items 164-166 open.
