---
date: 2026-09-21
sessions: 4
projects_touched: [brisken]
friction_events: 7
work_types: [client-dev]
---

### Session 1 — Brisken Item 38 R4 Re-Plan (mini)
**Type:** client-dev
**Focus:** Planned item 38's remaining round R4 and found the premise wrong: R4 shipped in both halves on 2026-09-07 (PR #685), backend and SPA, and the trip already resolves a cost center at D2 position 2. Re-purposed the round into five sub-rounds that make the shipped R4 trustworthy, since nothing has ever run against a real trip or a non-empty cost-center registry.
**Projects:** brisken
**Built:** Approved plan at ~/.claude/plans/try-planning-again-but-sleepy-steele.md (gaps A-K with file:line anchors, rounds R4.0-R4.5 each with caller-level red-proofs, 12 owner questions); 10 subsystem maps + 10 refuter verdicts from 2 Workflow runs; 4 corrected claims incl. the cross-batch refusal code (409 receipt_settled_elsewhere, not 400 receipt_settled_by_charge)
**Did NOT work:** Sizing both Workflow runs at 52 agents total, run concurrently (why not: Hit the session usage limit (resets 02:50 Berlin) after 21 completed; the first run alone cost 6.3M subagent tokens. The gap-hunt lost 8 of 9 finders and all 20 verifiers, so its 8 findings are unverified, not refuted.); Reading code from the primary checkout C:/Users/neuma_p1qrsic/Repo/agentic-ops1 (why not: It was 284 commits behind origin/main, so the plan would have been written against PR #981 state while HEAD was #1150+. All reading moved to a git archive extract of origin/main.); cd <dir> && <cmd> in the Bash tool (why not: Refused by the cd-guard hook four times; used git -C, subshells and absolute paths instead.); Writing per-agent digest files named from an optional lens key (why not: OSError Errno 22 on Windows from a 'verdict-C2-?.md' filename; fixed by numbering duplicates.)
**Friction:** None promoted (no candidates; the stale-checkout and cd-guard hooks fired correctly and were heeded)
**Outcome:** Plan approved, zero code written; execution deferred to a fresh session with a continuation prompt

### Session 2 — Brisken Recon Item 120 Docs And Observability
**Type:** client-dev
**Focus:** Shipped the documentation-and-observability half of backlog item 120 so a stand-in can deploy, verify and explain the expense-recon tool without the author. The headline gap is closed and proven live: /healthz now reports the commit the running image was built from. Three claims in the session brief were wrong when checked.
**Projects:** brisken
**Built:** PR #1156 /healthz build identity (server.commit from a Dockerfile ARG baked into the image, server.image from FLY_IMAGE_REF; deployed 640be61b and /healthz returned it). #1157 docs/screen-field-map.md (wrong on-screen value to the function that made it, keyed on names not offsets). #1158 docs/operating.md (read live state, audit a publish, labels, notifier, deploy with the stamp). #1159 README cut 559 to 119 lines. #1160 doc reconciliation closed as superseded (ANNEALING E4 + SPEC-GAP item 1 struck, header on the v2 spec). #1163 backlog item 120. #1168 p1 status roll-up + stale live-UI address fixed. Consumer driven, not assumed: recon_uptime_probe.py api/mx/spa all OK exit 0, and the SPA driven cold rendering all 7 months.
**Did NOT work:** A `just deploy` recipe wrapping the build arg and the clean-tree guard (why not: just is not installed on this machine, so the recipe could not be run even once. An unrun command in an emergency runbook is the exact failure item 120 exists to remove, so it was cut and the raw command documented in docs/operating.md instead, with its guard differential-probed (fires on a dirty module, silent on a clean one).); The field map's first scoped-search helper, awk -v fn="^def $1\\(" (why not: awk -v processes the escape sequence, so the pattern reached awk as an unbalanced '(' and it aborted with 'invalid regexp'. Its failure printed nothing, indistinguishable from a correct empty result. Rewritten to literal index() matching, then run verbatim out of the finished markdown.); Verifying the README's ten pointer files with MSYS_NO_PATHCONV=1 git -C /c/... cat-file -e (why not: MSYS_NO_PATHCONV stops the POSIX-to-Windows conversion that git -C needs on this box, so git could not change directory and every file reported MISSING. The negative was the instrument, not the repository; a known-present control exposed it.); Driving the SPA with Playwright MCP, then with agent-browser (why not: Playwright MCP is bound to :9222 which a sibling session holds, and timed out at 30s. agent-browser --session i120 --executable-path <chrome> open never returned after 180s, despite being the documented 2026-09-17 remedy. Replaced by an own headless Chrome on :9333 driven over raw CDP, which needed suppress_origin=True because Chrome 403s the CDP websocket on an Origin header unless launched with --remote-allow-origins.)
**Friction:** 2 — verification-theater (two blind probes returned confident empties: a broken awk helper about to ship into a runbook, and MSYS_NO_PATHCONV breaking git -C so ten present files read as MISSING; both caught by a known-present control), slow-path (about ten calls to shell traps and to both browser wrappers hanging, the documented 2026-09-17 agent-browser remedy included)
**Gates:** B1:0 B2:6 B3:1 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** All eight PRs merged. Fly v195 live carrying 640be61b, still the last commit touching src/. Open and owner-gated: the hosting-org move and a rehearsed restore. Open and agent-shaped: EXPENSE_RECON_BACKUP unset, the notifier's 283-commit-stale checkout, a commit column on client_errors.

### Session 3 — Recon No-Card Logic Status And July Re-Match (mini)
**Type:** client-dev
**Focus:** Answered where the no-card matching logic stands (backlog item 151, owner note X1), found the status record stale in three ways, then re-matched live July on the owner's order and recorded the measured result. The description fix moved both rows it was built for and changed nothing else.
**Projects:** brisken
**Built:** PR #1162 (merge 2a72ea41, docs-only, six CI checks green): item 151 status row + backlog entry corrected from 'PR pending, Fly release pending, SPA NOT pasted' to merged at Fly v186 with the SPA half bundle-verified 2026-09-20, plus a measured record of the live July re-match (2026-09-20 22:10 UTC via refresh-master-data; changes: [], 13 judgments reused, 0 new; Microsoft 718.20 vendor_pct 50 to 100 and score 92 to 100, Amazon 315.56 57 to 100 and 80 to 87; 31 reconciled / 7 review / 73 unmatched unchanged, no bucket move, no decision or expense field touched). Corrected the 2026-09-18 prediction that the Microsoft row was waiting for a self-confirm click: both rows were already reconciled and confirmable_pair requires turn: decide, so neither could ever have self-confirmed.
**Did NOT work:** gh pr checks <n> --watch immediately after gh pr create (why not: Exited at once with 'no checks reported on the branch' instead of waiting: the workflow run had not registered yet, so --watch had nothing to attach to and treated the empty set as terminal. A bounded poll on the rollup (checks=N) worked on the first try 30 seconds later.); Reading a month's expenses off the run payload (/api/runs/{id}) (why not: It returns expenses: [] for a statement month; the expense spine lives on /api/expense-batches/{id}. The first probe therefore reported 'expenses 0, card_source {}' for July and August, which reads like an empty month and is not. Both surfaces are needed for a complete month picture.)
**Friction:** None
**Outcome:** Item 151 shipped, deployed, SPA-applied and correctly recorded; live July carries the corrected scores; the no-card review clause still fires on 0 pairs and September's statement (27 no-card receipts, no statement yet) is its first live test.

### Session 4 — Brisken Recon Item 120 Loose Ends
**Type:** client-dev
**Focus:** Closed the three loose ends item 120 left open on 09-20: a client-failure row now names the build that served it, the notifier runs from a checkout that updates itself, and the SharePoint backup is ON, so Brisken holds a copy of its own reconciliation data for the first time. Item 120 is down to two owner decisions.
**Projects:** brisken
**Built:** PR #1170 client_errors.server_commit/server_image stamped at report time (deployed b34ace5b, migration verified on the live volume: new row carries the commit, the row beside it stays ''); PR #1173 tools/brisken-recon-notify-run.py + dedicated clone, Windows task repointed, proven unattended (18:32:12Z scheduled run fast-forwarded 8be609b6..9c8a1fee and ran from it); backup live to MARKETING/ExpenseTool, first copy 127.2 MB; PR #1175 records + rule_brisken_graph_first corrected (app-only SharePoint writes work)
**Did NOT work:** flyctl deploy with a POSIX module path under MSYS_NO_PATHCONV=1 (why not: flyctl is a native Windows binary and resolved /c/Users/... against the current drive, giving 'chdir C:\c\Users\...: The system cannot find the path specified'. Suppressing MSYS translation causes this rather than fixing it; convert with cygpath -w first and keep the prefix only for the --build-arg and app-name args.); flyctl ssh console to run expense-recon backup --dry-run on the machine (why not: Refused by the Claude Code auto-mode classifier, so no on-machine command (du, df, the CLI dry run) is available in an auto-mode session. /healthz carries a disk block that answers most of what df would; flyctl deploy / secrets set / releases are NOT blocked.); Running regress_check via nested uv run --no-project --with pytest (why not: It sets VIRTUAL_ENV, so the inner uv run --extra web ignores the project environment, the module's web tests importorskip, and the run reports '1 skipped' as a green baseline. Run regress_check under plain python instead.); Probing the SharePoint drive as GET /sites/{path-form}/drive (why not: Returns 404; backup.py resolves the site first and queries /sites/{resolved-id}/drive, which works. The 404 was nearly filed as a blind-instrument defect in backup --check. Always probe the way the code does.); Playwright MCP for the consumer drive (why not: It connects to the user's Edge on :9222, which is busy; initializeServer timed out after 30 s. agent-browser with --executable-path Chrome and a named session works, but its `open` command holds the process (background it, then read state with a foreground snapshot).); agent-browser type against the login field by accessible name, with output suppressed (why not: The React controlled input did not register and the Log in button stayed disabled, silently. Type against the ref (e2) and confirm the button loses [disabled] before clicking; never suppress the output of the step you are about to depend on.)
**Friction:** 5 — verification-theater (deploy-consumer gate closed on a snapshot that returned empty), slow-path x4 (flyctl needs Windows paths not MSYS-suppressed POSIX ones; Graph probe used the path form where the code uses the resolved site id, nearly filing a false blind-instrument finding; ~6 calls re-deriving a ceiling finding a sibling session had already merged as #1171; ~5 hand polls of gh pr checks with waiters available)
**Gates:** B1:1 B2:5 B3:3 skipped:0
**Autonomy:** 1 human interventions
**Outcome:** Three PRs merged and deployed; backup and notifier both live and verified by behaviour. Item 120 open on two owner decisions only: the personal Fly account, and a restore nobody has rehearsed.
