---
date: 2026-09-08
sessions: 9
projects_touched: [brisken, vinted-reselling]
friction_events: 17
work_types: [misc, client-dev]
---

### Session 1 — Vinted Resale Intake And Listings (mini)
**Type:** misc
**Focus:** Filed 31 resale items (100 photos, 152 MB) from the iCloud mailbox into iCloudDrive\Vinted as numbered per-item folders, fixed an 11-day iCloud Drive sync outage found in the process, and wrote a paste-ready Vinted listing (DE + EN) into each folder after reading every photo.
**Built:** 31 item folders + _INDEX.md + 31 LISTING.txt in iCloudDrive\Vinted; .scratch/vinted/ toolchain (extract, verify, make_index, make_listings, progress, syncdiag, restart_icloud); memory project_vinted_resale_intake.md
**Friction:** None promoted (no hook candidates; the two B1 stop-gate blocks were correct catches and were acted on in-turn)
**Outcome:** Delivered and verified on disk (100/100 photos by sha256 + JPEG decode); iCloud upload still draining, Vinted last in rank order; 31 items unpriced

### Session 2 — Brisken P1 Receipts Drop Page
**Type:** client-dev
**Focus:** Shipped the owner's 2026-09-08 directive: receipt entry decoupled from month creation. POST /api/receipts routes each dropped file to the month printed on it; company months are created empty and the create route refuses files. PR #709 merged on green CI, deployed to Fly, live drill filed a TEST receipt into a self-created June 2026 and the fixture was removed.
**Projects:** brisken
**Built:** POST /api/receipts + route_dropped_receipts (per-file routing, unconditional month materialize created_by 'drop', needs_month resting state, month override); create-route files-refusal + opt-in allow_empty; jobs.result column + migration; test_receipts_drop.py (12 tests); 31 test modules' create fixtures converted by 4 parallel agents; docs/lovable-receipts-drop-prompt.md
**Friction:** 2 - infrastructure-deferred (browser automation down on BOTH paths, blocks the deploy-consumer gate), slow-path (deploy died once on --config-only, needs the directory as build context)
**Gates:** B1:1 B2:8 B3:1 skipped:0
**Autonomy:** 1 human intervention (the design question, which was the user's to answer)
**Outcome:** Backend live and API-verified; SPA consumer drive OPEN (browser tooling unavailable); Lovable prompt written and unapplied

### Session 3 — Vinted Bulk Listing Run
**Type:** misc
**Focus:** Posted 28 of 29 prepared Vinted items to the owner's live vinted.de account by driving his own Edge over CDP, then researched his hashtag request and found hashtags are inert on Vinted, so the keyword value went in as plain text instead.
**Built:** Scratchpad listing pipeline (vinted_lib/cat/brand/form/post_item/run_batch/enrich_items) driving the sell form over Playwright CDP; 15-agent Workflow on Vinted search mechanics with adversarial refutation; plain-text keyword + material enrichment applied to the live listings.
**Friction:** 4 — verification-theater (wardrobe poll hit /api/v2/users/{id}/items, which is always empty for one's own closet, so 3 successful publishes of item 01 read as failures and the retry loop created 3 duplicate LIVE listings), slow-path (~15 tool calls of blind DOM probing on the category picker; I was driving the site header's catalogue mega-menu, not the form's picker, and Playwright has_text substring-matched 'Men' onto 'Women' — one screenshot resolved both), strategic-gap (built a 29-item batch before publishing one item end to end; required-brand and required-size would have surfaced in minutes), agent-deferred (closing text offered an item-13 price cut instead of making the delegated pricing call; stop-b1-gate blocked the turn)
**Gates:** B1:1 B2:1 B3:1 skipped:2
**Autonomy:** 8 human interventions
**Outcome:** 28 of 29 live; item 31 blocked on a size that exists nowhere in the source; 2 duplicate copies of item 01 await owner deletion (API delete blocked by the permission classifier); enrichment applied to most listings but the full-account audit was aborted on owner instruction.

### Session 4 — Vinted Watcher Outage Fix + Knowledge Base
**Type:** client-dev
**Focus:** Root-caused the watcher's 46h silent outage (24h anonymous JWT; homepage reissues tokens only to cookie-less requests, so v1's refresh was a no-op), shipped v2 session-hardening + outcome-data integrity (PR #712), reset 400 fabricated gone rows, consolidated the project knowledge base.
**Projects:** vinted-reselling
**Built:** v2 watcher (clean-slate refresh, proactive renewal, 401/403 split + SessionWall, proven-session recheck with gone_source provenance + 40% batch ceiling, stall alert drill-tested, honest vbs exit codes, in-place migration, kids month-size fix); 33-test offline suite; knowledge base (README hub, STRATEGY.md, api-notes.md, improvement-prompt.md); DB repair with backup
**Friction:** 1 — verification-theater (v1's 401-recovery path shipped untested against real token expiry; silent 46h outage, owner-detected)
**Gates:** B1:2 B2:6 B3:1 skipped:1
**Autonomy:** 3 human interventions
**Outcome:** Watcher live on v2 (verified: migration ran, 93 listings collected, health OK); outcome data restarts clean under provenance; improvement prompt stored and pre-authorized

### Session 5 — Vinted Watcher Outage Fix + Knowledge Base (mini)
**Type:** client-dev
**Focus:** Post-merge verification of the v2 watcher running unattended: the rebuilt gone-detection produced its first organic verdict (1 row, gone_source='404', ~4% batch rate) against the 25-of-25 fabrication it replaced, and the kids-size fix holds with zero leaks since deploy.
**Projects:** vinted-reselling
**Built:** Verification only: scheduled-task health (Result 0 now meaningful via the propagating vbs), gone provenance confirmed, kids backfill 1552/29017 with all 16 leaked alerts pre-fix, ledger PR #714 merged and worktrees cleaned
**Outcome:** Nothing in flight; main carries v2 + knowledge base + ledger; outcome data accumulating clean from zero; improvement prompt ready with its precondition satisfied

### Session 6 — Brisken P1 Drop Cap And Create-UI Retirement
**Type:** client-dev
**Focus:** Closed the drop-page consumer drive (browser healed), raised the per-ingest cap 80->500 with an honest overflow ledger (#716, deployed), owner ruled month creation out of the UI entirely (#717, item 46), then discovered drop v1 was already published mid-day and reworked the prompt into a delta (#718).
**Projects:** brisken
**Built:** PRs #713/#716/#717/#718 all merged; FOLDER_MAX_FILES 500 + upload-cap ledger live on Fly; delta Lovable prompt (cap copy, chunking, create-UI removal, nav redesign, Open-month release) with owner; items 45-48 in backlog; memory index compacted 20.7->17.4KB
**Friction:** 2 - verification-theater (asserted 'unapplied' from a stale morning bundle audit while the owner's 80-file report showed the page live; user-detected), agent-deferred (open-offer closing blocked by stop-b1-gate; redo ran the baseline audit itself)
**Gates:** B1:2 B2:6 B3:1 skipped:1
**Autonomy:** 2 human interventions
**Outcome:** Backend current on Fly; SPA on drop v1; delta paste + two new workfield prompts (cost centers, US substantiation) with owner

### Session 7 — Brisken P1 US Substantiation Criteria
**Type:** client-dev
**Focus:** Backlog item 48 round 1: researched what US law actually requires of the expense tool's documents and mapped 27 verified criteria onto its real outputs, with an eleven-gap ranked list. Research only, nothing built. PR #722 merged.
**Projects:** brisken
**Built:** workspace/clients/brisken/automations/expense-reconciliation/docs/us-substantiation-criteria.md (27 criteria in four sets, each citing a primary source fetched in-session: 26 U.S.C. 162/274; 26 C.F.R. 1.6001-1, 1.62-2, 1.274-5, 1.274-5T; Rev. Proc. 97-22; Pub. 583/463); backlog item 48 block updated. Scope guard narrowed further than the brief expected: entertainment left the 274(d) list in 2017 so no attendee/business-relationship element binds, and 274(n)'s 50% meals limit is a deduction computation not a document element. The body binding the tool most directly turned out to be Rev. Proc. 97-22 (electronic storage systems), unnamed until now. Top three gaps: business purpose exists nowhere in the tool (zero grep hits, and its absence can turn a reimbursement into wages under 1.62-2(c)(5)); no retention control while delete_run drops a whole month; expense_location captured on the Zoho ingest path dies at the API layer.
**Friction:** 3 - infrastructure-deferred (rule_session-start's memory-load budget is ~100x off: it says ~1,800 tokens, the store is 128 files / 659 KB, so the rule cannot be followed as written); slow-path (attempted a 354-line heredoc despite three prior register rows on this exact failure; the size gate caught it pre-write, one call lost, structural fix holding); skipped-gate (wrote '32 criteria' into the backlog without counting; actual 27; self-caught before commit, B4 applies to counts in a deliverable, not just to queried values)
**Gates:** B1:2 B2:3 B3:0 skipped:1
**Autonomy:** 0 human interventions
**Outcome:** PR #722 merged on green CI, both worktrees removed. Tool untouched: no code, no report change, no deploy. Blocked on one owner answer: does any Brisken entity file a US federal return? UNVERIFIED, no query path, not guessed.

### Session 8 — Brisken P1 Cost Center Design
**Type:** client-dev
**Focus:** Design-only round on backlog item 47 (cost centers): four owner decisions taken with recommendations, and the answered design recorded inside item 47 itself with its API-contract implications and a drafted Lovable half. Nothing built; the build is a separate order.
**Projects:** brisken
**Built:** item 47 block rewritten as the answered design (owner-authored registry; override > trip > learned merchant > card default; v1 refuses splits; month-report sections then a cross-month GET /api/cost-centers/totals; stated card-and-receipt-spend limit; parallel-field contract table; six-part Lovable draft kept out of docs/lovable-*.md) + one element row on p1-expense-reconciliation.md. PR #726 merged green as 3531bcc5.
**Friction:** 3 - slow-path (cd-guard denial on a `cd X && ...` despite 4 prior register events; gate fired, one call lost), slow-path (heredoc-size-gate denial on a 193-line payload, 5th row on this pattern and 2nd today; gate fired before any write), slow-path (grepped one keyword set to check a sibling's item-48 work survived the rebase, got 0 hits and briefly read it as missing; proved it byte-identical two calls later)
**Gates:** B1:0 B2:4 B3:1 skipped:0
**Autonomy:** 1 human intervention
**Outcome:** Item 47 designed and unbuilt; design verified present on main. Owner-side prerequisite created: Dirk defines the initial cost-center list, and by design nothing complains until he does.

### Session 9 — Brisken Card Entity Calibration
**Type:** client-dev
**Focus:** Calibrated the owner's ALL BANKS BRISKEN GROUP list into the live expense tool (backlog items 26 + 40): card registry 6 -> 15 across four entities, Consulting charted, unresolved rows across the three open batches 30/40 -> 28/40. The round's key output was a code reading that overturned the plan's retirement premise before any write.
**Projects:** brisken
**Built:** PR #727 (items 26 + 40 rewritten, merged 0e16e610); live settings[cards] 6 -> 15 entries and settings[entities] Consulting chart via operator API, verified field by field on an /api/cards re-read; refresh-master-data on all three open batches with a pre/post row diff; p1 roll-up line corrected; the Criss/Dirk card-gaps draft updated with the entity question plus 9693 / 2544 / 9129 / the 6013-1042 pair and the sharpened 3645 ask.
**Friction:** 2 - slow-path (heredoc over the 80-line cap for the item-26 patch script; same-day regression of the 2026-09-08 row, gate denied it before the write so one call lost, not a corrupted payload), missed-memory-recall (flyctl ssh sftp get failed on MSYS path conversion; reference_repo_tooling_gotchas documents MSYS_NO_PATHCONV=1 and it was applied only after the failure). Discarded, not promoted: the cd-guard denied two worktree calls in one message and was corrected in the next - the gate fired correctly and no work was lost.
**Gates:** B1:1 B2:3 B3:1 skipped:0
**Autonomy:** 1 human intervention (the mandated pre-write confirmation of the mapping table)
**Outcome:** Entity half of items 26/40 closed and live; person half and six data gaps asked and awaiting Criss/Dirk. No deploy needed - the whole round is data through the operator API.
