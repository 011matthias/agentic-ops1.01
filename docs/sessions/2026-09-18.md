---
date: 2026-09-18
sessions: 1
projects_touched: [brisken]
friction_events: 3
work_types: [client-dev]
---

### Session 1 — Brisken Recon Item 98 One Currency
**Type:** client-dev
**Focus:** The prompt's ground rules said item 98 was out of licence scope; the owner had reversed that the evening before, which made it the highest-ranked unshipped audit item. Built, adversarially reviewed, fixed, shipped and driven live.
**Projects:** brisken
**Built:** Item 98 (PR #1076, Fly v179): output/single_currency.py converts each listing row (a USD row is itself, a receipt settled by a reconciled USD charge converts at THAT CHARGE, everything else at the matcher's own reference rate, an unpriceable row is named not zeroed). expenses.csv fills the EXISTING Exchange Rate column and states the month's total; the month report prints each figure and rate under the amount and per-card figures on the sums lines. 19 tests, nine wiring points proven red by hand, suite 2440 passed. Record in PR #1077 (Shipped row 85).
**Did NOT work:** Asserting the PDF's per-row caption with an exact-text needle (why not: The caption renders correctly but wraps inside the 54-point Amount column, so pypdf returns '= USD 56.00 at\n1.12, the charge'. The needle reported a caption missing that was on the page; same shape as the prior session's Guess/GUESS miss. Flatten whitespace before matching PDF text.); Patching a module source with Python text-mode read_text/write_text (why not: Text mode translated CRLF to LF on read and wrote LF back, flattening the whole file and turning a 173-line addition into a whole-file diff. Use read_bytes/write_bytes on these CRLF sources.); Heading a partial figure 'Total in USD' with the caveat in parentheses (why not: On the first fixture that hit it (EUR 32.00 + USD 15.00, no EUR rate) it printed 'Total in USD: 15.00' for a month that cost more. A heading is what a reader carries away and a parenthetical cannot undo one; the label itself now reads 'Partial total'.); Editing the worktree while the adversarial reviewer was reading it (why not: It caught an inconsistent save mid-review (NameError from a half-applied edit) and had to re-verify every finding against hashes of the final files. Commit first, then dispatch the review against that SHA.); tools/regress_check.py for the wiring proofs (why not: It reports 'RED (no pytest summary line)' even when the mutated suite is green, so its verdict cannot distinguish a test that bites from one that does not. All nine proofs were run by hand instead: copy aside, single-line disable, targeted pytest, read the FAILED lines, restore, assert byte-identical.)
**Friction:** 3 - verification-theater (recorded Fly v178 in the backlog and a PR body without querying flyctl releases; a sibling had deployed v178 38 min earlier, mine was v179), slow-path (about seven calls to hooked shell traps: cd-guard x3, heredoc triple-quote x2, MSYS leading-slash advisories), slow-path (Python text-mode patch flattened service.py's CRLF to LF, needing a repair pass)
**Gates:** B1:0 B2:6 B3:1 skipped:0
**Autonomy:** 0 human interventions
**Outcome:** Item 98 live on Fly v179 and verified on the real consumer: July CSV closes Total in USD 58,187.69 (34 of 56 rows priced), August 2,808.91, both matching the pre-build prediction to the cent. p1 defect queue still empty; what remains is owner-gated or the unstarted audit items the reversal put back in scope (104, 123, 125-128).
