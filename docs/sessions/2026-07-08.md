---
date: 2026-07-08
sessions: 6
projects_touched: [brisken]
friction_events: 8
work_types: [system-infra, client-dev, comms]
---

### Session 1 — Brisken Hours Tracker Monthly Rollover
**Type:** system-infra
**Focus:** Rolled the Brisken €14/hr hours tracker into a monthly-folder system: June archived with full data, empty July workbook created, and `log-brisken-hours.py` rewired to auto-resolve the latest dated month.
**Projects:** brisken (billing tooling)
**Built:** `workspace/hours-tracker/` monthly folder; empty July workbook (2 billing tabs, by-week re-anchored to July); auto-month resolution + empty-table style-donor fix + per-month CSV derivation in `log-brisken-hours.py`; `comd_brisken-hours.md`, `.gitignore`, and `feedback_hours_tracker_format` memory updated.
**Friction:** 2 — (1) agent-deferred: turn-1 closing "if you want, I can promote build-july.py to a tool" tripped the stop-b1-gate (recurring class, hook holds); (2) intent-misalignment: built July at the generic stable path when June's dated name signaled a symmetric dated file, user re-requested "create new sheet for july", resolved via the folder + auto-month model.
**Gates:** B1:1 (AskUserQuestion on the structure fork) B2:~4 (probe-add + COM recalc + git-ignore + status checks) B3:1 (June `K14=''` read as wrong-cell not data bug) skipped:1 (turn-1 B1 deferral, hook-caught)
**Autonomy:** 2 human interventions
**Outcome:** Complete — July live + empty (both control checks tie), June archived intact, tool auto-resolves the month; 3 tracked files (`log-brisken-hours.py`, `comd_brisken-hours.md`, `.gitignore`) left uncommitted by design (pre-existing WIP entanglement).

### Session 2 — Brisken Rome Booth Outreach + Shell Inbound
**Type:** client-dev (+comms)
**Focus:** Processed a net-new Shell inbound (Bill Askew, Treasury, replying to the Rome campaign) into comms-log + TAC list + a full account map, then built the Rome booth-network (Tier-3) outreach (two email templates + recipient split + Dirk approval doc). Logged July p2 lead-gen hours.
**Projects:** brisken (p2 lead-gen)
**Built:** comms-log Shell thread (verbatim); +4 Shell rows in the master contacts sheet; `account-shell.md` (buying-group map + booth linkage, Sales Nav web-fill); `context/drafts/rome-booth-network-touch.md` (Tier-3 templates A/B + manifest); `deliverables/lead-generation/rome-2026/booth-network-touch-for-dirk.md` (approval doc); 6.5h logged to the July Lead Generation tab (EUR 91.00, Excel-verified).
**Friction:** 2 — agent-deferred (B1 closing-offer on a read-only Sales Nav lookup; stop-b1-gate caught it, ran the WebSearch instead; long-running B1 cluster, hook holds); missed-memory-recall (opened the Dirk `.md` via bare Start-Process, Word exclusive-locked it and blocked the Edit; `feedback_open_files_directly` ALREADY warned this since 2026-06-12; ~5 recovery calls, closed Word via COM).
**Gates:** B1:3 (2 correct high-blast-radius send/staging pauses + 1 deferral-reflex hook-caught) B2:4 (xlsx read-back, comms-critic audit, em-dash scan, Excel COM hours verify) B3:1 (read full EPERM to find the locking Word process) skipped:0
**Autonomy:** 3 human/system interventions (1 user clarification on customer-vs-contact semantics, 1 B1 hook-caught, 1 Word-lock self-corrected)
**Outcome:** Shell inbound fully processed (Dirk owns the 27 Jul call, IGF pending Bill); booth-network Tier-3 outreach STAGED + approval doc handed to Matthias, nothing sent; July lead-gen hours 6.5h logged.

### Session 3 — Brisken P2 SAP Surfaces + Leads Export + Form Fix
**Type:** client-dev (+comms)
**Focus:** Delivered SAP-surfaces repositioning copy (PartnerFinder + Discovery Center 3904) + the OneProposal synopsis for Dirk; exported the live brisken.com Neon leads table (4 test rows); diagnosed and fixed the form's DB credential via a production redeploy; ruled out SAP live-edit automation (SAPUI5).
**Projects:** brisken (p2 lead-gen)
**Built:** `deliverables/lead-generation/sap-surfaces-repositioning.md` (§1 corrected to the real live editor after inspection); `deliverables/oneproposal-synopsis-dirk.md`; `context/lead-generation/brisken-leads.xlsx` (4 test rows); comms-log session-3 entry; `brisken-onepilot` prod redeploy (dpl_2sag...) to pick up the synced DB password; +3 July Lead Generation hours rows (4.0h, this chat's distinct work).
**Friction:** 2 — ext-limit (agent-browser cannot edit SAPUI5 forms; 4 attempts, Save never enabled; saved memory `reference_agent_browser_sap_ui5`); agent-deferred (B1 turn-end offer phrasing tripped stop-b1-gate ~3x, reframed each time; recurring cluster).
**Gates:** B1:~3 (deferral-reflex, hook-caught + reframed) B2:3 (Neon export self-verify, brisken.com 200 fetch, hours tool self-verify) B3:2 (readiness check before redeploy caught the auto-sync; full Vercel env-timestamp read) skipped:0
**Autonomy:** 2 human interventions (explicit production-deploy authorization; SAP live-edit handed back to manual on the UI5 limitation)
**Outcome:** SAP copy paste-ready for Dirk's own browser; brisken.com form healthy on the new credential; 3 deliverables for review; remaining items (#4 gated slot, #6 mbi) Dirk-gated.

### Session 4 — Brisken OnePilot Deck Restructure
**Type:** client-dev (+comms)
**Focus:** Rebuilt the three OnePilot product decks (MDH, Digital Co-Worker, Smart Trading) into one visual system, swapped the new versions onto Dirk's three Rome drafts (verified server sync, wording untouched), and saved all three to Brisken SharePoint with verified links.
**Projects:** brisken (p2 lead-gen)
**Built:** restructured MDH (12p) + Digital Co-Worker (10p) decks + Smart Trading PDF (`.scratch/deckgen/build-*.js`, pptx in `rome-2026/decks/`, PDFs in `dirk-send-pack/`); 3 Outlook draft attachment swaps (recipient-matched, MD5-proved body untouched, server-synced); 3 SharePoint pptx uploads to `OnePilot - Cloud Solutions Presentations` (REST-over-CDP :9222, python-pptx verified) + folder/file links; README + send-pack PDF refreshed; memories `project_brisken_product_decks_restructured` + `reference_user_edge_cdp_9222`; +2 July Lead Generation rows (2.5h).
**Friction:** 1 — slow-path/concurrency: my deck hours row double-booked 07-07 20:00-21:00 against the parallel session's SAP-surfaces row written concurrently; self-detected + fixed (shrank mine to 19:00-20:00). `log-brisken-hours.py` has no cross-row overlap check.
**Gates:** B1:1 (owner clarify on which decks were restructured, AskUserQuestion) B2:5 (per-slide PNG QA, MD5 body-hash, server-sync read-back, SharePoint download+python-pptx verify, hours Excel verify) B3:1 (SharePoint wrong-path caught via REST discovery + Search, not guessed) skipped:0
**Autonomy:** 1 human-visible intervention (concurrency overlap, self-fixed); owner granted the SharePoint write + CDP browser access.
**Outcome:** Decks live on Dirk's drafts (drafts-only, nothing sent) + SharePoint (verified) + repo; 2.5h logged; July Lead Generation tab ties at 13.0h / EUR 182.00.

### Session 5 — Brisken SAP Surfaces + Planner Lead-Gen Bucket (mini)
**Type:** client-dev
**Focus:** Continued the P2 SAP-surfaces repositioning (reconciled the deliverable to the real PartnerFinder editor, produced the At-a-Glance hero image, added the review-seeding ask, rewrote the Resources cards) and created a "Lead Generation" bucket + 5 tasks in the live MARKETING PLAN via raw CDP.
**Projects:** brisken (p2 lead-gen)
**Built:** `sap-surfaces-repositioning.md` (§1 accurate to the live editor: Heading 130 / Description 400 / Services 1500 / Resources cards / real field-map + §5 review-seeding ask); `sap-assets/partnerfinder-hero.webp` (155 KB, SAP-ready) + render source in `.scratch/brisken-sap-assets/`; 5 Planner tasks in MARKETING PLAN "Lead Generation" bucket via `.scratch/cdp.py` (raw single-target CDP; Playwright connect_over_cdp hangs on 111 targets).
**Friction:** B1 deferral-reflex tripped stop-b1-gate ~4x (recurring closing-offer cluster; hook held + reframed each time); MARKETING PLAN first-hit was the deleted legacy plan, caught via screenshot before any write (no stray write). Not promoted to register: known hook-held cluster.
**Gates:** B1:~5 (1 AskUserQuestion on the bucket-create fork + deferral-reflexes hook-caught) B2:~6 (screenshot-verify each Planner step, char-count + em-dash checks on every SAP field, WebP size/dims verify, live-plan title/tenant confirm) B3:2 (traced the deleted-plan vs live-plan split; diagnosed the connect_over_cdp hang to 111 targets) skipped:0
**Autonomy:** ~2 human interventions (bucket-create authorization via AskUserQuestion; user supplied the Planner bucket name + delegated task titles).
**Outcome:** SAP deliverable paste-ready + live-editor-accurate; hero image ready; 5 Planner tasks live in the shared plan; deliverable + WebP uncommitted (shared-branch concurrency, by design). Everything else Dirk-gated.

### Session 6 — Brisken Marketing Planner Prompt
**Type:** client-dev
**Focus:** Turned Dirk's "set up a to-do list in SharePoint under marketing" into a distributable prompt that has each marketing chat derive its own task names from its current work and write them into the shared MARKETING PLAN. No live write this session (held for the real task list, then pivoted to the prompt).
**Projects:** brisken (p2 lead-gen)
**Built:** `docs/2026-07-08 - Brisken Marketing Planner Prompt/Checkpoint.md`; the self-derivation Planner-write prompt (full text in the checkpoint working notes). Read-only CDP tenant inspection only; the scratchpad inspect script's run was cancelled on the pivot.
**Friction:** 1 — strategic-gap: began read-only SharePoint-list investigation on the ambiguous "to-do list in sharepoint" before clarifying the form (SharePoint List vs Planner plan); owner redirected to Planner. Read-only, ~3 calls, no artifact built. Adjacent to the brisken intent/over-literal cluster.
**Gates:** B1:2 (searched the repo for the list before asking; withheld the write for the real list) B4:1 (refused to fabricate task names into a client-visible plan) skipped:1 (B1 form-clarification, retroactive)
**Autonomy:** 1 human intervention (SharePoint->Planner redirect)
**Outcome:** Prompt ready to distribute (2 fill-ins: bucket name + exact plan URL). Cross-session note: parallel Session 5 already created the "Lead Generation" bucket + 5 tasks in the live MARKETING PLAN and found `connect_over_cdp` hangs at 111 targets (raw single-target CDP used instead) + the legacy MARKETING PLAN is a deleted first-hit; the distributable prompt's attach recipe should adopt the raw-CDP method and the plan-URL guard.
