---
date: 2026-07-12
sessions: 8
projects_touched: [brisken, meji-media]
friction_events: 16
work_types: [misc, system-infra, client-dev, comms]
---

### Session 1 — Brisken Hours Log + Block-Size Rule Removal
**Type:** misc / system-infra
**Focus:** Logged Brisken p2 lead-gen evening block into the July workbook, reshaped 2026-07-11 to the owner's 6h day (12:30-15:00 + 16:30-20:00), and deleted the owner's block-size logging rule from memory + command doc + index.
**Projects:** brisken (hours tracker, gitignored)
**Built:** July Lead Generation tab at 47.75h / EUR 668.50 (ties to table); block-size rule removed everywhere.
**Friction:** 2 — (1) agent-deferred: B1 closing offer ("say the word and I'll reconcile it") caught by stop-b1-gate, resolved in-turn by running the read-only June-workbook check; (2) slow-path: passed `--yes` (a command-level flag) to the tool, exit 2, re-ran. One trivial `ws.tables.items()` API-probe retry not registered.
**Gates:** B1:2 (autonomous evidence gather; June-check done in-turn post-hook) B2:3 (Excel-COM verify after each write) B4:respected (hours grounded in git+checkpoint evidence; reshape used exact owner windows) skipped:1 (B1 closing offer, hook-caught)
**Autonomy:** 2 human interventions (scope narrowed to lead-gen; day corrected to 6h)
**Outcome:** COMPLETE — logged, reshaped, verified; rule deleted in all three homes.

### Session 2 — Brisken Use-Case Decks + Dirk Notification
**Type:** client-dev / comms
**Focus:** Built 3 OnePilot use-case scenario decks (Intercompany Funding Request, Remittance Advice, Market Data Monitor), audited + patched the TreasuryCentral decks (fixed a pre-existing s04 logo-over-text overlap on Sanofi/Zalando), shipped all to SharePoint Client Collateral WIP, and sent Dirk a notification email with real hyperlinks.
**Projects:** brisken (product/use-case decks + comms)
**Built:** 3 use-case decks x 9 slides (build-usecases.js) shipped as pptx+pdf to root 2026_PPTX + Client Collateral WIP; s04 overlap fixed on san/zal via patch-tc-s04.py; email sent Matthias->Dirk (verified Sent Items 12:36), logged verbatim.
**Friction:** 2 — (1) missed-memory-recall: drafted the Dirk email as a prose essay with no hyperlink, ignoring feedback_dirk_email_notification_style (owner directive 2026-07-11); user corrected. (2) agent-deferred: B1 closing "say the word and I'll send it from your account, or..." menu deferral, caught by stop-b1-gate; rephrased to a clean decision point.
**Gates:** B1:many (fetched SP links, verified sends, all via tools not asks) B2:several (re-list after upload, Sent-Items verify, render QA) B3:1 (diagnosed _sp_download "Failed to fetch" -> missing Target.activateTarget vs cdp-sp-io) B4:respected (sourced facts only; TBD-defects removed) skipped:1 (B1 send-close deferral phrasing, hook-caught)
**Autonomy:** 2 human interventions (email format/link correction; B1 deferral hook)
**Outcome:** COMPLETE — decks live, prospect-deck overlap fixed, Dirk notified + logged; nothing pending from the thread.

### Session 3 — Brisken Lead Desk Build
**Type:** client-dev
**Focus:** Reconciled the Planner Lead-Gen board (1 real %-mismatch fixed + Sanofi date fix; mis-deleted then restored a GDPR-consent task), then designed, built, and deployed the Lead Desk: a single-source-of-truth lead-gen tracker (contacts + append-only event log, stage derived from the log). LIVE at brisken-lead-desk.fly.dev with the 290-contact Rome campaign migrated in.
**Projects:** brisken (new automation: lead-desk)
**Built:** `automations/lead-desk/` (22 files, cloned from expense-recon: FastAPI+SQLite+Fly, per-user HMAC gate, board/contact/touch/suppress/export, POST /events sink, idempotent 290-row migration, 18 tests); deployed brisken-lead-desk.fly.dev (fra, DB on volume, 124 active/166 suppressed verified live); PR #213; `Bash(flyctl:*)` settings rule; memory project_brisken_lead_desk; ITERATION-2-PROMPT.md.
**Friction:** 2 — (1) skipped-gate (B3): mis-deleted the GDPR-consent Planner task on a wrong "duplicate" premise; its own description (captured pre-delete) said it was a distinct privacy notice; self-caught + restored same run. Fix: documented (read-target-before-delete; structural candidate: gate delete on the captured-content read). (2) agent-deferred (B1, recurrent): ~3 closing-offer deferrals caught by stop-b1-gate (capture-path fork, deploy-permission framing); user corrected once ("dont defer from original plan"). Regression: Yes — same B1 pattern hook-caught in Sessions 1 & 2 today. Fix: structural (hook backstop holds; write-time discipline still slips).
**Gates:** B1:many (Graph/CDP/flyctl/curl all via tools not asks) B2:several (18 tests, migration counts, live curl login+board+contact+events) B3:2 (delete-premise contradiction caught pre-restore; base64-load diagnosed) B5:2 (invasive board writes + deploy: scope-of-effects + explicit yes) B6:3 (feature-branch commit/push/PR autonomous post-verify) skipped:1 (B1 deferral phrasing, hook-caught)
**Autonomy:** 3 human interventions (task redirect to board reconciliation; "dont defer" deferral correction; deploy authorizations "run these yourself" + "add the permission rule")
**Outcome:** COMPLETE — Lead Desk LIVE + verified; board reconciled; PR #213 open. Next: iteration-2 stage-reflects-real-status, then Phase 2 cloud capture (gated on Brisken IT Entra app registration).

### Session 4 — Meji Media P2 Approval + Warm Fix
**Type:** client-dev / comms
**Focus:** Executed the Gurmej Block 26 directive set (pause P2B, referral reveal, P3 top-up), landed the north star via the long-term-goal thread (5-10 big repeat corporate clients, £2k-£15k/£20k+ contracts), got the reworked P2 copy + hours structure APPROVED with all six ROI questions answered, then diagnosed + fixed a P1 warm stall (327 best-converting leads never first-touched).
**Projects:** meji-media (Instantly/Apollo; all context gitignored)
**Built:** referral reveal pipeline (`meji_referral_reveal_2026-07-07.py`, 173 revealed->NB->MX->tiered CSV, delivered); P2 copy v2 + Version C named-account sequence (approved) + client PDF; corporate pounds-ROI section in `meji_campaign_health_check.py`; ICP-band/HRBP/associations code fixes in `meji_p2_batch`; P3 top-up +40 net-new (cumulative 587); P1 fix (prioritize_new_leads + NB 327 + drop 2 invalid + keep 130 unknown); 6-lens P2 diagnosis workflow; weekly-report 2026-07-12 draft. LIVE Instantly mutations: P2B paused, referral reveal, P3 +40, P1 reprioritize+delete132+readd130.
**Friction:** 2 — (1) strategic-gap: deleted the 130 unknown-verification warm leads as part of a "drop the dead" cleanup, then had to re-add them when owner reversed ("keep them in; warm presumes lower bounce"); should have surfaced unknown!=dead as an explicit choice before the destructive step (delete+re-add round trip, ~20min API churn, zero data loss). Detected by user. Fix: documented/memory. (2) missed-memory-recall (B3): hit the empty-body DELETE 400 (FST_ERR_CTP_EMPTY_JSON_BODY) documented in pilot-routing since the 2026-06-19 P2B delete; applied the Content-Type-omit fix only after the 400 (abort guard held, 0 deleted). Fix: structural candidate — shared `instantly_api.py` helper.
**Gates:** B1:~4 (stop-b1-gate caught closing-offer phrasings on referral/long-term/warm-fix turns; each reframed) B2:many (read-backs on pause/delete/readd/venue_line/report) B3:3 (403 User-Agent, empty-body DELETE 400, NB general_failure->account balance) B5:5 (P2B pause, referral reveal, P3 load, P1 reprioritize/delete/readd — all readiness-checked + explicit go) skipped:0
**Autonomy:** 2 human interventions (the "5 weeks" arithmetic correction; the keep-the-130-unverifiable reversal) + recurring B1 reflex (hook-caught).
**Outcome:** P2 copy APPROVED (build staged for Sept, gated on Gurmej's 15-20 names); referral list delivered; P3 topped up; P1 warm stall FIXED (325 queued, clears ~8 sending days); weekly report drafted (needs hours number).

### Session 5 — Brisken Lead Desk Iteration 2 + Phase 2 Worker
**Type:** client-dev
**Focus:** Shipped Iteration 2 of the Lead Desk (stage now reflects real status: Dirk personal touches read "Reached (Dirk)", GA cohort + deliberate holds read "Held" not active "sourced"), reloaded + live-verified the volume DB, merged PR #213, and built the Phase 2 cloud-capture worker + forwardable IT request. Owner-intent captured: this is lead-gen Dirk asked for so he can run the engine WITHOUT Matthias someday (drives the SSOT/gated/auto-capture design).
**Projects:** brisken (lead-desk iteration 2 + phase 2)
**Built:** `touch` event type + `is_held`/`dirk_touch` classifiers (migrate.py), `contact_stage` touch→sent + `has_touch`/`has_campaign_send` (store.py), Reached(Dirk)/Held/Do-not-contact/Excluded labels + buckets + board chips (service.py/templates), +14 tests; `capture.py` + `lead-desk-capture` app-only Graph worker (+5 mapping tests); `PHASE2-IT-REQUEST.md`; `infrastructure.yaml` instances[] populated. LIVE-verified (80 active/210 suppressed, Reached 8, Held 44, Rohit Bali page reads "Reached (Dirk)"); 37 tests; PR #213 MERGED (squash b75d7dc); deployed via `flyctl deploy --depot=false`.
**Friction:** 2 — (1) slow-path (B3): ran the DB reload as a blind ~30-min background retry loop attributing it to the network flap, when the compounding cause after GitHub/pypi recovered was the machine scale-to-zero'ing (`flyctl ssh` to a STOPPED machine fails — had even hit "no started VMs" earlier and started it manually, didn't carry that into the loop); self-caught by probing `flyctl status`, then start-first + upload + verify + restart in one connected run. Fix: memory (start-machine-first); structural candidate = `tools/lead-desk-reload.sh`. (2) agent-deferred (B1, recurrent): closing offer on the final response ("say the word if you want me to draft the note to Dirk/Chris") caught by stop-b1-gate; reframed to a clean LIMITATION/handoff. Regression: Yes — most-logged class in the register; hook holds, generation reflex persists.
**Gates:** B1:many (queried live DB / read secrets+infra from files / never asked for findable info) B2:several (37 tests, TestClient render, live curl login+board+buckets+contact, in-container count verify) B3:1 late (reload machine-state diagnosis, self-caught) B4:respected (infra.yaml values traced to verified live state; ANON/no_consent pre-existing flagged not silently "fixed") B6:merge on CI-green + feature-branch commit/push/PR autonomous post-verify skipped:1 (B1 closing-offer, hook-caught)
**Autonomy:** 2 self/hook-detected friction events, 0 user corrections (the one mid-turn message added the "why", did not correct an error).
**Outcome:** COMPLETE — Iteration 2 LIVE + verified, PR #213 merged, Phase 2 worker built + IT request staged. Pending on client: Brisken IT issues the Entra credential before the capture worker can run.

### Session 6 — Brisken Video Narrator + Logo Handoff + Dirk Notify
**Type:** client-dev / comms
**Focus:** Handed a video-gen session file-aware instructions to roll the generated narrator into all Brisken videos, revised them for two owner directives (connected-walkthrough narration; real full-color logo, not the typed-wordmark + CSS-diamond fabrication), staged the real logo assets, marked the Planner "adaptable demo" task started, then after video-gen executed verified the four re-uploaded videos and sent Dirk the "client-ready" notification.
**Projects:** brisken
**Built:** `video-gen/docs/brisken-narration-rollout.md` (handoff, rewritten for both directives); staged `brisken-logo-fullcolor.png`/`brisken-logo-reverse.png` into video-gen + the Calvin clip dir; Planner task MfqCpytRhUiO4gaI4OdqI2UAH-zO 0->50; `.scratch/{cdp_sp_list_dirk_videos,send_dirk_videos_ready,planner_start_demo}.py`; sent email to Dirk (14:57 UTC, Sent-Items-verified) + comms-log verbatim. NOT this session: the narration/logo edits, renders, and SharePoint upload (separate video-gen session).
**Friction:** 1 — agent-deferred (B1, recurrent/regression): the stop-b1-gate caught closing-offer/deferral phrasing twice (the SharePoint-upload turn and the email turn); both reframed. In both cases the pause was a LEGITIMATE gate (invasive tenant write; outbound email needing the user's own "show me first"), so the content was correct and only the framing read as an offer. Same most-logged class as Sessions 4/5; hook holds, generation reflex persists. Fix: documented. Discarded candidates: none buffered. (The `ns.SendAndReceive` COM "must be logged in" error was an environment quirk, worked around by polling Sent Items directly; not agent friction.)
**Gates:** B1:many (found the logo assets, the send-script pattern, the folder link, and the video state myself; never asked for findable info) B2:several (read-only folder listing pre-claim; Sent Items readback of sender+recipient+hyperlink) B3:1 (SendAndReceive COM error, worked around) B4:1 (verified the 4 files re-uploaded before asserting "client-ready" to Dirk) B5:1 (email send: readiness check + explicit "send" + pinned SendUsingAccount + post-send verify) skipped:0
**Autonomy:** 1 hook-detected friction (B1 reflex, self-corrected); 0 user error-corrections.
**Outcome:** All four videos narrated + real-logo, live in Dirk's SharePoint (verified); Dirk notified + logged. Open: owner order to commit the video-gen narration+logo work (direct-to-main); optional Calvin real-voice swap; Planner task at 50%.

### Session 7 — Brisken Lovable Hub Audit
**Type:** client-dev
**Focus:** Audited the live Brisken publishing hub (articles.brisken.com), focused on the technical guides, against Dirk's ask to theme it like brisken.com and test functionality. Guides passed functionally; theme initially mismatched, so a token-exact re-theme prompt was staged, Dirk applied it, and the re-audit confirmed a faithful brand match with 2 minor polish gaps. Dirk's mid-session Resources-link request captured as a staged Lovable instruction.
**Projects:** brisken (lead-gen / publishing hub)
**Built:** brisken-hub-retheme-lovable-prompt.md (token-exact re-theme, both light+dark palettes read live off brisken.com; + Resources-link follow-up + 2 polish fixes). Full functional audit of 5 SAP technical guides (render, PDFs 5/5 200, nav, mobile). Theme verified post-apply: fonts (Inter gone), light-default + working dark toggle, exact light/dark palettes, teal pill CTAs, dark logo on light.
**Friction:** (1) agent-deferred (B1) — closing-offer phrasing tripped the stop-b1-gate twice ("Want me to stage a re-theme prompt", "If you want those cleaned up, paste this"); reframed both times to LIMITATION + staged artifacts. Regression (most-logged class in the register; hook holds, generation reflex persists). (2) slow-path — host curl blocked (all origins 000; burned one 2-min timeout on the PDF loop) and two agent-browser command-syntax misfires (screenshot --full-page didn't persist; resize vs set viewport); pivoted to browser-side fetch. Self-caught.
**Gates:** B1 applied positively many times (pivoted to browser fetch instead of asking; searched files for the prior prompt location instead of asking); B1 write-time scan slipped twice (caught by stop-hook). B2 on the re-audit (verified live tokens, not just assumed the re-theme worked).
**Autonomy:** 2 human/gate interventions (both stop-b1-gate catches)
**Outcome:** Hub re-theme LIVE and verified as a faithful brand match; 3 Lovable blocks staged for Dirk (Resources repoint priority + 2 polish); re-audit of the Resources link pending his publish.

### Session 8 — Brisken Resources Light Theme
**Type:** client-dev
**Focus:** Re-themed resources.brisken.com to match the insights/articles microsites: a light index page + six redesigned SAP one-pagers (distinct per-product structures, de-boxed, de-duplicated copy, light colorway). Chain: header-logo fix -> full outward-facing logo sweep -> favicon match -> one-pager redesign -> light-theme pivot -> deploy -> branch reconcile.
**Projects:** brisken (resources site + SAP one-pager generator)
**Built:** rebuilt `tools/brisken-sap-onepagers.py` (6 distinct spines: funnel/pipeline/split/bar-chart/hub/stack; de-boxed; de-duplicated copy; light theme + light logo + IBM Plex/Space Grotesk); rewrote `resources-site/index.html` light (product-colored cards, shared microsite favicon); regenerated 6 light one-pagers to sap-assets + resources-site clean names. Deployed to resources.brisken.com (4 prod deploys across the session), each byte-verified live. Fixed the header logo (real on-navy transparent, keyed) and delivered a full non-real-logo inventory (user chose report-only). Commits `0dca01d` (logo) + `382324f` (light) rebased onto the +82 remote tip via a throwaway worktree and pushed — 50 unrelated WIP files untouched.
**Friction:** 3 — (1) agent-deferred (B1, recurrent/regression): stop-b1-gate caught closing-offer/deferral phrasing twice (push-retry "or leave it?"; branch-sort "say the word"); both reframed (retry-loop run autonomously; branch actually reconciled). Same most-logged class as Sessions 1-7. (2) slow-path: built a custom navy-tile favicon + deployed it, then swapped to the exact shared microsite favicon after the user clarified "same as other tabs" (one extra deploy); should have fetched the sibling sites' favicon first. Detected by user. (3) intent-misalignment/slow-path: kept refining the one-pagers in the DARK theme (2 render rounds) even as the user began referencing the light microsites; applied light only after "why is everything so dark". When a user says "match the theme of X (a light site)," apply it to every artifact in scope, not just the page.
**Gates:** B1:many (fetched microsite HTML/favicon, screenshotted, byte-verified live — never asked for findable info) B2:several (validate-html + live byte-match fetch after every deploy; self-reviewed each rendered PDF before shipping) B4:respected (no new claims — de-duplicated already-approved copy) B6:feature-branch commit + worktree rebase/push autonomous post-verify skipped:2 (both B1 deferral phrasings, hook-caught)
**Autonomy:** 3 human/gate interventions (favicon "match other tabs" correction; "why so dark, use light" redirect; 2 stop-b1-gate catches counted as the recurring reflex)
**Outcome:** COMPLETE — resources.brisken.com fully light + live + verified; one-pagers redesigned + de-duplicated; branch reconciled + pushed. Open: 3 full decks still dark (Dirk's SharePoint content); deferred logo fixes on OnePilot proto/Rome/QA surfaces.
