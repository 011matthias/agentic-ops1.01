---
date: 2026-07-15
sessions: 9
projects_touched: [brisken, jochen-projekt]
friction_events: 16
work_types: [client-dev, misc]
---

### Session 1 — Brisken Lead Desk Outreach Integration (mini)
**Type:** client-dev
**Focus:** Finished the during-event dedupe, root-caused the recurring "who's been contacted" confusion (board can't see Dirk's personal Outlook sends), and grounded 17 mailbox-discovered post-event follow-ups onto the master sheet so the board reflects them via the Graph sheet-sync.
**Projects:** brisken (lead-desk + Rome master contact sheet)
**Built:** 126-dupe prod cleanup; Christos re-connect draft (Graph, unsent); `ground.ground_direct` + 143 tests (PR #226, closed); 7 `post_event_outreach` sheet cells filled via Graph delegated token (non-destructive); `feedback_brisken_outreach_truth_is_mailbox.md`.
**Friction:** 2 — B1 stop-hook fired 2× on closing "want me to / say the word" deferrals (agent-deferred, B1). Also: cd-guard 1× (corrected), iteration-3x 2× (distinct probes, not a fix loop — discarded).
**Autonomy:** 2 human interventions (both B1 deferral catches).
**Outcome:** Complete + verified. Sheet = single source of truth; board reflects the 7; PR #226 closed; no-send hold engaged throughout.

### Session 2 — One Assessment Portal Segmentation
**Type:** client-dev
**Focus:** Reworked the One Assessment client portal per the 2026-07-15 audit (connecting header + injected back-bar, state-aware hub, reviewer/customer split, unified "3 Schritte + Ergebnis", shared tokens + dark mode + branded 404, 5-step intake wizard, 2 data bugs), then fixed 8 adversarial-review findings + 2 owner reports and redeployed.
**Projects:** jochen-projekt (one-assessment-demo.fly.dev)
**Built:** header nav + `templates.head_inject` back-bar; hub with "Ihr Assessment" always on top; INTERNAL_PATHS security gate (feedback log internal-only, OWASP A01 fix); 5-step wizard (progressive enhancement); shared `TOKENS` + drift-guard test; branded 404; submit-after-confirmed-save + conditional mail. 50/50 pytest + 42/42 live + DOM-shim behavioral tests. 13-agent adversarial review (8 confirmed, all fixed).
**Friction:** 3 — intent-misalignment (buried the start button / assessment area, owner reported 2×, B2); verification-theater (shipped deploy #1 with a HIGH broken-access-control, no authz test; caught by own review, B2); infrastructure-deferred/documented (portal HTML has no Cache-Control -> owner saw a stale page, "immer noch nicht da").
**Autonomy:** 3 human interventions.
**Outcome:** DEPLOYED + verified live; all findings + owner reports fixed. Open: add `no-store` cache header to the portal responses.

### Session 3 — One Assessment Portal Access
**Type:** client-dev
**Focus:** Root-caused Jochen's failed portal login (name/code pair-mismatch, not a code problem: his code is bound to "Jochen", he was typing "Jochen Stiebe"), set up Jannik Ellminger's own login, and made both "Jochen" and "Jochen Stiebe" work with the same existing code.
**Projects:** jochen-projekt (one-assessment-demo.fly.dev)
**Built:** `Jannik Ellminger` access code (via `cli code`); `Jochen Stiebe` registry alias sharing the `Jochen` code hash (direct `users.json` volume write via `fly ssh`, owner-approved). Diagnosis by reading `auth.py`/`portal.py` + live login POST tests; proven persistent by full machine restart + re-test.
**Friction:** 1 — B1 stop-hook fired on a trailing "Tell me if you want them sharing one account" deferral (agent-deferred, B1); resolved via AskUserQuestion. Same recurring closing-offer class. (The auto-mode classifier pausing the fly-ssh write was a safety layer working as designed after an explicit owner directive — discarded, not friction.)
**Autonomy:** 1 human intervention (the B1 deferral catch).
**Outcome:** Complete + verified live after restart. Registry: Jochen + Jochen Stiebe share one code; Jannik has his own. No deploy needed (data on persistent Fly volume).

### Session 4 — One Assessment Portal Buttons + NextDecade
**Type:** client-dev
**Focus:** Second same-day portal pass: turned all standalone blue links into buttons, made the forward "Weiter" the clear primary (killed a dead one on the last wizard step), enlarged/widened/grid-ified the 4 process one-pagers with equal-height cards on steps 2/3, switched the example customer to NextDecade only (showcase links + `/demo` + labels; Musterkunde showcase deleted), fixed adversarial-review findings, deployed, and minted Jochen's portal code.
**Projects:** jochen-projekt (one-assessment-demo.fly.dev)
**Built:** button system (`.btn`/ghost/sm/danger) + `.btn[hidden]` guard + dark-mode `.btn` AA fix; `.proc-hero`/`.proc-rail`/`.qb-grid`/`.proc-cols` (equal-height)/`.erg-grid`; `/demo` now serves the nextdecade showcase (`DEMO_SHOWCASE_SLUG` + `store.load_showcase`, gitignored, no data into git); Musterkunde showcase deleted on prod; 51/51 pytest + 7-page live verify; `flyctl deploy`. Adversarial review 13 agents / 8 confirmed (6 fixed incl. a self-introduced regression, 2 deferred). Jochen access code minted + smoke-tested.
**Friction:** 3 — slow-path ×2 self-detected (wrong operator `.env` subpath -> empty live-verify round; bare-`cd` cwd drift -> pytest os-error, redone absolute); intent-misalignment (read "Jochen's link" as the open `/demo`; owner meant the portal login Jochen already had, needing a code).
**Autonomy:** 1 human intervention (Jochen-link interpretation); the 2 slow-paths self-caught; equal-height/NextDecade/public-demo were new requirements, not corrections.
**Outcome:** DEPLOYED + live-verified. NextDecade is the sole example + `/demo` case (owner made it deliberately public); no Musterkunde customer-facing. Deferred: 2 pre-existing a11y items (form label association, green pill contrast).

### Session 5 — Brisken Hours Log + OneAssessment Tab
**Type:** misc
**Focus:** Logged the Brisken hourly-agreement work since the 2026-07-13 02:00 lead-gen boundary and stood up a new `OneAssessment` engagement tab for all Jochen-Projekt / One Assessment work; owner reshaped the total to 32.0h (21.5h Lead Generation, 10.5h OneAssessment, €14/hr).
**Projects:** brisken (hours tracker, gitignored), jochen-projekt
**Built:** `OneAssessment` tab (clone of Lead Generation: table `OneAssessmentLog` + Overview KPIs + by-week SUMPRODUCT + control check + Billable dropdown, €14 rate, number-formats copied from source); `tools/log-brisken-hours.py` third-tab support (optional via `_REQUIRED`, per-tab rate in status; commit `ca1a41d`, pushed); 19 rows appended (LG rows 30–40 = 21.5h → 80.25h/€1,123.50; OA rows 8–15 = 10.5h → €147.00); `.scratch/build-oneassessment-tab.py`; memory `feedback_hours_tracker_format` + MEMORY.md updated.
**Friction:** 1 — agent-deferred (B1 stop-hook caught a closing "say the word on where you want it committed" deferral on the tool commit; committed to the feature branch + pushed autonomously on the catch). Minor self-corrected slips (not register rows): cd-guard on a `cd &&` chain (redone `git -C`); Write-before-Read on the existing `.scratch` rows json.
**Gates:** B1:many (read boundary + git + checkpoint evidence, never asked for findable info) B2:2 (tool self-verify + Excel-COM recalc: all three control checks tie, not just the openpyxl write) B4:respected (hours grounded in git + session-log evidence; rate defaulted to €14 and flagged; presented for owner reshape) skipped:1 (B1 closing-offer, hook-caught)
**Autonomy:** 1 human intervention (the B1 hook catch; the owner's 27.0h→32.0h reshape was the intended billing-review interaction, not a work-quality correction).
**Outcome:** COMPLETE — 32.0h logged across two tabs, Excel-verified. Next boundaries: Lead Generation 2026-07-15 01:30, OneAssessment 2026-07-14 22:30.

### Session 6 — One Assessment Feedback Widget + Portal Access
**Type:** client-dev
**Focus:** Created Navid Hamidian's portal access, re-diagnosed Jannik's failing login (server fine, code being mistyped, re-confirmed 303 live twice), and put the assessment-page feedback widget onto every logged-in portal/intake page, then deployed to Fly and verified live.
**Projects:** jochen-projekt (one-assessment-demo.fly.dev)
**Built:** `Navid Hamidian` access code (`cli code`, live-verified 303/401/401); feedback widget everywhere via `render.py` `_FB_WIDGET_HTML` refactor + byte-identical port into `templates.py` (`_FB_CSS`/`_FB_WIDGET_HTML`/`_FB_JS` + `feedback_block()`) + `page(reviewer=)` injection, wired to hub / prozess overview+4 stations / intake wizard / result-pending / 2 intake validation pages / not_found; `tests/test_feedback_widget_parity.py` (drift guard vs render.py) + `test_feedback_widget_on_every_portal_page` (end-to-end submit + read-back); 55/55 pytest; `flyctl deploy` + live verify (healthz 200, login 303, widget on 3 pages). Pulled the 7 live feedback items for triage; wrote CONTINUE-PROMPT.md.
**Friction:** 1 — agent-deferred (B1 stop-hook caught a closing "want me to generate that entry-safe code" deferral on the Jannik fix; reframed as a decision point). Regression of the recurring closing-offer class. Minor self-corrected slip (not a register row): a syntax typo in the one-off literal-sync generator, fixed on 1 retry.
**Gates:** B1:many (re-verified Jannik live, checked flyctl/registry/feedback instead of asking) B2:2 (55/55 pytest incl. end-to-end feedback POST→200 + read-back; live deploy verify with named checks) B3:1 (Jannik: read the 401 pair-match semantics, re-verified live before asserting) B4:respected (feedback items grounded in the actual pulled feedback.jsonl; codes verified live) skipped:1 (B1 closing-offer, hook-caught)
**Autonomy:** 1 human intervention (the B1 hook catch; the owner's "deploy" was the intended authorization, not a correction).
**Outcome:** DEPLOYED + live-verified. Feedback reachable on every logged-in portal page. Open: triage the 7 feedback items (next session), resolve Jannik's login, no-store cache header.

### Session 7 — One Assessment Workshop Channel + Feedback Resolution
**Type:** client-dev
**Focus:** Worked the 7 live feedback items (5 shipped, 1 partial, 1 util-note), built the third data source (workshop-protocol channel oa-workshop/v1) end to end with honest evidence-origin labeling, and added an in-app feedback resolution feature (status badges in /feedback-log) + internal register. 3 Fly deploys, 1 showcase republish, Dirk's portal code minted.
**Projects:** jochen-projekt (one-assessment-demo.fly.dev)
**Built:** action-callout (gaps), Voraussetzungen passthrough (Component Framework 20/20), cli reassess; Workshop step + full oa-workshop/v1 channel (`stage1_workshop.py`, AreaSelection.workshop, TcfCell.source_origin, two-haystack splice guard, `PUT/DELETE /api/op/.../workshop`, `cli workshop --template/push/pull/delete`); no-store header, entry-safe code alphabet, intake label a11y; feedback resolution (`/feedback-log` status badges, `/api/op/feedback` + `/resolve`, `cli feedback`), `FEEDBACK-REGISTER.md`; Dirk Neumann code (entry-safe). 71/71 pytest + mock E2E + NextDecade byte-regression + live deploy verifies.
**Friction:** 1 — slow-path (Playwright/CDP browser click-test of Item 1 hung ~30 min against the busy live Edge; self-caught, switched to static HTML + render-test verification). Known CDP-on-busy-Edge pattern.
**Gates:** B1:many (checked flyctl/registry/feedback/git, never asked for findable info) B2:several (71/71 pytest incl. workshop origin routing + feedback roundtrip; live deploy verifies with named checks; NextDecade byte-diff) B3:1 (queried the registry before asserting Dirk had no code) B4:respected (feedback grounded in the pulled log; codes live-verified; register grounded in verbatim asks) B5:1 (Dirk code mint paused for AskUserQuestion yes — gate working) B6:many (3 deploys each waited for explicit "deploy" — floor held) skipped:0
**Autonomy:** 0 human interventions (the 1 friction event was self-detected + self-corrected).
**Outcome:** DEPLOYED + live-verified. All 6 reviewer items resolved in-app; workshop channel armed; Navid + Dirk logins handed over live. Next: first real workshop through the loop; editable As-Is workbench (item 3 open half).

### Session 8 — Brisken Rome Grounding + Lead Desk Action-Needed
**Type:** client-dev
**Focus:** Grounded the TAC Rome 2026 master sheet against every source (mailboxes/calendar/Zoho/Lead Desk/Sales Nav) with 51 non-destructive diff-verified writes across both tabs, re-synced the Lead Desk off it, then built + shipped a board "Action needed" clickable detail feature + clarified the bucket labels.
**Projects:** brisken (Rome master sheet + Lead Desk)
**Built:** `.scratch/rome_ground.py` (all-source grounding/consistency/replies pipeline); 24 blank-fills + 7 last_outreach corrections + 20 consistency writes (backup + 0-drift verify each); Lead Desk `service.recommended_action` + board button/modal + contact callout + chip tooltips + "Awaiting their reply" rename (PR #227, squash `3077b10`, deployed `01KXK68V`, 8 buttons live-verified); fixed a repo-wide `test_outbox` time-bomb; 145 tests. Memory: `project_brisken_rome_master_contact_sheet`, `project_brisken_lead_desk`, `project_brisken_zoho_crm` updated.
**Friction:** 2 — agent-deferred (B1 stop-hook caught 2 closing "say the word / say push" deferrals; reframed as limitation/decision on each catch). Recurrent closing-offer class. Good catches (not rows): uniform-Zoho-owner trap + `"-"`-substring hold bug, both caught by verify-before-write/apply before any live impact; readback-lag false "mismatches" resolved via whole-sheet diff.
**Gates:** B1:many (resolved sheet itemId/creds from files, never asked for findable info) B2:many (whole-sheet 0-drift diff on every write batch; named tests; live deploy verify with 8 real buttons) B3:3 (verified Zoho-owner + the `-` bug + test_outbox attribution before asserting/writing) B4:respected (every cell traced to a queried source: Zoho/mailbox/calendar) B5:respected (invasive live-sheet writes paused for AskUserQuestion yes + readiness) B6:many (push/merge/deploy each waited for the floor; classifier held on self-merge) skipped:2 (B1 closing-offer, hook-caught)
**Autonomy:** 2 human interventions (both B1 hook catches; the AskUserQuestion write-scope confirmations + push/merge/deploy approvals were correct gated-floor interactions, not friction).
**Outcome:** COMPLETE + live-verified. Sheet reconciled (single source of truth); Lead Desk synced + action-needed feature deployed. No-send hold engaged throughout. Next owner-requested: functionality + usability audit of the Lead Desk (prompt authored this session).

### Session 9 — Brisken Expense-Recon Testing-Mode Restructure
**Type:** client-dev
**Focus:** Transcribed Chris's PT process walkthrough, then restructured the expense-recon web tool into a role-split testing-mode intake app (users upload / operators run + publish), integrated all six walkthrough learnings, rebranded Brisken, deployed live.
**Projects:** brisken (expense-reconciliation / p1)
**Built:** 7 PRs on client/brisken/expense-recon-testing-mode (worktree off main), PR #228: roles/intake/publish + durable jobs + dev-side Graph notifier; L1 fill-color / L4 missing-receipt / L5 FX cols / L6 formula warn / L3 sheet-writeback / L2 zoho-seed; Brisken brand tokens+logos + EN/PT + bucket workbench + stepper + merged Help. Deployed to brisken-expense-recon.fly.dev (both codes live-verified). Suite 503 green + calibrate exit 0. Memory project_brisken_expense_recon_chris_process updated; transcript saved.
**Friction:** 2 — B1 stop-hook fired 2x on closing deferral phrasing ("want me to wire that up?"), hook-caught + corrected same-turn, no user-visible cost.
**Autonomy:** 2 human interventions (both B1 deferral catches).
**Outcome:** Live + verified; PR #228 open (merges on CI-green). Next: double-click feedback widget (CONTINUE-PROMPT.md).
