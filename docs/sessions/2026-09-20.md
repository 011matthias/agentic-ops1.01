---
date: 2026-09-20
sessions: 9
projects_touched: [brisken]
friction_events: 4
work_types: [client-dev]
---

### Session 1 — Expense-Recon Memory M2 M3 (mini)
**Type:** client-dev
**Focus:** MEMORY session iteration 2: shipped note item M2 (a merchant's card, learned from exclusive spend) and completed note item M3's owner-authorised read of two years of Zoho Books. The owner reviewed the derived seed and parked the write; the reason is a measurement and is recorded as backlog item 156.
**Projects:** brisken
**Built:** M2 (backlog 154, PR #1110, merge 90dc5c3b, Fly v189): registry card_key / card_key_learned / cards_seen, card_source "merchant" as the last link of the item-87 chain under the learned guard, a sign-off learner that writes a key only on unanimity and withdraws a learned one on a second card, wired on every settings-aware surface and deliberately not on the matcher; 10 route-level tests, three wires red-proven, suite 2575 -> 2601. M3 (backlog 156, PR #1118, merge a1854310): 2,355 expenses + 1,179 bills over 24 months across 7 orgs, 91 registry candidates + 32 new rules + per-merchant card observation derived into the gitignored client context, nothing applied. Also recorded M1 (PR #1094, Fly v184) as Shipped row 100.
**Did NOT work:** tools/regress_check.py on the CSV export wire (merchants=csv_merchants -> merchants=None) (why not: It reported the doubtful 'RED (no pytest summary line)'. The mutation did bite; only the tool's parse of pytest output failed. Re-done by hand with a cp backup, which showed the real failure (the CSV falls back to '(paid-through - assign)' / '(entity - assign)', 1 failed / 9 passed) and restored cleanly.); Deduping the M3 derived rules against live memory on the field name legal_entity_id (why not: GET /api/memory names that value `entity` on categories[]; `legal_entity_id` is the WRITE endpoint's name and reads None on all 103 live rows, so every existing rule counted as new (95 instead of 32). Caught by printing one row's keys instead of trusting the field name. Instrument-validity, the shape rule_behaviors B2 names.); Claiming a backlog number before the final push (why not: 152 was taken by TRACEABILITY's T3 and then 153 by T1 while the M2 branch was in flight, so the item and its two Shipped rows were renumbered twice (ending at item 154, rows 100 and 101). The textual merges stayed clean because both sides were appends; only the numbers had to move.)
**Friction:** None promoted (no candidates drained; the three dead ends are recorded as What Did NOT Work)
**Outcome:** M2 live on v189 and silent until a registry entry gains a card; M2's Lovable prompt written and not applied; M3 write parked by the owner behind a 52-account category table; M4 not started (next item would cross the 500k context band)

### Session 2 — Brisken Recon Ops Tail Follow-Ups (mini)
**Type:** client-dev
**Focus:** Follow-ups to the third ops pass across 2026-09-19 and 2026-09-20: rescued an abandoned session's uncommitted ledger work, then corrected my own item 123 finding twice as real data arrived. The cron does fire, and its cadence is worse than the first correction said.
**Projects:** brisken
**Built:** Three merged PRs, docs only, no code and no deploy. #1115 (28bd3b51) committed three ledger edits the second ops session left uncommitted in agentic-ops1-ops-fr for ten hours, its own skipped-gate row about a checkpoint that merged without the closing reply saying so. #1116 (18497295) corrected 'the cron has never fired': first scheduled run 2026-09-18T17:42:18Z, 46 minutes after the poll that concluded otherwise, and rewrote the item 123 status row that PR #1111 had silently reverted by merging an older copy of the file after #1112. #1121 (fed2d912) replaced that correction's own four-run estimate with 44 hours of data: 15 runs, all green, 14 gaps averaging 3h09m, worst 4h46m, 5.3% of the */10 requested rate. Resume YAML next-step reframed so a resume does not re-derive the wrong conclusion.
**Did NOT work:** Concluding 'the cron never fires' from 2h13m of polling (16 polls plus a re-read, all zero) (why not: The first real scheduled run came 46 minutes after the last poll. A zero only ever measured inside one window is a statement about the window, not the system. Same failure as the three blind instruments in PROMPT-STATUS, from the other side: there a probe could not see something present, here the wait was too short to see something that had not happened yet.); Characterising the cron cadence from the first four runs ('roughly every 2h15m') (why not: Over 44 hours the mean gap is 3h09m and the worst is 4h46m, so the estimate understated the blind window by more than half. Four samples of a best-effort scheduler describe the four samples. For a monitor the load-bearing number is the worst gap, not the mean.); Assuming a merged status-row edit stays on main in a file four sessions share (why not: PR #1111 opened earlier and merged later carrying an older copy of p1-expense-reconciliation.md, silently reverting #1112's item 123 row. Nothing warns about it; the loss surfaced only when a patch anchor failed to match during an unrelated edit. That file is a second collision point alongside the Shipped table.)
**Friction:** None
**Outcome:** main at fed2d912. All five fenced items (123, 125, 127, 128, 129) shipped and verified; nothing in the fence is mine to advance. The monitor works and carries the one live decision: an outage starting just after a run can go unseen for nearly five hours.

### Session 3 — Expense-Recon Operator Note Above The Forward (mini)
**Type:** client-dev
**Focus:** Backlog item 155 (note item T4): the filing instruction Dirk types ABOVE a forwarded invoice now reaches the expense row as expenses[].operator_note, display only. The boundary rule was derived by running the shipped function over the live mail archive rather than guessed.
**Projects:** brisken
**Built:** body_render.operator_note (cut at the LAST forward-header block, capped 40 lines / 2000 chars); intake_mail._archive_operator_note wired into _provenance_entry; expenses[].operator_note lifted in build_expense_view; tests/test_operator_note_155.py (11); the T4 pin in tests/test_intake_mail.py rewritten (it asserted the drop, no longer true); a contract pin in tests/test_view_contract.py; api-contract section; docs/lovable-operator-note-prompt.md + PROMPT-STATUS Not-applied row. PR #1123 (merge a2276f06) and follow-up PR #1126 (merge 3a1d5925). Deployed Fly v191.
**Did NOT work:** A 68-byte JPEG fixture for the mail-attachment tests (why not: intake_mail.py ~593 skips images under 4096 bytes as signature logos or tracking pixels, so every test that meant to exercise the ATTACHMENT path silently ran the body-only render path and passed. Caught only because the two-attachment test reported one provenance entry keyed 0000__rendered-body.pdf. Fixed by giving the fixture real size and asserting the provenance key, which names which path ran.); Cutting the body at the FIRST forward boundary (why not: A forward can be nested: Criss forwards Dirk's forward and his instruction sits BETWEEN the two header blocks, so the first-boundary cut returns nothing for 8 of the 30 live notes. The cut is made at the LAST header block in the head region instead.); flyctl with its own stored credential (why not: flyctl v0.4.71 answers 'no access token available' for every command while the token in ~/.fly/config.yml is accepted by api.fly.io (read-only probe: HTTP 200, viewer matneumann07@gmail.com). It is flyctl's config read that is broken, not the credential, so nobody needs to re-run flyctl auth login; exporting the same token as FLY_API_TOKEN works.); Reading the live months to verify the deploy (why not: operator_note is absent on all 85 mail-delivered rows and always will be for them: intake_provenance is written into the run snapshot at INGEST and read back from it, so a receipt already in a month keeps the provenance it arrived with. A confident '0 notes live' would have read as a broken deploy. Verified instead by running the deployed function in-machine over /data/inbound (94 archives, 88 readable bodies, 32 notes) with two named differential cases.)
**Friction:** None promoted (no candidates; the JPEG-size trap and the duplicate deploy are recorded in What Did NOT Work)
**Outcome:** Item 155 shipped, deployed and verified. Queue item 2 (pin set_tool_decision's statement_id) handed to the peer TRACEABILITY session agentic-ops1-71 as backlog item 157. Feedback note #70 is open and unfiled.

### Session 4 — Expense-Recon Item 155 Close-Out
**Type:** client-dev
**Focus:** Close-out of the TRACEABILITY queue: backlog item 155 (the filing instruction above a forwarded invoice reaches the expense row) shipped, deployed and verified; queue item 2 handed to a peer session running the same prompt. Full-mode audit of a session whose mini-checkpoint already landed.
**Projects:** brisken
**Built:** expenses[].operator_note end to end (body_render.operator_note, intake_mail._archive_operator_note, the build_expense_view lift), 11 route-level tests, the rewritten T4 pin, a contract pin, the api-contract section, docs/lovable-operator-note-prompt.md. PRs #1123 (a2276f06), #1126 (3a1d5925), #1127 (54c98d0b). Fly v191. Memory project_brisken_expense_recon_usability_loop updated with the three traps.
**Did NOT work:** A 68-byte JPEG fixture for the mail-attachment tests (why not: intake_mail.py ~593 skips images under 4096 bytes as signature logos or tracking pixels, so every test meant to exercise the ATTACHMENT path silently ran the body-only render path and passed. Fixed by sizing the fixture past the floor and asserting the intake_provenance key, which is the stored file NAME and so names the path that ran.); Cutting the mail body at the FIRST forward boundary (why not: A forward can be nested: Criss forwards Dirk's forward and his instruction sits BETWEEN the two header blocks, so a first-boundary cut returns nothing for 8 of the 30 live notes. The cut is made at the LAST header block in the head region instead.); flyctl with its own stored credential (why not: flyctl v0.4.71 answers 'no access token available' for every command while the token in ~/.fly/config.yml is accepted by api.fly.io (read-only POST graphql {viewer{email}} returns HTTP 200, matneumann07@gmail.com). It is flyctl's config read that is broken, not the credential, so flyctl auth login is NOT the fix; passing that same token as FLY_API_TOKEN works.); Reading the live months to verify the item-155 deploy (why not: operator_note is absent on all 85 mail-delivered rows and always will be for them: intake_provenance is written into the run snapshot at INGEST (service.py ~5851) and read back from it (~7114), so a receipt already in a month keeps the provenance it arrived with. A confident '0 notes live' would have read as a broken deploy. Verified instead by running the deployed function in-machine over /data/inbound (94 archives, 88 readable bodies, 32 notes) with two named differential cases.); Asserting the two-attachment case at row level (why not: An Invoice-*/Receipt-* pair in one mail is the invoice+receipt collapse's own signal and lands as ONE row, which is item 56's behaviour. The assertion moved to the stored provenance, one entry per delivered file, so the test cannot fail the next time that collapse rule moves.)
**Friction:** 4 - verification-theater (a 68-byte JPEG fixture put five attachment tests on the body-only path, green), branch-hygiene (two sessions on one prompt: my worktree taken, duplicate deploy v190/v191), slow-path (hand-polling gh pr checks with waiters already registered), missed-memory-recall (/feedback.jsonl not read at session start, as the loop memory requires)
**Gates:** B1:0 B2:5 B3:2 skipped:0
**Autonomy:** 1 human intervention
**Outcome:** Item 155 live on v191 and verified in-machine; queue closed. Waiting on the owner for the Lovable prompt; items 157 and 158 belong to the peer session.
### Session 5 — Expense-Recon Memory M4 (mini)
**Type:** client-dev
**Focus:** Note item M4: a free-prose `profile` on each merchant registry entry, read by the categorizer as context and fenced as untrusted data per rule_untrusted_inbound, plus backlog item 118's code half (a cost centre picked on a row now teaches its merchant at sign-off). Backlog item 157, Shipped row 104, PR #1131.
**Projects:** brisken
**Built:** merchant_registry.profile (capped 2000, parallel, on MerchantMatch incl. multi_category) + append_machine_note/is_machine_note; llm/client._merchant_profile_block (per-call nonce fence via untrusted.data_block, inner marker neutralised) + merchant_profile kwarg on both classify methods, passed only when prose exists; categorize threading from categorize_receipts_with_registry; GET /api/memory by_vendor[].profile; web/service.registry_cost_center_upserts_from_expense_run wired at sign-off; 28 tests in test_merchant_profile_m4.py; api-contract two sections; docs/lovable-merchant-profile-prompt.md (not pasted) + PROMPT-STATUS Not-applied row.
**Did NOT work:** Seeding the item-115 disagreement-read test via PUT /api/memory/categories with legal_entity_id "" (why not: The route refuses it: 400 memory_row_key_required, "legal_entity_id and a non-empty vendor are required". Seeded the rule under "Cloud Services" instead; a receipt with no company still recalls it through item 115's RECALL_VENDOR_ONLY path, which is what the test needed.); Adding the two new registry-summary counters without first grepping for exact-shape assertions on that dict (why not: Three assertions in test_web_merchant_registry.py pin memory.learned.registry by exact equality (item 116's carry-whole tests), so cost_centers_set / cost_centers_skipped_conflict broke all three. Cost one full 8.5-min suite run to discover what `grep "cards_seen": 0` would have shown in seconds. Fixed by extending the expected dicts, never by loosening the assertions.); Appending the api-contract sections with a heredoc (why not: The 112-line payload exceeded the 80-line heredoc cap and the gate refused it outright (documented friction 2026-08-22 / 2026-08-24). Wrote the block with the Write tool into the scratchpad and cat-appended it.)
**Friction:** None promoted (mini). One judged and discarded: three exact-shape assertions in test_web_merchant_registry.py went red on the two new registry-summary counters, found by the full suite rather than by a grep first. The pins did exactly their job, so this is the system working, not friction; the cost was one 8.5-min suite run.
**Outcome:** PR #1131 open, CI watched in background. Not merged, not deployed, SPA prompt not pasted. Suite 2613 -> 2643 passed / 2 skipped post-merge-from-main, ruff clean, three regress proofs each watched green -> red -> green.

### Session 6 — Expense-Recon Tool-Verdict Stamp And Three Notes (mini)
**Type:** client-dev
**Focus:** Merged and deployed inherited backlog item 155 (the operator's note above a forward), built item 158 (the set_tool_decision statement-id pin, opened as 157 and renumbered when a sibling merged that number first), and filed three unowned owner feedback notes as items 159, 160 and 161.
**Projects:** brisken
**Built:** PR #1123 merged a2276f06 + Fly v190 (item 155); PR #1129 merge f2a77bc1 (item 158, tests/test_tool_decision_statement_id_158.py, 3 route-level tests, suite 2643 -> 2646 passed / 2 skipped, ruff clean, test-only so no deploy); PR #1137 merge 211e1dee (items 160 + 161). Item 155 verified by running the shipped function in-machine over /data/inbound on the deployed image: 32 notes from 88 readable bodies of 94 stored .eml, every known carrier verbatim; cold Playwright drive of the September expenses route clean, login the only non-GET.
**Did NOT work:** Probing the live months for expenses[].operator_note to verify the item 155 deploy (why not: Returns 0 on all three months even on a correct deploy: intake_provenance is frozen into the run snapshot at ingest (service.py ~5851) and read back from it (~7114), so a receipt already in a month can never gain a new provenance field. The instrument cannot tell a correct deploy from a failed one, and its confident negative invites re-shipping work that already landed. Verify such a field by running the function in-machine over /data/inbound instead.); First in-machine archive scan of the deployed image (why not: Passed a Path to body_render.extract_body_text, which takes bytes. It reported 'readable bodies: 0' and exited 0, which reads exactly like a broken deploy. Only the print-what-you-found count exposed it.); The first merge-conflict resolver script (why not: Split on \n, which leaves a trailing \r on every line because the checkout is CRLF (core.autocrlf=true), then rejoined with \r\n, producing \r\r\n and lone CRs. p1-expense-reconciliation.md then conflicted as a WHOLE FILE on the next merge instead of at the two rows actually touched. Normalise on read and write LF; git re-expands on checkout.); Claiming backlog number 157 after merging origin/main (why not: A sibling merged 157 and Shipped row 104 for note item M4 while the PR was in review, so the number had to move to 158 with the PR already open (plus the test module rename). A number is only safe once YOUR PR has merged, not once picked from a fresh merge-base.)
**Friction:** None promoted (no candidates; the four dead ends are recorded in What Did NOT Work, each caught by its own guard in the same turn)
**Outcome:** TRACEABILITY queue closed. Live is Fly v192 and nothing merged is undeployed. Items 159, 160 and 161 are filed and unbuilt; the item 155 Lovable prompt is still unpasted.

### Session 7 — Brisken Recon Ops Tail Follow-Ups (mini)
**Type:** client-dev
**Focus:** Second half of 2026-09-20, after the day's first checkpoint. The owner took the item 123 decision, authorised the first live firing of the alert path, and asked for alerts at their Brisken mailbox instead of the developer's Gmail. The redirect turned out to be blocked at the sender, proven with two live refusals rather than inferred.
**Projects:** brisken
**Built:** PR #1128 (27c87f03) made the alert sender configurable (RECON_UPTIME_RESEND_FROM, passed from a repo variable; the dry run now names the sender), red-proven through the real caller and preflight-hooks --full 1968 to 1969 passed. PR #1132 (89a78d9e) recorded the owner's acceptance of the ~5h blind window, the first live firing (run 35517060243: issue #1125 opened, recovery commented, closed and mailed), and the remaining options. The alert path is proven end to end for the first time; a dry run cannot exercise the real GitHub and Resend calls, and this path was silently broken once before (#1106).
**Did NOT work:** Setting BRIEFING_TO to matthias.silva@brisken.com to redirect the alerts (why not: Resend's restriction is on the SENDER, not the destination: 403 'You can only send testing emails to your own email address (matneumann07@gmail.com)'. The workflow comment had said 'delivers only to the account owner's address' since day one, which is true but reads as a fact about recipients, so it pointed at the wrong knob. The 403 body names the fix explicitly.); Using no-reply@unpauseai.com as a verified sending domain (why not: 403 'The unpauseai.com domain is not verified'. Plausible because the platform sends transactional mail from that address, but the platform using it does not mean THIS Resend account has verified it. Cost one run to disprove rather than a code change, which is what making the sender configurable bought.); Reading the GitHub account's notification address to see whether the issue already reaches the Brisken inbox (why not: The gh token lacks the user and notifications scopes, and gh auth refresh needs an interactive OAuth flow this session cannot run. Left unverified and flagged rather than guessed; it is the cheapest remaining lead because the issue is the primary signal and GitHub mails it.)
**Friction:** None
**Outcome:** Item 123's scheduling question closed by owner decision. Monitor runs, issue-and-mail path proven, no open recon-uptime issue, app up, alerts landing in matneumann07@gmail.com. Getting them to the Brisken mailbox is now a credential question, not an engineering one. main at 9ea2b30f; no ops worktrees remain.

### Session 8 — Expense-Recon Traceability Items 159-161 (mini)
**Type:** client-dev
**Focus:** TRACEABILITY session: the three newly-filed feedback notes (#70, #71, #72) taken end to end. Each was diagnosed against the live app BEFORE any copy or code was touched, and two of the three turned out to be a different problem than the item described.
**Projects:** brisken
**Built:** Item 160 (PR #1140, merge edb98a4d, Fly v193): expenses[].uncategorized_lines names the receipt lines carrying no category, built from service.uncategorized_line_indexes, the one predicate _matched_category_review turns into the partial_uncategorized verdict, so the row cannot name a line the sentence is not about. 4 route-level tests + a two-way set-equality contract pin; suite 2646 -> 2651. Item 161 (PR #1141, merge 9dac571d): SPA copy only, chase.title reuses the words of the box above it plus a new chase.subtitle. Item 159 (PR #1142, merge 715e586e): closed as not reproducing, with a reopen condition. Two Lovable prompts written and NOT pasted (lovable-uncategorized-lines-prompt.md, lovable-chase-section-label-prompt.md).
**Did NOT work:** Item 161's filed hypothesis, that the page shows 85.3% matched above a list of 65 to chase and the two read as a contradiction (why not: Refuted by a cold drive: the rendered body of /runs/0603bb0e6f38 is 5,919 characters and contains neither '85.3' nor '14.9'; the only percentage on the page is an unrelated 3.3%. Both rates are payload fields the SPA never renders there, so the reader was never comparing them. The real defect is one population named twice four lines apart, a box reading 'Charges without a receipt 65' above a section reading 'Receipts to chase (65)' whose own table headers are the charge columns.); Settling item 159 by reading the published SPA bundle for the Add-more-receipts submit handler (why not: Did not converge. The i18n keys resolve (months.addReceipts.title, chase.title) and the component chunk is identifiable (chunk-expenses._batchId-LMSoldU3.js, which usefully proved it holds no receipts-view route and no window.open), but the upload call lives in the shared api module behind minified indirection. Four probe scripts in, the browser drive answered in one run what the static read could not.); Creating the scratch batch and reading or deleting it in the same breath (why not: POST /api/expense-batches returns a job_id and the batch is not readable until that job finishes, so the immediate GET 404'd, the cleanup DELETE 404'd, and the batch list printed seven months with no TEST row. All three read as 'it was never created' while the batch was still on its way; it appeared a minute later. Poll GET /jobs/{id} first. Compounding it, POST /api/runs/{id}/delete refuses a bare body with 400 delete_confirm_required and needs {"confirm": "<label>"}.)
**Friction:** None promoted (mini). One candidate left undrained on purpose: a heredoc-size gate fired correctly on a triple-quoted Python heredoc and the work went through the Write tool instead; candidates are machine-wide and siblings are live, so the drain was not run.
**Outcome:** TRACEABILITY queue empty. 72 feedback notes, none open. Live is Fly v193; nothing merged is undeployed. Two Lovable prompts wait on the owner.

### Session 9 — Recon Prompt Re-Audit And Down-Runbook (mini)
**Type:** client-dev
**Focus:** After the TRACEABILITY loop closed, the owner published a batch of Lovable prompts and asked which were still missing, then picked item 120's runbook third as the next piece. Both answered against live state rather than against the repo's own record.
**Projects:** brisken
**Built:** Prompt re-audit (PR #1146, merge 4e594123): crawled the published bundle (44 chunks, 1,193 KB) and matched each pending prompt's decisive field names. Seven of nine are live and moved to Applied; two are not, both re-checked with looser signatures before being called absent. Item 160 closed by a cold drive rather than by field names: July's Microsoft row renders '1 of 2 receipt lines still needs a category: 25.20.' and the old generic sentence is gone from the page. Item 120's runbook third (PR #1148): docs/if-it-is-down.md, written from values read off the live app and with every command verified against flyctl - three independent checks (API, port-25 MX, SPA), the FLY_API_TOKEN workaround, three failure shapes including the 2026-09-10 wedged machine (destroy, volumes fork, redeploy), deploy from a detached origin/main worktree, image rollback, and a call-it-up check that refuses /healthz alone.
**Did NOT work:** Answering 'which prompts are missing' from PROMPT-STATUS.md's Not-applied table (why not: The table was wrong in seven of its nine rows: the owner had published a batch and nothing had re-audited since. The file's own opening rule says a prompt's presence in docs/ says nothing about whether it was ever pasted, and that rule is what saved it. Only a crawl of the published bundle, matching the API field names an applied renderer has to name, gives the real answer.); Closing item 160 on the bundle containing its four i18n keys (why not: Bundle strings are not a render. The keys being present proves the paste landed, not that the row shows anything. The cold drive is what closed it, and it also proved the negative half: the generic 'One or more receipt lines still need a category' is gone from the page entirely, which no bundle grep could have shown.); Offering item 120 to the owner as one of several things we could do next (why not: B1 stop-gate blocked the message, correctly. The item splits three ways and only one third needed a decision at all: the hosting-org move needs Dirk and money, a rehearsed restore is a live-volume action, and the one-page runbook needed neither. The read-only half (enumerating what recovery docs already exist: backup-and-restore.md, which says no restore has been rehearsed, and the never-sent OWNERSHIP-HANDOFF.md) was mine to take first, and the remainder belonged in AskUserQuestion with a recommendation.)
**Friction:** One promoted in substance though mini omits the rows: a B1 stop-gate block on the closing message, which had offered item 120 as a choice instead of taking its read-only half. Correct block; the read-only half (enumerating what recovery docs already exist) was then taken and the remainder put through AskUserQuestion. One candidate left undrained: a heredoc-size gate that fired correctly; candidates are machine-wide and siblings are live.
**Outcome:** Two prompts still pending the owner's paste (item 161 chase-label, M4 merchant-profile), both handed over as pasteable text. Five owner-side data gaps verified live and quantified. Item 120's other two thirds stay open by design.
### Session 9 — Statement Fill-Colour Classifier Findings (mini)
**Type:** client-dev
**Focus:** Answered feedback note #69 ('where do you get this information from?') by tracing the 'marked yellow, so it is already booked' claim to the fill colour of cells in the stored workbook, and measured what that reader actually sees. Owner then ruled the follow-up: read every colour, infer nothing from any of them.
**Projects:** brisken
**Built:** No code. A live colour census of /data/runs/50622baec444/July2026.xlsx joined to the live payload on rows[].source_row (item 152's field): 9 distinct fills, of which the classifier reads 2 families; 84 of 112 rows carry a colour it cannot read; 41 of 112 rows get 'posted' from a 1-1 tie-break rather than a majority. Continuation prompt for backlog item 162 (hue-based reader, record-do-not-vote) handed in chat. Lovable prompt for item 155 re-handed verbatim from main.
**Did NOT work:** Answering note #69 from the code alone (why not: The code says yellow means posted, which is true and useless: the question was where the claim comes from, and that is the operator's own workbook formatting. Only reading the stored xlsx showed that 5 more colours are in use and that 84 of 112 rows carry one the reader ignores.); Treating 'every July row has an entry_status' as evidence the reader works (why not: All 112 rows classify, so the reader looks complete. The join to the sheet shows 84 of them contain an orange or blue cell that contributed nothing, and the verdict came from a yellow cell elsewhere in the row. A complete-looking output hid an incomplete input.); Reading item 142 as having closed note #69 (why not: Item 142 is marked SPA APPLIED + VERIFIED, but it only reshaped the hint's wording to name the workbook and the rule. The operator's actual question about the source was never answered, and the classifier gap behind it was never looked at.)
**Friction:** None new (the full audit for this session merged as PR #1135 / 9ce280e4)
**Outcome:** Item 162 specified and prompted, not built. Two owner decisions open: what orange and blue mean, and whether the tie-break default is right.
