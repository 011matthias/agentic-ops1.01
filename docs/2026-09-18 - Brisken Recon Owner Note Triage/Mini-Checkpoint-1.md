# Mini-Checkpoint: Brisken Recon Owner Note Triage

**Date:** 2026-09-18
**Status:** Triage done, three parallel build sessions handed their starter prompts, nothing shipped in code
**Type:** mini

---

## Summary
The owner's handwritten note on the expense tool was transcribed into nine items (four traceability, four memory, one matching), compared against the open backlog (three genuinely new: statement id, Zoho memory seed, vendor profile; two extensions; four already covered), and turned into three starter prompts that run as simultaneous sessions under the parallel-round protocol. Live reads settled the "vendors with several categories" question: the tool's category is stable for OpenAI, Anthropic and Lovable; what varies is the account, by paying company.

## What Was Done
- Transcribed the note; owner dropped the "Lovable/Anthropic/OpenAI brauchen Lösung" line (it is open item 108: their receipts park because the 9693 and 1176 statements are never loaded) and the Zoho Books booking integration (later), and added: statements need their own internal id.
- Compared the nine items to `p1-improvement-backlog.md` on origin/main `b28c5b6b` (later `25eebb99`, item 148 shipped by a sibling). Verified in code: receipts carry `document_id` throughout; hosted statements are keyed by file name in `statements[]` (no id; `store/statements.py` has `statement_id`/`content_hash` but only the CLI store uses it); charges carry content-derived `transaction_id` plus `_upload_anchors` id-to-row (none for PDF statements); mail intake keeps PDF/PNG/JPEG/WebP only, HEIC skipped, links never fetched.
- Integration review: recommended per-card statement attach (items 53/108, UI half), alerting/uptime off the laptop plus turning the shipped SharePoint backup on, Zoho Books as a one-shot seed, pinning categorisation. Withdrew "statements by mail" and "pull receipts from Criss's mailbox" (owner: not enough volume). "IDs leaving the tool" deferred by the owner into the later Zoho Books integration.
- Handed the Lovable prompt for the per-card "Add this card's statement" control (`CardTabs` action + `presetCardKey` on `AttachStatementDialog`, both month pages). NOT yet saved to the module `docs/lovable-*.md` nor rowed in PROMPT-STATUS; text below.
- Read-only live reads (operator login, no writes): registry 28 merchants, none of the three AI vendors, `multi_category` on nobody; memory 103 `merchant_category` rules (2026-08-06 Zoho seed, unvalidated), two for `anthropic` (Cloud Services -> "COGS - DEV Infrastructure (SAP Apps & others)", Corporate Services -> "Other Infra and IT Costs for Cloud Business"); July-September 53 rows for the three vendors, category "Software & Subscriptions" everywhere except model guesses on unitemized receipts ("Utilities & Premises" on Anthropic auto-recharge, "Professional Services" on Lovable via 1176); September 11 of 27 such rows have no company (card unknown); OpenAI paid from both 9693 and 3645, so it is not single-card.
- Owner declined the immediate registry write for the three vendors ("Not yet"); the write is a decision inside the Memory session's M1.
- Handed three starter prompts (below): Traceability T1-T4 and Memory M1-M4 with the SESSION LOOP, Matching X1 without it, each with its own worktree, branch family, browser session, checkpoint branch and deploy worktree so the three run at once.

## What Did NOT Work (and why)
- **Statements arriving by mail as the fix for item 108:** banks do not attach statements to mail (Chase sends a "statement ready" notice), so the mail path saves only the click-through, not the download; the fix is the per-card attach control in the SPA plus reading the card off the file. Withdrawn.
- **Reading months by `id` from `GET /api/expense-batches`:** the list items carry `batch_id` and `run_id`, not `id`; every `GET /api/expense-batches/None` 404'd until the key was corrected.
- **`MSYS_NO_PATHCONV=1 git -C /c/Users/... show origin/main:path`:** with path conversion off, the `/c/...` root is not converted and git reports "not a git repository"; use `C:/Users/...` for `-C` when the pathspec needs `MSYS_NO_PATHCONV=1`.
- **Running the checkpoint scaffold from the main checkout:** it sat 222 commits behind origin/main with a live sibling on `main`; the pre-flight and finalize ran in a fresh `docs/checkpoint-recon-note-0918` worktree instead (`--root`).

## Current Status
No code shipped this session. The three sessions have not been started. Owner rulings still standing: covered-class defects only until the EUR 600 licence is signed, EXCEPT the note's nine items, which the owner ordered built now (2026-09-18). brisken platform status line from pre-flight: unknown plan, no comms-log staleness reading. p2 status files `p2-product-decks.md` (57d) and `p2-targeting.md` (58d) are flagged stale by the sweep; not touched here (p2 is another workstream's).

## Next Steps
1. Start the three sessions with the prompts below (fresh chats, simultaneously).
2. Owner inputs the sessions will ask for: the 3x3 account table (OpenAI / Anthropic / Lovable x Cloud / Corporate / Consulting; 2 of 9 known from the seed); yes/no on writing the three merchants into the live registry (M1); yes/no on the one-time Zoho Books read (M3).
3. Paste the per-card statement Lovable prompt (below), or have one session save it as `docs/lovable-card-statement-attach-prompt.md` with a Not-applied row in `docs/PROMPT-STATUS.md`. Item 147's `lovable-card-accounts-prompt.md` is also unpasted and gated on setting the three parents in Settings, Cards.
4. Later, with the Zoho Books booking integration: IDs leaving the tool (ack subject, CSV `Reference#`, PDF captions).
5. Not ordered, still worth doing: uptime check off the laptop + `/healthz` with commit and disk (items 120/122/123), `EXPENSE_RECON_BACKUP=1` plus a restore rehearsal (119), a categorise cache like the reading cache (item 4).

## Files to Read First
- `workspace/clients/brisken/status/p1-improvement-backlog.md` (items 53, 72, 103-108, 115, 117, 118, 137, 147, 148)
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PROMPT-STATUS.md`
- `workspace/clients/brisken/automations/expense-reconciliation/docs/api-contract.md` (statements[], coverage[], item 115, item 138, item 147 sections)

---

## Prompts handed over 2026-09-18

### Paste into Lovable: per-card "Add this card's statement" (items 53 / 108, UI half)

````markdown
The app calls the FastAPI backend at `https://api.expenses.brisken.com` as a JSON API. Do NOT add Supabase or any database. Auth stays the existing `Authorization: Bearer <token>`. This edits `src/components/CardTabs.tsx`, `AttachStatementDialog` (today inside `src/components/ExpensesReviewGrid.tsx`), `src/components/RunWorkbench.tsx`, `src/components/ExpensesReviewGrid.tsx` and the EN + PT dictionaries in `src/lib/i18n.tsx`. No new backend field and no new request: everything reads the GETs the pages already make and posts to the attach route the dialog already uses. Render defensively: a payload with no `card_sections`, or an empty list, renders both pages exactly as today.

## Why

The backend takes one statement per card, as many times as needed (`POST /api/expense-batches/{id}/statement` is repeatable and each upload appends to `statements[]`). The screen still offers one generic "Add a statement" button in the month strip, and the dialog opens on whatever card is first in the picker. The bookkeeper reconciles card by card, and the card tabs already show which card has no statement (`statement: "not_loaded"` / `"not_recorded"`, the amber dot), but the tab gives her nothing to do about it. Five of the nine cards have never had a statement loaded. The fix: the card that is missing its statement is where the "add" lives, and the dialog opens already set to that card.

## 1. `AttachStatementDialog`: a preset card

Move the component out of `ExpensesReviewGrid.tsx` into its own file `src/components/AttachStatementDialog.tsx` with the same export and props, so both month pages can mount it. Then add one optional prop:

```ts
presetCardKey?: string; // a card key from GET /api/cards; the picker starts on it
```

- When `presetCardKey` is set and matches an active card in the `["cards"]` query, the card Select starts on that card. The picker stays changeable; nothing is locked.
- Under the picker, while the selection equals `presetCardKey`, a muted helper `t("months.attach.presetHelp", { card })`, where `card` is the option text the picker shows for it (label, `label_pt` under PT, digits appended). Replace the existing "filled from this card" helper on that row rather than stacking two lines.
- When `presetCardKey` names a card that is not in the list (inactive, or the registry changed), ignore it: the dialog opens exactly as today.
- Submit is unchanged: `account_id`, `account_card_currency` and `card_key` are built exactly as they are now. Do not send anything new.
- The `done` handling is unchanged (cache reset, then navigate).

## 2. `src/components/CardTabs.tsx`: the action on the tab

`CardTabs` gains one optional prop:

```ts
onAddStatement?: (cardKey: string) => void;
```

Under the tab row, on the existing one-line summary that renders when `active !== null`, append one more part at the END, only when `onAddStatement` is given, the selected section's `key` is not `""` (No card) and the section is a card (it has `digits` or a `label`):

- `statement === "not_loaded"` or `"not_recorded"`: an outline button `h-7 px-2 text-xs`, text `t("cardTabs.addStatement.forCard")`, `onClick={() => onAddStatement(section.key)}`.
- `statement === "loaded"`: the same button with the text `t("cardTabs.addStatement.another")`. A loaded card still takes the closing cycle after the mid-month partial, and this is the card it belongs to.
- `statement` `null` or absent: no button.

For a subcard whose `statement_on_account` is `true` (item 147 tree, may be absent), the button acts on the ACCOUNT: pass the `parent` key, so the statement is filed where it is stated. When there is no `parent`, pass the section's own key.

The "N cards with nothing this month" control after the tabs (`wb.filter.card.empty`, opened by `wb.filter.card.showEmpty`) lists the registry cards that have neither charges nor receipts this month (the `coverage[]` entries whose `key` is in no section). Wherever that list renders one card, give it the same outline button with `t("cardTabs.addStatement.forCard")` and `onAddStatement(entry.card_key)`. Use `card_key`, not `key`: `key` may be a `digits:` machine value and is never a registry key. Leave out the button on an entry whose `card_key` is `""` (a card the registry does not know cannot take a statement).

## 3. Wiring on both pages

**`ExpensesReviewGrid.tsx`** (Expenses): beside the existing `attachOpen` state add `const [attachCard, setAttachCard] = useState<string | undefined>();`. Pass `onAddStatement={(key) => { setAttachCard(key); setAttachOpen(true); }}` to `CardTabs`, and `presetCardKey={attachCard}` to the dialog. The strip's generic "Add a statement" in `MonthHeader` keeps calling `setAttachOpen(true)` with `setAttachCard(undefined)` first, so the generic button still opens on today's default. Clear `attachCard` when the dialog closes.

**`RunWorkbench.tsx`** (Matching): the page renders `CardTabs` but does not mount the dialog. Mount `AttachStatementDialog` here with the same `runId`, the same open state pattern and the same two setters, and pass `onAddStatement` to `CardTabs` exactly as on Expenses. If `MonthHeader` on this page already exposes `onAddStatement`, wire it to the same `setAttachOpen(true)` with no preset; if it does not, leave the strip as it is. A trip batch never gets the button (the tabs are empty on a trip, so this holds by construction).

After a `done` job the dialog already resets the run and expense-batch caches; the tab's amber dot and its summary line refresh from the new payload, and the card reads `loaded` with the file named in `statements[]`. Do not select a tab, scroll, or toast on top of that.

## 4. Do not change

- Which tab a row belongs to, every count, every filter, the All tab, the No card tab.
- `MonthHeader`'s "Add a statement" (`month.tab.addStatement`) stays, same place, same behaviour.
- The dialog's fields, its "Other account…" mode, the column-mapping retry and its `map_*` keys, the job poller and its error render.
- `SummaryBar`, the downloads, the statement lines under the page title (`wb.statement.loaded`), the statements table and its Read-as column.
- A month with fewer than two cards has no tabs and therefore no per-card button; the strip's generic button is the only way in there, as today.

## New strings

| Key | EN | PT-BR |
|---|---|---|
| `cardTabs.addStatement.forCard` | Add this card's statement | Adicionar o extrato deste cartão |
| `cardTabs.addStatement.another` | Add another statement for this card | Adicionar outro extrato para este cartão |
| `months.attach.presetHelp` | This statement will be filed to {card}. Change the card above if the file belongs to another one. | Este extrato será registrado em {card}. Troque o cartão acima se o arquivo for de outro. |

## Checks

Bundle: `cardTabs.addStatement.forCard`, `cardTabs.addStatement.another` and `months.attach.presetHelp` in the i18n chunk; `presetCardKey` in the shared chunk that carries `AttachStatementDialog` (no longer in `chunk-expenses._batchId` only, since Matching imports it too).

Browser, read-only, EN then PT, on August 2026 (`/runs/074a7b8905d7` and `/expenses/074a7b8905d7`): the tab of a card with the amber dot shows "Add this card's statement" at the end of its summary line; clicking it opens the dialog with that card already selected in the picker and the helper naming it; Escape closes it, nothing uploaded, no request other than GETs. A loaded card's tab shows "Add another statement for this card". "Show N cards with nothing this month" lists the remaining cards, each with the button. The No card tab shows no button. Both pages behave the same. PT: "Adicionar o extrato deste cartão", "Adicionar outro extrato para este cartão", "Este extrato será registrado em …".
````

### Paste into a fresh Claude Code chat: TRACEABILITY (T1-T4)

````markdown
/comd_resume brisken

Owner directive 2026-09-18: the items in this prompt have priority and are to be built now; the covered-vs-quote licence question does not gate them. You are the TRACEABILITY session. Two sibling sessions (MEMORY and MATCHING) run at the same time on the same repo and the same Fly app: follow the parallel-safety rules in "How to work" exactly.

Read first: `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`, then `docs/api-contract.md` in that module (sections "The statements a month has taken: `statements[]`", "Re-reading a month's statements", "Cards under an account", "Every decision overwrites..." under item 104), backlog items 30, 72, 103, 104, 106, 114 in `workspace/clients/brisken/status/p1-improvement-backlog.md`, and the memories `project_brisken_expense_recon_usability_loop`, `project_brisken_expense_recon_mail_intake`, `feedback_recon_no_live_writes_criss_acts`, `feedback_continuation_prompt_carries_loop`, `rule_untrusted_inbound`.

## Where it stands

The owner's 2026-09-18 note, traceability half, four items. Live facts read 2026-09-18 on origin/main `b28c5b6b`:

- A receipt already has a stable id: `document_id`, used throughout `web/service.py` (363 references) and recorded per file by the mail intake. The acknowledgement mail does not name it (`intake_mail.py` ~1605: subject "Receipt received: {subject}"); exposing ids OUTSIDE the tool (ack, CSV, PDFs) is DEFERRED by the owner to the later Zoho Books integration. Do not build that half.
- A statement has NO id in the hosted app. A `statements[]` entry (`web/service.py` ~9902, the row builder) is keyed by `file` (the stored name on disk); `_upload_anchors` (~9759) keeps a `transaction_id -> source_row` map per upload in the snapshot's `statement_anchors`; a PDF statement has no anchors at all. `store/statements.py` defines `statements(statement_id, content_hash, ...)` and `statement_transactions`, but that is the CLI store; the hosted month state (`web/store.py`: runs, decisions, decision_history, receipt_claims, expense_field_overrides, ...) never uses it.
- A statement line has a stable id: `transaction_id` is content-derived (item 29 PR 2a: sha1 over account/card/date/canonical amount/currency/vendor/reference, `-{n}` for repeats). Item 72 is still open: `rematch_month` persists the raw outcome while the view shows the effective one.
- Mail intake keeps PDF, PNG, JPEG and WebP attachments only; HEIC is skipped and named in the acknowledgement (item 106, NOT built: HEIC conversion); a bill behind a link is never fetched. Whether a forwarded mail attached as `message/rfc822` (`.eml`), a multi-receipt PDF, or a receipt embedded in the body alongside another attachment is read end to end is NOT verified anywhere: measure it on the live inbound archive before assuming.

## Queue, in order (note items T1-T4)

Append each to the backlog as its own item when you first push, taking the next free number at that moment (siblings append too); keep "(note item T2)" in the heading so cross-references survive renumbering.

1. **T2 Statement identity.** Give every statement upload a `statement_id` that is content-derived (sha256 of the stored bytes, as `store/statements.py` already computes as `content_hash`; 16 hex chars is enough), as a PARALLEL field on every `statements[]` entry, absent on entries written before it, and carry it on `coverage[].statements` alongside the file names without changing that list's type (add `coverage[].statement_ids` beside it). The same bytes uploaded twice, or re-read after a restore, yield the same id; a corrected file yields a new one. Record it in `statement_anchors` keyed by id as well as by file, so the writeback and the re-read can address an upload by id when two per-card exports share a filename. Contract rule 1: add, never retype.
2. **T3 The receipt-to-line link survives as a record.** On `rows[]` (run payload) and `expenses[]` (batch payload) expose, parallel and absent when unknown: `statement_id` and `source_row` for the charge side (PDF statements: `source_page` and the index on that page instead of a row), and `document_id` for the receipt side where a row does not already carry it. Every stored decision (`web/store.py` decisions and `decision_history`) and every `receipt_claims` row must carry `statement_id` next to `transaction_id`, so a re-read that mints new transaction ids can still be walked back through the anchors. Prove it end to end on a constructed fixture: attach a workbook, pair a receipt, re-read the month, and assert the booked expense still resolves to the same `document_id`, `statement_id` and sheet row. Then read item 72 and decide whether closing it is inside this item (persisting the effective outcome is what makes the record true) or its own; state the call in the PR body.
3. **T1 Receipt identity, inside the tool.** Audit where a receipt loses its `document_id` on the way through the tool: set-aside pages restored, copies collapsed as duplicates, neighbour-month borrowing, trip settlement, the month report's numbering fallback (item 68's "rows 52-54 read none" finding). Every surface a receipt appears on (grid row, unmatched list, copies set aside, the report's caption) must carry the same `document_id`; where it does not, fix it. Nothing outbound (ack, CSV, PDF) in this item.
4. **T4 The entire email is read.** Enumerate on the live inbound archive (read-only: `GET /api/inbound/log`, and the stored `.eml` files via a read-only DB/volume copy) which shapes exist: HEIC photos, nested `message/rfc822` forwards, multi-receipt PDFs, a body receipt beside an attachment, inline `cid:` images. Build for the shapes that exist: HEIC to JPEG at the mail boundary (`pillow-heif`, pinned in `pyproject.toml`, Dockerfile rebuilt), nested `.eml` parts unpacked one level with their own attachments and body, and a body that carries a receipt even when an attachment is present rendered as its own file. A bill behind a link stays unfetched (rule_untrusted_inbound: inbound text never chooses a URL); the acknowledgement already asks for the PDF. Pin each shape with a fixture in `tests/`.

When the queue is empty, follow SESSION LOOP step 9.

## How to work

- Your worktree, cut fresh: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 fetch origin` then `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add -b client/brisken/p1-trace-<slug> C:\Users\neuma_p1qrsic\Repo\agentic-ops1-trace origin/main`. One branch per queue item (`p1-trace-t2`, `p1-trace-t3`, ...), reusing the `agentic-ops1-trace` directory after each merge (`git -C <it> checkout -b ... origin/main` after a fetch). Never edit, commit or stash in the main checkout; never `git stash` anywhere. A hook refuses `cd X && ...`; use `git -C`, `uv run --directory`, absolute paths.
- Siblings (MEMORY in `agentic-ops1-memory`, MATCHING in `agentic-ops1-match`) edit `web/service.py`, `web/app.py`, `tests/test_view_contract.py`, `docs/api-contract.md`, `docs/PROMPT-STATUS.md`, the backlog and the status file all day. Append at the END of the relevant block; never renumber, re-sort or reflow. Before the first push `git rebase origin/main` is fine; after any push only `git merge origin/main`, never rebase, never force-push. Re-run the module suite after every merge from main. On a conflict in a shared file keep both sides. Before claiming a backlog number or a Shipped row number, merge main and take the next free one.
- Live reads only. API `https://brisken-expense-recon.fly.dev` (also `api.expenses.brisken.com`): `POST /api/login {"code": ...}` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env` (never print it). Months: July `50622baec444`, August `074a7b8905d7`, September `51a22ad72864`; list `GET /api/expense-batches` (key `batch_id`). Never fetch `GET /runs/{id}/expense-report.pdf` live (it writes render outcomes); `reconciliation-report.pdf` is read-only. No writes to Criss's months; any production mutation beyond a deploy goes through `AskUserQuestion` as a decision with a recommendation, never "say the word". Windows Python cannot open `/c/...` paths: pass `C:\...`. Read-only DB copy: `flyctl ssh console --pty=false` with an in-machine `sqlite3.backup`, then `MSYS_NO_PATHCONV=1 flyctl ssh sftp get -a brisken-expense-recon /tmp/<f> 'C:\...\scratchpad\<f>'`; delete both copies when done.
- Build: route-level tests through the FastAPI app, at least one through the CALLER the fix changed; one `uv run tools/regress_check.py --test "<cmd>" --cwd <module dir> --file <src> --replace "<wired>" --with "<disabled>"` per fix, watched going red (it prints "RED (no pytest summary line)" on some runs even when green: when in doubt, mutate by hand with a backup and restore with `cp`, never `git checkout --`). Suite: `uv run --directory workspace/clients/brisken/automations/expense-reconciliation --extra web --extra dev pytest -q` (about 4 minutes, 2,280+ tests). Ruff on the module: `uv run --no-project --with ruff ruff check src tests` from the module dir. Python heredocs with triple quotes are blocked by a hook: write scripts with the Write tool. Document every new field in `docs/api-contract.md` and pin every new list field in `tests/test_view_contract.py` in the same PR.
- Ship: commit on the item branch with explicit pathspecs, push, `gh pr create` (suite count before and after plus the regress line in the body), wait for CI (`gh run list --branch <b>` filtered by head SHA; `expense-recon-tests.yml` runs the module suite), `gh pr merge --squash --delete-branch` on green. The backlog move to Shipped and the status file update land in the same PR.
- Deploy (pre-authorized, one Fly app shared with the siblings): after the merge, probe the live API for YOUR field first; a sibling's deploy may already carry your commit (`flyctl releases -a brisken-expense-recon`). If absent and no release is in progress: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add --detach C:\Users\neuma_p1qrsic\Repo\agentic-ops1-deploy-trace origin/main`, confirm your merge is in `git log -1` there, `flyctl deploy . -a brisken-expense-recon --remote-only` from the module directory, then remove that worktree. Then a live API probe of the changed field and a cold browser read-back.
- Cold browser drives: headless Playwright with `channel="chrome"` (`uv run --no-project --with playwright python <script>`), or `agent-browser --session recon-trace` (never the default session: a sibling will navigate it). Log in at the gate, wait for localStorage `erc-token`, dismiss "Leave feedback anywhere" ("Got it"), use JS `click()`; record every non-GET request and assert only the login.
- SPA work: write `docs/lovable-<slug>-prompt.md` (background, exact JSON shapes, render rule, EN + PT-BR strings, do-not-change list, checks), add a Not-applied row to `docs/PROMPT-STATUS.md`, and hand the whole prompt in the reply inside a FOUR-backtick fence under the label **"Paste into Lovable:"**. Label every fence: "Paste into Lovable:" or "Continuation prompt for Claude Code:". The SPA needs no change for T2, T3 and T4 unless a field is meant to render; say so in one line when it does not.
- Checkpoint branches: `docs/checkpoint-trace-<n>` in `C:\Users\neuma_p1qrsic\Repo\agentic-ops1-ckpt-trace`, cut off origin/main at checkpoint time; run `uv run tools/repair-session-log.py docs/sessions/<date>.md --check` before pushing (three sessions write the same log today). Remove only the worktrees and branches YOU created; print `git worktree list` and `gh pr list --state open --search trace` at every stop.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while this session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `finalize --root <that worktree>`; write the checkpoint prose after `finalize`; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````

### Paste into a fresh Claude Code chat: MEMORY (M1-M4)

````markdown
/comd_resume brisken

Owner directive 2026-09-18: the items in this prompt have priority and are to be built now; the covered-vs-quote licence question does not gate them. You are the MEMORY session. Two sibling sessions (TRACEABILITY and MATCHING) run at the same time on the same repo and the same Fly app: follow the parallel-safety rules in "How to work" exactly.

Read first: `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`, then in that module `src/expense_recon/merchant_registry.py` (docstring), `src/expense_recon/learning/{capture,consult,store}.py`, `src/expense_recon/seed_registry.py`, `docs/api-contract.md` sections "A corrected category comes back next month" (item 115), "Publishing saves the month's corrections to memory" (item 88), "A generic word is not a merchant alias" (item 117), backlog items 4, 9, 23, 47, 88, 109, 115, 116, 117, 118 in `workspace/clients/brisken/status/p1-improvement-backlog.md`, and the memories `project_brisken_expense_recon_merchant_registry`, `project_brisken_expense_recon_usability_loop`, `project_brisken_zoho_books`, `reference_brisken_zoho_expense_orgs`, `feedback_recon_no_live_writes_criss_acts`, `feedback_no_invasive_action_without_ask`, `feedback_continuation_prompt_carries_loop`.

## Where it stands

The owner's 2026-09-18 note, memory half, four items, plus Dirk's clarification the same day: binding a merchant to one category works about 90% of the time; the exceptions are OpenAI, Anthropic and Lovable, whose receipts book to several places. Live facts read 2026-09-18 (read-only, origin/main `b28c5b6b`):

- Registry (`GET /api/settings` `merchants`): 28 merchants, NONE of them OpenAI, Anthropic or Lovable; `multi_category` set on nobody. Precedence: reviewer override > rule saved under (company, vendor) > rule with no company > registry default > rules the vendor's other companies agree on > model line read > vendor guess.
- Memory (`GET /api/memory`): `merchant_category` 103 rules (all from the 2026-08-06 Zoho Books posting-history seed, `validated` empty, so they fill only receipts with no readable items), `vendor_alias` 0, `merchant_fx` 0, `merchant_entity` 0, `field_correction` 0. Two seeded rules for `anthropic`: Cloud Services -> "COGS - DEV Infrastructure (SAP Apps & others)", Corporate Services -> "Other Infra and IT Costs for Cloud Business".
- July-September rows for the three vendors (53): the tool's CATEGORY is "Software & Subscriptions" everywhere except where the model guessed on a receipt with no line items (Anthropic "Auto-recharge credits" -> "Utilities & Premises", Lovable on the Consulting card -> "Professional Services", a handful empty). What varies by company is the ACCOUNT, which is what Dirk calls the category. September (`51a22ad72864`): 27 such rows, 11 with no company at all, because their card is unknown (OpenAI receipts print no card; the 9693 and 1176 statements are not loaded, item 108). Dirk pays OpenAI from 9693 (Cloud) AND 3645 (Corporate), so "one vendor, one card" does not hold for OpenAI.
- Learnable fields per merchant today (`learning/capture.py`): vendor, tax_label, paid_through, card_key. The registry entry holds aliases, category, zoho_account, multi_category, cost_center. `settings["merchants"]` is WHOLE-MAP REPLACE on save (a Lovable editor that drops a field erases it).
- The owner declined, on 2026-09-18, an immediate write of the three merchants into the live registry ("not yet"). Item 23 (owner directive 2026-08-22) cut the live Zoho connection; a one-shot READ of Zoho Books history is a different act and needs the owner's yes.

## Queue, in order (note items M1-M4)

Append each to the backlog as its own item when you first push, next free number at that moment, "(note item M1)" kept in the heading.

1. **M1 Merchant-to-category is the default; the exceptions vary by company, on the account.** Build so that Dirk's 90% rule is what the tool does: (a) the registry default beats the model for every merchant that has one (verify the precedence live on the three vendors after the entries exist); (b) the exceptions are expressed as rules keyed on company + vendor carrying the ACCOUNT, validated so they apply to itemized receipts too, with the category staying the registry's; (c) the Memory page and `GET /api/memory` show, per vendor, one row per company so a reader sees the split; (d) the live registry write for OpenAI / Anthropic / Lovable (category "Software & Subscriptions", no account, `multi_category` OFF, aliases `Anthropic, PBC`, `Anthropic, PBC @anthropic`, `Anthropic, PBC (@anthropic)`, `Lovable Labs`, `Lovable Labs Incorporated`, `Lovable Labs Incorporated (@lovable)`, `OpenAI`) is a production mutation: put it to the owner through `AskUserQuestion` with the read-modify-write diff of the 28 existing entries shown, and do it only on yes. The account per company for the three vendors (9 cells, 2 known from the seed) is Dirk's input: ask for it once, in the closing reply, as a 3x3 table to fill.
2. **M2 Card-specific, exclusive vendor spend.** `card_key` is already learnable per merchant; find what consults it (item 87's chain: pick > printed method or hint > printed number > confirmed private > remembered card) and make the fact explicit and visible: a registry field `card_key` (parallel, editable in Settings, learned at publish when every receipt of that merchant over the learned months paid on one card, never when two cards were seen), shown on the row as `card_source: "merchant"`. The MATCHING session owns how the matcher uses it (scoping a receipt to that card); you own the fact, its learning and its display. Measure first on July-September which merchants are single-card in practice and which are not (OpenAI is not), and put the list in the PR body.
3. **M3 Seed memory from two years of Zoho Books.** The 2026-08-06 seed exists somewhere (`learning_cli.py` or a scratch driver; the memory `project_brisken_zoho_books` names `.scratch/zoho.py`, gitignored, possibly gone): find it or rewrite it in your scratchpad. Scopes already granted: `ZohoBooks.expenses.READ`, `ZohoBooks.bills.READ`, `ZohoBooks.accountants.READ`; token `ZOHO_BOOKS_REFRESH_TOKEN` plus `ZOHO_CLIENT_ID` / `ZOHO_CLIENT_SECRET` / `ZOHO_DC` in the gitignored `workspace/clients/brisken/context/.env`; 8 orgs (ids in the memory file; the card-recon-relevant ones are Cloud Services 697686691, Consulting 808232536, Corporate Services 822741658, GmbH 696750461). Zoho Expense was never set up in 6 of 8 orgs: read Books expenses and bills, not expense reports. Pull 24 months, one read, into a scratch JSON; derive (i) merchant registry candidates (canonical name, aliases by the item-117 vocabulary, majority category, `multi_category` where the history genuinely splits within one company), (ii) (company, vendor) -> account rules with `validated` = "zoho-seed-2026-09" and a count, (iii) a per-merchant card observation for M2. Apply nothing live before an owner yes through `AskUserQuestion`: one question for the read (it touches Zoho once, against the 08-22 directive's spirit), one for the write (registry + memory, with counts and a sample of 20 rows shown). Store the derived JSON in the module's gitignored context, never in the repo.
4. **M4 A profile of unstructured knowledge beside the registry.** Add `profile` (free text, parallel, absent until set) to each registry entry: what the business buys from this merchant, on which card and for which company, anything Criss or Dirk would tell a new bookkeeper. The categorizer reads it as context for that merchant's receipts (prompt-level, marked as untrusted data per `rule_untrusted_inbound`: it informs the category, never instructs), the Memory page shows it, the Settings editor edits it (whole-map replace: the Lovable prompt must carry the field on every save or it is erased), and the learning path may append observations to it only as clearly marked machine lines. Then read item 118 (a cost-center pick teaches nothing) and close it if the same registry-upsert path makes it a one-line addition; otherwise leave it open and say so.

When the queue is empty, follow SESSION LOOP step 9.

## How to work

- Your worktree, cut fresh: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 fetch origin` then `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add -b client/brisken/p1-memory-<slug> C:\Users\neuma_p1qrsic\Repo\agentic-ops1-memory origin/main`. One branch per queue item (`p1-memory-m1`, ...), reusing the `agentic-ops1-memory` directory after each merge. Never edit, commit or stash in the main checkout; never `git stash` anywhere. A hook refuses `cd X && ...`; use `git -C`, `uv run --directory`, absolute paths.
- Siblings (TRACEABILITY in `agentic-ops1-trace`, MATCHING in `agentic-ops1-match`) edit `web/service.py`, `web/app.py`, `tests/test_view_contract.py`, `docs/api-contract.md`, `docs/PROMPT-STATUS.md`, the backlog and the status file all day. Append at the END of the relevant block; never renumber, re-sort or reflow. Before the first push `git rebase origin/main` is fine; after any push only `git merge origin/main`, never rebase, never force-push. Re-run the module suite after every merge from main. On a conflict in a shared file keep both sides. Before claiming a backlog number or a Shipped row number, merge main and take the next free one. `learning/` and `merchant_registry.py` are yours alone; `matching/` is MATCHING's, `intake_mail.py` is TRACEABILITY's: do not edit them, add a local helper instead.
- Live reads only. API `https://brisken-expense-recon.fly.dev` (also `api.expenses.brisken.com`): `POST /api/login {"code": ...}` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env` (never print it). Months: July `50622baec444`, August `074a7b8905d7`, September `51a22ad72864`; list `GET /api/expense-batches` (key `batch_id`). Never fetch `GET /runs/{id}/expense-report.pdf` live. No writes to Criss's months; the two live writes in this queue (registry, memory seed) and the Zoho read go through `AskUserQuestion` as decisions with a recommendation and the diff shown, never "say the word". Windows Python cannot open `/c/...` paths: pass `C:\...`.
- Build: route-level tests through the FastAPI app, at least one through the CALLER the fix changed; one `uv run tools/regress_check.py --test "<cmd>" --cwd <module dir> --file <src> --replace "<wired>" --with "<disabled>"` per fix, watched going red (on a doubtful "RED (no pytest summary line)" mutate by hand with a backup; restore with `cp`, never `git checkout --`). Suite: `uv run --directory workspace/clients/brisken/automations/expense-reconciliation --extra web --extra dev pytest -q` (about 4 minutes, 2,280+ tests). Ruff: `uv run --no-project --with ruff ruff check src tests` from the module dir. Heredocs with triple quotes are blocked: write scripts with the Write tool. Document every new field in `docs/api-contract.md` and pin every new list field in `tests/test_view_contract.py` in the same PR. The accuracy gate (`categorization_gate.py`, `tools/recon-accuracy-guard.py`) must stay green: a precedence change that moves a labelled category is a regression, not a feature.
- Ship: commit with explicit pathspecs, push, `gh pr create` (suite count before and after plus the regress line in the body), wait for CI (`gh run list --branch <b>` by head SHA; `expense-recon-tests.yml` runs the module suite), `gh pr merge --squash --delete-branch` on green. Backlog move to Shipped and the status file in the same PR.
- Deploy (pre-authorized, one Fly app shared with the siblings): after the merge, probe the live API for YOUR field first; a sibling's deploy may already carry your commit (`flyctl releases -a brisken-expense-recon`). If absent and no release is in progress: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add --detach C:\Users\neuma_p1qrsic\Repo\agentic-ops1-deploy-memory origin/main`, confirm your merge is in `git log -1` there, `flyctl deploy . -a brisken-expense-recon --remote-only` from the module directory, remove that worktree. Then a live API probe and a cold browser read-back.
- Cold browser drives: headless Playwright with `channel="chrome"` (`uv run --no-project --with playwright python <script>`), or `agent-browser --session recon-memory` (never the default session). Log in at the gate, wait for localStorage `erc-token`, dismiss "Leave feedback anywhere", use JS `click()`; record every non-GET request and assert only the login.
- SPA work (M1's per-company Memory rows, M2's `card_key` in Settings, M4's `profile` editor and display): write `docs/lovable-<slug>-prompt.md` (background, exact JSON shapes, render rule, EN + PT-BR strings, the whole-map-replace warning, do-not-change list, checks), add a Not-applied row to `docs/PROMPT-STATUS.md`, and hand the whole prompt in the reply inside a FOUR-backtick fence under the label **"Paste into Lovable:"**. Label every fence: "Paste into Lovable:" or "Continuation prompt for Claude Code:".
- Checkpoint branches: `docs/checkpoint-memory-<n>` in `C:\Users\neuma_p1qrsic\Repo\agentic-ops1-ckpt-memory`, cut off origin/main at checkpoint time; run `uv run tools/repair-session-log.py docs/sessions/<date>.md --check` before pushing (three sessions write the same log today). Remove only the worktrees and branches YOU created; print `git worktree list` and `gh pr list --state open --search memory` at every stop.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while this session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `finalize --root <that worktree>`; write the checkpoint prose after `finalize`; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````

### Paste into a fresh Claude Code chat: MATCHING (X1)

````markdown
/comd_resume brisken

Owner directive 2026-09-18: this item has priority and is to be built now; the covered-vs-quote licence question does not gate it. You are the MATCHING session, one item. Two sibling sessions (TRACEABILITY and MEMORY) run at the same time on the same repo and the same Fly app: follow the parallel-safety rules in "How to work" exactly.

Read first: `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`, then in that module `src/expense_recon/matching/deterministic.py` (`_normalize`, `_vendor_score`, `_card_keys`, `uniqueness_verdicts`), `src/expense_recon/matching/types.py` (`Transaction`: `vendor_from_statement`, `raw_text`, `original_amount`; `Receipt`: `detected_vendor`, `payment_mode`), `config/match-tuning.json`, `docs/api-contract.md` sections for items 133 and 137, backlog items 28, 35, 41, 69, 72, 133, 137 in `workspace/clients/brisken/status/p1-improvement-backlog.md`, `docs/optimize/brisken-recon-tuning-v1/SUMMARY.md`, and the memories `project_brisken_recon_matching_program`, `project_optimize_s1_recon_scorer_design`, `project_brisken_expense_recon_testing_loop`, `feedback_recon_no_live_writes_criss_acts`.

## Where it stands

The owner's 2026-09-18 note, matching item (X1): the statement description must count in matching, and when the card on an expense cannot be identified, another logic must take over so the expense is still matched on other criteria. Facts on origin/main `b28c5b6b`:

- A charge carries the bank's `vendor_from_statement` (parsed) and the full `raw_text`; `_vendor_score` compares the receipt's vendor against the parsed vendor only. Reference tokens inside a description (`AMAZON* Z11US7DF5`, an order id, a masked card fragment, a city) reach nothing today except item 69 round A's second duplicate key (normalized reference + total + currency).
- Card scoping (item 137, shipped): a receipt that prints a card is matched against that card's charges only; a receipt printing none is matched against every card's charges. A charge's card comes from the statement's per-row card column when there is one, else the upload's account id (`coverage_key` resolution). Item 41 (an undefined payment method suggests a private expense) and item 35 (the unknown-card strip lists tender words, not cards) are open and adjacent; item 28 measured card extraction off a scan at 2 in 5 with invented digits, so a scan is not a card source.
- The matching program (item 69) closed 2026-09-15 at July 31 right / 0 wrong, August 8 right / 0 wrong, bundles 70/95. The judge is the human labels in the main clone's gitignored `context/expense-reconciliation/expense-reports/csv/by-month/*_live_*` (`labels.csv`, `notes.csv`); the instrument is `tools/recon-match-attribution.py --live DB --run-id ID --labels CSV` (replays a hosted month with no model call) and `--bundle DIR --asset TUNING.json` for the six scorer bundles; set `RECON_MODULE_SRC` to the tree you measure. Refuted levers, never re-run: `fx_date_window_days` < 5, `fx_base_amount_match_pct` 0.005, `fx_reference_match_pct` 0.015. Item 72 (raw vs effective persistence) is open and is the TRACEABILITY session's to decide, not yours.
- The MEMORY session is building a per-merchant `card_key` fact (a vendor that only ever pays on one card). If it lands before you finish, the matcher may scope a receipt with no printed card to that card; design the hook for it (a scoping input the matcher reads when present, absent today) and do not block on it.

## The item (X1), in order

1. **Measure first, both halves.** On read-only local copies of July `50622baec444`, August `074a7b8905d7` and September `51a22ad72864`, list every unmatched receipt and every receiptless charge, and for each say which description tokens (references, order ids, masked digits, location words, amounts inside the text) a human would use to pair it, and whether the receipt's card is unknown. Count how many of the labelled-right pairs already rest on the parsed vendor alone and how many labelled-wrong pairs a description signal would have prevented. Put the table in the PR body; it is the before.
2. **Description as deterministic evidence.** Add a description signal beside `_vendor_score`: reference / order-id tokens shared between `raw_text` and the receipt's `detected_reference` or line text, and a masked-digit fragment in the description matching the receipt's printed last digits, each as evidence that can PROMOTE a candidate that already has clean amount and date evidence, never create one on its own (the S1 run's lesson: vendor-shaped signals promote, they do not floor). Knobs in `config/match-tuning.json` with defaults mirrored in code, pinned tests for the boundary cases (a token that appears in every charge of the month is not evidence; a generic word is not a token, reuse item 117's vocabulary).
3. **When the card is unknown.** Define the fallback explicitly, in one place, and document it in `docs/api-contract.md`: a receipt with no printed card, no picked card, no remembered card (the MEMORY hook) and no settled charge is matched across all cards on amount, date, currency, description evidence and the uniqueness gate, with `card_source: "none"` and a reason code the row can show; a charge whose card the statement does not name (no card column, account default) is matched the same way and reads `card: "account default"` rather than a card it never named. A pair formed with no card evidence carries `requires_review` when any rival within the tolerance exists on another card; a pair with card evidence on both sides is not reviewed for that reason. Item 41's "suggest private" stays a separate, later step and is not built here.
4. **Measure after.** Re-run the attribution on July, August and September and the six bundles; report every per-row move (not class totals), the labelled-wrong count (must stay 0), and the bundle score (must not fall below 70/95). A rule that moves nothing on the three months and the bundles is documented as a dead end in the PR and not shipped.
5. **Ship, deploy, verify live** on a cold browser drive of one row whose reason or card source changed, then the closing report.

## How to work

- Your worktree, cut fresh: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 fetch origin` then `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add -b client/brisken/p1-match-description C:\Users\neuma_p1qrsic\Repo\agentic-ops1-match origin/main`. Never edit, commit or stash in the main checkout; never `git stash` anywhere. A hook refuses `cd X && ...`; use `git -C`, `uv run --directory`, absolute paths.
- Siblings (TRACEABILITY in `agentic-ops1-trace`, MEMORY in `agentic-ops1-memory`) edit `web/service.py`, `web/app.py`, `tests/test_view_contract.py`, `docs/api-contract.md`, `docs/PROMPT-STATUS.md`, the backlog and the status file all day. Append at the END of the relevant block; never renumber, re-sort or reflow. Before the first push `git rebase origin/main` is fine; after any push only `git merge origin/main`, never rebase, never force-push. Re-run the module suite after every merge from main. On a conflict in a shared file keep both sides. Before claiming a backlog number (append this item as its own entry, "(note item X1)" in the heading) or a Shipped row number, merge main and take the next free one. `matching/` is yours alone; `learning/` and `merchant_registry.py` are MEMORY's, `intake_mail.py` is TRACEABILITY's: do not edit them.
- Live reads only. API `https://brisken-expense-recon.fly.dev` (also `api.expenses.brisken.com`): `POST /api/login {"code": ...}` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env` (never print it). Never fetch `GET /runs/{id}/expense-report.pdf` live. No writes to Criss's months; a re-match of a live month after the deploy is a production mutation: put it to the owner through `AskUserQuestion` with the replay's per-row moves shown, never "say the word". Read-only DB copy: `flyctl ssh console --pty=false` with an in-machine `sqlite3.backup`, then `MSYS_NO_PATHCONV=1 flyctl ssh sftp get -a brisken-expense-recon /tmp/<f> 'C:\...\scratchpad\<f>'`; the DB's `runs.work_dir` is `/data/runs/<id>`, rewrite it in the local copy; delete both copies when done. Windows Python cannot open `/c/...` paths: pass `C:\...`.
- Build: the matcher's tests plus at least one route-level test through `rematch_month`; one `uv run tools/regress_check.py --test "<cmd>" --cwd <module dir> --file <src> --replace "<wired>" --with "<disabled>"` per rule, watched going red (on a doubtful "RED (no pytest summary line)" mutate by hand with a backup; restore with `cp`, never `git checkout --`). The attribution gate `tools/tests/test_recon_match_attribution_gate.py` must go red when the matcher is mutated, else the tool stopped measuring. Suite: `uv run --directory workspace/clients/brisken/automations/expense-reconciliation --extra web --extra dev pytest -q` (about 4 minutes, 2,280+ tests). Ruff: `uv run --no-project --with ruff ruff check src tests` from the module dir. Heredocs with triple quotes are blocked: write scripts with the Write tool. Document every new field in `docs/api-contract.md` and pin every new list field in `tests/test_view_contract.py` in the same PR.
- Ship: commit with explicit pathspecs, push, `gh pr create` (the before/after tables, suite count before and after, the regress lines in the body), wait for CI (`gh run list --branch <b>` by head SHA; `expense-recon-tests.yml` runs the module suite), `gh pr merge --squash --delete-branch` on green. Backlog move to Shipped and the status file in the same PR.
- Deploy (pre-authorized, one Fly app shared with the siblings): after the merge, probe the live API for YOUR field first; a sibling's deploy may already carry your commit (`flyctl releases -a brisken-expense-recon`). If absent and no release is in progress: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add --detach C:\Users\neuma_p1qrsic\Repo\agentic-ops1-deploy-match origin/main`, confirm your merge is in `git log -1` there, `flyctl deploy . -a brisken-expense-recon --remote-only` from the module directory, remove that worktree. A stored month is not re-matched by a deploy; say so, and put the re-match to the owner as above.
- Cold browser drives: headless Playwright with `channel="chrome"` (`uv run --no-project --with playwright python <script>`), or `agent-browser --session recon-match` (never the default session). Log in at the gate, wait for localStorage `erc-token`, dismiss "Leave feedback anywhere", use JS `click()`; record every non-GET request and assert only the login.
- SPA work: only if a new reason code or `card_source` value needs a sentence. Then write `docs/lovable-<slug>-prompt.md` (exact JSON, render rule, EN + PT-BR strings, do-not-change list, checks), add a Not-applied row to `docs/PROMPT-STATUS.md`, and hand the whole prompt in the reply inside a FOUR-backtick fence under the label **"Paste into Lovable:"**. Otherwise say in one line that the item has no SPA surface.
- If context passes 500k before the item is done: commit and push what exists, open the PR marked unfinished, then checkpoint as below and hand back a continuation prompt labelled "Continuation prompt for Claude Code:" with the before table and the remaining steps.
- At the end: invoke `/comd_checkpoint --mini` (never hand-roll it), topic `Expense-Recon Matching X1`, ledger edits on `docs/checkpoint-match-1` in `C:\Users\neuma_p1qrsic\Repo\agentic-ops1-ckpt-match` cut off origin/main at checkpoint time, `finalize --root <that worktree>`, `uv run tools/repair-session-log.py docs/sessions/<date>.md --check` before pushing (three sessions write the same log today), PR, merge on green. Remove only the worktrees and branches YOU created (`agentic-ops1-match`, `agentic-ops1-ckpt-match`, `agentic-ops1-deploy-match`, `client/brisken/p1-match-description`, `docs/checkpoint-match-1`), then print `git worktree list`, `git branch -a | grep -i match`, `gh pr list --state open --search match` and stop only when all three are clean.

## The final reply, in this order

1. Shipped: PR, merge SHA, Fly release, suite count before and after, the regress lines with the test that went red.
2. Measured: the before table, the per-row moves after, labelled-wrong count, bundle score.
3. Live: which route was driven and what rendered; whether a live re-match is pending the owner's yes.
4. The Lovable prompt in full under "Paste into Lovable:", or the one line saying none.
5. Cleanup proof: the three outputs above.
6. Anything left open, named as open.
````
