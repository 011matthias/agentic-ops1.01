# Mini-Checkpoint: Brisken Recon Prompts Applied And The Bank Transfer Exit

**Date:** 2026-09-17
**Status:** Eight published SPA prompts verified and recorded; the bank-transfer exit shipped, deployed (Fly) and cold-driven; one new defect found while verifying it
**Type:** mini

---

## Summary
The owner published the SPA twice during this session, so the queue's "verify the pasted prompts" step turned from a no-op into eight verifications (PR #1069). Three owner rulings were taken, and the one that needed building shipped as PR #1074: a row settled off the card system now has an exit that is not a lie. Verifying that deploy surfaced item 146, a pre-existing disagreement between two person counters on the same payload.

## What Was Done
- **Bundle audit, twice.** First crawl 46 chunks / 1,222 KB: bulk-confirm-dialog (101), private-card-in-picker (139), one-download-each (141), booked-hint-names-workbook (142), entity-from-charge (111). Forty minutes later the i18n chunk hash had moved and the bundle read 1,242 KB, carrying card-tabs (138), duplicates-side-by-side (140) and the account-picks removal (note #61). Five controls validated on both crawls before any absence counted.
- **Seven of the eight driven cold** from the login gate in EN and PT (headless Chrome via Playwright, 1440x900). **Zero POST/PUT/PATCH/DELETE besides `POST /api/login` on every drive.** Dialogs opened and closed with Escape; no card, confirm or reject ever chosen. Evidence per prompt is in the PROMPT-STATUS Applied rows. PR #1069.
- **Three owner rulings.** (a) Item 115: a Zoho-seeded, unvalidated rule does NOT beat a disagreeing line read; today's behaviour stands. (b) Item 144: build a bank-transfer exit. (c) Item 115's residual: leave the re-categorize path unbuilt.
- **Item 144 shipped** (PR #1074, deployed to Fly, live-checked and cold-driven). A row whose disposition carries a `how` leaves `needs_person`, reads `can_mark_private: false`, and gets reason code `needs_entity_settled_outside` instead of an instruction to assign a paying card. `needs_entity` stays: the row carries no bill-to field, so filling the company from the filename would be a fabricated value.
- **One fact, stamped once.** `resolve_batch_row_cards` decides `settled_off_card` per row and stamps it on the resolution; the boxes, the review sentence, the private option and `card_review.n_needs_person` all read that stamp rather than deriving it again. Twelve person-owed surfaces were enumerated; five changed, seven are named with why each is correct to leave.
- **Item 146 filed**, found verifying the deploy (below).
- **Ledger hygiene:** the Shipped table carried two rows numbered 79 and two numbered 80 after sibling merges; the later pair is now 82 and 83, and item 144 took 84.

## What Did NOT Work (and why)
- **The first bundle crawler saw 7 chunks of 46 and reported every renderer key ABSENT.** This build writes its dynamic imports with BACKTICK template literals (`` `./chunk-months-CX8qc0wY.js` ``); the reference regex only accepted quotes and parentheses, so it never followed them. A confident negative from a structurally blind probe is the dangerous direction: it reads as "the owner did not publish" and would have closed the queue item as a no-op.
- **Item 144's equality test passed on a fixture that omitted the population that breaks it.** `test_the_two_person_counts_agree_on_every_row` asserts `summary.n_needs_person == card_review.n_needs_person` on a constructed batch with no decided copies. It is green, and the claim it licensed ("the two counts cannot disagree about a row") reached a PR body and a message to the owner. Live, July reads 13 and 15: the strip counts the two decided copies that item 94's ruling puts in no box. The gap is the copy count and predates item 144, but the test could never have found that, because the fixture had no copies in it.
- **The account-picks removal could not be driven in the UI.** `/settings` renders as a jump-nav over lazily-mounted sections; the Legal entities section never mounted under the drive script's click, so its two UI checks are unrun and only its bundle check (§3) is evidence.
- **`expenses.brisken.com` no longer serves the API.** `POST /api/login` and `/healthz` both return the SPA shell with 404 there. The API is `brisken-expense-recon.fly.dev` (the SPA itself calls `api.expenses.brisken.com`). Cost a diagnosis cycle at session start; the scratch `api.py` now defaults to the Fly host.
- **Python's default urllib User-Agent is refused by Cloudflare** in front of that host: `403 error code: 1010` before any auth. A browser UA string passes.

## Current Status
Fly carries item 144 on top of v177's four defects. July's Tricarico invoice reads the new reason, offers exactly nine cards and no private option, and has left `needs_person`; the control row (Hostinger 2026-07-28, no disposition) still carries the old reason, so the change did not leak. `summary.n_needs_person` 14 to 13. Brisken platform line: unknown plan, no ops data; comms-log 9 days stale. Two status files are stale by the 21-day threshold: `p2-product-decks.md` (56d) and `p2-targeting.md` (57d), both p2 workstreams this session did not touch.

## Next Steps
1. Item 146: one condition in `build_card_review` so the strip skips a decided copy the way `expense_boxes` does.
2. Publish the five prompts still out: `lovable-error-codes` (130, backend live), `lovable-invoice-read-as-statement`, `lovable-untrusted-flag`, `lovable-charge-category` (109), `lovable-receipt-chasing` (107).
3. Criss sets the Tricarico invoice's company by hand; the tool has no field to fill it from.
4. Owner: quotes for #98 and #104; operations #119-#128; cost-center data #118; Dirk's statement for card 9693 (#108); #129's UI prompt, which nobody has written.

## Files to Read First
- `workspace/clients/brisken/status/p1-improvement-backlog.md` (items 144, 146, Shipped rows 82-84)
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PROMPT-STATUS.md` (the eight new Applied rows, and the two crawler lessons above them)
- `workspace/clients/brisken/automations/expense-reconciliation/docs/api-contract.md` (the item-144 section)

---

## Continuation Prompt

````markdown
/comd_resume brisken

Continue the Brisken expense-recon (p1) defect loop. Read first: `docs/2026-09-17 - Brisken Recon Prompts Applied And The Bank Transfer Exit/Mini-Checkpoint-2.md`, then in `workspace/clients/brisken/status/p1-improvement-backlog.md` items 144 and 146 in full and the Shipped table rows 82-84, then `workspace/clients/brisken/automations/expense-reconciliation/docs/PROMPT-STATUS.md` (the eight new Applied rows and the two crawler lessons above them), and the memories `project_brisken_expense_recon_usability_loop`, `project_brisken_expense_recon_voids_audit`, `project_brisken_recon_matching_program`, `feedback_recon_no_live_writes_criss_acts`, `project_brisken_retainer_600_licence`, `feedback_continuation_prompt_carries_loop`, `feedback_end_iterations_with_remaining_items`.

## Where it stands

- **The owner published the SPA TWICE on 2026-09-17 night**, and eight prompts are now applied and recorded (PR #1069, docs only): `lovable-bulk-confirm-dialog` (101), `lovable-private-card-in-picker` (139), `lovable-one-download-each` (141), `lovable-booked-hint-names-workbook` (142), `lovable-entity-from-charge` (111), `lovable-card-tabs` (138), `lovable-duplicates-side-by-side` (140), `lovable-remove-account-picks` (note #61). Seven were driven cold from the login gate in EN and PT with **zero non-GET besides `POST /api/login` on every drive**; the account-picks removal is bundle-only (see below). Evidence per prompt is in the PROMPT-STATUS Applied rows; do not re-verify them.
- **Item 144's bank-transfer exit shipped** (PR #1074, merged on green CI, deployed to Fly, live-probed and cold-driven). A row whose `settled_outside` carries a `how` leaves `needs_person`, reads `can_mark_private: false`, and gets reason code `needs_entity_settled_outside` ("This expense was settled outside the card system, so no card will name the company it belongs to. Set the legal entity on the row; the export shows a placeholder until then.") instead of an instruction to assign a paying card. `needs_entity` STAYS: the row carries no bill-to field (`customer`, `legal_entity_id`, `entity_source` all empty on the live payload), so filling the company from the filename would be a fabricated value. One fact stamped once: `resolve_batch_row_cards` decides `settled_off_card` per row and stamps it on the resolution; the boxes, the review sentence, the private option and `card_review.n_needs_person` all read that stamp.
- Live after that deploy: July's Tricarico row (`0017__2026-07-30__BRISKEN_Consulting_LLC__Rodrigo_Tanure_Tricarico_Con__...`) has `boxes: ['uncategorized','needs_entity','needs_company_or_person']`, `can_mark_private: false`, `review.reason_code: needs_entity_settled_outside`; `summary.n_needs_person` 14 to 13; `n_settled_outside` 1. The control row (Hostinger 2026-07-28, no disposition) still carries the old reason, so the change did not leak.
- **Three owner rulings taken 2026-09-17.** (a) Item 115: a Zoho-seeded, unvalidated rule does NOT beat a disagreeing line read; today's behaviour stands, no code. (b) Item 144: build the bank-transfer exit (shipped, above). (c) Item 115's residual (a re-match never re-categorizes stored rows): leave it unbuilt; the correction reaches a month at its next arrival.
- **#133 rule (b) is on main** from sibling PR #1053 (`_merchant_precedence`, `matching/deterministic.py:1719`), deployed at Fly v170. Do not rebuild it.
- Owner ruling still standing: covered-class defects only until the EUR 600 licence is signed; new function is quoted separately.
- Waiting on the owner or Criss, not on you: publish `lovable-error-codes-prompt.md` (item 130, backend live), `lovable-invoice-read-as-statement`, `lovable-untrusted-flag`, `lovable-charge-category` (109), `lovable-receipt-chasing` (107); quotes for #98 and #104; operations #119-#128; cost-center data #118; Dirk's statement for card 9693 (#108); #129's UI prompt (nobody has written it); Criss setting the Tricarico invoice's company by hand.

## Queue, in order

1. **Item 146 (filed this session, covered defect).** `card_review.n_needs_person` counts decided copies that `expense_boxes` deliberately excludes by item 94's ruling, so one payload prints two answers: live July reads `summary.n_needs_person` 13 against `card_review.n_needs_person` 15, and `summary.n_needs_entity` 14 against `card_review.n_needs_entity` 16. The gap is exactly the 2 decided copies (Aposto Karlsruhe, Lovable Labs Incorporated), it predates item 144, and both counters moved together when item 144 landed. Fix: `build_card_review` (`web/service.py` ~6386) skips a row the grid treats as a decided copy, the same shape item 144 used. **The test that matters is the one item 144's fixture could not be:** the fixture must contain a decided copy, or the equality assertion passes while being false. Decide en route whether `n_needs_entity` takes the same exemption (item 94's ruling suggests yes; state which and why).
2. **Verify the remaining prompts when the owner publishes.** Bundle check first, no login: `python <scratch>/bundle.py "<comma-separated keys>"`. Baseline 2026-09-17 night: **46 chunks / 1,242 KB**, i18n chunk `chunk-i18n-Lood8exC.js`. **The crawler's reference regex MUST accept backticks**: this build writes dynamic imports as `` `./chunk-months-CX8qc0wY.js` ``, and a quotes-and-parens-only regex sees 7 chunks of 46 and calls every renderer key absent. Validate the five controls (`ready_to_post`, `coverage`, `seen_undefined`, `period_suggestion`, `n_duplicate_copies`) before any absence counts. Decisive keys for what is still out are in the PROMPT-STATUS Not-applied table. **Safety:** never click "Confirm all matched", "Reject N shown", a card-picker option or "Paid with a private card" unless the bundle carries that prompt's dialog keys; cancel or Escape in every dialog.
3. **The account-picks UI check is unrun.** `/settings` renders as a jump-nav over lazily-mounted sections and the Legal entities section never mounted under the drive script's click, so only its bundle check (§3) is evidence. If a future drive can mount that section, run checks 1 and 2 and upgrade the row.
4. **Two stale p2 status files** the sweep flags: `p2-product-decks.md` (56d) and `p2-targeting.md` (57d). Update in place or delete per W1 §4; do not nurse them.

Before each item: `gh pr list -R 011matthias/agentic-ops1.01 --state open`, `git worktree list`, and a grep of recently modified transcripts in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/*.jsonl` for the item number (skip a claimed item), and read `GET /feedback.jsonl` for new notes (69 lines on 2026-09-17 night, last is #69, all answered but #61 which a sibling holds; a new note outranks the queue).

## How to work

- **The API host is `https://brisken-expense-recon.fly.dev`.** `expenses.brisken.com` serves only the SPA now: `POST /api/login` and `/healthz` both return the SPA shell with 404 there. The SPA itself calls `api.expenses.brisken.com`. Use `expenses.brisken.com` for the bundle crawl and the browser drive, the Fly host for API reads.
- **Python's default urllib User-Agent is refused by Cloudflare** in front of the API (`403 error code: 1010`, before any auth). Send a browser UA string on every request.
- Live reads: `python <scratch>/api.py GET /api/path "<python expr on d>"` (bearer from `POST /api/login` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env`, held in memory only). `MSYS_NO_PATHCONV=1` for every `/api/...` arg in Git Bash, or the path is rewritten and the call returns nothing. Windows Python cannot open `/c/...` paths: pass `C:\...` forms. The months list is `GET /api/expense-batches`, a month is `GET /api/expense-batches/{id}` and its run is `GET /api/runs/{id}`.
- Parallel builder subagents work well: one git worktree off origin/main per item (`client/brisken/p1-item-<N>-<slug>`), a `general-purpose` Agent per worktree in the background with a self-contained brief (hard rules below, measurement, docs, commit on the branch, do NOT push), `uv run tools/bg_watch.py watch` per builder. You review the diff of the load-bearing functions, merge origin/main, push, PR, wait for CI by head SHA, merge on green, deploy, verify. **Send a builder back with SendMessage when its result trades one wrong outcome for another** — round 2 on item 144 is why the two counters move together at all.
- Never `git stash`. Stage explicit paths. Never `git checkout --` to restore a backup (it restores from HEAD); use a `cp` backup and a `cp` restore. The primary clone runs behind origin/main: use `( cd X && ... )` or `git -C`; a bare `cd` is blocked by a hook.
- No writes to Criss's live months; reads only. Never probe publish live. `GET /runs/{id}/expense-report.pdf` WRITES render outcomes: never fetch it live (render locally from a read-only DB copy; `reconciliation-report.pdf` is read-only). Never print or save the operator code or a token; tell every subagent the same.
- Every backend fix: a route-level test through the caller, then `uv run tools/regress_check.py` with single-line literals (sources are CRLF; "no pytest summary line" means reproduce by hand with a cp backup and cp restore). Python heredocs with triple quotes are blocked by a hook: write scripts with the Write tool. Do not import a pytest fixture from another test module (CI ruff F811). Full module suite in the FOREGROUND before the PR: `uv run --extra dev --extra web pytest -q -p no:cacheprovider` (no `-n`; about 5 min, 2421 tests today). Deploy with `flyctl deploy . -a brisken-expense-recon --remote-only` from the module dir of a clean detached origin/main worktree (pre-authorized).
- After a deploy: a live API probe of the changed field, then a cold consumer drive with `<scratch>/drive.py` (headless Playwright `channel="chrome"`: goto, set localStorage `brisken.lang`, fill the first input with the code, Enter, goto the route, read `body` inner text, record non-GET requests, which must be none besides login). **Dismiss the feedback widget first**: `[data-fb-widget] [role=dialog]` overlays the page and intercepts every pointer event, so a Playwright click times out until its "Got it" button is clicked. The month page shows one section at a time: switch with a JS click on the summary pill button and show decided rows with a JS click on `button[role=switch]`; a Playwright `locator.click` on the pill does not switch it, and a `?view=` param is stripped on load. A disabled button's tooltip lives on a wrapper `span[tabindex='0'][data-state]`, not the button. The Playwright MCP tools cannot do this drive: the login gate needs the operator code typed into a tool call, which would print it. Say plainly what the drive covered rather than the bare word "verified".
- In the same PR, update the backlog item's heading/status and its Shipped row (newest-first, highest on main is 84), plus `status/p1-expense-reconciliation.md`. SPA work goes out as a Lovable prompt in the reply inside a FOUR-backtick fence ("Do NOT add Supabase or any database", EN + PT-BR strings, a checking list), saved as `docs/lovable-<slug>-prompt.md` with a Not-applied PROMPT-STATUS row.
- **Merge races are the tax.** Siblings merged repeatedly during this session; every PR needs main merged once or twice, and CI builds the MERGE, so a scan-style test can go red on code that is green locally. For the backlog Shipped table keep origin/main's rows first and renumber yours above main's highest (match rows by content, never by index); for api-contract and PROMPT-STATUS keep main's section first and append yours. **When you append a superseding section, fix the superseded sentences in place too** — a contract asserting both is worse than one asserting the wrong thing once.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while that session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `--root <that worktree>` BEFORE the `finalize` subcommand (it is a global flag); write the checkpoint prose after `finalize` or rename your file to the number it prints; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````
