# Mini-Checkpoint: Brisken Recon Traceability T2

**Date:** 2026-09-18
**Status:** T2 shipped and live (PR #1095, Fly v185); T3, T1, T4 handed to the next session
**Type:** mini

---

## Summary
The TRACEABILITY session (one of three parallel sessions on the owner's 2026-09-18 note) shipped note item T2 as backlog item 150: every statement upload now carries a content-derived `statement_id`, carried on `coverage[].statement_ids`, recorded in the anchors by id, and addressable on the writeback route. The T4 shapes were enumerated on the live archive (91 mails) and most of T4 turns out not to exist there; T3 was not started because it would cross the 500k context line.

## What Was Done
- T2 built, proven and shipped: `statement_content_id` (sha256 of the stored bytes, 16 hex) computed by both callers of `build_statement_entry` (attach and re-read); parallel field on `statements[]`, absent on entries written before it; `coverage[].statement_ids[]` pinned in `tests/test_view_contract.py` on both payloads; `statement_anchors` keyed by file AND id (`_anchor_keys`); `GET /runs/{id}/statement-categorized.xlsx?statement_id=` (wins over `?file=`, unknown id 404). Five route-level tests in `tests/test_statement_identity_t2.py`; six wiring points proven red by hand (one-line mutation each, sha256-equal restore); one existing pin moved with the contract (`test_statement_append.py` anchors key set). Suite 2511 -> 2516 on the branch, 2524 passed / 2 skipped on the merged tree; ruff clean; CI green. PR #1095, merge `32af69f6`, deployed Fly v185 from a detached worktree.
- Live after v185: July and August `statements[]` entries carry no `statement_id` (recorded before it; they gain one at their next re-read, not triggered), `coverage[].statement_ids` reads `[]` on every card on both months and both payloads (was absent on v184). Cold headless drive of `expenses.brisken.com/expenses/074a7b8905d7` after login: "2 statements" panel and `August2026.xlsx` render, no error strings, the only non-GET request was the login. No renderer exists for the id, so the drive is a no-regression check, not a field render.
- Backlog item 150 (renumbered from 149 after the MEMORY sibling's #1094 took 149 first), Shipped row 90, status-file row, contract section at the end of `docs/api-contract.md`. Merge from `origin/main` after the push resolved two append-at-the-same-spot conflicts (backlog, contract) keeping both blocks, sibling's first.
- T4 enumeration, read-only, on the live archive: `GET /api/inbound/log?detail=1` (100-entry window: kept parts pdf 64 / png 3 / jpeg 6; skipped: two `.ics`, one `image.png`; 51 body-only ingested) and an in-machine MIME scan of all 91 stored `.eml` files via `flyctl ssh console -C` (script in the session scratchpad, deleted from the machine after the run): 0 HEIC, 0 nested `message/rfc822`, 49 of 51 PDFs one page and 2 two-page, 19 mails with two or more PDFs (each part is already kept as its own file), 2 mails with inline `cid:` images (`multipart/related`; the intake keeps a cid image when it is at least 4096 bytes and skipped the one `image.png` as signature furniture), 48 body-only mails, 1 bill-behind-link (the 2026-08-24 AWS notice item 106 already answers). Python's `msg.walk()` already descends into a nested rfc822 part, so a forwarded-as-attachment mail's attachments are read today.

## What Did NOT Work (and why)
- **Claiming backlog number 149 before merging main:** the MEMORY sibling merged #1094 with item 149 minutes earlier; the merge conflicted on the backlog and the contract and the item was renumbered to 150. The protocol's "merge main, then take the next free number" is the rule; the first push went out on a stale number.
- **`agent-browser --session recon-trace open`:** hung past 180s and was killed; the deploy-consumer gate nevertheless closed on that call, which asserted nothing. Headless Playwright with `channel="chrome"` drove the gate cold in about 25s and is what the verification rests on.
- **Resolving the conflict hunks with a `\n`-only regex:** the two files are CRLF, so the first resolver matched 0 hunks; `\r?\n` matched both.

## Current Status
T2 live (Fly v185, PR #1095). Nothing outbound (ack, CSV, PDFs) carries the id, by the owner's deferral to the Zoho Books integration. T3, T1 and T4 open, in that order. Item 72 is closed by item 103 (Shipped row 75) but its backlog heading still lacks the marker. Ops status for brisken: unknown plan (no `platform` section in `infrastructure.yaml`).

## Next Steps
1. T3 (receipt-to-line link as a record): `statement_id` beside `transaction_id` on `decisions`, `decision_history`, `receipt_claims`; `rows[]` / `expenses[]` charge-side fields; re-read walk-back fixture. Design notes in the continuation prompt below.
2. T1 (receipt identity inside the tool): audit where a receipt loses `document_id` (set-aside restore, copies, neighbour-month borrowing, trip settlement, the month report's numbering fallback).
3. T4 narrowed by the evidence: pin the nested-rfc822 walk with a fixture, decide the "body repeats the attached invoice" question on a judged sample; HEIC and nested-.eml unpacking are not live shapes.
4. Mark item 72's heading closed by item 103 in the next client PR.

## Files to Read First
- `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`
- `workspace/clients/brisken/automations/expense-reconciliation/docs/api-contract.md` (the T2 section at the end; item 104's "A decision history, with a name on every line")
- `workspace/clients/brisken/status/p1-improvement-backlog.md` items 150, 104, 103, 72
- memory `project_brisken_expense_recon_usability_loop` (2026-09-18 TRACEABILITY paragraph)

---

## Continuation prompt (handed in the closing reply as well)

````
/comd_resume brisken

Owner directive 2026-09-18: the items in this prompt have priority and are to be built now; the covered-vs-quote licence question does not gate them. You are the TRACEABILITY session. Two sibling sessions (MEMORY and MATCHING) run at the same time on the same repo and the same Fly app: follow the parallel-safety rules in "How to work" exactly.

Read first: `workspace/clients/brisken/automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`, then `docs/api-contract.md` in that module (the T2 section "A statement upload has an identity" at the end, "The statements a month has taken: `statements[]`", "Re-reading a month's statements", "A decision history, with a name on every line (item 104)"), backlog items 150, 104, 103, 72, 106 in `workspace/clients/brisken/status/p1-improvement-backlog.md`, and the memories `project_brisken_expense_recon_usability_loop` (2026-09-18 TRACEABILITY paragraph first), `project_brisken_expense_recon_mail_intake`, `feedback_recon_no_live_writes_criss_acts`, `feedback_continuation_prompt_carries_loop`, `rule_untrusted_inbound`.

## Where it stands

- **T2 SHIPPED** (backlog item 150, PR #1095, merge `32af69f6`, Fly v185). `statements[].statement_id` = sha256 of the stored bytes (16 hex), absent on entries written before it; `coverage[].statement_ids[]` beside the file names; the snapshot's `statement_anchors` records each upload's row map under its file name AND its id (`service._anchor_keys`); `statement_content_id(path)`, `statement_entry_by_id(run, sid)`; `GET /runs/{id}/statement-categorized.xlsx?statement_id=`. Live July `50622baec444` and August `074a7b8905d7` carry NO `statement_id` until their next re-read (not triggered; no live writes) and `coverage[].statement_ids` reads `[]` on both. A test fixture that attaches a workbook gets an id at once (`tests/test_statement_identity_t2.py` is the pattern: attach, both GETs, re-read, writeback).
- **T3 NOT started** (would have crossed 500k). Design notes from the code read: `web/store.py` `decisions` (PK run_id + transaction_id; writers `set_decision` ~1216, `set_tool_decision` ~1248, three more INSERT sites ~1302/1328/1355, `rekey_decisions` ~1190 moves rows to new ids by `UPDATE OR REPLACE`), `decision_history` (created in the CREATE block, `append_history`; lines built by `web/decision_history.py` `make_entry(row_key=tx_id, row_kind=ROW_CHARGE)` at twelve write sites in `web/app.py`, see item 104), `receipt_claims` (PK receipt_run_id + document_id; `upsert_receipt_claim` ~1629, `replace_claims_by_run` ~1690 called from the `rematch_month` commit at `service.py` ~12732 with (receipt_run_id, document_id, transaction_id) triples). Columns are added in `RunStore._migrate` (`PRAGMA table_info` + `ALTER TABLE ADD COLUMN`, ~510-575). The charge side: `Transaction` (`matching/types.py` ~128) has `source_row` and `source_file` but NO page; `ingest/statement_pdf.py` parses page text joined together and records no page per charge, so "PDF statements: `source_page` and the index on that page" needs a parser change or is stated as not recorded (state the call in the PR body). `rows[]` builder `service.py` ~3646 (`"transaction_id": tx_id`, `"chosen_document_id": held_doc`); `expenses[]` = `_receipt_view` ~2572 (carries `document_id`; the charge side of an expense is only `settled_by` for cross-batch, so the in-month charge must be derived from the effective settlement). Cheapest fill for the three tables: derive `statement_id` + `source_row` per transaction from the run snapshot (`statement_anchors` id-keyed entries: which id's map holds the tx) in ONE helper and pass it from the callers, or resolve it inside the store from `get_run` (a snapshot JSON parse per write; bulk routes write hundreds). Item 72 is CLOSED by item 103 (Shipped row 75: the commit persists the effective outcome); mark item 72's heading in the T3 PR, do not rebuild it.
- **T1 NOT started.** Pointers: set-aside restore (`set_aside[]`, item 52 / notes #52-#53), decided copies (`copies_set_aside`, item 94: `service.copies_set_aside_entries` ~7790), neighbour-month borrowing (`from_batch` + `kind`, `receipt_sources`), trip settlement (`settled_by`), the month report's numbering fallback (item 68's "rows 52-54 read none"; `output/` month report builder, receipt pages placed by `caption_mark` per the 2026-09-17 memory paragraph).
- **T4 EVIDENCE gathered, build narrowed.** Live archive (91 stored `.eml`, scanned in-machine): 0 HEIC, 0 nested `message/rfc822`, 49/51 PDFs one page + 2 two-page, 19 mails with 2+ PDFs (each part is already kept), 2 mails with inline `cid:` images (kept at >= 4096 bytes, `web/intake_mail.py` ~560-602; the one skipped `image.png` was signature-sized), 48 body-only, 1 bill-behind-link. `msg.walk()` already descends into a nested rfc822 part. Do NOT build HEIC conversion or nested-.eml unpacking for shapes the archive does not hold; what remains: (a) a fixture in `tests/test_intake_mail.py` pinning that a forwarded-as-attachment `.eml`'s PDF and body are read (proves the walk), (b) the 21 mails whose HTML body repeats the attached invoice's totals: judge a sample before rendering a body beside an attachment, since most would be duplicates of the PDF; if nothing is worth building, close T4 with the evidence and the reopen condition.

## Queue, in order

1. **T3 The receipt-to-line link survives as a record.** On `rows[]` and `expenses[]` expose, parallel and absent when unknown: `statement_id` and `source_row` for the charge side (PDF statements: state that no page is recorded, or record one), and `document_id` for the receipt side where a row does not already carry it. Every stored decision (`decisions`, `decision_history`) and every `receipt_claims` row carries `statement_id` next to `transaction_id`. Prove end to end on a constructed fixture: attach a workbook, pair a receipt, re-read the month (the re-read rekeys decisions by sheet row through the anchors), assert the booked expense still resolves to the same `document_id`, `statement_id` and sheet row. Mark item 72 closed by 103 in the same PR.
2. **T1 Receipt identity, inside the tool.** Audit where a receipt loses `document_id`; every surface a receipt appears on (grid row, unmatched list, copies set aside, the report's caption) carries the same `document_id`; fix where it does not. Nothing outbound.
3. **T4 The entire email is read**, narrowed as above.

Append each to the backlog as its own item when you first push, taking the next free number AFTER `git merge origin/main` (siblings append too; 149 and 150 are taken); keep "(note item T3)" in the heading so cross-references survive renumbering. When the queue is empty, follow SESSION LOOP step 9.

## How to work

- Your worktree, cut fresh: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 fetch origin` then `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add -b client/brisken/p1-trace-t3 C:\Users\neuma_p1qrsic\Repo\agentic-ops1-trace origin/main`. One branch per queue item (`p1-trace-t3`, `p1-trace-t1`, `p1-trace-t4`), reusing the `agentic-ops1-trace` directory after each merge (remove the worktree, cut again off origin/main after a fetch). Never edit, commit or stash in the main checkout; never `git stash` anywhere. A hook refuses `cd X && ...`; use `git -C`, `uv run --directory`, absolute paths. The GitHub slug is `011matthias/agentic-ops1.01` (pass `--repo` to `gh`; `MSYS_NO_PATHCONV=1` for colon pathspecs, and then give `-C` a `C:/...` path).
- Siblings (MEMORY in `agentic-ops1-memory`, MATCHING in `agentic-ops1-match`) edit `web/service.py`, `web/app.py`, `tests/test_view_contract.py`, `docs/api-contract.md`, `docs/PROMPT-STATUS.md`, the backlog and the status file all day. Append at the END of the relevant block; never renumber, re-sort or reflow. Before the first push `git rebase origin/main` is fine; after any push only `git merge origin/main`, never rebase, never force-push. Re-run the module suite after every merge from main. On a conflict in a shared file keep both sides (the files are CRLF: a resolver regex needs `\r?\n`). Before claiming a backlog number or a Shipped row number, merge main and take the next free one.
- Live reads only. API `https://brisken-expense-recon.fly.dev` (also `api.expenses.brisken.com`): `POST /api/login {"code": ...}` with `EXPENSE_RECON_OPERATOR_CODE` from `workspace/clients/brisken/context/.env` (never print it; read it inside a script). Months: July `50622baec444`, August `074a7b8905d7`, September `51a22ad72864`; list `GET /api/expense-batches` (key `batch_id`). Never fetch `GET /runs/{id}/expense-report.pdf` live (it writes render outcomes); `reconciliation-report.pdf` is read-only. No writes to Criss's months; any production mutation beyond a deploy goes through `AskUserQuestion` as a decision with a recommendation, never "say the word". Windows Python cannot open `/c/...` paths: pass `C:\...`. In-machine read-only scans: base64 a script into `flyctl ssh console -a brisken-expense-recon --pty=false -C "sh -c 'echo <b64> | base64 -d > /tmp/x.py && python3 /tmp/x.py; rm -f /tmp/x.py'"` (the image has pypdf). Read-only DB copy when needed: in-machine `sqlite3.backup`, then `MSYS_NO_PATHCONV=1 flyctl ssh sftp get -a brisken-expense-recon /tmp/<f> 'C:\...\scratchpad\<f>'`; delete both copies when done.
- Build: route-level tests through the FastAPI app, at least one through the CALLER the fix changed; prove each wiring point red by hand (cp the source aside, one-line mutation via a script written with the Write tool, targeted tests, cp back, sha256 equal) because `tools/regress_check.py` prints "RED (no pytest summary line)" whether or not the suite failed; never restore with `git checkout --`. Suite: `uv run --directory workspace/clients/brisken/automations/expense-reconciliation --extra web --extra dev pytest -q -p no:cacheprovider` (about 6 minutes, 2,524 passed / 2 skipped on `32af69f6`; run it in the background and wait for the notification). Ruff on the module: `uv run --no-project --with ruff --directory <module dir> ruff check src tests`. Python heredocs with triple quotes and heredocs carrying backslashes are blocked by hooks: write scripts with the Write tool. Document every new field in `docs/api-contract.md` and pin every new list field in `tests/test_view_contract.py` (both contract dicts and both MUST_COVER sets) in the same PR.
- Ship: commit on the item branch with explicit pathspecs, push, `gh pr create --repo 011matthias/agentic-ops1.01` (suite count before and after plus the red-proof lines in the body), wait for CI (`gh pr checks <n> --repo ...`; `test` is the module suite), `gh pr merge --squash --delete-branch` on green. The backlog move to Shipped and the status file update land in the same PR.
- Deploy (pre-authorized, one Fly app shared with the siblings): after the merge, probe the live API for YOUR field first; a sibling's deploy may already carry your commit (`flyctl releases -a brisken-expense-recon`; v185 carries T2). If absent and no release is in progress: `git -C C:\Users\neuma_p1qrsic\Repo\agentic-ops1 worktree add --detach C:\Users\neuma_p1qrsic\Repo\agentic-ops1-deploy-trace origin/main`, confirm your merge is in `git log -1` there, `MSYS_NO_PATHCONV=1 flyctl deploy "<module dir>" --config "<module dir>/fly.toml" -a brisken-expense-recon --remote-only`, then remove that worktree. Then a live API probe of the changed field and a cold browser read-back.
- Cold browser drives: headless Playwright with `channel="chrome"` (`uv run --no-project --with playwright python <script>`; `agent-browser` hung twice on this host). Log in at the gate (the code read from `.env` inside the script), wait for localStorage `erc-token`, dismiss "Leave feedback anywhere" by clicking the "Got it" text, go to `/months`, click the month, land on `/expenses/{id}`; record every non-GET request and assert only the login. The deploy-consumer gate does not see a script drive: state the coverage explicitly in the closing text.
- SPA work: write `docs/lovable-<slug>-prompt.md` (background, exact JSON shapes, render rule, EN + PT-BR strings, do-not-change list, checks), add a Not-applied row to `docs/PROMPT-STATUS.md`, and hand the whole prompt in the reply inside a FOUR-backtick fence under the label **"Paste into Lovable:"**. Label every fence: "Paste into Lovable:" or "Continuation prompt for Claude Code:". The SPA needs no change for T3 and T4 unless a field is meant to render; say so in one line when it does not.
- Checkpoint branches: `docs/checkpoint-trace-<n>` in `C:\Users\neuma_p1qrsic\Repo\agentic-ops1-ckpt-trace` (trace-1 is used), cut off origin/main at checkpoint time; `checkpoint_scaffold.py pre --root <that worktree>` and `finalize --root <that worktree>`; run `uv run tools/repair-session-log.py docs/sessions/<date>.md --check` there before pushing (three sessions write the same log today). Remove only the worktrees and branches YOU created; print `git worktree list` and `gh pr list --repo 011matthias/agentic-ops1.01 --state open --search trace` at every stop.

## SESSION LOOP (copy this whole section verbatim into every continuation prompt you write)

1. Measure YOUR session's context, not the machine's. `uv run tools/session_state.py --status` prints the most recently active session on the machine; trust its `context=` only when its `session=` matches the UUID in your scratchpad path. Otherwise read your own transcript: the last assistant record's `usage` (input_tokens + cache_read_input_tokens + cache_creation_input_tokens) in `~/.claude/projects/c--Users-neuma-p1qrsic-Repo-agentic-ops1/<your-session-uuid>.jsonl` (keep a small script for it in your scratchpad), or the per-session `[PRESSURE: ...]` advisory. On 2026-09-17 `--status` read a sibling's 474k while this session sat at 307k. Bands on the 1M window: moderate 300k, high 500k, critical 700k. A resumed session starts near 170k before any work.
2. Measure before starting each item and after each merge or deploy. A small backend item with tests, suite, deploy and live check cost about 45k on 2026-09-17 (item 102); a matcher item with before/after measurement cost about 225k (item 137, 173k to 396k); a two-builder PDF restructure is budgeted at 150-250k. Never start an item that would cross 500k.
3. At moderate: keep replies short, read targeted line ranges instead of whole files, do not re-read what is already in context.
4. When the next item will not fit, or context is at or past 500k: leave nothing uncommitted (commit and push the branch; open the PR even if the item is unfinished and say so in its body; merge only on green and only when the item is actually done). Never stop half-deployed: if a merge landed, deploy it and verify it first. Close browser sessions and clear `tools/bg_watch.py` entries.
5. Checkpoint: invoke the `/comd_checkpoint --mini` skill (never hand-roll it), with the ledger edits in a `docs/...` worktree off origin/main and `finalize --root <that worktree>`; write the checkpoint prose after `finalize`; do not clear friction candidates that belong to sibling sessions; commit, push, PR, merge on green.
6. Write the next continuation prompt: first line `/comd_resume brisken`; where it stands (what shipped this session with PR numbers and Fly version, what is in flight and why, what waits on the owner or Criss); the remaining queue in order with the exact code pointers and live evidence the next session needs; the "How to work" section; then this SESSION LOOP section verbatim. Deliver it in the reply inside a FOUR-backtick fence, together with any Lovable prompt written this session, and append the same continuation text to the checkpoint file.
7. The closing reply of every loop iteration lays out, in the chat itself and not only inside the prompt, the items still left to work through: the remaining queue in order, then what waits on the owner or Criss (owner directive 2026-09-17).
8. At critical (700k): stop right after step 4's commit and push, then do steps 5, 6 and 7.
9. When the queue is empty and no new feedback note is open, do not write another prompt: say the loop is done and list what shipped and what waits on the owner or Criss.
````
