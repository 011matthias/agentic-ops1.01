# Anneal Ledger

One row per `/comd_system-dev` (anneal) cycle. The convergence record for the
self-annealing loop: it answers "is the toolkit getting sharper, or just
bigger?" across cycles.

Generated by `uv run tools/anneal-metrics.py --append` (Phase 6.5 of
`comd_system-dev`). The tool fills every column except **Top Finding**, which the
agent fills with the cycle's headline defect/decision.

Read it as a trend, not a snapshot. Convergence looks like: `NetD` trending to 0
or negative (consolidation), `ChangeSet` smaller each cycle (`Smaller? = yes`),
`Drift` and `Unres` falling. Oscillation or monotonic growth in any column is the
signal to stop tweaking that area and rethink (see the spec's convergence test).

Columns: **NetD** = net asset delta vs the prior row (negative = consolidation,
good). **ChangeSet** = distinct primitive files touched in git since the prior
cycle. **Smaller?** = is this cycle's ChangeSet smaller than the last? **Drift** =
count of documented-vs-actual mismatches. **Skills(dir/adv)** = real `skil_` dir
count / the count CLAUDE.md advertises. **Rules(files/LOC)** = file count / total
lines. **Unres** (basis change 2026-07-20): the cell reads `A (+H held)` where
`A` = actionable unresolved and `H` = gate-held (`No (caught by hook ...)` rows;
a live gate contains every occurrence, they can never flip to Yes, so only `A`
is the convergence signal). Rows before 2026-07-20 are single ints on the OLD
parser, which mis-read the 305 register rows carrying a 7th note cell and
under-counted (90 vs true 211 measured 2026-07-20); do not compare across the
basis change.

| Date | Cmds | Skills(dir/adv) | Agents | Tools | Rules(files/LOC) | RegRows | Unres | Recur% | Mem% | Drift | NetD | ChangeSet | Smaller? | Top Finding |
|------|------|-----------------|--------|-------|------------------|---------|-------|--------|------|-------|------|-----------|----------|-------------|
| 2026-06-18 | 29 | 34/53 | 12 | 44 | 15/2199 | 200 | 67 | 1.5 | 28.5 | 3 | - | - | - | baseline; 3 drift items (skills 51-vs-34, rules 12-vs-15, rules-LOC 2199 over 500 budget) to resolve next cycle |
| 2026-06-18 | 29 | 34/53 | 12 | 45 | 15/2185 | 200 | 67 | 1.5 | 28.5 | 0 | +1 | 0 | - | CONVERGING: drift 3->0 (counts fixed; LOC budget retired for per-file ceiling + no-duplicated-bans); skill-map 113->27 (-77 FP via pack-module + vendored-doc fences, -9 real dead-pointers repointed); rules single-sourced to anti_slop (-14 LOC, +voice-ban consistency test); gates strengthened (instantly curl-body POST; +assert-live-origin H6, the sole CREATE). 0 new rules/skills. ChangeSet n/a (same-day baseline); real change-set 21 files |
| 2026-07-12 | 30 | 35/54 | 12 | 54 | 16/2315 | 369 | 83 | 0.8 | 21.1 | 0 | +12 | 58 | no | BUILD round (prior 06-18 was audit-only, deferred these). Headline: live-sibling-session guard = session_registry.py + 14th canonical hook, kills the 6x infra-deferred cross-session collision (feedback_worktree_for_concurrent_sessions). Also edge_cdp.py (raw-CDP, ends the connect_over_cdp 180s hang), Dirk 3-surface mailbox watcher, CLAUDE.md count drift 3->0. NetD +12 / ChangeSet 58 span 24d of upstream merges since the same-day 06-18 baseline, NOT this cycle (own footprint: +3 tools +1 hook, 0 new rules/skills/cmds). Unres 67->83 = 82-basis parser-true recount (basis change, not regression). B1 closing-offer deliberately untouched (gate works; tinkering = accretion). Advisory: 3 rule files >250-line ceiling = split candidates next. |
| 2026-07-20 | 31 | 36/55 | 12 | 64 | 18/2532 | 531 | 136 (+58 held) | 1.0 | 23.0 | 2 | +14 | 64 | no | TOP FINDING: the Unres instrument itself was broken twice over — (a) 305/531 register rows carry a 7th note cell the old parser mis-read (fix cell counted as resolved), so true backlog was 211 not 90; (b) gate-held rows (stop-b1 closing-offer class) can never flip to Yes, so Unres could only rise. Both fixed: parser shape-anchors Resolved (PR #272), Unres now = actionable (+held). Register sweep flipped 18 stale rows whose fixes had shipped (sibling detector, demo validator, + 6 fixes shipped this cycle in #272: cd-guard bypass, charmap env, scorer-lock worktree, outreach-truth tool, em-dash demo term, merge-not-live advisory) and normalized 19 hook-held rows. Basis change: do not trend-compare Unres to prior rows. Drift=2 here is the pre-#272 tree; #272 zeroes it. NetD +14 / ChangeSet 64 again span 8d of parallel client sessions, not this cycle (own footprint: +1 tool, 0 new rules/skills/cmds, 1 CONSOLIDATE sweep). Verdict: instrument repaired; next cycle is the first honest read. |
