# Neon Postgres connection string.
# Auto-injected by the Vercel Neon Marketplace integration (do not commit a real value).
DATABASE_URL=

# Incoming webhook for new-lead alerts (Microsoft Teams or Slack).
# Leave blank to disable notifications (leads still persist to Postgres).
NOTIFY_WEBHOOK_URL=
