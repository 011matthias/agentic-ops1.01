---
project: brisken
workstream: p1-expense-reconciliation
group: ""
spec: p1
state: active
updated: 2026-09-18
---

# Brisken / Expense Reconciliation (p1)

AI-assisted expense reconciliation tool for Brisken: turn Chris's multi-day
reconciliation grind into minutes of review, with a 1:1 Zoho journal export.
Scope is the "working tool" (single-tenant, Brisken-only) per Dirk's directive;
the multi-tenant SaaS in spec v2 is deferred. Per-slice authority is
`automations/expense-reconciliation/BLUEPRINT.md` + `ANNEALING.md`; this is the
roll-up.

**This app is the consolidation target for the whole Brisken estate**
(2026-09-09, `../TARGET-ARCHITECTURE.md`). It already owns the dedicated
IPv4, the `expenses.brisken.com` MX, the rDNS and the cert, so the plan
merges the websites and the lead desk INTO this app behind a Host-header
router rather than moving anything out. Port 25 is what forces it: an MX
target cannot point at an edge host, which rules out Vercel, Netlify,
Cloudflare and Azure Container Apps. Nothing is scheduled; ownership
transfer (`../OWNERSHIP-HANDOFF.md`) comes first.

The backend is hosted and running on real data at brisken-expense-recon.fly.dev
(API-only since v31; gated by `EXPENSE_RECON_OPERATOR_CODE`). The UI is the
Lovable SPA at brisken-reconcile-dash.lovable.app. Verify the deployed origin,
not localhost, after backend edits (`flyctl deploy`).

**Hosting changed 2026-09-10 after a ~50-minute outage.** The machine
auto-stopped cleanly, then wedged in flyd (reported `stopped` when signalled,
`active` when started), so every proxy auto-start was refused and both the UI
and the port-25 MX 502'd until the owner reported it. Root cause underneath: the
host holding the volume was out of capacity. Recovery was machine destroy,
`volumes fork` onto a fresh host, redeploy of the identical image. Two standing
consequences:

- The live volume is now `recon_data_v2` (`vol_vgnplk8909y0q184`, zone 778b).
  The old `recon_data` (`vol_4m3p65dn1nqkowzv`) is kept **unattached** as a
  rollback copy; destroy it once the new one has run clean for a few days. Any
  doc naming the old ID as live is stale.
- Both services are **pinned always-on** (`auto_stop_machines: false`,
  `min_machines_running: 1`, ~EUR 2/mo). Do not restore scale-to-zero: it put
  the MX behind an auto-start that can fail.

`automations/expense-reconciliation/fly.toml` mirrors the platform config as
of 2026-09-11 (always-on both services, `recon_data_v2`, 1024 MB). Until that
day it still said scale-to-zero, `recon_data` and 512 MB, so a `flyctl deploy`
from the module directory would have undone the 2026-09-10 recovery. Re-sync
it from `flyctl config show -a brisken-expense-recon` whenever the platform
side changes. Full detail: memory `project_brisken_expense_recon_fly_hosting`.

**2026-09-11: July and August 2026 reconciled 0 because the Excel parser kept
Chase's printed sign.** Criss's workbooks print purchases negative; the CSV
parser had canonicalized that since 3.15 but the xlsx sibling never did, so
every charge reached the matcher as -15.00 against a 15.00 receipt (Criss:
"ele ve que tem recibo mas nao associa"). Fixed in the parser (Type column
per row, else majority inference), the hosted column guess now maps Chase's
`Type`, and an entity-less receipt (mailed / dropped, no card hint yet) is
unscoped in the matcher instead of silently unpairable. The two live months
are repaired in place by `POST /api/expense-batches/{id}/statements/reread`
(rebuilds the charges from the stored files; a re-upload would have doubled
the month). Backlog items 55 (this) and 56 (invoice+receipt copies make a
pairing ambiguous) carry the detail.

**2026-09-11 evening: the void list (backlog items 57-64) is on record and
round 1 shipped.** Readiness can no longer say "ready to post" on a month
the matcher could not see: `summary.month_health` on both payloads names
the broken input (sign / currency / entity / card) whenever exact same-day
same-amount pairs sit unproposed, and `ready_to_post` is false while it
does (item 57; August as uploaded on 2026-09-10 would have read `broken`,
suspect `sign`). Every commit of `rematch_month` now leaves an event the
dev notifier mails as one line ("August 2026: 14 of 111, pool 7") (item
58). **Round 2 (2026-09-14)** took both open rulings and shipped them: a
charge's entity now comes from ITS card and is blank when the registry
cannot name that card (item 59, ruling "blank beats a wrong posting";
`n_charges_no_entity` counts the gap, one card definition closes it), and
an invoice and its receipt are one matcher candidate (item 56, ruling
"collapse automatically"; a `not a duplicate` ruling re-expands the pool
and re-matches the month). Deployed v116 and both live months refreshed on
an owner yes: July 24 to 27 reconciled and 15 to 11 review, August 7 to 3
review, every ambiguous invoice+receipt pick gone on both, zero new model
calls (the judgment cache keys on charge ids, which a refresh preserves).
The prediction that ~77 August charges would go entity-less was WRONG and
the correction is worth keeping: all nine cards ARE defined in the live
registry with entities, and "not in your card list" was the batch's
upload-time registry SNAPSHOT, not the live one. The refresh pulled the
current registry in, so those rows now read "Credit Card Chase Visa - 3645
/ 3876 / 0340". `coverage[].known` answers "did this batch know the card";
`/api/settings` answers "is the card defined".

**Round 3 (2026-09-15): both round-1/2 Lovable prompts are APPLIED and item
60 shipped.** The owner published; the bundle audit (48 files, 975 KB, with
known-present control fields validating the crawl first) finds
`month_health`, `n_exact_pairs`, `n_charges_no_entity` and the matching i18n
keys, and the CHARGES WITHOUT A COMPANY tile renders on both months. Two
renderers have no live case to exercise them and are recorded as such: the
blocked readiness bar (both months are healthy) and the "Card not defined"
chip (every card is defined). Item 60 then shipped:
`rows[].candidates[].held_by` names the charge holding a candidate receipt,
so a dispossessed charge stops reading as "No receipt found". Items 61-64
are the next rounds.

**Parallel round (2026-09-15), item 68: the Receipt column answers "is it in
the report".** "Attached" was an answer about a FILE, decided before
renderability is known, so an unrenderable receipt counted as covered while
its own caption page said the file could not be rendered.
`expenses[].receipt_in_report` and `summary.n_receipts_in_report` answer
"has a PAGE", derived from item 67's `receipt_render` (which item 67 records
from `prepare_evidence`, the function the builder uses to admit a page)
rather than decided a second time, and ABSENT until known rather than
guessed. The reconciliation report is now built from
the reviewer's live overlay, so an expense she deletes leaves both documents
at once instead of sitting in the reconciliation one until the next re-match.
Read live first: neither half reproduces on August or July today (31 of 31
and 51 of 51 attached AND paged, identical expense sets across the two
documents), so the fixtures are constructed. SPA half
`docs/lovable-receipt-coverage-prompt.md`. One live finding left open: July's
listing has 54 rows for 51 expenses and rows 52-54 read "none" although every
expense has a file, because the evidence numbering falls back to one caption
per receipt when the account fan-out does not align.

**Every Lovable prompt is applied as of 2026-09-07** (the owner pasted and
published the four pending prompts — R1 person/private, months
origin+refusals, R3 trips, R4 settled-by — bundle-verified same day),
verified against the published bundle by the field names each renderer has
to read. The authority is
`automations/expense-reconciliation/docs/PROMPT-STATUS.md`; where a row
below still says a SPA half is "remaining" or "owner to apply", it is a
record of that round, not the current state. Expect the not-applied list
to fill again: the habit is that a backend round ships the same day its
prompt is written.

**July receipt gap, re-probed live 2026-09-10.** July batch `50622baec444`
holds 32 receipts and NO statement; against the 111 July rows in Zoho Books
(107 of them card 2838) that is 21 charges covered (14 exact, 7 across the
EUR/USD rate with vendor agreement) and 90 with no receipt, worth USD 4,356
plus EUR 900. Three blockers, none of them a search problem:

1. **Zoho Expense was never SET UP in 6 of the 8 orgs; there is no disabled
   user to re-enable.** The `400/6018` "your account is disabled" reads like a
   membership problem and is not one. Dumping the full `/organizations` records
   splits them cleanly: the two readable orgs carry `user_status: 1 / Active`,
   `is_quick_setup_completed: true`, plans **Zoho One** (Cloud Services) and
   **FREE** (GmbH); all six locked orgs carry plan **TRIAL**,
   `is_quick_setup_completed: false`, and **no `user_status` field at all**.
   GmbH being FREE and working proves this is not about paying. They are Zoho
   *Books* orgs that Expense merely lists.
   **Consequence for July: the receipts are probably not locked away, they were
   never filed.** Books holds the charges from the card feed with no attachment
   on 108 of 111 rows, and Expense is live only in Cloud Services, where the 8
   we already pulled came from. The remainder exists only as mail in inboxes and
   photos on phones. **CONFIRMED 2026-09-10** by the owner signing in as Dirk:
   Expense serves a "Join your existing organizations" screen listing exactly
   those six with a Join button, over the line "You belong to the following
   organizations in other Zoho finance products." Not joined, never used.
   Joining provisions an EMPTY org and recovers nothing, so it is not the move.

2. **The mail intake is the working route, and Dirk is already using it.**
   Correcting the 2026-09-09 attribution twice over: the "sent to Criss /
   unreachable" group is neither reachable-via-Zoho nor stuck. `GET
   /api/inbound/log?detail=1` shows 16 forwards from dirk.neumann@brisken.com
   between 2026-09-07 and **2026-09-10T06:43**, each self-filed by the month
   printed on the receipt (2026-05/06/07/08/09) — Lovable, AT&T, Base44/Wix,
   Pressmaster, Anthropic, Eleven Labs, OpenAI, i.e. exactly the vendors called
   unreachable. One Lovable July receipt is in the July batch already. Two
   forwards created zero documents, correctly: the AT&T "bill is ready to view"
   and the "August transaction summary" are notices, not receipts, so those
   charges stay uncovered.
3. **No July card statement exists within reach.** SharePoint's Chase archive
   stops at `20260704` (June cycle) and card **2838 has zero statement files in
   SharePoint in any year**; the local `Chase2838_full_activity.csv` ends in
   June (0 July rows). So the tool cannot answer "which July receipts are
   missing" for itself; the 111-row chase list comes from Zoho Books via a
   scratch script. Owner deferred this ask 2026-09-10.

**Criss's mailbox: unblocked 2026-09-10, the fix was Exchange not Graph.** The
receipt collection point was unreadable and the reflex ("expand the Graph
scope") would not have worked: the app token already carries tenant-wide
`Mail.Read`, and the refusal was `403 [RAOP] Blocked by tenant configured
AppOnly AccessPolicy settings`. The Exchange Application Access Policy that
`rule_brisken_graph_first` described as absent had since been created and was
enforcing, so mailbox reach is **group membership**, not API permission. Proven
by differential probe on one token in one run (dirk OK 2692, matthias OK 450,
criss 403).

**The policy propagated, and her mailbox holds the July receipts
(2026-09-11).** Re-probed with the same differential script: dirk OK 2691,
matthias OK 451, **criss OK 32606**. A folder-by-folder sweep of her 81
folders over 2026-06-25..2026-08-15 found 976 messages in the window and 179
from chase-list vendors, 34 of them with attachments. Cross-referencing the
97 still-uncovered July charges gives **20 charges with a receipt-shaped mail
in the window**, behind 9 distinct mails; downloading those yielded **19
receipt and invoice PDFs** (Anthropic x8, Lovable x6, Supabase x2, Microsoft
x1, plus a screenshot). Files in `.scratch/recon-july/criss-receipts/`,
manifest `criss-receipt-manifest.json`.

*How far that verification actually goes, which is less far than the count
suggests.* Reading amounts out of the PDFs confirms only TWO charges on a
DISCRIMINATING amount: Supabase 92.70 (2026-07-29) and Anthropic 52.26
(2026-07-29). The rest carry round figures (25.00 / 50.00 / 100.00) that
several different July charges share, so the amount narrows the vendor but
cannot say WHICH charge a given receipt covers; and four PDFs yielded no
extractable text at all (three Anthropic `WWT1PNYP` invoices and the
Microsoft `G170925639`), so a text probe is blind on them. **The honest
conclusion is that hand-matching in a script is the wrong instrument**: the
app's own OCR plus its matching engine is what exists to answer this, and
running these 19 files through the real ingest is both the better match and
the exercise of the real path. That ingest writes into Criss's live July
month `50622baec444` and is therefore invasive; the owner has chosen
verify-then-ingest and a separate explicit go is still required before the
write. A false positive was caught and removed on the way: the substring
"chase" matches inside "Purchase Order", which paired the JPM Chase annual
fee with an Archer-Daniels-Midland mail.

Owner ran `.scratch/recon-july/add-criss-to-graph-policy.ps1` with an Exchange
admin sign-in. Real scope group is **`sg-marketing-ops-mailboxes`** (not the
runbook's remembered "GraphOps Allowed Mailboxes", which is why the script reads
`ScopeName` off the live policy instead of trusting the file); members went 2 to
3, `Test-ApplicationAccessPolicy` returned **Granted**, and the control mailbox
`marketing@brisken.com` still returned **Denied**, so the policy is still
restricting and the Granted means something. Our in-code allowlist was widened in
the same session, since the two have to move together.

Four routes an agent CANNOT take here, all tested, recorded in
`context/graph-access-policy-runbook.md` so they are not re-derived: Graph group
write (403, no `Group.*`/`Directory.*`/`Exchange.ManageAsApp`), Exchange app-only
PowerShell (needs a certificate plus that role; we hold a client secret),
`Connect-ExchangeOnline -Device` (works, but the code expires in 15 min unused),
and Edge CDP (Chromium ignores `--remote-debugging-port` on the default
`--user-data-dir`). The admin sign-in is irreducibly human.

**Where the missing 89 can actually be recovered from.** Split by route rather
than by blocker:

| Route | Charges | Value |
|---|---|---|
| Re-downloadable from a vendor billing portal | 44 | USD 3,398 + EUR 900 |
| Only ever a paper slip or phone photo | 45 | USD 932 |

About 79% of the missing money is not lost; a SaaS invoice history does not
expire. Work list by size: Anthropic 12 charges / 1,158.15, LinkedIn 2 / 689.80,
Crossmedia 1 / EUR 900, Lovable 5 / 255.00, SaaSRise 197.00, Network Solutions
3 / 170.11, OpenAI 2 / 155.04, JPM Chase annual fee 150.00 (statement-only, no
receipt exists), Pressmaster 135.00, AT&T 129.36, Supabase 92.70, Perplexity
4 / 63.84, then a tail under 60. The 45 slips are Brazilian supermarkets, German
restaurants and hotels averaging USD 21; a few (Twilio, Fireflies, Rize, GoDaddy,
Proton) are portal-recoverable and landed in that bucket only because the
keyword list missed them. Ingest once files exist: drop onto the Receipts page
or `POST /api/receipts`, which routes each file by its own printed date; cap 500
per ingest since item 45. Script: `.scratch/recon-july/routes.py` regenerates
the split.

Intake is clean (78 ingested, 19 dismissed, nothing pooled or held). A draft
asking Dirk to re-enable his Expense login was written and then DELETED the
same session: its central ask was believed false once the org records were
read. Owner has Dirk's access and is doing the web-UI check directly.
Method note worth keeping: two wrong diagnoses in one session, both from
reading an error string instead of the record behind it. `6018 "account is
disabled"` is not evidence a user was disabled, and "no receipt mail in the
two mailboxes we read" is not evidence the receipt is unreachable.

## Elements (index)

State at a glance only. Live slice-level status, next actions, and detail are in
`automations/expense-reconciliation/BLUEPRINT.md` + `ANNEALING.md` (the authority);
this table is the index, not a second record.

| Element | State | Tracked in |
|---|---|---|
| Mail intake: the whole message is walked (backlog item 154) | **evidence 2026-09-18, walk pinned; build NOT started** | `tests/test_intake_mail.py` pins the `message/rfc822` descent and that an operator note above a forward reaches no receipt. Not built by evidence: HEIC conversion, a nested-.eml unpacker, rendering a body beside an attachment. OPEN: carry the operator's prose onto provenance as a display-only note (never routing; untrusted inbound) |
| Receipt identity on every surface (backlog item 153) | **shipped 2026-09-18** (pending PR; deploy pending) | `set_aside[].document_id` beside `file` in `set_aside_view`, from the entry's stored receipt (absent on a legacy parse-issue entry). The audit across live July, August and September is in `docs/api-contract.md`: every other surface already carried the id. No SPA change |
| Charge origin: the receipt-to-line record (backlog item 152) | **shipped 2026-09-18** (pending PR; deploy pending) | `web/statement_origin.py` (the snapshot key `statement_origins` + the reader, shared by `service.py` and `store.py`); `statement_file` / `statement_id` / `source_row` / `source_page` on `rows[]` and `expenses[]`, `transaction_id` on `expenses[]`; `statement_id` columns on `decisions`, `decision_history` and `receipt_claims`; `Transaction.source_page` from the Chase PDF parser. Live July/August resolve through the anchors fallback and gain ids at their next re-read. No SPA change: nothing renders it yet |
| Outside-in uptime monitor (backlog item 123) | **shipped 2026-09-18** (PR #1098; repo workflow, nothing to deploy) | `tools/recon_uptime_probe.py` + `.github/workflows/expense-recon-uptime.yml`: /healthz, port-25 banner, SPA shell every 10 min; one `recon-uptime` issue + one mail per outage to `BRIEFING_TO`. First dispatch (dry_run): all three OK, port 25 reachable from the runner. PR #1106 fixed the alert path (the probe step's DOWN exit aborted under `bash -e` and skipped the notifier). Cron: no scheduled run had fired 70+ min after the merge; being watched |
| Accuracy scorer in CI + before a deploy (backlog item 127) | **shipped 2026-09-18** (PR #1099; CI job + dev-machine hook, nothing to deploy) | `tools/recon_accuracy_check.py` (`ci` exact-equality gate on synthetic bundles under `tests/fixtures/accuracy/`; `real` over the gitignored months vs `tools/recon-accuracy-baseline.json`), `.claude/hooks/recon-accuracy-deploy-gate.py`. Baseline all 76.0, 0 wrong |
| Mailbox STARTTLS, opportunistic and self-signed (backlog item 125) | **shipped 2026-09-18** (PR #1101; deploy pending) | `web/smtp_tls.py` + Controller `tls_context`; `transport_tls` on provenance. After deploy: EHLO must show `250-STARTTLS`, next real mail lands `transport_tls: true`. Open: CA cert via DNS-01 |
| Guards: reader parity, vocabulary pins, prompt pin, listener e2e (backlog item 128) | **shipped 2026-09-18** (PR #1100; tests only, nothing to deploy) | `tests/test_reader_parity.py`, pins appended to `tests/test_view_contract.py` (12 re-match triggers, not the 10 the contract listed), `tests/test_extraction_prompt_pin.py`, `tests/test_smtp_listener_e2e.py`. Not built: reader-version stamp per statement |
| Re-match visible on the month (backlog item 129, notes #53 #54) | **backend shipped 2026-09-18** (PR #1103; deploy pending); **SPA prompt written, not pasted** | `last_rematch` / `rematch_pending` on both month payloads; `docs/lovable-rematch-visible-prompt.md` + PROMPT-STATUS Not-applied row. After deploy: August payload carries `last_rematch.trigger`; after paste: header reads "Last matched …" |
| Merchant-to-category is the default; account varies by company (backlog item 149, note item M1) | **shipped 2026-09-18** (pending PR; not deployed) | The registry default stamps every receipt of its merchant before any line read; the (company, vendor) rule decides the ACCOUNT (its own company, else no-company, else the vendor's rules when they agree; a seeded rule under another category waits for validation); `provenance` names the rule. `GET /api/memory` `by_vendor[]` shows one line per company under each vendor, with the registry merchant and the category its receipts read; `categories[].seeded`. Receiptless charges consult the registry on re-match. Owner-gated: the live registry write for OpenAI / Anthropic / Lovable (diff shown, yes required) and Dirk's 3x3 account table. SPA half `docs/lovable-memory-by-company-prompt.md`, not pasted |
| A dropped pile is read in parallel (backlog item 148) | **shipped 2026-09-18** (pending PR; not deployed) | The routing pass reads the dropped files on a bounded pool (`_DROP_READ_WORKERS = 6`) instead of end to end, and reports `reading receipts (7/40)` through the job's existing `stage` (the published SPA renders that string verbatim, so nothing to paste). Stub at 0.2s per read, 40 files: 8.21s -> 1.42s reading, filing unchanged at 0.11s. The ledger is byte-identical to the serial one for the same folder, a read that raises is still that file's problem alone, and a typed `month` still reads nothing. The `_MATERIALIZE_LOCK` span and the ingest behind it stay sequential |
| A card can sit under an account (backlog item 147) | **shipped 2026-09-18** (pending PR; not deployed) | The registry entry takes an optional `parent` holding another card's key, validated one level deep on save with five named codes, and `card_sections[]` renders the tree on both month pages and in both PDFs: an account first, its subcards behind it, its figures the sum of itself plus them, its own card's figures kept in `own`, and a statement covering the account named once instead of once per card. Owner 2026-09-18: "only 2838 has subcards, no where else", "just do it, no quoting". Parentage is data a person sets; nothing infers it. **The registry has no committed seed** (`settings["cards"]` lives only in the app's SQLite, default `{}`), so the owner sets the three parents once in Settings, Cards and the tree appears on the next read. SPA half `docs/lovable-card-accounts-prompt.md`, not pasted |
| The card strip counts the rows the boxes count (backlog item 146) | **shipped + deployed 2026-09-18** (PR #1086, Fly v181). Live July now reads 13 / 13, 14 / 14, 7 / 7, 0 / 0 and August is unmoved, exactly the prediction. **Visible effect on Criss's screen: none**, which the item as filed got wrong and this row said too until a post-merge bundle read: the SPA reads `summary` for all four counters and `card_review` for none (every occurrence across the 44 chunks is `i.summary.<field>`). A contract fix, not a screen fix | `build_card_review` takes the month's decided copies and its four box-twin counters skip them, so the strip and `summary` cannot answer the same question differently. Live July before the fix: `n_needs_person` 13 against 15, `n_needs_entity` 14 against 16, `n_suggested_private` 7 against 9, each gap exactly the two decided copies; August agreed on all four already. Predicted live effect: July's strip reads 13 / 14 / 7, August unmoved. The grouping still counts copies on purpose (the assignment surface has no twin to disagree with) |
| Item 144's review-reason copy | **APPLIED and DRIVEN 2026-09-18, EN and PT; CLOSED** | July's Tricarico row of 2026-07-31 (BRL 27,203.34) reads the prompt's own sentence in both languages instead of the backend prose, with no generic paying-card reason on the page, and the row's state beside it unchanged: "Paid by bank transfer", the paid-through picker, no person question. July `summary.n_needs_person` still 13. **Found by the re-crawl, not by being told**: the owner had already published it and the prompt was minutes from a second hand-over |
| The three unverified prompt halves of 2026-09-18, driven (backlog items 109 and 130) | **two CLOSED, one promoted to a prompt, 2026-09-18** | charge-category (109) is on screen in EN and PT: 205 category comboboxes and 89 Guess chips on August's Charges-without-a-receipt view, the LOVABLE row reading "Meals & Entertainment / Guess" exactly as its check 1 asks. error-codes (130) is applied and WIRED in five of six sections: 16 `toast.error(errorText(e, t))` call sites in the workbench route chunk with zero `.message)` left, and the row chips driven as NEAR MISS / QUASE IGUAL on one row. Its §6 is the one real gap: the not-found and crash screens are client React components with English in the JSX, so a Portuguese reader meets English on exactly the two screens where something has gone wrong. Prompt `docs/lovable-error-page-lang-prompt.md`, copy only, no backend gate. The prompt's check 4 was never passable: that route is the SPA's own missing-run screen and issues no API refusal. Bundle 44 chunks / 1,174 KB, fourth rehash in about 26 hours |
| A company invoice paid by wire has an exit that is not a lie (backlog item 144's open question, owner ruling 2026-09-17) | **shipped 2026-09-17** (pending PR; not deployed) | A row the reviewer settled OFF the card system (`settled_off_card`, stamped once on the row's card resolution) leaves `needs_person` on both counts that ask it (`summary.n_needs_person` and `card_review.n_needs_person`), is not offered the private-card button, and its review line reads `needs_entity_settled_outside`, asking for the entity instead of for a paying card that will never exist. `needs_entity` stays and nothing is auto-filled: the row carries no bill-to field, so the tool does not know the company. Known live effect is July's Tricarico invoice; no local fixture records dispositions, so the month-wide count needs a live `summary.n_settled_outside` read |
| Eight Lovable prompts the owner published 2026-09-17 night (items 101, 111, 138, 139, 140, 141, 142 and note #61) | **applied + verified 2026-09-17** (bundle 46 chunks / 1,242 KB after two publishes in one evening; seven of the eight driven cold from the login gate in EN and PT with 0 writes; account-picks removal is bundle-only, its entity-editor section never mounted under the drive script) | `PROMPT-STATUS.md` Applied rows; backlog items 101 / 111 / 138 / 139 / 140 / 141 / 142 and note #61 |
| Voids-audit defects 101 / 96 + 97 / 131 + 133 (bulk half) / 112 + 111; prompts for owner notes #65, #66, #68, #69 | **shipped + deployed 2026-09-17** (PRs #1032 v161, #1035 v163, #1036 v164, #1045 v166; prompts #1040). 101: Confirm all matched uses the owner's pairing rule (`confirmable_pair`), `summary.n_confirm_matched` (live July 0, was 27 booked rows; August 1). 96/97: reconciliation PDF moves booked charges out of the to-do (July 72 -> 24), captions use the screen's words, Why column; month report captions from one pass. 131: a model verdict AT 0.20 unbinds unless the rate is clean (below 0.20 unchanged); 133: Confirm all Ready also needs the pairing rule, matcher rule (b) proposed only. 112: an arrival re-matches the neighbouring month whose statement period covers it (item 113 mark). 111: a receipt settling a charge takes its card's company and person on the Expenses page, CSV and month report (live July 19 rows, box 14). SPA prompts NOT applied: `lovable-bulk-confirm-dialog`, `lovable-entity-from-charge`, `lovable-private-card-in-picker`, `lovable-one-download-each`, `lovable-booked-hint-names-workbook`. |
| Voids-audit defect 113 (a failed or interrupted re-match left the month stale) | **shipped + deployed 2026-09-17** (PR #1026, Fly v159). Months owing a re-match carry `rematch_pending`, retried by the next arrival and at boot; `/api/operator/state` lists them, the notifier mails failures. No SPA surface | backlog item 113 |
| Voids-audit defect 106 (a mailed forward that created no expense) | **shipped + deployed 2026-09-17** (PR #1017, Fly v157). Intake row reads "Nothing added: ...", `not_added` per file + `n_no_expense`, the sender's acknowledgement says no expense was added and why; live: the 3 old forwards read "Nothing added" on the published page. SPA styling prompt `lovable-no-expense-mail-prompt.md` not pasted | backlog item 106 |
| Private card option + voids-audit defects 94 / 95 / 99 / 100 / 116 | **shipped + deployed 2026-09-17** (PRs #987 v149, #993 v151, #997 + #998 v152, #1009 v155). Private card: `can_mark_private`, company card OR private card (400 `company_card` / `private_card`), SPA applied. 95: gated rows keep their category in the export. 116: sign-off keeps merchant keys. 99: `month_complete` beside `ready_to_post` (July 24 need a receipt / 11 need a charge; August 8 / 100 / 10 / 1 guessed). 100: publish refused unless complete or `override`, `published_by` recorded, classic runs refused. 94: decided copies leave every count and total (August 20 expenses, USD 2,033.86 / EUR 668.00). SPA prompts for 99/100 and 94 pending paste. Open ruling: gray 'já no recurring' charges block July | backlog items 94, 95, 99, 100, 116; PROMPT-STATUS |
| Voids-audit defects 117 + 121 (one-word merchant aliases; held-mail alerts) | **shipped + deployed 2026-09-17** (PRs #1003 / #1001, Fly v153). 117: generic-word aliases ignored and refused when new, fuzzy hits need the merchant's lead word; replay 0 of 133 live receipts changed. 121: a held mail's sender (ours only) gets one notice, the operator alert is unchanged; no settings write. Also 2026-09-17 on owner yes: the three legacy July test runs deleted. Items 94 / 95 / 99 / 100 / 116 shipped by a parallel session the same day | backlog items 117, 121, 100; api-contract "A generic word is not a merchant alias" |
| Settings page in seven tabs; a settings save says what it wrote | **shipped + deployed + SPA applied 2026-09-17** (item 91, PRs #969/#972; the PUT refuses an unknown key and answers `applied[]`/`ignored[]`; published bundle audited and driven cold in Chrome) | backlog item 91; api-contract "What a settings save wrote"; `docs/lovable-settings-tabs-prompt.md` |
| 2026-09-17 feedback notes #54-#60 + item 78 | **backend shipped + deployed 2026-09-17** (PRs #979/#982): two-digit card endings name a card only when one card ends in them (#60, row field `card_ending`; no live row moved, the one instance was hand-fixed); every receipt arrival refreshes the month's card copy from Settings (#54 / audit 108); clearing a category pinned (item 78). SPA prompt `docs/lovable-feedback-0917-prompt.md` published by the owner, NOT yet bundle-verified. **Open:** notes #52, #53, #62, #63; #61 parked on item 23; June 2026 has 3 rows printing a defined card number with no card until its card copy is refreshed (owner/Criss action; September self-heals on its next arrival) | api-contract (three 2026-09-17 sections); PROMPT-STATUS Not-applied row |
| 2026-09-17 voids audit: 40 findings appended to the backlog as items 94-133, unranked; eight load-bearing functions recorded as protect-lines | **recorded 2026-09-17**, nothing built by the audit (owner ruling: no feature shipping before the October licence); draft #108 (arrivals read today's card list) shipped separately in PR #979; owner ranks the block next | backlog items 94-133 + the 27-line set-aside list beneath them; memory `project_brisken_expense_recon_voids_audit`; report delivered in chat 2026-09-17 |
| Untrusted inbound: mail and documents are data (ECC item 6, `rule_untrusted_inbound` code half) | **shipped + deployed 2026-09-17** (PR #973, Fly v147) | The extraction prompt carries a system rule, and file name + PDF text go inside nonce-fenced data blocks (the fingerprinted instruction text is unchanged, so the extraction cache stays valid). A deterministic regex scan (never a model) over document text, file name, mail subject and body raises `expenses[].untrusted_instructions` plus a `check` review (`reason_code: untrusted_instructions`), ranked after the fixable per-row checks so it never hides a missing amount. Mail content sets no recipient, reply, ack, rule or fetch; injected mail is not acked; outsiders stay un-acked. Flags are stamped at ingest and only READ at view time, so existing receipts do not re-flag. Live after deploy: a read-only census of all 6 batches (131 rows) shows the key on every row (0 before), 0 flagged, and review reason/state counts identical to the pre-deploy baseline; the live SPA renders September's 40 rows with no crash report or fallback. The flagged-row render was driven on the published SPA against a local backend on the merged code, seeded through the real intake path (no live-month write). Open: PT copy for the reason, backlog item 93 |
| Item 90 step 4: the two Settings FX rates | **offered 2026-09-17 evening and declined by the owner; do not re-ask** | Read-only round, nothing written to Settings. Offered with the scope of effects and the drift measured off the ECB table stored in the August month: typed EUR:USD 1.162275 against the ECB's July average 1.141748 is +1.80%, BRL:USD -1.48%; August +0.26% and -0.92%. Owner chose to leave both rates, so the 2% ECB band stays shipped and unreached on live months. Two corrections to item 90's own text, both from a read-only DB copy: July and August hold frozen rates in `runs.config` but **September, May, June and January hold none**, so those four (not only future months) take today's typed rates on their next statement attach, `apply_master_data` folding Settings in with `setdefault` at creation and attach only; and item 132's drift advisory reads zero on both months because each stored summary predates v167 (July re-matched 16:59:25Z, August 11:35:23Z, v167 shipped 17:28:24Z), so it appears on the next re-match with no action needed |
| Zoho decoupling: the app stops SAYING it is connected (item 23 layers 2-4) | **rounds 1 / 3 / 6a shipped + deployed 2026-09-15** (PR #881, Fly deploy verified on live data); seven rounds remain, ordered in backlog item 23 (PR #883) | Owner asked a second time what the app has to do with Zoho (first was in-app note #27, 2026-08-21), then ruled "rename or delete whatever is necessary to make the app better". The open product question in item 23 is ANSWERED WITH EVIDENCE: 64 of 341 account cells on the live July workbook carry a real GL code the category alone does not give (`Meals & Entertainment` -> `E100010-31 - Travel Expense | Food`), 236 are the fallback echo, 41 blank, so the account layer STAYS and is renamed; the journal export emits 3 of July's 26 reconciled charges with every account line a placeholder, so THAT is the deletion. Shipped: `output/posting_common.py` lifts the nine helpers two surviving modules import out of `zoho_export.py` (so the journal can die without its callers), and every backend-owned human string moves to one glossary - **Account** in the report PDFs (already there), **Posting account** where a distinction is needed, **Expense report category** only for the source document's own category. Live after deploy: the workbook reads `Posting account` on three sheets and `already posted (yellow row)`, the download is `expenses-<id>.csv`, and a COLD browser drive through the login gate shows zero Zoho on the workbench. Kept deliberately: "Zoho Expense report PDF" (the real name of a file format Criss receives; the backend error must match the SPA dropdown) and `cfg["zoho"]` (persisted in `runs.config` and every `run.local.json` on the volume, so it needs a read-time fallback, round 9). The writeback column into HER workbook is renamed and the lookup accepts the old header PERMANENTLY, or a workbook she has already had written back grows a second account column beside the stale one. NOT yet done and load-bearing: the journal deletion cannot ship backend-only, because the published SPA calls `/runs/{id}/zoho.csv` from two lazy chunks; and `zoho_account` is WRITTEN by the settings and memory editors, so it needs a parallel-field migration, not a rename. Verification: suite 1832/2, the 13 initial failures were tests asserting the old literals (moved, not loosened), `regress_check` on the dual-read reported TEST BITES | backlog item 23 (the ordered plan, the three round-7 silent-failure traps, and the decisions not to migrate the two SQLite columns or rename `has_coa`) |
| Report totals in `Decimal`, and an unreadable amount captioned + counted instead of dropped (backlog item 65, from item 49 / storage-description section 12 row 13) | **shipped** 2026-09-15 | Both PDF total sites and the new `output/_pdf_common.py` money helpers; `summary.n_amounts_unreadable` on the batch payload. Live check first: both months' printed totals already equalled a Decimal sum to the cent with zero unreadable rows, so what shipped is the arithmetic class plus defence in depth on a public builder. Backlog Shipped row 35; SPA half `docs/lovable-amounts-unreadable-prompt.md` (Pending) |
| 2026-09-06 owner program: company months vs trips (declared, both reconcile), mail materializes the month, person attribution via the card, unknown payment methods suggest a private expense | **CLOSED 2026-09-07**: all four rounds merged in order (#686/#687/#688/#685) and deployed (v103-v106); the R2 flip is LIVE (dismissals landed, flag ON 2026-09-07, #695); all four Lovable prompts applied + bundle-verified 2026-09-07; the travel alias is SET (`travel`) and the TEST- alias drill PASSED 2026-09-07 (see the R3 row); the owner-side remainder is DATA entry (person per card, 0340/3645/1672 identities - item 26/40), of which the ENTITY half landed 2026-09-08 from the owner's ALL BANKS list: registry 6 cards -> 15, Consulting charted, unresolved rows across the three open batches 30/40 -> 28/40; persons and the remaining identities are still owner-side - see the loop brief's post-program ranking | backlog items 38-42 + the re-ranked loop brief (PRs #672 / #674 + the 2026-09-06 capture PR: item 42 refusals split, R2 rollout protocol, person-timing rule, ride-along assignments). Supersedes the 2026-08-24 "pre-creating months intrudes" ruling (noted in place) |
| R3 of the program: trip entity + declared batch_type + travel-alias routing (item 38 surface half) | **shipped + deployed** (PR #688 merged third per program order, Fly v105 2026-09-07; healthz + `/api/trips` `{"trips": []}` + `n_pooled_travel: 0` verified live, SPA months + inbound driven clean). §5 bundle-verified 2026-09-07 (settings chunk round-trips `travel_alias` in the intake save); alias SET to `travel` the same day (owner pick, operator-API write, round-trip verified, person aliases untouched) and the full TEST- alias drill PASSED live: mailed a TEST- receipt to travel@expenses.brisken.com via the MX, rested as `pool_kind: travel` / "Travel, waiting for its trip" WITHOUT minting a month (auto-materialize ON), trip suggestion named the one TEST- trip, join-trip materialized the trip batch (`created_batch: true`, batch_type `trip`, vision-read 23.50 EUR taxi receipt, absent from the months screen), batch delete pooled the mail back (`pooled_back: 1`), fixtures removed to zero (0 trips, 3 months, pool 0, drill archive dismissed). Travel routing is LIVE | Trips CRUD at `/api/trips` (batch materializes on first join, create-with-receipt, one batch per trip under a creation slot); `batch_type` declared at creation, absent = company, month routing structurally blind to trips; travel mail rests with an exactly-one-trip suggestion, joining is a click (`POST /api/inbound/{archive}/join-trip`); roster-mismatch flag on trip rows via R1's person field; rebase interplay pinned (item 39's materializer never touches travel mail). Adversarial review: 10 findings fixed pre-PR. Final merged tree: suite 1478/2, calibrate OK, eight RED-proofs. `docs/lovable-trips-prompt.md` applied + bundle-verified 2026-09-07 |
| R4 of the program: cross-batch settlement + trip report (item 38 deep half) | **shipped + deployed** (PR #685 merged LAST per program order, Fly v106 2026-09-07; live TEST- drill: a trip receipt settled a month statement charge, a second month could NOT re-settle it, trip report rendered with title/range/roster/person section, Playwright drove the published workbench showing the settled row at 99%; fixtures removed to zero, live state 0 batches / 0 trips) | receipt_claims registry (PK receipt_run_id+document_id) makes one-receipt-one-charge global: advisory read excludes foreign-settled receipts, commit-lock re-check downgrades raced pairings, UNIQUE backstop, reviewer sync (reject releases, re-pick moves, cross-run steal 409s), cascades on delete. trip_pool_for_month spans a month's pool over date-overlapping trips (private-confirmed / claimed / id-colliding excluded); borrowed receipts ride the snapshot as copies only; trip create + add re-match overlapping months. settled_by provenance both ways (absent-unless-set). Trip report sections per person (roster order, off-roster flagged, continuous numbering). Load-bearing pin held: zero trips = machinery inert (stubbed-vs-live byte-compare). Suite 1496/2, six RED-proofs. docs/lovable-r4-settled-by-prompt.md applied + bundle-verified 2026-09-07 |
| Receipt entry decoupled from month creation: the Receipts drop page | **shipped + deployed + consumer-driven** (2026-09-08 owner directive; backlog item 44. PR #709 merged + Fly-deployed; consumer drive PASSED 2026-09-08: TEST- drop materialized June 2026 (`created_by: drop`, `month_source: receipt`), published SPA driven COLD via agent-browser (login gate through to the months list, real "June 2026" label + 1 receipt, batch page rendered 2026-06-11 / TEST-DRILL CAFE / 18.90 EUR, no fallback strings), fixture deleted, live state back to 3 real months / pool 0. Browser tooling healed by `agent-browser install` (fresh Chrome 152) after the 2026-09-08 dual outage) | `POST /api/receipts` is the ONE manual entrance: each dropped FILE routes on its own dates through the same `resolve_receipt_month` brain mail uses, materializing absent months (`created_by: "drop"`, unconditional — the flag gates a STRANGER's mail, not an operator's own drop), then claiming their pooled mail. A file with no readable date RESTS as `needs_month` rather than falling to the arrival month (a silent guess is what item 29's routing rebuild removed); the release valve is an explicit `month` override, believed like a typed date. Zips refused, one file per receipt. `POST /api/expense-batches` now 400s files on a company month and creates it EMPTY — the container a statement lands in (January: 78 of 80 charges had no receipt), with the empty refusal surviving as opt-in `allow_empty` that never sanctions an all-rejected upload (the mail materializer's floor). Trip creates unchanged. The per-file ledger rides a new `jobs.result` column, read from `GET /jobs/{id}`. 31 test modules' create fixtures converted to create-empty-then-add; suite 1497. Same day (item 45): per-ingest cap raised 80 → 500 and a >cap month group now ledgers its overflow `rejected`/`upload-cap` BEFORE the ingest call (pre-fix the tail was silently skipped while rows read `filed`); both mutation-proven. SPA v1 (drop page, badge, empty state) APPLIED by owner mid-day 2026-09-08, bundle-verified same evening (chunk-receipts reads n_filed/n_needs_month; /api/receipts + "Recibos"/"Dos recibos" in i18n). Pending: the DELTA in the reworked `docs/lovable-receipts-drop-prompt.md` — cap copy + chunking, item-46 create-UI removal, nav redesign, Open-month release (after publish: bundle re-audit + browser drive, update PROMPT-STATUS.md) |
| Cost centers: expenses attributed to projects and purposes | **BUILD STARTED 2026-09-10, steps 1-5 of 6 shipped; backend COMPLETE, Lovable prompt written** (owner order "start building"). `src/expense_recon/cost_centers.py`: the owner-authored registry (`settings["cost_centers"]` = {name: {kind, note, active}}, whole-map replace at the settings edge like merchants/cards/entities), the resolution chain override > trip > merchant > card, and the settings wiring (SETTINGS_DEFAULTS key, PUT validation, derived read-only `cost_center_options` on GET). 24 tests across `test_cost_centers.py` (registry) and `test_cost_centers_api.py` (through the API, so the WIRING is pinned and not only the helper); suite 1511 -> 1535, ruff clean. Both mutations proven to bite: disabling the empty-registry short-circuit reddens the two contract tests, disabling the PUT branch reddens the API suite. Two judgment calls beyond the design: a name differing only in case is refused at the edge rather than silently overwriting, and an INACTIVE centre still accepts a reviewer override while refusing to be applied by a default. **Step 2 shipped 2026-09-11**: the three CARRIERS (card `default_cost_center`, merchant-registry `cost_center`, and a real `cost_center` column on trips with the idempotent ALTER the deployed volume needs) plus the RESOLUTION onto the row. `resolve_batch_row_cost_centers` runs the chain over the same card pass `person` uses; every row carries `cost_center`, `cost_center_source`, `cost_center_source_label`, `needs_cost_center`; `n_needs_cost_center` sits beside `n_needs_person` and `cost_center_options` beside `entity_options`. The override joins `EXPENSE_HEADER_FIELDS`, so it rides the existing field-override path into `edited_fields` with no second mechanism. 29 further tests (`test_cost_center_carriers.py`, `test_cost_center_resolution.py`), all through the settings PUT / the trips routes / `GET /api/expense-batches/{id}`; suite 1535 -> 1564, ruff clean. Regress-checked at six wiring points, each green->red->green. Three decisions worth carrying: (a) the empty-registry contract now has exactly ONE home -- the first draft guarded it in both `registry.resolve` and the service, which made neither load-bearing, so the service now guards only the expensive merchant sweep; (b) a name is validated ONLY at the row override (400 on an undefined name, stored in the registry's own spelling), never at the three carriers, because their screens are edited independently and the edit ORDER must not matter; (c) both registries are read LIVE from settings rather than the batch config snapshot, so the day the owner defines the first cost center the existing months resolve without a refresh pass (the card default is the exception: it rides the card snapshot like `person`). Re-pinned in the same round per rule 4: `test_view_contract.py` (whose fixture now DEFINES a cost center, so `cost_center_options[]` is observed FILLED -- pinning it against an empty list would have pinned the path and not the element kind) and `docs/api-contract.md`. **The step-2d question is answered by the spec itself and needed no owner call**: the prior session flagged that the design never says how a trip carries a cost center, but item 47's API-contract section states it outright ("trip object gains `cost_center`; POST / PUT /api/trips accept it"), so a real column was built rather than the same-name interpretation. **Step 3 shipped 2026-09-15**: the month report grouped by cost center. `build_expense_report` partitions a COMPANY month's listing on the row's resolved cost center through the same `sections` slices item 38 built for per-person trip reports (the builder's section dict gained a generic `caption` / `label` pair beside item 38's `person` / `on_roster`, plus a `sections_heading` / `sections_note` rendered once above the partition). Named centres in name order, captioned `Name (kind)` with their own sums; unassigned rows form the FINAL section, captioned as unassigned, never hidden; the stated limit rides the note as `COST_CENTER_SCOPE_NOTE` in `cost_centers.py`, one constant for both surfaces so the totals route (step 5) carries the identical wording. The route hands the report the LIVE settings, as the grid reads them. The report does NOT re-check the registry: it partitions only when the chain resolves or flags a row, so a month with no cost center defined stays the flat listing it was and that decision derives from the contract's single home in `CostCenterRegistry.resolve`; a trip report is unchanged whatever the registry holds. 5 route-level tests (`test_cost_center_report.py`, every one through `GET /runs/{id}/expense-report.pdf`) plus one builder test; suite 1564 -> 1570, ruff clean. Regress-checked at three wiring points, each green -> red -> green: the partition gate in `build_expense_report`, the empty-registry short-circuit in `resolve` (which reddens the flat-listing test THROUGH the report, proving the report's silence is derived and not duplicated), and the route's `settings=` argument. One test was dropped rather than faked: a split receipt across two sections needs mock classification responses at the API level, which no existing test wires; slice alignment is pinned at builder level instead. **Step 5 shipped 2026-09-15**: `GET /api/cost-centers/totals?from=&to=`, the only surface that aggregates ACROSS batches. `build_cost_center_totals` scans every expense batch (months and trips), takes the export's own rows (`_expense_export_inputs`), resolves each through the same chain the grid and the report run, leaves confirmed private rows out (reimbursements owed, not company spend), and emits per centre `{name, kind, active, n_rows, n_batches, totals}` name-sorted plus an explicit `unassigned` bucket, `n_batches` (scanned), `n_rows`, `n_undated` (a row with no date cannot be excluded by a range, so it always counts and is declared), and `note` = `COST_CENTER_SCOPE_NOTE` verbatim. Every ACTIVE centre is listed at zero; an inactive one only while history sits on it. Range inclusive on the row's edited date; malformed or inverted range is a 400 with prose. With no cost center defined the list is empty and everything is unassigned: the roll-up states the fact, the row flag stays silent. Also closed the step-2 gap the Lovable prompt exposed: refresh-master-data now emits `row_cost_centers` (item 40's `row_persons` contract) counting rows whose RESOLVED cost center the card-default refresh moved, computed with the batch's real trip on both sides. 7 route-level tests (`test_cost_center_totals.py`) + 1 refresh test in `test_cost_center_resolution.py`; suite 1570 -> 1578, ruff clean. Regress-checked green -> red -> green at four points: the route's range arguments, the private-row exclusion (8-space anchor, totals only), the trip's cost center into the roll-up, and the refresh count. Contract re-pinned in `docs/api-contract.md` (totals subsection + the carriers bullet). **Step 6 written, not applied**: the Lovable half is promoted to `docs/lovable-cost-centers-prompt.md` (registered in PROMPT-STATUS as not applied; §1 Settings > Cost centers and §2 the card field ship FIRST because both maps are whole-map replace and a stale bundle erases them on save; nobody types a cost center until the published bundle proves the round-trip). On `client/brisken/p1-cost-centers` in worktree `C:/Users/neuma_p1qrsic/Repo/agentic-ops1-cc`, PUSHED, no PR yet: the backend is complete, so the next move is the PR (CI-gated auto-merge) and then the Fly deploy, which is what gates the prompt paste | **design answered** (2026-09-08 owner directive; backlog item 47). Four decisions taken in an owner round: the cost-center list is OWNER-AUTHORED only (`settings["cost_centers"]`, whole-map replace like `cards`/`merchants`; the tool never invents or learns a NAME) — decided against the recommended grow-while-reviewing option, so the empty-registry case is load-bearing: an empty list must resolve nothing AND flag nothing, or the field ships and every row in every month reads `needs_cost_center`. Resolution is override > trip > learned merchant > card `default_cost_center` > unresolved; person is deliberately NOT a resolver (it cannot separate Dirk's own "Nicolas in Brazil" from "Nicolas on Lidar") and only ranks the picker, and category is not one either (co-varying the two dimensions destroys the point of cutting the money a second way). v1 refuses splits, with per-line-item (the `books_as` fan-out) as the v2 shape if a real shared cost appears. Surface ships in two steps: month-report listing grouped by cost center first (nearly free — `build_expense_report_pdf` already takes the `sections` partition item 38 built for per-person trip reports), then a cross-month `GET /api/cost-centers/totals` (the only genuinely new surface: all 57 routes enumerated, nothing aggregates across batches). Stated limit carried on both surfaces: this tool sees card and receipt spend only, never contractor invoices or salaries, so a cost-center figure is not a total project cost. All API fields PARALLEL (rule 1) with a `cost_center_source_label` beside the enum per rule 5. Lovable half DRAFTED inside item 47, deliberately not in `docs/lovable-*.md` (PROMPT-STATUS audits that folder as pasteable and every field is unbuilt) | backlog item 47 (the answered design, API-contract implications and the drafted Lovable half all live in that block) |
| Statement ingest (CSV + XLSX) | done | BLUEPRINT (ingest) |
| Transaction identity (content-derived, stable under append) | done (PR 2a of the living month) | backlog item 29; `ingest/_common.assign_content_ids` |
| Extraction baseline survives the bake | live (PR #628, deployed Fly v94 2026-08-25) | backlog item 29 / PR 2b-1b. `rematch_month` committed the BAKED receipt pool as the snapshot, but the snapshot is also the audit baseline: `raw` echoed the reviewer's own edit, clearing an edit could not revert (the OCR value was gone), and the learning harvest keyed corrections on the corrected vendor instead of the original, so an attached month taught the merchant book nothing. Fixed by `extracted_receipts`, a parallel snapshot key, first-write-wins PER document; grid / export / learning / batch-list counts compose from it, matching and the reports keep the baked pool. Pre-2b runs fall back to what they had; nothing migrates. `service.baseline_receipts` |
| The month stays open once its statement is loaded | live (PR #629, deployed Fly v95 2026-08-25) | backlog item 29 / PR 2b-2a. The guard was TWO layers: nine ROUTES through `_mutable_expense_run_or_error` plus five refusals inside the service functions beneath them, so lifting the routes alone would have moved the 400 one layer down. Receipts, set-aside restore, card assignment and master-data refresh are now open all month, each followed by `service.rematch_after_change` (outside the caller's lock — `_BATCH_ADD_LOCK` is not reentrant), skipped when nothing was added. A re-match failure is REPORTED not raised, so an OpenAI outage cannot mark a receipt that landed as `held_failed`. Pool: a statement-bearing month claims its mail and reports `pool_month_state: "reconciling"` (was `closed`); `status_label` already carries the prose, so no SPA change. STILL CLOSED, each pinned by a test: a second statement upload, and the four expense-edit overlay routes |
| Statement uploads fold in by identity | live (PR #632, deployed Fly v96 2026-08-25) | backlog item 29 / PR 2b-2b-1. The parse / content-id / dedupe foundation under gradual statement uploads, landed on its own and proven neutral before the `statements[]` surface goes on top. `ingest/_common.merge_transactions` folds a parsed statement into a month's charges by identity, keyed on the content id PR 2a made stable, adding no second definition of sameness; the occurrence suffix keeps two identical coffees apart ACROSS uploads, not only within a parse. First-write-wins (a re-supplied row keeps the object the month committed, so decisions and `source_row` stay pointed at it), `existing` passes through untouched, and a sign contradiction lands as two rows. Beside it `read_statement_upload` splits the statement half out of `execute_statement_attach` and `month_transactions` reads the charge block alone. NEUTRAL BY CONSTRUCTION and the tests say so: `prepare_statement_attach` still refuses a second upload, so on the attach path the fold IS the identity function and no test can prove it wired. What bit, by mutation: disabling the dedupe reddens 7, stubbing the extracted read reddens 6. Suite 1325 -> 1336 |
| A month takes several statements (per card, partial then cycle) | merged, not yet deployed | backlog item 29 / PR 2b-2b-2. `POST .../statement` is append-capable and repeatable; the refusal came out of BOTH layers (route gate + `prepare_statement_attach`), and the two tests that pinned the closed door now pin the open one. The fold stops being inert. Each upload is recorded in `statements[]` (parallel field on both payloads, written by `rematch_month` inside the commit lock). THE ANCHOR IS PER UPLOAD, NOT PER CHARGE: a charge sits in every file that prints it, at a different row in each, so `statement_anchors` ({file: {tid: row}}, snapshot-only) is what the sheet writeback places rows from. Built the planned per-charge `source_file` way first and reverted it in review: it would have annotated the closing cycle (the workbook Criss works from, and the default download) only for the charges the cycle introduced. Also from review: a same-name second upload overwrote the first on disk, and an empty anchor map read as no map. The account-id / sign-contradiction hazards are SURFACED as an `advisory`, never deduped, decided once for both. `rematch_month` now refuses any commit that would drop a charge the month gained meanwhile (replaces `require_no_statement`, covers the receipt re-match too). `?file=` selects which statement to write back. Suite 1336 -> 1352, nine mutations, every guard red under its own |
| The same purchase twice, marked where it is read | live | Detection has run since Tier-1 #4 and works; it just never reached a screen. `duplicate_groups` is a side list of ids, so the grid showed both copies of the Pressmaster FZCO invoice (135.00 USD, two files) with nothing to tell them apart. Now every member of a live group carries `duplicate` on its own row, on the grid and on all three workbench surfaces, naming the group, its place in it, and the first copy it repeats; the hand-match picker is included because it holds every receipt and is the one place both copies could be assigned to two charges. `summary.n_duplicate_copies` says how many copies are redundant. A dismissal clears the marker from rows, count and document; `confirmed` keeps it, because acknowledging a duplicate is not removing it. Totals still sum every row: the tool flags, the reviewer deletes. The reconciliation PDF names the groups instead of counting them. Fixed alongside: `POST /duplicates/resolve` always answered with the workbench summary, so an expense grid got a reply missing its own header fields. Suite 1375 -> 1392, seven mutations each proven red. Live on the April batch: both Pressmaster rows marked, `n_duplicate_copies` 1; three of the six live months carry a duplicate nobody had seen. SPA half `docs/lovable-duplicates-prompt.md` (owner applies; until then the flag shows in the reconciliation PDF, not on the grid) |
| Which cards a month has loaded, and how far each has got | live | backlog item 29 / PR 3, the coverage surface. `coverage[]` on BOTH review payloads: one row per card the month knows about, carrying the uploads that covered it, the span of ITS charges, the run summary's own four bucket counts over its charges alone, and its share of the unreconciled money. Registry cards with nothing loaded get a row on purpose: "which cards have I not loaded yet" is unanswerable from a list of the ones she has, and on the live January month two of the three cards charged are ones the registry has never met. `charge_states` is now the ONE place a charge's effective bucket is decided, so the grid, the workbench rows and the summary counters cannot report a month at two different stages of done; the grid pays for its own `snapshot_from_dict` + `apply_decisions` to answer with the reviewer's decisions applied. The reconciliation document gains a coverage table and sections its charge listing per card, grouped on `rows[].coverage_key` so a section and the table cannot disagree. Review found both defects, both in card identity: the unknown-card key is now namespaced (`digits:`) because a card's operator-chosen SLUG can be digits that are not its own, and resolution now runs before the digit check so an alias-only name ("CorpServ") resolves instead of falling to the no-card row. Eleven mutations, every guard red under its own. Suite 1352 -> 1375. SPA half `docs/lovable-coverage-prompt.md` (owner applies; until then a month with two workbooks still offers one download button) |
| Receipt OCR (vision + PDF text) | done | BLUEPRINT slice 2 |
| LLM categorizer (gpt-4o-mini, OpenAI Brisken key) | live | BLUEPRINT "Provider Pivot" |
| Deterministic matcher | done | BLUEPRINT slice 3 |
| Date+amount accuracy program (scorer + structure + tuning) | live (PRs #404/#405/#406, deployed 2026-07-23) | Owner directive "date+time+amount, most accurate": time verified nonexistent in all sources → date+amount. Pinned scorer `recon-match-accuracy` (95 labelled pairs, 2024 holdout guard) + FX_BASE_AMOUNT path + self-derived monthly rates + band-scoring fix + review-zone deferral + bilateral-uniqueness gate + tuned `fx_base_amount_match_pct=0.01`. Train composite 8.9→31.5, determ-correct 3→55/95, **0 wrong** at every step; live April re-run (no LLM) 20 clean + 13 teed-up review, byte-identical to local replay. Journal: `docs/optimize/brisken-recon-tuning-v1/`; ANNEALING 2026-07-23 entry |
| Card as a matching signal (WS3) | live (PR #317, deployed 2026-07-22) | `Transaction.card_last4` + optional `card` column map (CSV/xlsx + hosted guess); card-scoped candidates now key on the CHARGE's card, not the account id. On the real 01-05 month software-vs-Food FX-false-pairs 4 -> 0. Also: `Match.card_score` (tie-break + workbench), card into the FX-judgment prompt, optional `matching.llm_second_pass_unmatched` (OFF) |
| Excel statements canonicalize the sign too; statement re-read repairs a month in place | **shipped 2026-09-11** (backlog item 55) | `ingest/statement_xlsx.py` mirrors the CSV 3.15 paths; `inspect.guess_column_map` maps `Type`; `match_month` treats an empty receipt entity as unscoped; `POST .../statements/reread` + `service.reread_statements`. Tests: `test_statement_reread.py` (route-level, three regressions of the real source proven RED first) |
| A broken month is never "ready to post"; every re-match is announced | **shipped 2026-09-11 evening** (backlog items 57 + 58, void-list round 1) | `web/month_health.py`: `summary.month_health` on BOTH payloads (`state` broken when the matcher proposed nothing while exact same-day same-amount pairs sit in the pool; `suspects` names sign / currency / entity / card from the matcher's own scoping rules); `ready_to_post` = no undecided AND health ok. `rematch_log` in the snapshot (one event per `rematch_month` commit, trigger + counts, capped 50) -> `rematches[]` on `/api/operator/state` -> `brisken-recon-notify.py` mails one line per event. Tests: `test_month_health.py`, `test_rematch_log.py`, `tools/tests/test_recon_notify_diff.py`; three regress proofs RED first. SPA half: `docs/lovable-month-health-prompt.md` (owner applies) |
| A charge's entity comes from its card; an invoice and its receipt are one candidate | **shipped 2026-09-14** (backlog items 59 + 56, void-list round 2, both owner-ruled) | `service.stamp_charge_entities` (per-row entity via `resolve_card`, blank for a card the registry cannot name) + `summary.n_charges_no_entity`; `duplicates.collapsed_duplicate_copies` keeps every copy after the first out of the matcher pool unless the group is ruled `ignore`, and `POST /api/runs/{id}/duplicates/resolve` re-matches a reconciling month. Tests: `test_charge_entity.py`, `test_duplicate_collapse.py`; five regress proofs RED first. SPA half: `docs/lovable-charge-entity-prompt.md` (APPLIED 2026-09-15, bundle-audited) |
| A candidate another charge holds says so | **shipped 2026-09-15** (backlog item 60) | `rows[].candidates[].held_by` (parallel, absent when uncontested) + `summary.n_charges_receipt_taken`, both from the EFFECTIVE verdict so a reassignment moves them. Tests: `test_receipt_taken.py` (3, route-level via `POST /manual-match`); two regress proofs RED first. SPA half: `docs/lovable-receipt-taken-prompt.md` (owner applies) |
| A receipt settled outside the card | **shipped 2026-09-15** (backlog item 62) | `POST`/`DELETE /api/runs/{id}/receipts/{doc}/settled-outside`; the run payload drops it from `unmatched_receipts`, `n_unmatched_rec` and month health's pair scan and counts `summary.n_settled_outside`, the grid keeps the row with `settled_outside {how, note, at}`, and the month report still prints it behind a tender caption (owner ruling). `unmatched_receipts[].suggested_settled_outside` suggests, never applies; live-read caveat: all three named July invoices carry an EMPTY `payment_mode`, so it fires on none of them today. Tests: `test_settled_outside.py` (15, route-level through both payloads); four regress proofs RED first. SPA half: `docs/lovable-settled-outside-prompt.md` (owner applies) |
| The parallel round: items 61-68 + 16 + 17, ten sessions at once | **open 2026-09-15** (setup #824) | Protocol `automations/expense-reconciliation/docs/PARALLEL-ROUND-PROTOCOL.md`: own worktree per item, append-only edits to the shared files, merge-not-rebase, probe the live API before deploying (a sibling's release carries your merge), own `agent-browser --session`, Lovable prompt as text, checkpoint on a fresh `docs/` worktree, cleanup ending in three empty outputs. Items 65-68 are the four standalone defects carved out of item 49. CI does not run the module suite; each PR carries its local count and regress line |
| Criss's "Recibo nao estar abrindo", diagnosed; and the flag underneath it fixed | **SPA half OPEN, backend shipped + deployed 2026-09-15** (backlog item 52, PR #860, Fly v128) | The cause is the SPA, not the API. On **Review expenses** (`/expenses/{batchId}`) every row's **View receipt** button is enabled, carries a click handler, and does nothing: driven live with `fetch`, `window.open`, `createObjectURL` and anchor clicks all instrumented and positively controlled, a click produced no request, no dialog, no iframe and no error, on 31 August rows and 32 September ones. The workbench has no way into a receipt at all (the UNMATCHED RECEIPTS Document column is plain text). The recorded lead is refuted: the published bundle references neither `receipt_image_available` nor `has_receipt_image`, and all 31 August / 51 July receipts serve 200. SPA half: `docs/lovable-view-receipt-prompt.md` (PROMPT-STATUS, not applied; verify by drive, not bundle grep). Backend half shipped in the same round: `service.receipt_image_file` is now the one resolver for `receipt_image_available`, gated as the image endpoint is, because the RUN payload had been deciding it from the document id's SHAPE and so called every mail-arrived and dropped receipt unavailable (all 17 unmatched on live August, each of which the endpoint served 200). Tests: `test_receipt_image_available.py` (5, four through the routes, the file-removed differential included); both wiring points regress-checked RED first |
| A statement that holds no charge is refused, on both the attach and the re-read | **shipped + deployed 2026-09-15** (backlog item 51, PR #859, Fly v128) | `service.statement_read_nothing` + two guards. `execute_statement_attach` refuses before the fold when the upload parsed no charge, so the month keeps its exact state: no charge, no `statements[]` entry, no `statement_anchors` entry, and no graduation to the workbench. That last one is what the drill had understated: `rematch_month` stamps `has_statement: True` on commit, so an empty file used to turn an open month into a reconciliation holding zero charges, which reads on screen like a month that reconciled. The reason rides the job's existing `error` status, the channel the re-read already refuses on, so the SPA needs nothing. Keyed on `n_rows` and never on `n_new`: the same file twice adds no NEW charge and still lands. `reread_statements` refuses the same shape, where it would REPLACE a live month's charges with nothing, exempting an entry already recorded as `n_rows: 0` so no legacy month is wedged (live probe: none of the 6 months holds one). Tests: `test_statement_zero_rows.py` (7, all through the routes); both wiring points regress-checked RED first. Contract section in `docs/api-contract.md` |
| A month's pool reaches the months either side of it | **shipped 2026-09-15** (backlog item 61) | `service.adjacent_pool_for_month` joins the trip pool inside `rematch_month`: neighbours are decided by LABEL (`month_from_label`, previous and next), eligibility by this statement's OWN period (`statement_period_for_month`, min..max of its charges; the label's calendar month widened by 3 days only where no statement exists). Borrowed copies ride the existing `borrowed_receipts` / `receipt_sources` keys with `kind: "adjacent"`, so the claims protocol is untouched and one receipt still settles one charge. `rows[].candidates[].from_batch` names where a borrowed candidate lives (new, parallel, absent on own-pool candidates, and it names a TRIP borrow too) + `summary.n_adjacent_borrowed`. Live evidence: August holds two 08-31 Google receipts (71.64 / 75.09) whose charges post 09-01, and its own 08-01 Google 71.64 charge has no candidate at all. Tests: `test_adjacent_month_pool.py` (6, four route-level); one regress proof RED first. SPA half: `docs/lovable-adjacent-month-prompt.md` (owner applies) |
| Sign canonicalization + refunds bucket + deterministic FX (Tier-1) | done (PR #285) | BLUEPRINT 3.15; no-LLM 0/36->29/36 on Criss's April |
| Dev notifier: operator "run now" uploads fire an email | **live + scheduled 2026-07-22** | `tools/brisken-recon-notify.py`. Was doubly broken: unscheduled AND dead since the v31 cutover (logged in via the deleted `POST /login`, and all 4 mail links pointed into the deleted HTML UI, including the publish ping that goes to the USER). PR #373 moves it to `/api/login` + bearer, handles the throttle's 429, and points links at `APP_URL` (the SPA). Registered as Windows task `BriskenReconNotify` (15-min repeat), `LastTaskResult 0` over two fires, 0 missed; state baselined so the first fire did not mail the backlog. NOTE: the task runs from the PRIMARY clone (it needs the gitignored `context/.env`), which currently carries the fix as an uncommitted local edit identical to main — `git checkout -- tools/brisken-recon-notify.py` before pulling there |
| Mail intake routing (the month pool) | live (PRs #599 + #601, deployed Fly v87 2026-08-24); **live 2026-09-06: ALL month batches deleted (post demo-test), 23 mails pooled (Aug 12 / Jul 5 / Sep 6), 7 more stranded "month deleted" awaiting per-archive re-ingest; the whole intake stream waits on a manual month-open (which itself needs a manual first-receipt upload — empty batch refused). Read-only audit: `%TEMP%/claude/recon-probe/manual_input_audit.py`; ranked findings in `docs/2026-09-06 - Expense-Recon Manual-Input Analysis/Checkpoint.md`** | Emailed receipts route by the month PRINTED on the receipt, not by whichever batch is open; mail whose month has no batch rests in the pool (`pooled`) and is claimed automatically on batch create / rename / boot / replay. Deleting a month pools its mail back. Arrival extraction warms the content-addressed cache, so the read costs nothing extra at batch time. `intake_mail.py` (`route_archived`, `claim_pooled`, `pool_deleted_batch`, `resolve_receipt_month`); SPA half `docs/lovable-month-pool-prompt.md` |
| Intake trust: who we recognise | live (this round, 2026-08-24); **the `known_senders` list is EMPTY on production — Dirk's `dirk_.neumann@icloud.com` still gets no ack until it is listed** | Backlog item 30 (a) + (c). Settings `intake.known_senders` names outside addresses that count as ours; `graph_notify.send_mail` takes an explicit per-call `allow_external` and asserts the structural recipient guard BEFORE consulting it. A known sender's body-only mail now renders ON ARRIVAL (all six mails held on 2026-08-24 delivered no file at all: a forwarded vendor receipt IS the body), reusing the operator render path unchanged; a render failure alerts, because nobody watches an automatic render. Strangers still hold — the mailbox takes mail from anyone and we do not pay a vision call per newsletter. Settings editor `docs/lovable-known-senders-prompt.md` (owner applies). Item 30 (b) shipped the same day: every refusal is written down (`inbound/refusals.jsonl`, DATA-stage guards included) and surfaced as `n_refused` + `refusals[]`, so "did anything bounce?" is answerable for the first time |
| Published SPA parity (audited 2026-08-24) | live at brisken-reconcile-dash.lovable.app; intake page is **`/inbound`**, not `/intakes` | Driven against the deployed API. LIVE: the two report-PDF buttons, body-only View/Add-as-PDF/Dismiss, month-pool sections 1/2/4/5/6 (the "Arriving" mislabel is GONE — the SPA uses its own status map, not `status_label`), intake quick-wins, READY tile, cards R3. NOT applied: month-pool section 0 (`status_kind`/`status_label`, the durability fix) and section 12 (refusals strip), `known_senders` editor, and the `open-intake` cleanup — **the stale "Accepted senders" editor is still live and the backend has ignored `intake.senders` since #587, so it silently discards input**. Unverified: re-ingest, issue codes. Audit trap: per-row actions are an icon-only `button[aria-label="Actions"]` menu, so a button-text scan reports none |
| Cross-run memory (Phase 2) | in-progress | BLUEPRINT Phase 2 |
| Jinja HTML UI retirement (API-only backend) | DONE — deployed Fly v31, 2026-07-22 | PR #350, redone ast-first per the SPA-cutover checkpoint manifest: 17 HTML routes + 14 bare decorator twins removed, `_wants_json` collapsed, `templates/` (12 files) + `static/` (4 files) deleted, role plumbing stripped (operator is the only role; gate keyed on `EXPENSE_RECON_OPERATOR_CODE`, the only code set on Fly). app.py 1518 -> 1071 lines; suite 772 -> 712 green. Deployed on owner order + live-verified (healthz 200; `/` + `/login` answer JSON 401, no HTML; SPA-origin CORS intact). Rollback = redeploy v30 via `flyctl releases` |
| Zoho journal CSV export | in-progress | BLUEPRINT slice 4 |
| Run history + doctor pre-flight | done | BLUEPRINT slice 5/5b |
| COA pre-write validation gate | live (Fly, per-entity) | BLUEPRINT 4.11 (PR #202/#203/#205) |
| Export idempotency (4.8) + direct Zoho posting (4b) | built 2026-07-28, **OFF by default** | Owner order: build shut-off, cross-reference-before-upload duplicate guarantee. `zoho/idempotent.py` PostLedger (write-ahead intent, content-hash conflicts, ambiguous→`--verify` against Zoho) + `expense-recon zoho-post` (posts the reviewed export CSV by Reference#, dry-run default, draft status). Gates: config `zoho.post.enabled` AND env `EXPENSE_RECON_ZOHO_POST=1` AND org allowlist AND `--go`; Fly has neither creds nor env → hosted path inert. Live use additionally needs per-action owner yes + Books journal-scope re-consent. ANNEALING resolved-2026-07-28 |
| Match ground-truth labeling (`label propose/accept/check`) | done | `labeling.py`; optimize-loop prep — labels per month-bundle in gitignored context |
| Label fixture: 6 production-shape bundles (CSV stmt + ER PDF) | done (2026-07-17), **re-validated TRUSTWORTHY 2026-07-22** | `context/.../csv/by-month/`: labels.csv per month, `label check` OK on all 6; 141/218 labeled (95 confirmed / 46 no_charge), 77 excluded. The 2026-07-22 37/95 scare was a measurement artifact (labels replayed against the WRONG statement source; against each bundle's own run.json it was 41/95, and the residual was the matcher's own FX bugs, since fixed by #405). Fixture is the scorer ground truth for `tools/scorers/recon-match-accuracy.py` (S1 shipped as PR #404) |
| ER-PDF ingest hardening (ISO-ccy amounts, inline rows, per-token format) | done (PR #263) | `expense_report_pdf.py`; all 6 real ERs parse to-the-cent vs printed totals |
| Web-download exports carry Tier-2 receiptless categories | done (PR #294) | `service._charge_cats` threaded into all 4 `regenerate_*`; zoho honors `export_receiptless_learned` |
| SPA JSON API for the Lovable front end | live, SPA at PARITY + published | `/api` + bearer + CORS, Lovable-hosted (PRs #290/#291/#293); NOT `/api/v1`; SPA repo `011matthias/brisken-expense-review` (TanStack Start). Path 1 chosen 2026-07-21: extend this SPA, do NOT rebuild from the plan's prompt. 2026-07-22: production URL = **brisken-reconcile-dash.lovable.app** (existing CORS regex already covers `*.lovable.app`, no backend change). PRs brisken-expense-review #1 (runs made reachable: all-runs + intake tables, publish/unpublish, 8 bare paths -> `/api`, §18 resolve, `/settings`, `/compare`, `/intakes/$intakeId`, `card_pct`) and #2 (publish control names the action) merged AND published; full review loop verified live incl. a publish round-trip with state restored |
| §17 disposition, §16 export-approved gate, §18 duplicate resolve | backend live (deployed 2026-07-21, verified) | PRs #296/#297/#298; `/api/runs/{id}/disposition`, `/duplicates/resolve`, `GET/PUT /api/settings` all live + inert-by-default; run detail carries `duplicate_groups` + `disposition` |
| Local no-API-key test loop: `run.local.json` per run | live (PR #299, deployed 2026-07-21) | `prepare_run` writes a self-contained config (minus `llm`/`coa_validation`) beside the uploads in `/data/runs/<id>/`, so `flyctl sftp` + `expense-recon --config run.local.json` reconciles locally with NO OpenAI call. Existing live run `b67133b8df98` backfilled |
| SPA mutation parity (`/api` twins) | live (PR #318, deployed 2026-07-22) | 11 mutations mounted on `/api` (decisions, confirm-matched, categories, manual-match, forget, commit-memory, feedback, publish, unpublish, memory/reset, intakes+files+run). `_wants_json` branches one handler across both surfaces. `auth.path_requires_operator` now canonicalizes the `/api` prefix — future twins inherit their operator rule, do NOT add duplicate regexes. SPA can now finish a full review |
| Run management (F9) + in-flight runs on dashboard (F3) | live (PR #410, deployed 2026-07-23) | `POST /api/runs/{id}/rename` + `/delete` (delete drops the run row + decision/override/duplicate edit rows, rmtree's `work_dir` guarded to `data_root/runs`, resets a deleted run's intake back to the queue); `/api/operator/state` gains a `processing` list of still-running jobs (a run row exists only once its pipeline finished). Store: `set_run_label`/`delete_run`/`list_active_jobs` + 8 tests. 4 accumulated test runs deleted via the endpoint; `b67133b8df98` (0/94 baseline) kept. **SPA does NOT yet render either — the Lovable-side wiring is the remaining half** |
| Entity-less batches + card review (Cards R3): per-receipt entity chain, `card_review` strip, hint assignment + learning, refresh-master-data | shipped (PR #559, 2026-08-21) | Entity resolves override -> hint assignment -> card registry (ambiguity refused) -> stamped value -> `needs_entity`; same chain in grid, export (`(entity - assign)` placeholder, never blocks, re-export folds assignments in), and statement graduation (chain entities baked into the matcher pool). Generic tenders never auto-resolve or learn (compound + DE vocab). 3-lens adversarial review executed + pinned; restore/attach writes serialized under the batch lock. Lovable half: `docs/lovable-cards-r3-prompt.md` (owner applies) |
| Card registry (Cards R1+R2): `settings["cards"]` + composition + `GET /api/cards` + Zoho decoupling | live (PRs #555 + #556, 2026-08-21) | R2: per-card Zoho-OPTIONAL advisories (setting "cards"), journal credit + expense Paid-Through resolve via the conservative `resolve_account_map` (bare-digit keys match labels, ambiguity keeps the placeholder: two adversarial-review rounds executed and pinned the wrong-money scenarios), `merchants_inert` on GET settings, Lovable prompt `docs/lovable-zoho-decoupling-prompt.md`. |
| Card registry R1 detail | live (PR #555, 2026-08-21) | Owner directive (feedback wave): cards get their own identity, Zoho optional. `cards.py` (multi-digit identity: statement marker "2838" + plastic last-4 "1672" on one card), `effective_cards` read-time composition over the legacy maps + presets (no write migration), field-aware resolution, one resolver replacing three matchers. Zero behavior change on seeded data (legacy pins green). Adversarial-review round: 13 confirmed findings fixed/documented pre-ship. R2 (Zoho decoupling of exports/warnings), R3 (entity-less batch + card review), R4 (mixed-entity) next — backlog item 10. Lovable half: `docs/lovable-cards-prompt.md` |
| Master data: fx_reference_rates + card_entities + card_accounts (settings) | live | `store.SETTINGS_DEFAULTS` maps applied at run creation. Live: BRL:USD + EUR:USD rates, `card_entities {"2838":"Corporate Services"}`, and (set 2026-07-23, owner-confirmed) `card_accounts {"2838":"CHASE VISA - 2838 - TRAVEL"}` — the card's own credit-card liability account (code L100-50-1100-0000), resolution-verified against the CS chart; NOT the checking 9388 payment leg. Keyed by exact `tx.account_id`; unmapped → visible placeholder, never a guess |
| Zoho seed `com`-DC fix | done (PR #410) | `learning_cli._ZOHO_DC_DOMAINS` gained a `com` alias (US .com); `ZOHO_DC=com` previously fell to the eu default → `invalid_client` on token refresh, so `memory seed-zoho` could never reach the tenant. Still blocked behind the token expense-scope re-consent (owner gate), but the trap is gone |
| SPA compare (server-computed diff) | live (PR #301, deployed 2026-07-21) | `GET /api/compare?a=&b=` wraps `compare_runs`; closes the last frontend-computation hole so Lovable computes no diff |
| §14 configurable automation | planned | plan Phase 6 (`~/.claude/plans/async-beaming-perlis.md`), optional; behavior-changing (auto-confirm), default-OFF |
| SPA memory screen (`/api/memory`) | live (deployed v25, verified 2026-07-21) | PR #304: `GET /api/memory` + `POST /api/memory/forget`, operator-gated (mirrors HTML `/memory`); serializes `build_memory_view` / reuses `forget_memory_vendor` |
| LLM owns category+account + AI Category CSV columns + hosted LLM default-on (WS1) | live (PR #313, deployed 2026-07-21) | `categorization.override_er_category` keeps the LLM/learned account; reconciled CSV gains `AI Category`/`AI Zoho Account`/`AI Category Source`; hosted runs use LLM by default (`EXPENSE_RECON_DEFAULT_LLM`/`_OVERRIDE_ER_CATEGORY`) |
| Vision receipt-image read + deterministic root-group adjudication (WS2) | live (PR #315, deployed 2026-07-21) | `ingest/expense_report_images.py` reads the ER PDF's receipt IMAGES (fills the ~9/20 summary rows with no vendor); `adjudicate_receipts` overrides the report's category only on a heavy Zoho root-group mismatch (`Category Decision` column: kept ER / AI override (heavy) / review); categorizer chart now built from `coa_validation` so override+adjudication fire on hosted runs; vision default-on (`EXPENSE_RECON_VISION_RECEIPTS`, ~$0.14/run). NOTE: ADOBE/ANTHROPIC are receiptless statement charges (FX-false-paired to Food receipts) -> a MATCHING problem = WS3, NOT fixed here. Plan: `~/.claude/plans/plan-out-how-we-playful-boole.md` |
| Login throttle on `/api/login` | live (PRs #367 + #369, deployed Fly v32/v33 2026-07-22) | `web/ratelimit.py` + `login_failures` table. Per-caller 5 failures/15min then 60s lockout doubling to a 1h cap; global 50/15min then 300s. Only failures count, a success clears the caller, the throttle runs BEFORE the code check. Callers bucket by IPv6 **/64** (a /128 key gave one end site 2^64 fresh buckets). Verified live: 5x401 -> 429 `Retry-After: 60` scope=ip -> right code still 429 while locked -> expiry -> 200 -> record cleared. Live-probe finding: Fly's proxy OVERWRITES `Fly-Client-IP`, so the per-caller key is the real peer and is not forgeable; stored key confirmed on the volume as `2003:c6:3f3c:3200::/64`. Stdlib only, env-tunable, no-op when the gate is off |
| Matching: card-contradiction gate on FX auto-resolution (matcher-v2, WS3) | done (2026-07-23, `client/brisken/matcher-v2-eval`) | Resolves the ~14 no_charge frontier from `brisken-recon-tuning-v1`. A clean bilaterally-unique FX_BASE_AMOUNT/FX_REFERENCE pair no longer auto-resolves when the receipt's payment_mode card is absent from the statement (`card_signal==0.0`, behind `card_scoping`); demotes to judgment (guarantee holds). Pinned scorer: all-6 37.2->65.2, nc_matched 14->0, determ_ok 55/95 UNCHANGED, 0 wrong; guard 4/4; suite 783 green + 3 pins; hosted parity holds. Vendor was measured NOT separable (26/55 true pairs <0.2 sim); card is the clean split. Detail: ANNEALING resolved-2026-07-23 |
| Matching: ADOBE/ANTHROPIC receiptless-charge FX-false-pair + second-chance pass (WS3) | not-started | distinct from the card gate above: receiptless USD subscription charges FX-false-pair to unrelated EUR Food receipts; optional second-chance pass over unmatched (RECOMMEND-OFF per the open decision below, ~1-2 rescues/95). Plan WS3 |
| Run-not-found recovery (SPA) | live (SPA #4, published) | a `/runs/<id>` URL whose run was deleted (or a stale tab/bookmark) dead-ended on "Failed to load run: run not found" with no way back — the "app is broken" report 2026-07-24 was this, not an outage. `ApiError.status==404` now shows a plain deleted-run message (EN/PT) + a Dashboard button on ANY load failure. Backend was healthy throughout |
| Hosted-run Zoho-creds regression fix | live (PR #435, Fly v38, 2026-07-24) | since the master-data ship (v35), `apply_master_data` injected a `zoho:` block with no `coa_source`, which `_build_chart_of_accounts` read as a live-API chart pull → every hosted upload died at the receipt-images stage on "Zoho credentials missing from environment" (Fly has no ZOHO_* vars). Fabricated block now declares `coa_source:"none"` (chart comes from the `coa_validation` fallback); `card_accounts` still carried for the journal. Had broken ALL hosted runs for ~a day |
| Emailed-receipt upload per charge + FX suggest floor | live (PR #437, Fly v39, 2026-07-24) | owner directive: receipts that reach Criss by email (not the ER export) leave their charge unmatched. `POST /api/runs/{id}/transactions/{tx}/receipt` stores the file under the run work dir, reads it into a Receipt (run LLM when set, bare-filename otherwise), appends to the snapshot pool, records the pair as a confirmed decision (manual-match path); re-upload replaces. Also `matching.fx_judgment_suggest_floor` (0.2): a real FX verdict below it is unbound to the plain unmatched buckets — kills the p=0.10 OpenAI-vs-construction-materials "suggestion" the reviewer flagged. No-LLM stub (0.5) always passes, scorer path untouched |
| Per-receipt image preview | live (PR #440, Fly v40, 2026-07-25) | the vision join already knew each expense row's ER-PDF image page but discarded it; now persisted (`Receipt.receipt_image_page`, snapshot-serialized) and served: `GET /api/runs/{id}/receipts/{document_id}/image` renders that page to PNG on demand, or streams an uploaded manual receipt; `receipt_image_available` per receipt gates the SPA control. Pre-existing runs have no mapping and 404 cleanly; runs from v40 on carry it |
| Structured FX breakdown on cross-currency candidates | live (PR #445, Fly v41, 2026-07-25) | each cross-currency candidate carries an `fx` object (server-computed per the api.ts "frontend does zero business logic" rule): charge amount+ccy, receipt amount+ccy, `zoho_rate`/`zoho_converted` (the receipt's `exchange_rate`/`base_amount`), `implied_rate` (charge/receipt, same direction so they compare in one column), `converted_gap`+`_pct`. Same-currency → `fx:null`. Lets a reviewer see WHY an FX pair is uncertain instead of decoding the prose reason |
| Honest receipt-based match rate | live (PR #446, Fly v42, 2026-07-27) | the dashboard "match rate" divided matched charges by ALL charges → 22.3% on April where 57 of 94 charges never had a receipt, making a working tool look broken. Added `receipt_match_rate` (receipts placed over receipts that exist) + `n_receipts_matched` to both summaries; live April run reports **34/37 = 91.9%** beside the 22.3% charge figure. SPA header reframe pending publish. Matcher diagnosis (local, no-LLM, vs 21 labelled April true pairs): 18/21 clean determ, ~0 wrong — near ceiling; the low headline was purely the denominator. Statement-PDF lever tested NOT viable (only the wrong-cycle Chase PDF is on hand and Chase PDFs do not expose per-charge foreign amounts) |
| Bulk digital-receipt folder attach (sub-universe re-match) | backend live (PR #449, Fly v43, 2026-07-27) | bulk sibling of the emailed-receipt attach: `POST /api/runs/{id}/receipts/folder` ingests a whole folder (or `.zip`) of receipts Criss has only digitally, then re-matches the sub-universe {undecided, non-credit charges} × {new + still-unmatched receipts} via `match_month` + judgment and splices the result back beside untouched confirmed/rejected/posted work (does NOT pre-assign — produces review candidates). Background job (vision OCR is minutes) → `{job_id}`, SPA polls `GET /jobs/{id}`. Reuses the run's `MatchingConfig` via `build_match_cfg` extracted from `reconcile` (0.2 FX floor + card scoping); `card_last4` now snapshot-round-trips so the WS3 card gate survives a re-match. Reconciliation guarantee holds (every receipt → a candidate or `unmatched_receipts`); dedup vs existing receipts surfaced as `n_possible_duplicates`, never dropped; content-addressed `folder:` ids → idempotent re-upload + image preview. LLM sourced from the run block or the env default, cost in the `folder_ingest` summary. 810 pass / 2 skip; live v43 healthz 200 + API-only gate intact. **SPA folder/zip picker + ingest-progress + suggestions-in-review is the remaining Lovable half.** |
| Categorization visible on reconciled rows (`posting_category`) | backend live (PR #453, Fly v45, 2026-07-27) | the view exposed a category only for receiptless charges; matched charges buried it in candidate line items and never sent the posting ACCOUNT. `row.posting_category = {category, zoho_account, source}` resolved server-side for matched AND receiptless rows (override-aware, mirrors the export's `_ai_category_cells`), null for an uncategorized matched receipt. Fixed "categorization doesn't show in the UI" |
| Receipt-first "generate expenses" mode (Dirk note #1, "the flow is backwards") | **LIVE** — merged PRs #460/#461/#462, deployed to Fly with `EXPENSE_RECON_RECEIPT_FIRST=1`, full month loop verified against the deployed origin 2026-07-28; Lovable month-lifecycle UI published | Statement-independent Mode A: upload receipts → Zoho-quality expenses (Zoho Books Expenses import CSV, one row per expense), statement reconciliation stays Mode B (`reconcile()` byte-for-byte untouched). Phase 1 tax/VAT+payment-hint extraction parity (83bf2b4), 2 `cli.generate_expenses()` (93704d6), 3 `output/zoho_expense_export.py` + `run()` mode dispatch (8ff4d51), 4 web layer behind `EXPENSE_RECON_RECEIPT_FIRST` — `POST/GET /api/expense-batches` (decoupled upload step), receipt-spine `build_expense_view`, field/category/entity edits + manual add/delete, `GET /runs/{id}/expenses.csv` (7647959c), 5 categories read-only + settings `entities` registry (org_id/scope_groups/default_paid_through/account_picks; wins over the /data provisioning file) + curated `account_options` (b698d21e), 6 learning from explicit edits only — `merchant_entity` + `field_correction` tables, consult in `generate_expenses` ONLY, `/api/memory` + forget cover them (a0d6ccf1). Suite 858→895 green. Plan: `~/.claude/plans/start-agentic-ops-expense-cheeky-blanket.md`. Remaining: Phase 7 Lovable UI (needs SPA repo access), Phase 8 flag flip + Criss e2e; validate `EXPENSE_COLUMNS` against the tenant's real Zoho Expenses import (owner) |
| Review-by-exception state + safe Confirm-all | backend live (PRs #454/#455, Fly v46/v47, 2026-07-27) | UX reframe (owner-approved concept): the tool triages, Criss judges only exceptions. `row.review = {state, reason, reason_code}` — **ready / check / pick / none** with a PT-localizable code — computed server-side so the SPA groups + bulk-confirms with no logic. `POST /api/runs/{id}/decisions/confirm-ready` confirms ONLY ready∩pending∩matches (never `/bulk`), caps 1000 + reports remainder, reversible in-app pre-export. `adjudication_available` run flag so `ready` is never mislabeled "account verified" on a keyword/no-override run. **Designed + adversarially verified via an 8-agent workflow that caught 4 highs before code** (structural partial-uncat detection; confirmed-match ≠ confirmed-category; `review_unresolved`+empty-provenance → check; the adjudication-availability gap). 841 pass / 2 skip incl. the Confirm-all safety invariant. Concept mockup: artifact afb9422d. **SPA render (grouped queue, plain chips, honest Confirm-all) is the remaining Lovable half.** |
| Canonical merchant registry (receipt-first) | **live** — PR #476, Fly deployed + seeded 2026-07-29; seed-mapper fix PR #481 | Settings-backed `settings["merchants"]` = {canonical: {aliases, category, zoho_account}}, seeded (28 merchants from the 5 live runs: 7 Travel / 21 Meals) + self-improving. `merchant_registry.MerchantRegistry` resolves each receipt's vendor exact-alias → rapidfuzz `token_set_ratio` → unmatched; `generate_expenses` stamps the canonical DISPLAY vendor + a REGISTRY categorization that preempts the LLM (order registry > learned > llm; reviewer override always wins). Grid `vendor={display,raw,source}` (BREAKING: was a string; live Lovable UI updated + published) + `posting_category.source=registry\|learned\|llm\|override`. Reviewer vendor/category edits upsert back into `settings["merchants"]` on `/commit-memory` (raw→alias, chosen category→default); `/memory/forget` clears learned SQLite but NOT the registry (durable, edited in the Merchants editor). `vendor_clean` at extraction + deterministic `vendor_names.clean_vendor_name` fallback. Seed CLI `expense-recon-seed-registry` (leaf-based label→bucket for the nested chart + noise filter, PR #481). Reuses Phase-6 learning + coa_validation + LLM categorizer unchanged; `reconcile()` untouched. Suite 1013 green. Lovable Merchants editor + vendor hint built+published by owner (prompt `docs/lovable-merchants-prompt.md`). **Editor tidy pending (owner):** merge OCR/casing dup entries (MEGA CENTER/CENTRE, Fenix/Ki-Massa pairs) + fix chart-mislabeled cats (MEGA CENTER = construction, reads Travel). |
| Zoho import headers + card-first `has_coa` fix + real entity picker (feedback r1) | **live** — PRs #494/#497/#498, deployed + live-verified 2026-08-07 | (#494) `EXPENSE_COLUMNS` validated vs the tenant's real Zoho "Import Expenses" sample (`sample_expense.xls`): `Amount`→`Expense Amount`, `Vendor Name`→`Vendor`, `Reference Number`→`Reference#` so Zoho auto-maps; rows positional, data untouched; live CSV header verified. (#497) card-first `has_coa` bug: `resolve_entity`/`apply_master_data` matched the card only as a label SUFFIX (`endswith`), but Chase labels it FIRST (`"2838 - May 2026"`), so Criss's May run resolved to the raw label → `has_coa:false` + "missing setup" despite correct config; new `_card_key_matches` reuses the matcher's `_card_keys` digit-token extraction (exact/suffix kept). `has_coa` is snapshotted at creation → Criss must **re-run May** to pick it up. (#498) entity dropdown was empty because `entity_options` only read `settings["entities"]` (empty for Brisken); new `available_entities()` unions CoA provisioning + `card_entities` targets + registry; exposed on `GET /api/settings` (live: `["Cloud Services","Corporate Services"]`); create-form blank already accepted. Lovable UI (entity dropdown + currency demoted under Advanced) applied by owner + DOM-verified live. Handoff: `docs/lovable-feedback-r1-prompt.md`. **3 r1 design items PARKED for owner:** entity-from-upload-column (per-run vs per-row), currency inference from vendor/location, per-vendor multi-category (REVERSES registry-preempts-LLM precedence — needs explicit call). |
| Card-number → Paid Through (receipt-first) + grid provenance | **live** — PRs #467/#470, Fly v51/v52, 2026-07-28; `card_accounts` populated live | Receipt-first export resolves each expense's Zoho "Paid Through" from the last4 the OCR already reads off the receipt (`payment_hint` → `payment_mode`), mapped via settings `card_accounts` (last4 → account). Order: override → receipt's own → **card-derived** → run default → placeholder; an unknown/cash card falls through, never mis-posts (B4). `resolve_paid_through() → (account, source)`; `_paid_through` delegates (exported account byte-identical); `build_expense_view` adds `row.posting_paid_through = {account, source}` (source card\|override\|receipt\|default\|unassigned) so the SPA can show the resolved account + a "from card ···NNNN" badge. Live `card_accounts` = 5 corporate cards (2838/0113/6013/9693/8311), owner-confirmed, derived last4-from-account-name off the cached COA. **SPA render (Paid-Through cell + card-accounts settings editor) is the remaining Lovable half — prompt delivered this session.** |
| SPA view-shape contract (parse_issues crash, backlog item 21) | **shipped** — `tests/test_view_contract.py` + `docs/api-contract.md`, 2026-08-22; the SPA half is PUBLISHED and live-verified | The batch page went blank on React error #31 for every batch carrying a parse issue: `parse_issues` had shipped as objects (`{file, line, message, severity}`) since 2026-07-22 while the SPA typed it `string[]` and rendered items directly. The SPA is a separate repo with no type-check against this API, so nothing could see the mismatch until it reached Criss. Owner published the Lovable fix (string-tolerant `ParseIssue[]` renderer); DOM-probed batch `ae61e122a505` (which carries one real object-shaped issue): 36 rows render, quarantine note visible, no error boundary, zero console errors. Recurrence-kill: the contract test pins the ELEMENT type of every list field on BOTH view payloads over HTTP, fails on an unpinned field / a type flip / a fixture that stopped covering one; proven by regressing both emission sites to `string[]` (3 of 4 tests fail). Scalar flips out of scope by design. Suite 1218 / 2 skipped, calibrate green |
| The reconciliation document (backlog item 24, second half) | **shipped** 2026-08-23 | Owner: no target application for the reconciliation either, "we just need to think of what the best course of action is". A CSV carries the charge list and none of the evidence, and nothing reads it, so the deliverable is a document: header, then WHAT NEEDS ATTENTION first (charges with no receipt, receipts with no charge, duplicate groups), then every charge with its matched receipt and status, then the receipts — matched ones captioned with the charge they settle, unmatched ones captioned as unmatched, because a receipt nobody could place is exactly what a reader needs to see. Built from `build_view`, the workbench's own payload, so document and review screen cannot disagree. A clean month says "Nothing. Every charge has a receipt..." rather than showing an empty section. Shared PDF primitives extracted to `output/_pdf_common.py` so the two documents stay one product. XLSX stays the working sidecar; CSV stays available and demoted. 8 tests; suite 1187 / 2 skipped |
| The month's expense report PDF (backlog item 24) | **shipped** 2026-08-23 | Owner directive: there is no target application at all, so the output is a document — "an expense report like in Zoho with an organized listing then all the receipts". `GET /runs/{id}/expense-report.pdf` builds the numbered listing from `build_expense_rows` (the export's own rows, via a shared `_expense_export_inputs` so CSV and PDF cannot diverge), then appends each receipt behind a caption. Evidence is per DOCUMENT: a split receipt writes two listing rows and appears once, captioned "Expenses 3, 4". Renderability is decided before the caption is written, so a file that exists but cannot be turned into pages says so instead of leaving a caption with nothing behind it. reportlab added (text-layer listing, DejaVu/Arial for the accented vendor names). 10 tests; suite 1178 / 2 skipped, calibrate green. SPA half: `docs/lovable-month-report-prompt.md`. **Open: the reconciliation half** — recommendation is the same document shape (charges + matches + exceptions + evidence) with the XLSX as the working sidecar |
| The month is a document, not a file (backlog item 24) | **shipped** 2026-08-23, deployed | Owner: there is no target application any more. `GET /runs/{id}/expense-report.pdf` builds the listing from the export's OWN rows (`_expense_export_inputs` shared with the CSV, so money cannot drift between document and file), then appends every receipt behind a caption naming the expense it proves. Evidence is per DOCUMENT rather than per row, so a receipt split across two accounts appears once, captioned "Expenses 3, 4". Renderability is decided BEFORE the caption is written: a file that exists but cannot be turned into pages says so, instead of leaving a caption with nothing behind it (which reads as "the receipt is here"). Module deliberately named `output/month_report_pdf.py` — `ingest/expense_report_pdf.py` already exists and is the input-side parser; a first attempt at the obvious name silently clobbered 598 lines of its tests. Live-verified from production: April 2026 = 74 pages, totals matching the CSV exactly. Follow-up #585 fixed the Receipt column printing row numbers as page references (`p. 1`) — now `attached` / `none`. SPA half: `docs/lovable-month-report-prompt.md` |
| The reconciliation is a document too (backlog item 24, second half) | **shipped** 2026-08-23, deployed | No target application here either, and a CSV carries only the charge list. A reconciliation's product is evidence a month is COMPLETE, so `GET /runs/{id}/reconciliation-report.pdf` renders from the workbench's own `build_view` payload (reader and reviewer cannot see different reconciliations): header, then **what needs attention** FIRST (unmatched charges, unmatched receipts, duplicate groups — the only part anyone must act on), then every charge with its matched receipt and status, then the receipt pages. A clean month says "Nothing. Every charge has a receipt" rather than showing an empty section. XLSX stays the working sidecar (Criss works in Excel; her fill-colour is data). Live-verified: January = 39 pages, 80 charges, 0 matched, USD 20,228.68 unreconciled, 78 charges with no receipt — a finding for the owner, not a bug |
| The paying card, read off the scan (backlog item 28) | **shipped** 2026-08-24, deployed | PR #592. Owner: make sure the card an expense should be attributed to can be extracted from the receipt scan. It could not be — asked to transcribe four faded digits the extractor landed 2 in 5 and invented the rest (`1234` three times, once for a receipt that plainly prints 1672). Three cheaper fixes were measured and REFUTED before anything was built: repeat reads return the identical wrong answer (stable, not noisy, so a second-read vote buys nothing), a verbatim transcription beside the interpreted value agrees even when both are wrong (same look at the page) and sometimes loses digits the interpretation kept, and gpt-4o reads no better than gpt-4o-mini. What worked was changing the QUESTION: the answer only ever has to be one of the handful of cards Brisken holds, so the extractor gets those last-4s from the run's own registry snapshot and picks one or declines. Deny-by-default (no registry = no list = no pick); the card list joins the extraction-cache key, since the same photo asked against a different set of cards is a different question. Vision also moves to gpt-5-mini — reading a receipt is a different call from categorizing text and only the reading was failing. Measured 3 runs each over 7 problem receipts: dates 0/6 live -> 3.0/6 with the list -> 5.0/6 with the model; cards 1/5 -> 3.0/5 -> 3.7/5; zero false positives on the two receipts printing no readable card. Live-verified with a namespaced TEST batch (deleted after): a receipt printing `VISA - ******2838` came back date 2026-04-11 (the cache held 2023-04-11), hint `VISA ...2838`, card-2838, entity Corporate Services via `card`. CAVEAT worth knowing: the mechanism is only as good as the registry, and production's registry still has no 0340 card and no 1672 identity on the Chase card, so those receipts stay unpickable until item 26's data entry happens — item 26 is now load-bearing for this feature, not just for the MISSING ENTITY count |
| Implausible expense dates (backlog item 25) | **shipped** 2026-08-24 | PR #590. Eleven of the live April batch's 36 readings were dated 2020-2023, one of them on line two of the month's report PDF. Diagnosed by pulling the stored readings off the volume and comparing each to its receipt image: the raw extractions already held those years (parse is a plain `fromisoformat`, so that layer was innocent) and the receipts do not print old dates. Two model mechanisms, both readable off the images: `receipt_33` prints `Data: 2026-04-22` in its fiscal block and `26-04-22` (YY-MM-DD) on the card slip, and the model read the slip day-first as 2022-04-26; `receipt_03` prints `02/04/2026` and the model returned 2023-04-02, day and month right and the year invented. The prompt was tightened AND measured rather than assumed (6 of 11 fixed; 24 of the 25 already-correct readings byte-identical, the 25th changed to the value the receipt actually prints; none worse) — but five stay wrong, so the load-bearing half is the deterministic guard: a date outside the batch's month reaches the reviewer as `check` / `date_outside_period`, and the report PDF names the expense numbers it distrusts. Nothing is auto-corrected. The month comes from the operator's label, failing that from the batch's own dates by strict plurality, failing both from nowhere — no period means nothing is ever flagged, because a guessed month would put good rows under suspicion. A date the reviewer typed is believed, so a genuinely re-issued old invoice can be cleared instead of sitting in review forever. Regressed the real source: all four e2e tests go red at 0 flagged of 11. Suite 1206 / 2 skipped, calibrate green. Residue as backlog item 27: the guard catches a wrong month, not a wrong day inside the right month |
| Mail intake: anyone may submit (owner directive 2026-08-23) | **shipped** 2026-08-23, deployed Fly v83 | PR #587. The `@brisken.com` sender allowlist and the envelope/header agreement check are DELETED; `intake.senders` is ignored (dropped on save rather than rejected, so a dead key cannot 400 an edit to the live keys beside it). A hotel mailing an invoice, a faculty member on a private address, and a supplier's billing robot now all land. From is forgeable, so the allowlist bought tidiness rather than security. Unchanged: recipient must be at our own domain (not an open relay — live-probed post-deploy, `victim@gmail.com` still 550), day budget, in-flight ceiling, disk floor, zip refusal, quarantine + review. The GLOBAL day cap (200) is now the real spend ceiling, since a rotating From evades the per-sender one. The auto-ack's recipient is now untrusted, so `graph_notify`'s own @brisken.com guard is what stops backscatter: an outside submitter is ingested and never replied to (pinned with the sender ENABLED, or the test passes for the wrong reason). First handler-level test of `handle_DATA` — the old gate lived there and nothing exercised it. SPA half: `docs/lovable-open-intake-prompt.md` (only if the "Accepted senders" editor was ever built) |
| Re-ingest for stranded mail (backlog item 19) | **shipped** 2026-08-23 | Owner said build it. A mail whose receipts were ingested into a month that was later deleted had no path back: replay skips `ingested` mail (correctly), the expenses went with the month, and the delivered bytes sat in the custody archive unreachable from the app. `POST /api/inbound/{archive}/re-ingest` puts ONE archive's attachments into the month that is open now, under a CAS transition (transient `re_ingesting`, so a second click, a dismiss or a replay pass all refuse rather than interleave). Deny-by-default: only mail carrying the `batch_deleted` stamp qualifies, which is what keeps it from becoming a way to copy one month into another; a body-only archive is told to use render-ingest instead. No bulk version, on purpose. Sync route, so the batch lock never reaches the event loop (the #572 AST guard passes it). 5 tests, all red before the endpoint existed, and the guard proven by regressing it. Suite 1168 / 2 skipped, calibrate green. SPA half: `docs/lovable-re-ingest-prompt.md` |
| Cards R4 export half + per-entity chart gate (backlog item 10) | **shipped** 2026-08-22 | Owner answers: mixed-entity months export as ONE file with the entity as a column (pinned by test; the export already did this after R3), cash and personal tenders stay per-month assignments rather than cards (no code needed), and per-entity Zoho accounts are moot under the no-ties-to-Zoho directive. The real find was beside the ruling: `CoaGate` assumed one entity per run and `apply_to_config` looked up that one entity, so every entity-less batch — which is every Cards R3 batch — got no `coa_validation` block at all and exported un-gated. `MultiEntityCoaGate` now dispatches each row to its own entity's chart (diversion runs per entity so review notes name the right company; an unknown entity stays ungated, fail-open), and provisioning injects one entry per known entity when the batch has no single one. Proven both ways: the e2e test fails without the provisioning fix; forcing a single gate fails the per-entity test. Suite 1163 / 2 skipped, calibrate green. Left in item 10: persisted cards migration, intake dropdown unification |
| Zoho decoupling, layer 1: the live connection (backlog item 23) | **shipped** 2026-08-22 | Owner directive: "zoho does not matter anymore, the app should have no connection or ties to zoho anymore". Deleted `src/expense_recon/zoho/` (API client + idempotent journal poster), `zoho_post_cli.py`, the `expense-recon zoho-post` dispatch, the `coa_source: "api"` live chart pull, the `seed-zoho` memory importer, and the `ZOHO_*` credential check in doctor — about 1,600 lines plus 65 tests of that code. Hosted behavior is unchanged: the Fly app carries no `ZOHO_*` env and the web layer never imported the client. `coa_source` now defaults to `"none"` and an old config asking for `"api"` fails loudly with the migration route instead of running chart-less. Guard: `tests/test_no_zoho_connection.py` fails on any Zoho host, credential read, or re-added subcommand (all 4 tests proven red against the pre-deletion tree). Suite 1158 / 2 skipped, calibrate green. Layers still standing, by design: the `zoho_account` FIELD names (SPA-coordinated rename), the chart-of-accounts gate (worth keeping, needs a non-Books chart source), and the export artifact (blocked: what does the CSV import into now?) |
| Upload-rejection codes (backlog item 20) | **shipped** 2026-08-22 | The four upload refusals (unsupported type / empty or unreadable / too large / upload cap) shipped as English prose only, on a surface Criss meets exactly when something went wrong. Now every rejection also carries `{code, file, suffix, limit}` in a PARALLEL `issue_details` / `upload_issue_details` list at all three emission sites (batch create, folder ingest, add receipts); `issues` keeps its wording and its `string[]` type, because enriching it in place is the move that blanked the batch page the same week. `service.upload_issue` builds sentence and code together. Pinned in the contract test and proven both ways (empty details fail the non-vacuity guard; retyping the prose list fails the element check). SPA half `docs/lovable-issue-codes-prompt.md` is optional — the English sentences render unchanged until it is applied |
| Count semantics: one name, one question (backlog item 22) | **shipped** 2026-08-22, deployed | Operator note on the live app: "it sayz 35 categorized but when you click on open it says only 5 categorized". Same batch (ae61e122a505), same key, two meanings — the list counted expenses carrying a category (35 of 36, true), the batch page counted rows whose review state was `ready`, so the 30 Cards-R3 rows awaiting a legal entity read as uncategorized and NEEDS CATEGORY claimed 31 when 1 needed a category. Fix: `service.categorized_counts` is the single rule behind every payload, readiness gets its own `n_ready`, and `service.batch_list_summary` derives the list screen from the same live overlay the batch page renders (the stored summary is frozen at ingest, so an edit never moved it). Reviewing the diff caught a blind `except Exception` swallowing a closed-store bug in the new derive — narrowed, and the test that exposed it is pinned. 2 regression tests, both red on the pre-fix code; suite 1223 / 2 skipped, calibrate green. `docs/api-contract.md` now carries the counts table (meaning flips are the sibling class the type contract cannot see). SPA needs no change; optional READY tile in `docs/lovable-ready-tile-prompt.md` |
| Non-receipt quarantine + "null"-label sanitize (receipt-first usability loop, iter 1+2) | **live** — PRs #516/#518, Fly v58, 2026-08-13 | (#516) vision extractor classifies every file (`document_type`: receipt \| statement \| report_summary \| other, defaults receipt so exclusion must be earned); `generate_expenses` + the incremental batch add exclude non-receipts with a warning ParseIssue (grid `parse_issues` / ingest `issues` / CLI print). Trigger: ER-00215's own summary page exported as a phantom **8,796.35 BRL** expense; Criss's real May folder held 7 Chase statement PDFs among 27 files. Live-verified on both sets (7/7 statements excluded, 20/20 receipts kept; smoke10 summary page excluded, money fields stable). (#518) gpt-4o-mini intermittently returns the STRING "null" for category/zoho_account; truthy, so it reached the CSV as a literal `null` Expense Account — payload parse now collapses sentinel spellings to None → `(uncategorized - assign)`. Suite 1043. The then-open drift target shipped as iter 3 (next row) |
| Multi-category flag + variance chip + books_as depiction (receipt-first usability loop, iter 6) | **shipped** — PR #543, 2026-08-19 | Owner rulings 2026-08-19: splits are truth (depict, never collapse); category variance is surfaced for human judgment. `multi_category: true` on a merchants entry keeps NAME canonicalization but stops category auto-stamping (per-receipt judgment); grid rows carry `category_variance` (chip + drill-down) and `books_as`/`is_split` via shared `expense_posting_parts` (grid == export by construction, override-aware). Item 6 rode along (vendor ≠ acquiring bank prompt line; cache fingerprint bump). 7 tests; suite 1075/2. **SPA render (chip, Lançado-como breakdown, Merchants toggle) is the remaining Lovable half — prompt `docs/lovable-variance-books-as-prompt.md`.** |
| Mail intake: the app's own mailbox (expenses.brisken.com) | **LIVE** — PR #548 deployed 2026-08-21; dedicated IPv4 149.248.221.114, MX/SPF/DMARC on expenses.brisken.com public, drill PASS (relay 550, spoofed sender 550, allowlisted PDF 250 -> ingested with submitted_by -> test row deleted) | In-app aiosmtpd listener (:2525 -> Fly public port 25, dedicated IPv4 required; MX for expenses.brisken.com). Custody-before-ack: raw mail + acceptance log land on /data BEFORE the SMTP 250 (451 on archive failure). Submission is OPEN to any sender (owner directive 2026-08-23; the @brisken.com deny-by-default allowlist and the envelope/header agreement check are deleted, PR pending). What still refuses in-protocol: a recipient outside our own domain (no relaying), the day budget, the in-flight ceiling and the disk floor. The auto-ack's own @brisken.com recipient guard is now the only thing keeping confirmations inside the tenant, so an outside submitter is ingested but never replied to. Attachments (incl. inline images; zips refused at this boundary) route into the newest open expense batch via the incremental add — now serialized by a batch-mutation lock (concurrent ingests were silent last-write-wins receipt loss) with snapshot-keyed dedupe (killed jobs can no longer make lost receipts resend-proof) and gap-proof dest indexing. Per-file submitted_by provenance (To-alias > From-sender; first-write wins) exposed on grid rows. Abuse guards reserved at accept time: in-memory day budget (zero-file mail charges 1 unit), in-flight route ceiling, free-disk floor. Held mail (no batch / body-only / no valid files / failed or interrupted jobs) archived + visible via GET /api/inbound/log, drained by POST /api/inbound/replay-held (replayed ONLY on job-done); startup reconcile flips killed ingests back to held. 17 tests; suite 1096/2. Hardened by 3-lens adversarial review pre-ship (6 highs fixed). M365→MX hop proven 2026-08-21 (authorized single Graph send: 9s mail-to-ingest). Lovable half published + live-verified (submitted-by chip, Email intake settings section, held strip absent at 0); Lovable skipped the aliases editor, closed via `PUT /api/settings`: dirk/criss/matthias → full names. Round 2 (2026-08-21, owner directives: auto-ack yes, retention start, structural fixes): `web/graph_notify.py` guarded internal-only Graph sender (sends as matthias.silva, recipients hard-asserted @brisken.com, daily cap 50, X-Auto-Response-Suppress); auto-ack to the real sender on successful ingest (idempotent per archive, auto-generated inbound never acked, `intake.auto_ack` toggle); held-mail alerts to `intake.alert_recipients` (default matthias.silva) the first time an archive lands held; retention sweep at boot (`intake.retention_years` default 10 per AO §147); named operator codes (`EXPENSE_RECON_OPERATOR_CODES` code:label pairs, label signed into the token, feedback notes stamped, legacy shared code still valid). Zoho-export person column DROPPED (owner: attribution lives in the app; receipts inject via email). Access Policy runbook for Dirk/IT: `context/graph-access-policy-runbook.md`. Intake overview (owner ask 2026-08-21): each archive meta records the `documents` its mail created (empty = all duplicates); `GET /api/inbound/log?detail=1` joins entries to their expense rows ({document_id, vendor, date, total, currency}, `deleted: true` for operator-removed rows, + `batch_label`), one view build per referenced batch; Lovable "Email intake" overview page prompt handed. **Remaining: body-only mail rendering (evidence-gated; Dirk's real held mail is the test case); owner: run Access Policy runbook, distribute per-person codes, faculty announcement, publish overview prompt** |
| Feedback capture on every page (owner directive) | **live** — PRs #544+#545, deployed + UI-verified 2026-08-19 | `POST /api/feedback` accepts explicit `run_id` (path parse `/runs/{id}` stays as fallback), so notes on expense-batch pages attribute to the batch whatever the SPA route is called. Widget history: live notes prove the double-click capture worked on `/`, `/runs/{id}`, `/memory` (Criss's r1 notes carried pos/selector/anchor) but was never mounted on the receipt-first batch pages. 2 regression tests (fail on pre-fix src). **SPA global mount (double-click on every page incl. batches, popover EN/PT, explicit run_id) is the remaining Lovable half — prompt `docs/lovable-feedback-capture-prompt.md`.** |
| Set-aside strip + one-click restore (receipt-first usability loop, iter 4) | **shipped** — PR #538, 2026-08-16 | Quarantined uploads become first-class snapshot `set_aside` entries (file, display, reason code, stored extraction); grid exposes `view.set_aside` + `summary.n_set_aside`; mid-month exclusions survive later adds (previously vanished on the next add's ingest-summary overwrite); `POST /api/expense-batches/{id}/set-aside/restore` reuses the stored reading (no fresh vision call) + normal memory/registry/categorize pass; legacy May run derives the strip from parse warnings and restores by re-extraction. 5 regression tests (fail on pre-fix src); suite 1068/2. **SPA strip render is the remaining Lovable half — prompt `docs/lovable-set-aside-prompt.md`, owner to apply.** |
| Extraction cache + CLI merchant registry (receipt-first usability loop, iter 3) | **shipped** — PR #536, 2026-08-15; Fly deploy same session (see backlog Shipped table) | Same photo, same answer: `llm/extraction_cache.py` stores the RAW vision payload keyed on prompt/schema fingerprint + model + document content hash (file name excluded); `OpenAIClient.extract_receipt` answers identical content from the store (hits free, parse stays live so parser fixes apply to cached readings). On via `llm.extraction_cache_path` (config-dir relative, resolved in `run()`) or `EXPENSE_RECON_EXTRACTION_CACHE`; fly.toml sets `/data/extraction-cache.sqlite`. Trigger: the 2026-08-15 pre-fix baseline drifted on 8 of 9 smoke10 rows vs 2026-08-13 incl. a BRL→EUR currency flip + 50.50→50.00 tax drift. Companion (backlog item 2): CLI expense runs build the web path's MerchantRegistry from `expense.merchants` / `expense.merchants_path` (loud ConfigError when configured-but-broken), and `zoho_expense_export` prefers `canonical_vendor` over raw OCR (grid display rule). Verified: suite 1063/2 (+20 tests incl. drift reproduced without cache); smoke10 run twice on fixed code → CSVs byte-identical, cache 10 rows all from run 1 (run 2: zero extraction calls). Residual watch: categorize calls uncached (backlog item 4) |
| Matching improvement program (backlog item 69): measure, then rounds A + B | **Phase 0 + round A done 2026-09-15** (round A: PR #874, Fly v130, both months re-matched live; August's three wrong auto-matches are gone, July unchanged at 26 right / 0 wrong; the scorer re-pin shipped as #871). Round B queued | Both live months labelled (82 decisions) and attributed gate by gate with `tools/recon-match-attribution.py`; the six-bundle scorer's fixture re-keyed to content ids (it had been unrunnable since PR 2a) and its floor re-measured (train 49.8 / all 65.5 / 0 wrong / guard PASS). July: 26 right, 1 wrong, 10 demoted with the correct charge teed up. August: 7 right, 5 wrong, 2 unverifiable. Round A = duplicate copies by reference + inherited card; Round B = uniqueness gate refined (spoken-for rivals, vendor dominance, masked-BIN card key); item 63 (#829) is the sibling's round 0. Decisions in the backlog entry. |
| A month is two views, Expenses and Matching (backlog item 79) | **LIVE 2026-09-16 evening: both prompts published and verified (bundle audit + cold browser drive on July, August, September; residues in backlog item 79).** `docs/lovable-month-views-prompt.md` (no backend gate) and item 23 round 4's consumer half `docs/lovable-journal-callers-prompt.md`; backend alongside SHIPPED (PR #900, Fly v133: `updated_at` on both payloads, `held_by` widened to review-row holders; August `n_charges_receipt_taken` 0 -> 1, consumer driven) | Owner direction 2026-09-16: overview of the receipt-created expenses, and a separate overview of the matching with the four classes each in its own view. Grounded live: the two month pages link one way only, `/runs/{id}` crashes on the four months with no statement, July shows 85 workbook-posted charges as open work under a "Posted 0" chip, and the thirteen-tile bar pins 301 of 900 px. Plan: one shared month strip with Expenses / Matching tabs on the existing two routes; on Matching five cards (receipts without a charge, charges without a receipt, needs review, credits on the statement, matched), one view at a time, decided rows folded into a record with the existing undo (item 79's note #46 constraint), the tile bar retired, match rate off the screen. Leaves the Status cell to item 76, so 76's SPA half targets the new cards. Decisions and open questions in backlog item 79 |
| The card list leaves the top of the month (backlog item 71) | **LIVE 2026-09-16**: prompt published by the owner, all four browser checks pass (evidence row in `PROMPT-STATUS.md`); backlog item 71 closed | Owner report: the card list buries the work. Measured live: the coverage table + statements box push the first charge row 1.7 screens down on the workbench, the card strip 2.3 to 3 screens on Review expenses, and almost no decision comes from them this month. Owner picked card chips in the filter bar + a banner only when something needs action. The drive also found the Card filter counting every option as the month total and a masked card number shown as its ending. `docs/lovable-card-chips-prompt.md`, verify by browser drive after publish |
| The tip band is merchant-scoped | **shipped 2026-09-15** (backlog item 63, PR #829) | `amount_probable_tolerance_pct` (20%) is the restaurant-tip allowance, and a tip never changes who was paid, but the band ignored the merchant: it offered ADOBE 16.23 a Lovable 15.00 receipt and ANTHROPIC 104.95 an Obsidian 96.00 one, and on the LIVE months BOUND five such pairs (`chosen`), including MICROSOFT 718.20 settled by a Zoho Books 576.00 receipt. A same-currency pair outside the exact tolerance now needs vendor agreement by `_vendor_score`, or no vendor named on one side; `amount_probable_min_vendor_score` 0.50, file-tunable, 0.0 disables. Exact amounts and every FX path untouched (S1 measured vendor non-separable for FX: 26/55 true pairs <0.2 sim). August pairs 71 -> 30, 41 different-merchant gone, all 30 true kept, exact 5 -> 5; calibrate invariant OK / no double-binding. No payload field change, so no Lovable half. **Open, pre-existing, not this PR:** the S1 scorer fixture no longer replays (`label transaction_id 'chase-2838-family:177' does not resolve in 01-06-2025_ER-00194`, reproduced on pristine origin/main), so the matcher's anti-overfit guard is currently non-functional |
| A rejected pairing says so, and the undo is the route that made it | **shipped 2026-09-15** (backlog item 16, PR #828) | `rows[].candidates[].rejected` on every candidate of a rejected charge (parallel, absent otherwise) + `summary.n_rejected_pairings`, both read from the CURRENT verdict. Charge-level because the engine is: `apply_decisions`, `effective_settlements` and `sync_claim_for_decision` read the status alone, and a bulk reject writes `chosen_document_id` NULL. No `DELETE` route: `POST .../decisions` with `"status": "pending"` already resets the charge under the lock, re-derives the claim and hands the receipt back, and the test drives that reversal end to end. Live finding: ZERO rejected decisions exist in production (all 223 charge rows on the two statement months are `pending`), so the fixture is constructed and the SPA half has no live case to exercise it. Tests: `test_rejected_pairings.py` (5, route-level through the decision and bulk routes); two regress proofs RED first. SPA half: `docs/lovable-rejected-pairing-prompt.md` (owner applies) |
| One unrenderable receipt does not cost the month its report (item 67) | **shipped** 2026-09-15 | The renderability probe copies pages the way assembly does, so a password-protected or damaged receipt is caught before its caption is written instead of raising mid-assembly and 500ing the whole month; every file is guarded on its own; the caption names the file and the reason; `expenses[].receipt_render` + `summary.n_receipts_unrenderable` carry it to the review screen (absent until a report was built); a receipt contributes at most 60 pages. Live census before the build: both months render and 0 of 102 stored receipts are affected today, so the defect was latent, not active. `docs/api-contract.md` (receipt_render); SPA half `docs/lovable-render-failed-prompt.md` |
| An unrecognised statement `Type` label keeps its printed sign; every statement upload records how it was read | **shipped 2026-09-15** (backlog item 64, parallel round) | (a) Sign canonicalization ran on "is this a credit", so any other label was read as a purchase and abs'd, and a German "Lastschrift" export would have turned its credits into charges silently. `_common.DEBIT_TYPE_VALUES` + `is_known_type` make "recognised" the union of both vocabularies, which is load-bearing: `Sale` and `Fee` are not credits either, and treating those as unknown would flip all 109 live August purchases. An unknown label keeps the printed sign and earns one info-severity `parse_issues` note naming it and its row count (capped at ten plus a summary, for a Type column mapped onto the wrong source column). (b) `statements[]` entries carry `column_map` + `card_currency` (parallel, absent on earlier entries) and `reread_statements` reuses them; before this it recovered both from `config.statement`, which describes only the LATEST upload, so a two-statement month re-guessed the earlier file and read every file at the last upload's currency. Live check first: both months hold ONE statement each, carrying only `Sale` / `Payment` / `Fee`, so neither gap reproduces in production. Tests: `test_statement_parser_gaps.py` (route-level; three regressions of the real source proven RED first). SPA half: `docs/lovable-statement-record-prompt.md` (owner applies) |
| Workbench filter + sort (item 17) | **mostly already live; SPA delta pending owner paste** (2026-09-15). The control bar shipped with PR #454/#455's Lovable half and the 2026-07-27 backlog note predates it: vendor search, BUCKET toggles, STATUS, SORT (Default / Vendor A-Z / Date x2 / Amount x2), six FILTERS toggles, Clear filters. Vendor A-Z, the operator's "alphabetic", verified correct on the live August month by browser drive. Backend change for this item: NONE. A `vendor_sort_key` parallel field was built, green and regress-proven, then measured against the live sort, found to duplicate a better client-side collator, and reverted | The drive found one real defect and two traps, all recorded in `docs/api-contract.md`: `amount` is a display string (`f"{v:,.2f}"`), so "Amount, high to low" puts August's `ZOHO* ZOHO-ONE 2,484.00` and `SAP SE 1,574.24` at the BOTTOM of the 93-row unmatched group, below a 1.16 charge; `section` is a display lane where `is_posted` wins (July's 85 `posted` rows are 49 unmatched / 24 reconciled / 11 review / 1 refund, so an "unmatched" filter keyed on it reports 24 of 73 — use `effective_bucket`); `coverage_key` has two live shapes, so a card filter joins `coverage[]` for the label rather than parsing the key. Delta prompt: `docs/lovable-workbench-filter-sort-prompt.md` (amount parse, card / company / has-candidates filters, per-option counts, URL state) |
| Every period writer takes the batch lock; no write destroys stored bytes (backlog item 66) | **shipped** 2026-09-15, PR #846 | The manual per-charge attach and the bulk receipt-folder ingest were the last two paths rewriting the WHOLE period record from a copy read before their own work started, outside `_BATCH_ADD_LOCK`. Both now commit under it against a fresh re-read, in the `rematch_month` shape: refuse if the month was deleted, refuse a folder commit that would leave a concurrently-uploaded charge in no bucket, and carry receipts that arrived mid-flight into the pool as unmatched rather than letting them vanish. `post_manual_receipt` hands the locked span to `run_in_threadpool` (item 18's rule: an OCR ingest holds that lock for minutes and an `async def` blocking on it parks the loop, takes /healthz down and gets the machine restarted mid-ingest); the existing AST guard named it the moment the lock went in, which is the caller-level proof the fix is wired. The file half: a re-attach archives every file the charge already holds into `manual-receipts/superseded/` under a versioned name before writing, and the snapshot records the swap (`receipt_files`, stored only, absent on both view payloads); a queued-upload replacement archives instead of unlinking. One correction to the item as written, since it falls out of the same mechanism: the different-NAME re-attach left two files in the glob the image endpoint reads, from which `sorted(...)[0]` could serve the superseded one, so a charge now holds exactly ONE current file. Four regressions of the real source proven RED first; the interleaving test drives both routes at once with a rendezvous at the pre-write read. Suite 1550 -> 1557. `docs/electronic-storage-system-description.md` updated where it asserted these defects (§4.4, §6.3, §8.1, §9, §12 rows 3 and 14) |
| A change made in a month sticks: category on a needs-review row, edits on a statement month, whole-receipt reclassify (backlog item 70) | **LIVE 2026-09-16**: backend PR #870, Fly v129; SPA half `docs/lovable-month-edits-prompt.md` published and browser-verified (no disabled control on August's Review expenses, View receipt opens there, a statement-month edit answers 200 and survives a reload); backlog item 70 closed. Follow-up: a category cannot be cleared once set (item 78) | Criss 2026-09-14: "I set the right category and nothing happens." (A) a review row's `posting_category` read the held receipt, which a review row lacks; it now reads the candidate Confirm takes and carries `posting_category_proposed: true`. (B) the five expense-edit routes refused any month with a statement (both live months); they now take the edit and re-match when the field is one the matcher reads (vendor, date, total, currency, reference, legal_entity, add, delete), with the result under `rematch`; the bake now starts from the extraction baseline so a cleared edit really reverts. (C) reclassify without `line_index` covers every line, and a changed category drops the account chosen for the old one. Suite 1724 -> 1746, seven regress proofs RED first |
| One purchase is one candidate by its number; the kept copy inherits its copy's card (backlog item 69, round A) | **built 2026-09-15** on `client/brisken/p1-matching-round-a`, not yet merged or deployed; no SPA change needed | Phase 0 measured three wrong auto-matches on August, two of them a re-mailed copy of one document that item 56's vendor/date key never saw (the copies spell the vendor differently or carry the mail's date), the copy without a card then binding a stranger's charge of the same amount. `find_duplicate_receipts_by_reference` (normalized reference + total + currency, vendor and date ignored; a digit string that is the receipt's own date or total is no reference) lists beside the vendor/date groups with `duplicate_groups[].basis: "reference"` (parallel, absent otherwise), never merges with them so every saved resolution keeps its group, and collapses the same way. `inherit_card_from_copies` runs in `rematch_month` before the card chain, so the Lovable 50 invoice takes card 1176 / Consulting from its receipt copy and leaves the Corporate Services statement's scope; the inherited values persist with the entity bake and are re-derived from the baseline each re-match; grid and export apply the same function. Measured on the DB copy with labels as judge: August wrong 3 -> 0, July 0 -> 0, clean 26 / 7 unchanged, no clean pair lost, six bundles byte-identical (55/95, 0 wrong), scorer 49.8 / 15.7 / 65.5 and guard 4/4 unchanged. Attribution tool mirrors the app by calling its functions. Tests: `test_reference_duplicates.py` (52, route-level + unit) and the `basis` contract pin; four regress proofs RED first. Review fixes applied the same day (backlog 69, "Round A review fixes"): a reference shared across different totals is an account id and forms no group; a reviewer's `ignore` and an operator's hint assignment both stop the card lend; the attribution tool assembles the bundle pool as the app does and proves which module tree it measured; September 2026 will show `n_needs_entity` 21 -> 17 on deploy (four invoice copies take their receipt copy's card); item 72 opened for the raw-vs-effective persistence split; suite 1800, eleven regress proofs RED first |
| A clean pair keeps its match when its rivals are spoken for or name another merchant (backlog item 69, round B) | **SHIPPED 2026-09-15** (PR #877, merge `5af6a9a4`, Fly v131, both live months re-matched; July RECONCILED 26 -> 31 and REVIEW 12 -> 7 on the workbench, August 7 -> 8, 0 wrong on both) | The bilateral-uniqueness gate demoted 36 of the six bundles' 95 receipts and 11 more across the two live months with the correct charge already teed up in review, because ANY other clean rate-derived pair sharing the receipt or the charge blocked it. Two refinements, no threshold moved: a rival already claimed by a bank-printed EXACT match elsewhere is spoken for and does not block; a pair with a live rival left keeps its right when its `_vendor_score` is >= 0.5 and beats every rival's by 0.25 (vendor only PROMOTES a pair that already carries clean rate evidence, the opposite direction from the FX vendor floor the S1 run refuted). Plus the masked-BIN fix in `_card_keys`: a digit run immediately followed by a mask character is the issuer's BIN, so `42463153XXXXXX38` names no card instead of an absent one. The gate is now the public `uniqueness_verdicts`, which `match_month` applies and the attribution tool imports instead of mirroring. Measured with labels as judge: July clean 26 -> 31 (review 10 -> 5), August 7 -> 8 (the masked-BIN fix; the gate refinements moved nothing there), six bundles 55 -> 70 of 95, 0 wrong anywhere, no clean pair lost; scorer train 49.8 -> 56.8, holdout 15.7 -> 19.2, all 65.5 -> 76.0, determ_wrong 0, guard 4/4, calibrate exit 0. Cost recorded not netted: 12 bundle receipts the labeler had EXCLUDED as undecidable now auto-match, every one onto a same-merchant charge. Item 72's live instance did NOT close (it is a pass-1 ambiguous tie, which the gate never reaches). Suite 1800 -> 1831; four regress proofs RED first, including one that reddens the attribution tool's tests by mutating the matcher, which proves the shared import. No SPA change, no Lovable prompt (`reason` is already rendered verbatim) |
| A one-day gap is not a date mismatch (backlog item 80, note #44) | **shipped 2026-09-16** (PR #902; SPA half `docs/lovable-date-gap-prompt.md` pending owner paste) | `rows[].candidates[].date_gap_days` + `date_gap_zone` (none / lag / mismatch) on both candidate emission sites, absent when a date is missing; bounds in `service.DATE_GAP_ZONES` with the evidence. Label only, matcher untouched. Until the prompt is published the workbench still shows "date mismatch" on July's six one-day rows |
| The FX block shows the receipt converted at our rate (backlog item 81, note #43) | **shipped 2026-09-16**, PR #903; SPA half `docs/lovable-fx-reference-prompt.md` pending owner paste | `rows[].candidates[].fx` gains six parallel `reference_*` keys (rate, source, converted, signed gap, signed percentage, band), absent when the pair has no rate. The rate comes from the matcher's own lookup over the run's frozen config, so the fields populate on deploy with no re-match and the printed rate always appears in the matcher's reason. Live: 27 FX candidates on July and August, 0 with a Zoho conversion, every one with a rate (26 `match`, 1 `review`); July AMAZON reads 276.08 EUR x 1.162275 = 320.88 USD, difference -5.32 (-1.66%). Suite 1832 -> 1843 alone, 1875 after merging main (items 79 + 80); three regress proofs RED first. `zoho_*` untouched (item 23 round 7 renames them) |
| A statement row says what it is; a card payment is not a refund (backlog item 73, note #42) | **shipped 2026-09-16** (PR #905; SPA half `docs/lovable-row-type-prompt.md` pending owner paste) | `rows[].row_type` (purchase / payment / refund / reversal / fee / interest) from the workbook's Type label, else the sign; `rows[].entity_source` (card / batch / none). Buckets, matching and counts unchanged. July's -9,664.81 and August's -7,823.16 "Payment Thank You-Mobile" read `payment`; August's annual membership fee reads `fee`. Stored months read the Type cell back from `raw_text`, no re-read. The payoff row's company came from card 2838, not the upload, so the item's secondary claim did not reproduce. Until the prompt is published the page still files the payoff under "Refund" |
| A corrected date moves the receipt to its month (backlog item 77, note #34) | **shipped 2026-09-16** (PR #910; SPA half `docs/lovable-month-move-prompt.md` pending owner paste) | `expenses[].month_move` + `summary.n_month_moves` offer the move on a reviewer-typed date outside the batch's window; `POST .../expenses/{doc}/move` carries reading, file and edits into the month (created when absent) and re-matches both. Locale half measured and not built (the reported misread is a glyph). Amendment fields `time` / `invoice_number` / `receipt_number` in the response schema only. Deployed Fly v137. Live fix run on owner yes 2026-09-16: the Parada receipt moved from January 2026 (`4ceaeb461386`, now empty, kept) into July, where it settled `MP *PARADAOBRIGAT` USD 6.20 (`fx_reference` 0.99, 32.00 BRL at 0.192448 = 6.16, +0.68%); no other July row moved, 0 new model calls |
| Duplicates mean one thing each (backlog item 74, notes #37/#41/#45/#46) | **shipped 2026-09-16** (PR #914; SPA half `docs/lovable-duplicates-decided-prompt.md` pending owner paste) | Charge-side duplicate detection deleted. Every receipt group is decided by the tool on a recorded `basis` (hash, document number, a PDF printing the other's number, different numbers, different cards, vendor/date/amount, then the statement), with `state` / `decided_by` / `verdict` and `summary.n_duplicate_groups_open`. Matching unchanged on both months and the six bundles. Live on Fly v138. July's Google ruling reset with owner approval (now reviewer / distinct): the two 71.64 charges wait on one pick between the two invoices. Until the prompt is published the page still shows "Possible duplicates" with its advisory sentence and both buttons per card |
| A row says whose turn it is; clean exact pairs confirm themselves (backlog item 76, notes #38/#39/#49) | **shipped 2026-09-16** (PR #925, Fly v139; SPA half `docs/lovable-turn-prompt.md` published and driven 2026-09-16) | `rows[].turn` (decide / confirmed / rejected / posted / none), `decided_by` + `decided_rule`, `summary.n_self_confirmed`. After each re-match the tool confirms exact pairs with one candidate, a ready category and vendor 75 or more, marked as its own, re-judged every re-match, never over a person's verdict; undo is the ordinary reset. Applied to July and August by an owner-approved master-data refresh: July ELEVENLABS 5.00 + ANTHROPIC 50.54, August LOVABLE 15.00 / 25.00 + ANTHROPIC 51.38, nothing else moved. The page reads "Already booked" / "Nothing to decide" / "Confirmed automatically" and offers Reject / Confirm only on the 1 (July) and 7 (August) rows left to decide |
| The unmatched lists say what they hold (backlog items 83 + 75, notes #40/#46) | **LIVE 2026-09-17**: shipped 2026-09-16 (PR #932); SPA half `docs/lovable-unmatched-reasons-prompt.md` published by the owner, bundle-audited and browser-driven EN + PT (reason lines 1 / 4 / 4 / 1 on August, GOOGLE Workspace "paired with another charge" on July; evidence in `PROMPT-STATUS.md`) | A decided duplicate copy leaves `unmatched_receipts`, the hand-match picker, `n_unmatched_rec` and the near-miss pool for `copies_set_aside[]` + `n_copies_set_aside`; every unmatched receipt and charge carries `reason_code`. View-time only: no re-match, no write, the months read it on the next GET after deploy. Predicted: July receipts without a charge 13 -> 11 + 2 set aside, August 21 -> 10 + 11 |
| The Expenses view's boxes open their rows (backlog item 84) | **LIVE 2026-09-17**: shipped 2026-09-16 (PR #935); SPA half `docs/lovable-expense-boxes-prompt.md` published, driven: every July tile lists exactly its number (49 / 3 / 14 / 33 / 24) | `expenses[].boxes[]` on every row and every box count summed from the rows; `n_needs_company_or_person` for the merged MISSING ENTITY + NEEDS PERSON box; Categorized per row by every line; MISSING RECEIPT IMAGE 2 / 1 -> 0 / 0 (the files open). View-time only, no write |
| Controls that read as text are buttons; "booked" names its workbook (backlog items 85 + 86, notes #47/#48/#50/#51) | **LIVE 2026-09-17**: prompt written 2026-09-16 (PR #938), published by the owner, driven EN + PT ("Show 48 booked rows", "48 rows marked yellow in July2026.xlsx, already booked" with its tooltip, no underline classes) | `docs/lovable-controls-as-buttons-prompt.md`: 20 month-page controls become outline small buttons, fold toggles name what they show, the booked fold names `statements[].file` with the yellow / grey rule as its tooltip |
| A month's corrections are saved to memory at sign-off (backlog item 88) | **shipped 2026-09-16** (PR #940); SPA half `docs/lovable-memory-at-signoff-prompt.md` published 2026-09-17, bundle-verified only (no month has been published, so the toast has no live case) |
| One card fix per expense row, remembered at sign-off (backlog item 87, note #33) | **shipped 2026-09-17** (PR #947; SPA half `docs/lovable-card-fix-prompt.md` pending the owner's paste) | `card_key` on the field PUT, `expenses[].card_source`; Publish remembers the fix per vendor, applied later only when a receipt prints no card number. Fixes two strip learnings that never resolved again. 24 of July's 33 no-company rows had no path but "Confirm private" |
| "Reconciled" on the months list becomes "Matched with statement" (backlog item 89, owner 2026-09-17) | **prompt written 2026-09-17** (PR #947; SPA-only, pending the owner's paste) | `docs/lovable-matched-with-statement-prompt.md`: the Statement column badge shown whenever a statement is loaded stops claiming the month is reconciled | Publish saves the month's corrections (`memory` on the publish reply), never twice for the same corrections, never failing the publish. No month has been published yet, so the first save happens at the first publish |
| A reference rate per month, from the ECB (backlog item 82, owner ruling 2026-09-16) | **shipped 2026-09-17** (PR #951; SPA half `docs/lovable-ecb-rates-prompt.md` pending the owner's paste). Inert on July and August by design: their frozen Settings rates win | `matching.fx_ecb_monthly_rates` fetched at month creation and statement attach, read for each charge's month; `reference_rate_source: "ecb_month"` + `reference_rate_period`. Simulated first: the ECB rate is more accurate (July mean deviation 1.78% -> 0.44%) but moves July -1 right / +2 coincidental auto-matches and August nothing; on six bundles it beats one static rate (68 vs 64, 0 vs 3 wrong). The two Settings rates still win for new months until removed. A 2% clean band under ECB rates measured July 32 right, bundles 70: a separate lever |
| The entity list in the operator's own order (backlog item 92, Criss via owner 2026-09-17) | **LIVE 2026-09-17**: backend PR #971 deployed v146; SPA rounds 1 and 2 published and verified cold (press, hold, drag; arrows removed). A non-alphabetical order is saved live | `settings['entity_order']` (list of names) orders `entity_options` in every picker; unnamed entities follow A-Z, stale names ignored. Open owner-side data: all 9 cards name `Corporate Services` / `Cloud Services` / `Consulting` while the entity settings sit under the LLC names, so those settings do not reach the cards' charges (backlog item 92) |
| Matching separated by card (backlog item 137, owner 2026-09-17) | **shipped 2026-09-17** (PR #1016, Fly v156; SPA half `docs/lovable-cards-differ-prompt.md` SPA half applied 2026-09-17) | The card the tool resolved for a receipt (picked, hinted, remembered) scopes and ranks its candidates; a pick scopes hard while its card offers a candidate, a pair on another card asks for review. `rows[].cards_differ` + `summary.n_cards_differ`: live July 0, August 1 (LOVABLE 25.00 on 3645, receipt picked as 2838, tool-confirmed; goes back to pending at Criss's next August re-match). Attribution class tables and six bundles (70/95) unchanged |
| Both PDFs organized by card (backlog item 138, owner 2026-09-17) | **shipped 2026-09-17** (PR #1028) | Reconciliation report and month report share one grouping (`_pdf_common.card_sections`): per card its statement line, exceptions, charges or expenses, then its receipt pages; No card last. The reconciliation report now receives the reviewer's card picks, so item 137's cards-differ pair is named in the document. Real August rendered locally: 73 and 68 pages, 9693's two receipts get their own section. Open: the months-page half Criss asked for (tabs per card), a new presentation awaiting the owner's build-or-quote call |
| An interrupted drop runs again at boot (backlog item 114, audit 2026-09-17) | **shipped 2026-09-17** (PR #1033, Fly v162) | Sidecar `drops/<job>.json` keeps the month pick; the boot pass re-runs an interrupted drop once under its job id, gives up a second cut-off, deletes leftover drop folders and `drop-add-*` copies. No SPA field; month-deletion custody not built (reviewer corrections) |
| An invoice read as a statement page stays an expense (backlog item 105, audit 2026-09-17) | **shipped 2026-09-17** (PR #1037, Fly v165; SPA copy prompt `docs/lovable-invoice-read-as-statement-prompt.md` optional, not applied) | Statement verdict with vendor, non-zero total, reference and a line amount is kept with a note and a `check` until its category is confirmed; stored July invoices (AWS, Tricarico) still need Criss's restore click |
| The ECB rate's own 2% clean band + drifted-Settings-rate advisory (backlog items 90 + 132, audit 2026-09-17) | **shipped 2026-09-17** (PR #1048, Fly v167) | `fx_ecb_match_pct` 0.02 for ECB-sourced pairs only; `setup_advisories[]` `fx_rate_drift` beyond 1 point. Inert live until the owner removes the two Settings rates (item 90 step 4); July/August FX candidates all `settings` / `match` at deploy |
| One receipt never on two charges + the list counts what the page counts; a wire is not a private card; a correction is recalled; every refusal carries a code (backlog items 103, 144, 115, 130, audit 2026-09-17) | **shipped 2026-09-17** (PRs #1056 / #1057 / #1060 / #1061, Fly v177) | A pass-1 tie holds its receipts and "spoken for" reaches tie detection; one effective derivation behind the four charge counters on the months list, the stored summary, the re-match event and its reply (live July 31/7/73/1 on both screens, was 8/72 on the list). Grid Books-as runs the export's chart gate; eight write routes answer with the run GET's own dispatch; a bank-transfer tender and a settled-outside receipt stop suggesting a private card (live `n_suggested_private` 8 -> 7). Memory is consulted for a receipt with line items, reaches a row with no company, and sign-off teaches confirmed pairs (0 live lines move today; a simulated July sign-off teaches 11 categories, 4 aliases, 1 FX rate). 285 refusal sites carry a stable `code` beside the unchanged English, advisories carry their values, nine source-scan tests fail on a new uncoded refusal; SPA prompt `docs/lovable-error-codes-prompt.md` not pasted |
| An exact-amount pair from another merchant yields to the right merchant (backlog item 133 rule (b), audit 2026-09-17) | **shipped 2026-09-17** (PR #1053, Fly v170) | Demotion to review at 0.55 only when a rival charge for the same receipt agrees on the merchant and that receipt is its first choice; after the ambiguity pass. Replay over July, August and the six bundles: 0 receipts moved |
| A decision history with a name on every line (backlog item 104, audit 2026-09-17) | **shipped 2026-09-18** (PR #1081, Fly v180; SPA fold + Undo button `docs/lovable-decision-history-prompt.md`, NOT pasted) | Append-only `decision_history`; one line per change that moved a value, at twelve write sites (the item said four). `who` is the signed session token's label, not the server environment, so Criss and the developer are distinguishable. `GET /api/runs/{id}/history` and a per-line undo refused with `history_superseded` when the row has moved since. Duplicate rulings recorded but not undone here. Credential half DECLINED by the owner 2026-09-18. Every existing month starts empty, which is the truth for a period nothing recorded |
| One currency for a month's receipts (backlog item 98, audit 2026-09-17) | **shipped 2026-09-18** (PR #1076, Fly v179) | `output/single_currency.py`: a settled receipt converts at its own charge, everything else at the matcher's reference rate, a row nobody can price is named rather than counted as zero. `expenses.csv` fills the existing `Exchange Rate` column and states the total; the month report prints each figure and rate under the amount. Live July USD 58,187.69, August USD 2,808.91, both as predicted. Silent on a month already in one currency. The report PDF was not driven live (that route writes render outcomes into the run, item 67) |
| A statement upload has an identity (backlog item 150, note item T2, owner 2026-09-18) | **shipped 2026-09-18** (pending PR; no SPA change, nothing renders the id yet) | `statements[].statement_id` = sha256 of the stored bytes (16 hex), parallel and absent on entries written before it; `coverage[].statement_ids[]` beside the file names; `statement_anchors` keyed by id as well as by file; `?statement_id=` on the writeback route. The same bytes twice share one id, a corrected file gets a new one, a re-read keeps it. Live July and August gain ids at their next re-read, which this item does not trigger |
| The description counts, and the no-card fallback is defined once (backlog item 151, owner note X1, 2026-09-18) | **shipped 2026-09-18** (PR pending, Fly release pending; SPA line `docs/lovable-no-card-evidence-prompt.md`, NOT pasted) | `strip_reference_tokens` keeps an order or invoice number inside the bank description out of the merchant comparison (July Microsoft `vendor_pct` 50 to 100 at the next re-match, crossing the self-confirm floor); `card_evidence(tx, receipt)` is the one definition of where a pair's cards came from, on every candidate; a receipt naming no card is matched across every card and asks for review with `review_code: no_card_rival_on_other_card` when a deterministic rival sits on another card. Measured on July, August, September and the six bundles: 0 class moves, labelled-wrong unchanged, bundles 70/95. Description-token promotion and masked card fragments: measured dead ends, not built. Next: the owner's re-match of July shows the Microsoft row confirming itself; September's statement is the first live test of the review clause |

## Open decisions / gates

- Spec-incorporation backend (§16/§17/§18 + `/api/settings`) DEPLOYED to Fly 2026-07-21
  from `origin/main` #299 (full suite 670-green first; idempotent DB migration verified safe
  on Criss's live 94-row run). Live app now at #299.
- SPA production URL DECIDED 2026-07-22: `brisken-reconcile-dash.lovable.app`. A custom
  `recon.brisken.com` stays optional; it would need the CORS regex widened + a Fly deploy.
- **July 2026 receipt sweep, 2026-09-09 (Criss ask: pull the July receipts that
  are not in the tool).** July went from 8 receipts to **26** (EUR 17,907.84 /
  USD 14,995.21). 8 came out of Zoho Expense (org CLOUD SERVICES, the German meal
  and supermarket photos), 8 out of Dirk's and Matthias's mailboxes over Graph
  (Google Workspace x2, Anthropic x3, GitHub, Eleven Labs, Network Solutions).
  Every other source was searched and is exhausted: Books answers "Receipt not
  attached" for 108 of 111 July rows when asked for the file directly (Hostinger
  ran as the control in the same pass and did return its file), the Books document
  inbox holds only AP bills, and 8,263 SharePoint finance files hold statements,
  not card receipts. July then went to **32** when six more Anthropic
  receipts came out of Matthias's iCloud, covering the period since Anthropic
  billing moved onto the Brisken card (Mastercard 3964 to Visa 2838, between
  2026-05-23 and 2026-06-12); five clear the 07-14 chase rows exactly, and the
  drop entrance materialised June and August months for the rest. CORRECTION to
  an earlier line here: the Anthropic 214.20 row is NOT a currency misread.
  Anthropic denominates the subscription in euro and bills usage credits in
  dollars, so EUR is right as booked. The one row that does need a human glance
  is Normandie Seine: no date, and 6.60 where Zoho Expense records
  2026-07-05 / 6.00. Evidence and the full map:
  `context/zoho-receipts-july-2026/README-july-receipt-gap.md`, with byte-verified
  copies in `uploaded-2026-09-09/`.
  **Two blockers, both owner-side.** (a) Six of eight Zoho Expense orgs refuse
  every endpoint with 400/6018 "your account is disabled", including Corporate
  Services (107 of the 108 July charges) and Tech LTDA (the Brazilian spend); that
  is a user membership setting, not a scope, so an Expense admin has to enable our
  API user. (b) Ten chase-list vendors (Lovable, Perplexity, OpenRouter, Supabase,
  Vercel, Resend, Adobe, Wix, SaaS Rise, PressMaster) have no receipt mail in
  either allowlisted mailbox; Dirk forwards receipts to
  Cristiane.Cavalcanti@brisken.com and several vendor accounts bill her directly,
  so either the Graph allowlist widens to her mailbox or she forwards July into the
  tool's mail intake.

- **Brisken domain, 2026-09-08 (owner directive: a name without "fly" in it).** The API
  half is LIVE: `api.expenses.brisken.com` (GoDaddy `A`/`AAAA` to the app's dedicated
  `149.248.221.114` + `2a09:8280:1::12b:572b:0`, Fly cert issued after the
  `_acme-challenge.api.expenses` CNAME went in; `/healthz` 200 over TLS). The old
  `brisken-expense-recon.fly.dev` keeps answering, so nothing had to move at once. The SPA
  half is owner-side: add `expenses.brisken.com` in Lovable settings, send back the
  `lovable_verify` value for the `_lovable.expenses` TXT, then paste half 2 of
  `automations/expense-reconciliation/docs/lovable-brisken-domain-prompt.md`. CORS already
  allows the SPA host (PR #751). `expenses.brisken.com` keeps its MX, so mailed receipts are
  unaffected: Lovable's five other Brisken sites use an `A` to `185.158.133.1`, and an A
  record coexists with an MX where a CNAME would not.
- **Lovable merge != live:** merging the SPA repo's main syncs the Lovable EDITOR only; the
  published site keeps serving the last explicitly-published build. Verify with a structural
  DOM probe, not a label. Publishing is a dashboard action the agent cannot perform.
- Fly HTML-UI deletion DEPLOYED (v31, 2026-07-22, owner order). If Criss surfaces on the
  old UI instead of the SPA, rollback = redeploy v30 via `flyctl releases`. The
  `/api/login` rate-limit half of the hardening is now CLOSED (v33, PRs #367/#369);
  one shared operator code is still the whole boundary, so a second factor or a
  per-person code remains the open half.
- **`matching.llm_second_pass_unmatched`: RECOMMEND LEAVING OFF** (evaluated 2026-07-22,
  zero LLM spend). The pass can only pair an unmatched charge with a still-unclaimed
  receipt, so its ceiling is computable deterministically: across all 6 labelled
  bundles it is **1-2 rescues out of 95 confirmed pairs**. The structural reason is
  that only 13 receipts remain unclaimed across the 6 months against 559 unmatched
  transactions; the unmatched bucket is dominated by receiptless statement charges
  (the ADOBE/ANTHROPIC class), and no matching pass can find a receipt that does not
  exist. Realising <=2 rescues would cost ~40 LLM calls per run (the `max_calls` cap,
  hit every month) and each rescue lands in `judgment_required` = MORE review for
  Criss, not less. Flipping the default needs an owner order; there is no case for one.
- **Access shared + tool in active use (2026-08-07).** The owner was handed the SPA
  URL (`brisken-reconcile-dash.lovable.app`) + operator code to send on. A reviewer is
  now testing: a May-2026 statement run exists, and 3 feedback notes came in via the
  in-app capture (emailed to Matthias, "Expense recon: 3 new feedback notes", 2026-08-07)
  — legal-entity dropdown, "default currency doesn't make sense", vendor→multi-category.
  Backend for the dropdown shipped (#498); the other two are parked design items.
  Next proof-of-life: Criss re-runs May (the existing run keeps the stale `has_coa`
  warning; a fresh run resolves clean).
- Concurrency: two sessions share this module (active `agentic-ops1-recon` worktree); before
  any backend build here, `git fetch origin main` + `git log` first (4th collision-class event).
- COA gate DEPLOYED 2026-07-01: `coa-provision.json` + `zoho-books-coa.json` on the Fly `/data`
  volume, `EXPENSE_RECON_COA_PROVISION` set, deployed (v10). Verified in-container on the real
  files: Corporate Services (822741658, 177 accts) + Cloud Services (697686691, 199 accts) resolve;
  a `(DO NOT USE)` account diverts. Remaining: authenticated end-to-end confirm with Chris on a real
  statement (the app is password-gated, so only a logged-in run exercises the full upload->review path).
- Joint call with Chris (Brisken finance manager) not yet scheduled (Dirk to brief her).
- Legal retention period to confirm (Dirk's guess ~7 yrs US, unconfirmed).
- No further client data coming: the ER PDFs + Chase export in hand are illustrative
  samples, not a reconciling dataset; build to sample shapes, validate accuracy in
  production (`project_brisken_no_further_data` memory).

## Pointers

- Spec: `specs/1-spec/p1-expense-reconciliation-functional-spec.md` (v2; SaaS scope deferred)
- Build authority: `automations/expense-reconciliation/BLUEPRINT.md`, `ANNEALING.md`
- Platform state: `infrastructure.yaml` (note: its judgment-layer "stubbed" line predates the OpenAI pivot)
