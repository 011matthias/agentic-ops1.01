# Findings that belong to other Planner tasks

1. **"Run personal outreach to the 17 Rome partner and SAP contacts".** Ten fob-encoded contacts carry Dirk's `personal outreach DN` stamp and were routed OUT of this roster so the templated token note does not undercut his personal emails: Richter (Eprox), Diet (INTENSUM), Szczecina (KPMG), Sharandakov (LeverX), Kiosses + Meyerhoff (Nagarro), Reinsfelder (SINVA), Stiebe (Target Networks), Ramos (Tradeweb), Koekkoek (Zanders). Their LinkedIn touch is currently owned by nobody: the Tier-2 session already posed the same question to Dirk (watch-list add / generic note / separate partner motion) and it is still open. Whoever runs the partner motion should pick them up.

2. **"Support the live Accenture (Ashok) MDH referral".** Ashok Kumar is fob-encoded and would otherwise land in this roster; he is routed to the referral task since he already responded to the Rome outreach. Any LinkedIn connect for him belongs inside that thread, not in a templated batch.

3. **"Rome booth/token-network: GDPR consent email".** Same base segment (`fob_encoded`). No consent or data-usage language was put into any connect note here, per the owner direction that the re-connect and the privacy notice stay separate surfaces. The consent email's audience math is independent of this roster's dedup (it runs off `booth_network_send`).

4. **"Rome Tier 3 booth/token-network: email outreach".** This roster's dedup arithmetic (91 fob = 29 templated + 11 routed + 51 removed, name-matched against hottest-5 / Tier-1 / Tier-2) is directly reusable; see `segmentation.md`. The email task should also note the three job-change flags in `resolved-profiles.md` (Katkoria, Boxberger, Zang possibly no longer at the sheet's company), since a company-anchored email reads worse than a LinkedIn connect when the person has moved.

5. **"Build the CRM contacts master sheet" / "Upload the Rome contacts to the CRM".** `tier3-roster.csv` adds 24 verified LinkedIn URLs (2 brand-new: Forst, Hacikyaner) that the master sheet does not carry, plus one suspect on-file URL (Blauth, see `resolved-profiles.md`). Worth folding in at CRM-upload time.

6. **Name-normalization pitfall for any future name-dedup.** "Teisner-Kjær" on the live sheet only matches the Tier-2 roster's "Teisner-Kjaer" if æ/ø/å are transliterated before comparison; plain Unicode NFKD does not decompose æ. The first dedup pass this session missed him because of exactly this.

7. **CORRECTION (2026-07-13): this roster is the booth/token network, NOT the Tier 3 tier. The real Tier 3 is Dirk's 25 Outlook drafts.** The owner pointed at Dirk's draft box as the authority. Verified live: Dirk's Drafts hold 25 emails subject-lined "T3 Following up from the SAP conference in Rome", and 25/25 map to the master sheet's `Tier=T3` (cold reconnect); only 4 of them are `fob_encoded` and only 3 overlap this roster (Katkoria, Forst, Blauth). The T3 send pack (`context/drafts/rome-t3-cold-reconnect.md`) states the rule outright: "Tier column is the authority." The Planner task's premise ("booth/token-network, fob_encoded, same segment as the T3 email task") is a mislabel; the T3 email task is scoped to `Tier=T3`, not `fob_encoded`. So this 29-person roster is correctly the booth/token network LinkedIn motion (plan track 1, "network foundation"), but it is NOT the Tier 3 tier. Two consequences: (a) rename this deliverable's motion "booth/token network" in your head, not "Tier 3"; (b) the real Tier 3 (the 25 cold-reconnect prospects Dirk is emailing) currently gets email only and has no LinkedIn roster. See memory `project_brisken_rome_tier3_is_drafts.md`. Whether the booth/token motion still runs as its own track is an owner decision. The real Tier 3 LinkedIn roster (the 25 draft recipients) is now built in `tier3-real/` (roster + both runbooks + resolved-profiles; 12 of 25 profiles resolved, 13 Sales Nav only), on owner request 2026-07-13.

8. **Tier-2 roster (task-5) mis-assigns Line Ehlers (DSV) and Sergey Timeshov (BSTDB): they are T3, not T2.** Both appear in `output/leadgen-task-5/tier2-roster.csv`, built from `dirk_notes`. But the authority disagrees: both carry `Tier=T3` on the sheet and a "T3 Following up from the SAP conference in Rome" draft in Dirk's Outlook, and neither has a T2 draft. So Dirk emails them as T3. Surfaced 2026-07-13 when the owner noticed they were already-added to the Sales Nav list under the Tier-2 batch. Task-5 should drop them from its Tier-2 roster; they are covered in `tier3-real/`. (The other 3 cross-roster overlaps, Katkoria/Forst/Blauth, are legitimate: they are the only 3 T3 people also at the booth, so they correctly appear in both the booth roster and tier3-real.) Same root cause as the whole T3 mislabel: rosters built from `dirk_notes` or `fob_encoded` drift from the sheet `Tier` column + Dirk's drafts, which are the authority (memory `project_brisken_rome_tier3_is_drafts.md`).
