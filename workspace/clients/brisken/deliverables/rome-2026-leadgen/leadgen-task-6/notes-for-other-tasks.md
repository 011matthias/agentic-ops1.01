# Notes for other Lead Generation tasks

Surfaced while building the Calvin clip brief. Not acted on, per the no-cross-task
rule. Board positions are the open-task order as of 2026-07-09.

**"Gather customer reviews on the SAP Store and Discovery Center listings" and
"Refresh the SAP Discovery Center product listing".** The 2026-06-17 Store audit
found only two buyable Brisken listings, Market Data Hub and Trade Automation.
Digital Co-Worker, Remittance Advice Gate and Bank Fee Portal are not on any SAP
channel. Both tasks need to know which listings actually exist before planning
review-seeding for them.

**"Prepare Zalando / Sanofi TreasuryCentral demo collateral".** The naming
reconciliation in the brief's section 7 applies to both: `OnePilot Agents` is the
category, `Calvin` names one agent, and "digital co-worker" is retired. If either
deck reaches a prospect using the old label while the clip uses the new one, the two
assets read as two products.

**"Publish the LinkedIn thought-leadership 4-post launch batch".** The 1:1 feed cut
specified in the runbook's export matrix is built for exactly this surface. Worth
sequencing the clip into the batch rather than posting it separately, if the recording
lands in time.

**"Brainstorm an adaptable client demo to replace the static decks".** Path A of the
runbook produces a clean, masked, full-resolution screen recording of the live Calvin
flow. That footage is reusable source material for whatever replaces the decks, so
the two tasks should share one recording session rather than booking the demo tenant
twice.

**"Deploy the Rome post-event asset hub".** The clip needs a first-party home, and the
runbook proposes `resources.brisken.com`. If the asset hub is already going up on a
Brisken-owned origin, the clip should land inside it rather than getting its own
one-off page. Coordinate before either ships a landing page.

**"Rome Tier 2 warm-engaged: email outreach" and "Rome Tier 3 booth/token-network:
email outreach".** Both are candidate carriers for this clip as the soft CTA. Neither
can use it until the recording exists. If either task is scheduled before the demo
capture, it ships without the clip rather than waiting.
