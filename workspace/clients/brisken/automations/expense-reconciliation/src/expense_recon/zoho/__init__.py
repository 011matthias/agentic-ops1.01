"""Zoho Books integration (BLUEPRINT slice 4.7 / 4b).

`client.py` holds the OAuth + read API client. The journal-posting
path is intentionally absent until the account-scope decision lands
with Chris (posting real journal entries is irreversible).
"""
