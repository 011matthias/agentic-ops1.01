# Deliverable gate config for Meji Media handoff package
# This is an internal partner-handoff document, not client-facing.
# Recipient (Matthias) is a teammate with full repo access.
deliverable:
  allow_categories:
    - leaked-path
    - slop:unverified-stat
    - voice:em-dash-substitute
  # reason: "Partner-handoff doc references skill modules and internal paths
  # the recipient needs. Approximations like '~9 weeks', '~40% of ops budget',
  # '~426 ops' come from comms-log narrative entries, not deliverable stats.
  # Em-dash-substitute rule is German-style strict; this doc targets an
  # English-speaking colleague where mid-sentence '--' reads as natural prose
  # cadence (label-aside-detail). Bold-label and section-header substitutes
  # were converted to colons; remaining '--' are intentional asides."
