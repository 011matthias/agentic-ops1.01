# HTTP client
httpx>=0.26.0

# Environment & validation
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# JSON schema validation (used by zod equivalent in Python)
# (no extra deps needed — stdlib json + pydantic covers it)
