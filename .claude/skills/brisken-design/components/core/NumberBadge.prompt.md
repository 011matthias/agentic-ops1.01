The numbered step marker. Brisken has no icon set — this badge and the teal tick do the work icons would.

```jsx
<NumberBadge n={1} />
<NumberBadge n={3} size={43} />
```

Radius is a quarter of the size (never a circle, never a pill). Numerals only — no icons inside.
