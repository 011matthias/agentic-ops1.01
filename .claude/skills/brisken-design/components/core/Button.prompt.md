The site's call to action. There is exactly one primary style: solid `#0E7C86`.

```jsx
<Button href="/demo">Book a demo</Button>
<Button variant="ghost">More details</Button>
<Button variant="onDark">Open TreasuryCentral</Button>
```

Labels are sentence case and plain: "Book a demo", "Contact us", "Show all applications". No arrows-as-decoration except the brand's `→` on link-style buttons. Radius stays 8px; never pill.
