The "AFTER · THE DIGITAL CO-WORKER, IN ORDER" list. One short sentence per step, present tense, full stop.

```jsx
<StepList steps={[
  'Reads the funding email.',
  'Pulls out amount, currency, entity and value date.',
  'Checks the group cash position and the intercompany limit.',
]} />
```

Always close the list with the human: a `<Band tone="quiet">A person approves the moves that matter.</Band>`.
