The site's Answers section: real search questions, opened by a `+`.

```jsx
<FaqRow question="How do I get Bloomberg market data into SAP TRM automatically?" open={open === 0} onToggle={() => setOpen(0)}>
  The Market Data Hub pulls every rate from any source and pushes it into SAP, on a schedule or on demand.
</FaqRow>
```

Questions are the one place Brisken writes a headline as a question. Answers stay two or three sentences.
